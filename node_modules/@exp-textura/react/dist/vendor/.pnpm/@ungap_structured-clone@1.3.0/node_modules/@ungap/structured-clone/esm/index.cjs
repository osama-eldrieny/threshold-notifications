"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const deserialize = require('./deserialize.cjs');
const serialize = require('./serialize.cjs');

/**
 * @typedef {Array<string,any>} Record a type representation
 */

/**
 * Returns an array of serialized Records.
 * @param {any} any a serializable value.
 * @param {{transfer?: any[], json?: boolean, lossy?: boolean}?} options an object with
 * a transfer option (ignored when polyfilled) and/or non standard fields that
 * fallback to the polyfill if present.
 * @returns {Record[]}
 */
const structuredClone$1 = typeof structuredClone === "function" ?
  /* c8 ignore start */
  (any, options) => (
    options && ('json' in options || 'lossy' in options) ?
      deserialize.deserialize(serialize.serialize(any, options)) : structuredClone(any)
  ) :
  (any, options) => deserialize.deserialize(serialize.serialize(any, options));

exports.deserialize = deserialize.deserialize;
exports.serialize = serialize.serialize;
exports.default = structuredClone$1;
//# sourceMappingURL=index.cjs.map
