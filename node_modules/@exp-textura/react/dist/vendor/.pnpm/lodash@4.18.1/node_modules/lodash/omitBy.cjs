"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _baseIteratee = require('./_baseIteratee.cjs');
const negate = require('./negate.cjs');
const pickBy = require('./pickBy.cjs');

var omitBy_1;
var hasRequiredOmitBy;

function requireOmitBy () {
	if (hasRequiredOmitBy) return omitBy_1;
	hasRequiredOmitBy = 1;
	var baseIteratee = _baseIteratee.__require(),
	    negate$1 = negate.__require(),
	    pickBy$1 = pickBy.__require();

	/**
	 * The opposite of `_.pickBy`; this method creates an object composed of
	 * the own and inherited enumerable string keyed properties of `object` that
	 * `predicate` doesn't return truthy for. The predicate is invoked with two
	 * arguments: (value, key).
	 *
	 * @static
	 * @memberOf _
	 * @since 4.0.0
	 * @category Object
	 * @param {Object} object The source object.
	 * @param {Function} [predicate=_.identity] The function invoked per property.
	 * @returns {Object} Returns the new object.
	 * @example
	 *
	 * var object = { 'a': 1, 'b': '2', 'c': 3 };
	 *
	 * _.omitBy(object, _.isNumber);
	 * // => { 'b': '2' }
	 */
	function omitBy(object, predicate) {
	  return pickBy$1(object, negate$1(baseIteratee(predicate)));
	}

	omitBy_1 = omitBy;
	return omitBy_1;
}

exports.__require = requireOmitBy;
//# sourceMappingURL=omitBy.cjs.map
