"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _baseCreate = require('./_baseCreate.cjs');
const _getPrototype = require('./_getPrototype.cjs');
const _isPrototype = require('./_isPrototype.cjs');

var _initCloneObject;
var hasRequired_initCloneObject;

function require_initCloneObject () {
	if (hasRequired_initCloneObject) return _initCloneObject;
	hasRequired_initCloneObject = 1;
	var baseCreate = _baseCreate.__require(),
	    getPrototype = _getPrototype.__require(),
	    isPrototype = _isPrototype.__require();

	/**
	 * Initializes an object clone.
	 *
	 * @private
	 * @param {Object} object The object to clone.
	 * @returns {Object} Returns the initialized clone.
	 */
	function initCloneObject(object) {
	  return (typeof object.constructor == 'function' && !isPrototype(object))
	    ? baseCreate(getPrototype(object))
	    : {};
	}

	_initCloneObject = initCloneObject;
	return _initCloneObject;
}

exports.__require = require_initCloneObject;
//# sourceMappingURL=_initCloneObject.cjs.map
