"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const identity = require('./identity.cjs');
const _overRest = require('./_overRest.cjs');
const _setToString = require('./_setToString.cjs');

var _baseRest;
var hasRequired_baseRest;

function require_baseRest () {
	if (hasRequired_baseRest) return _baseRest;
	hasRequired_baseRest = 1;
	var identity$1 = identity.__require(),
	    overRest = _overRest.__require(),
	    setToString = _setToString.__require();

	/**
	 * The base implementation of `_.rest` which doesn't validate or coerce arguments.
	 *
	 * @private
	 * @param {Function} func The function to apply a rest parameter to.
	 * @param {number} [start=func.length-1] The start position of the rest parameter.
	 * @returns {Function} Returns the new function.
	 */
	function baseRest(func, start) {
	  return setToString(overRest(func, start, identity$1), func + '');
	}

	_baseRest = baseRest;
	return _baseRest;
}

exports.__require = require_baseRest;
//# sourceMappingURL=_baseRest.cjs.map
