"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isArray = require('./isArray.cjs');
const _isKey = require('./_isKey.cjs');
const _stringToPath = require('./_stringToPath.cjs');
const toString = require('./toString.cjs');

var _castPath;
var hasRequired_castPath;

function require_castPath () {
	if (hasRequired_castPath) return _castPath;
	hasRequired_castPath = 1;
	var isArray$1 = isArray.__require(),
	    isKey = _isKey.__require(),
	    stringToPath = _stringToPath.__require(),
	    toString$1 = toString.__require();

	/**
	 * Casts `value` to a path array if it's not one.
	 *
	 * @private
	 * @param {*} value The value to inspect.
	 * @param {Object} [object] The object to query keys on.
	 * @returns {Array} Returns the cast property path array.
	 */
	function castPath(value, object) {
	  if (isArray$1(value)) {
	    return value;
	  }
	  return isKey(value, object) ? [value] : stringToPath(toString$1(value));
	}

	_castPath = castPath;
	return _castPath;
}

exports.__require = require_castPath;
//# sourceMappingURL=_castPath.cjs.map
