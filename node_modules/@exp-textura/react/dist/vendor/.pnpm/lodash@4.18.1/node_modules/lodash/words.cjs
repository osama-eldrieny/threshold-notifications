"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _asciiWords = require('./_asciiWords.cjs');
const _hasUnicodeWord = require('./_hasUnicodeWord.cjs');
const toString = require('./toString.cjs');
const _unicodeWords = require('./_unicodeWords.cjs');

var words_1;
var hasRequiredWords;

function requireWords () {
	if (hasRequiredWords) return words_1;
	hasRequiredWords = 1;
	var asciiWords = _asciiWords.__require(),
	    hasUnicodeWord = _hasUnicodeWord.__require(),
	    toString$1 = toString.__require(),
	    unicodeWords = _unicodeWords.__require();

	/**
	 * Splits `string` into an array of its words.
	 *
	 * @static
	 * @memberOf _
	 * @since 3.0.0
	 * @category String
	 * @param {string} [string=''] The string to inspect.
	 * @param {RegExp|string} [pattern] The pattern to match words.
	 * @param- {Object} [guard] Enables use as an iteratee for methods like `_.map`.
	 * @returns {Array} Returns the words of `string`.
	 * @example
	 *
	 * _.words('fred, barney, & pebbles');
	 * // => ['fred', 'barney', 'pebbles']
	 *
	 * _.words('fred, barney, & pebbles', /[^, ]+/g);
	 * // => ['fred', 'barney', '&', 'pebbles']
	 */
	function words(string, pattern, guard) {
	  string = toString$1(string);
	  pattern = guard ? undefined : pattern;

	  if (pattern === undefined) {
	    return hasUnicodeWord(string) ? unicodeWords(string) : asciiWords(string);
	  }
	  return string.match(pattern) || [];
	}

	words_1 = words;
	return words_1;
}

exports.__require = requireWords;
//# sourceMappingURL=words.cjs.map
