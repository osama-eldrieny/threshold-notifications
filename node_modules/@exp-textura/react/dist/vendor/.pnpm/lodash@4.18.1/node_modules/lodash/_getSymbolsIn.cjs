"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _arrayPush = require('./_arrayPush.cjs');
const _getPrototype = require('./_getPrototype.cjs');
const _getSymbols = require('./_getSymbols.cjs');
const stubArray = require('./stubArray.cjs');

var _getSymbolsIn;
var hasRequired_getSymbolsIn;

function require_getSymbolsIn () {
	if (hasRequired_getSymbolsIn) return _getSymbolsIn;
	hasRequired_getSymbolsIn = 1;
	var arrayPush = _arrayPush.__require(),
	    getPrototype = _getPrototype.__require(),
	    getSymbols = _getSymbols.__require(),
	    stubArray$1 = stubArray.__require();

	/* Built-in method references for those with the same name as other `lodash` methods. */
	var nativeGetSymbols = Object.getOwnPropertySymbols;

	/**
	 * Creates an array of the own and inherited enumerable symbols of `object`.
	 *
	 * @private
	 * @param {Object} object The object to query.
	 * @returns {Array} Returns the array of symbols.
	 */
	var getSymbolsIn = !nativeGetSymbols ? stubArray$1 : function(object) {
	  var result = [];
	  while (object) {
	    arrayPush(result, getSymbols(object));
	    object = getPrototype(object);
	  }
	  return result;
	};

	_getSymbolsIn = getSymbolsIn;
	return _getSymbolsIn;
}

exports.__require = require_getSymbolsIn;
//# sourceMappingURL=_getSymbolsIn.cjs.map
