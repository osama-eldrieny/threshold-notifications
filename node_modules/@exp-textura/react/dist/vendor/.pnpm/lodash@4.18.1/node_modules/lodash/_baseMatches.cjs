"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _baseIsMatch = require('./_baseIsMatch.cjs');
const _getMatchData = require('./_getMatchData.cjs');
const _matchesStrictComparable = require('./_matchesStrictComparable.cjs');

var _baseMatches;
var hasRequired_baseMatches;

function require_baseMatches () {
	if (hasRequired_baseMatches) return _baseMatches;
	hasRequired_baseMatches = 1;
	var baseIsMatch = _baseIsMatch.__require(),
	    getMatchData = _getMatchData.__require(),
	    matchesStrictComparable = _matchesStrictComparable.__require();

	/**
	 * The base implementation of `_.matches` which doesn't clone `source`.
	 *
	 * @private
	 * @param {Object} source The object of property values to match.
	 * @returns {Function} Returns the new spec function.
	 */
	function baseMatches(source) {
	  var matchData = getMatchData(source);
	  if (matchData.length == 1 && matchData[0][2]) {
	    return matchesStrictComparable(matchData[0][0], matchData[0][1]);
	  }
	  return function(object) {
	    return object === source || baseIsMatch(object, source, matchData);
	  };
	}

	_baseMatches = baseMatches;
	return _baseMatches;
}

exports.__require = require_baseMatches;
//# sourceMappingURL=_baseMatches.cjs.map
