"use client";
import { __require as require_asciiWords } from './_asciiWords.js';
import { __require as require_hasUnicodeWord } from './_hasUnicodeWord.js';
import { __require as requireToString } from './toString.js';
import { __require as require_unicodeWords } from './_unicodeWords.js';

var words_1;
var hasRequiredWords;

function requireWords () {
	if (hasRequiredWords) return words_1;
	hasRequiredWords = 1;
	var asciiWords = require_asciiWords(),
	    hasUnicodeWord = require_hasUnicodeWord(),
	    toString = requireToString(),
	    unicodeWords = require_unicodeWords();

	/**
	 * Splits `string` into an array of its words.
	 *
	 * @static
	 * @memberOf _
	 * @since 3.0.0
	 * @category String
	 * @param {string} [string=''] The string to inspect.
	 * @param {RegExp|string} [pattern] The pattern to match words.
	 * @param- {Object} [guard] Enables use as an iteratee for methods like `_.map`.
	 * @returns {Array} Returns the words of `string`.
	 * @example
	 *
	 * _.words('fred, barney, & pebbles');
	 * // => ['fred', 'barney', 'pebbles']
	 *
	 * _.words('fred, barney, & pebbles', /[^, ]+/g);
	 * // => ['fred', 'barney', '&', 'pebbles']
	 */
	function words(string, pattern, guard) {
	  string = toString(string);
	  pattern = guard ? undefined : pattern;

	  if (pattern === undefined) {
	    return hasUnicodeWord(string) ? unicodeWords(string) : asciiWords(string);
	  }
	  return string.match(pattern) || [];
	}

	words_1 = words;
	return words_1;
}

export { requireWords as __require };
//# sourceMappingURL=words.js.map
