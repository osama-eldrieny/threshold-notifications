"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _baseIsEqual = require('./_baseIsEqual.cjs');
const get = require('./get.cjs');
const hasIn = require('./hasIn.cjs');
const _isKey = require('./_isKey.cjs');
const _isStrictComparable = require('./_isStrictComparable.cjs');
const _matchesStrictComparable = require('./_matchesStrictComparable.cjs');
const _toKey = require('./_toKey.cjs');

var _baseMatchesProperty;
var hasRequired_baseMatchesProperty;

function require_baseMatchesProperty () {
	if (hasRequired_baseMatchesProperty) return _baseMatchesProperty;
	hasRequired_baseMatchesProperty = 1;
	var baseIsEqual = _baseIsEqual.__require(),
	    get$1 = get.__require(),
	    hasIn$1 = hasIn.__require(),
	    isKey = _isKey.__require(),
	    isStrictComparable = _isStrictComparable.__require(),
	    matchesStrictComparable = _matchesStrictComparable.__require(),
	    toKey = _toKey.__require();

	/** Used to compose bitmasks for value comparisons. */
	var COMPARE_PARTIAL_FLAG = 1,
	    COMPARE_UNORDERED_FLAG = 2;

	/**
	 * The base implementation of `_.matchesProperty` which doesn't clone `srcValue`.
	 *
	 * @private
	 * @param {string} path The path of the property to get.
	 * @param {*} srcValue The value to match.
	 * @returns {Function} Returns the new spec function.
	 */
	function baseMatchesProperty(path, srcValue) {
	  if (isKey(path) && isStrictComparable(srcValue)) {
	    return matchesStrictComparable(toKey(path), srcValue);
	  }
	  return function(object) {
	    var objValue = get$1(object, path);
	    return (objValue === undefined && objValue === srcValue)
	      ? hasIn$1(object, path)
	      : baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG);
	  };
	}

	_baseMatchesProperty = baseMatchesProperty;
	return _baseMatchesProperty;
}

exports.__require = require_baseMatchesProperty;
//# sourceMappingURL=_baseMatchesProperty.cjs.map
