"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _arrayReduce = require('./_arrayReduce.cjs');
const deburr = require('./deburr.cjs');
const words = require('./words.cjs');

var _createCompounder;
var hasRequired_createCompounder;

function require_createCompounder () {
	if (hasRequired_createCompounder) return _createCompounder;
	hasRequired_createCompounder = 1;
	var arrayReduce = _arrayReduce.__require(),
	    deburr$1 = deburr.__require(),
	    words$1 = words.__require();

	/** Used to compose unicode capture groups. */
	var rsApos = "['\u2019]";

	/** Used to match apostrophes. */
	var reApos = RegExp(rsApos, 'g');

	/**
	 * Creates a function like `_.camelCase`.
	 *
	 * @private
	 * @param {Function} callback The function to combine each word.
	 * @returns {Function} Returns the new compounder function.
	 */
	function createCompounder(callback) {
	  return function(string) {
	    return arrayReduce(words$1(deburr$1(string).replace(reApos, '')), callback, '');
	  };
	}

	_createCompounder = createCompounder;
	return _createCompounder;
}

exports.__require = require_createCompounder;
//# sourceMappingURL=_createCompounder.cjs.map
