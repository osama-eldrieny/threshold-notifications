"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _baseMatches = require('./_baseMatches.cjs');
const _baseMatchesProperty = require('./_baseMatchesProperty.cjs');
const identity = require('./identity.cjs');
const isArray = require('./isArray.cjs');
const property = require('./property.cjs');

var _baseIteratee;
var hasRequired_baseIteratee;

function require_baseIteratee () {
	if (hasRequired_baseIteratee) return _baseIteratee;
	hasRequired_baseIteratee = 1;
	var baseMatches = _baseMatches.__require(),
	    baseMatchesProperty = _baseMatchesProperty.__require(),
	    identity$1 = identity.__require(),
	    isArray$1 = isArray.__require(),
	    property$1 = property.__require();

	/**
	 * The base implementation of `_.iteratee`.
	 *
	 * @private
	 * @param {*} [value=_.identity] The value to convert to an iteratee.
	 * @returns {Function} Returns the iteratee.
	 */
	function baseIteratee(value) {
	  // Don't store the `typeof` result in a variable to avoid a JIT bug in Safari 9.
	  // See https://bugs.webkit.org/show_bug.cgi?id=156034 for more details.
	  if (typeof value == 'function') {
	    return value;
	  }
	  if (value == null) {
	    return identity$1;
	  }
	  if (typeof value == 'object') {
	    return isArray$1(value)
	      ? baseMatchesProperty(value[0], value[1])
	      : baseMatches(value);
	  }
	  return property$1(value);
	}

	_baseIteratee = baseIteratee;
	return _baseIteratee;
}

exports.__require = require_baseIteratee;
//# sourceMappingURL=_baseIteratee.cjs.map
