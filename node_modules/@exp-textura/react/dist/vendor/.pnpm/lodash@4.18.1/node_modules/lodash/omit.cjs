"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _arrayMap = require('./_arrayMap.cjs');
const _baseClone = require('./_baseClone.cjs');
const _baseUnset = require('./_baseUnset.cjs');
const _castPath = require('./_castPath.cjs');
const _copyObject = require('./_copyObject.cjs');
const _customOmitClone = require('./_customOmitClone.cjs');
const _flatRest = require('./_flatRest.cjs');
const _getAllKeysIn = require('./_getAllKeysIn.cjs');

var omit_1;
var hasRequiredOmit;

function requireOmit () {
	if (hasRequiredOmit) return omit_1;
	hasRequiredOmit = 1;
	var arrayMap = _arrayMap.__require(),
	    baseClone = _baseClone.__require(),
	    baseUnset = _baseUnset.__require(),
	    castPath = _castPath.__require(),
	    copyObject = _copyObject.__require(),
	    customOmitClone = _customOmitClone.__require(),
	    flatRest = _flatRest.__require(),
	    getAllKeysIn = _getAllKeysIn.__require();

	/** Used to compose bitmasks for cloning. */
	var CLONE_DEEP_FLAG = 1,
	    CLONE_FLAT_FLAG = 2,
	    CLONE_SYMBOLS_FLAG = 4;

	/**
	 * The opposite of `_.pick`; this method creates an object composed of the
	 * own and inherited enumerable property paths of `object` that are not omitted.
	 *
	 * **Note:** This method is considerably slower than `_.pick`.
	 *
	 * @static
	 * @since 0.1.0
	 * @memberOf _
	 * @category Object
	 * @param {Object} object The source object.
	 * @param {...(string|string[])} [paths] The property paths to omit.
	 * @returns {Object} Returns the new object.
	 * @example
	 *
	 * var object = { 'a': 1, 'b': '2', 'c': 3 };
	 *
	 * _.omit(object, ['a', 'c']);
	 * // => { 'b': '2' }
	 */
	var omit = flatRest(function(object, paths) {
	  var result = {};
	  if (object == null) {
	    return result;
	  }
	  var isDeep = false;
	  paths = arrayMap(paths, function(path) {
	    path = castPath(path, object);
	    isDeep || (isDeep = path.length > 1);
	    return path;
	  });
	  copyObject(object, getAllKeysIn(object), result);
	  if (isDeep) {
	    result = baseClone(result, CLONE_DEEP_FLAG | CLONE_FLAT_FLAG | CLONE_SYMBOLS_FLAG, customOmitClone);
	  }
	  var length = paths.length;
	  while (length--) {
	    baseUnset(result, paths[length]);
	  }
	  return result;
	});

	omit_1 = omit;
	return omit_1;
}

exports.__require = requireOmit;
//# sourceMappingURL=omit.cjs.map
