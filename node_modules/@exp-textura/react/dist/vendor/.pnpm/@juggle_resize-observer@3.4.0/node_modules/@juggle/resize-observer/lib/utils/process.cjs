"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const hasActiveObservations = require('../algorithms/hasActiveObservations.cjs');
const hasSkippedObservations = require('../algorithms/hasSkippedObservations.cjs');
const deliverResizeLoopError = require('../algorithms/deliverResizeLoopError.cjs');
const broadcastActiveObservations = require('../algorithms/broadcastActiveObservations.cjs');
const gatherActiveObservationsAtDepth = require('../algorithms/gatherActiveObservationsAtDepth.cjs');

var process = function () {
    var depth = 0;
    gatherActiveObservationsAtDepth.gatherActiveObservationsAtDepth(depth);
    while (hasActiveObservations.hasActiveObservations()) {
        depth = broadcastActiveObservations.broadcastActiveObservations();
        gatherActiveObservationsAtDepth.gatherActiveObservationsAtDepth(depth);
    }
    if (hasSkippedObservations.hasSkippedObservations()) {
        deliverResizeLoopError.deliverResizeLoopError();
    }
    return depth > 0;
};

exports.process = process;
//# sourceMappingURL=process.cjs.map
