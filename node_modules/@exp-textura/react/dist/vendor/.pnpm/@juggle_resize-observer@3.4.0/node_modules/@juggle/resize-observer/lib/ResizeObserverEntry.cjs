"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const calculateBoxSize = require('./algorithms/calculateBoxSize.cjs');
const freeze = require('./utils/freeze.cjs');

var ResizeObserverEntry = (function () {
    function ResizeObserverEntry(target) {
        var boxes = calculateBoxSize.calculateBoxSizes(target);
        this.target = target;
        this.contentRect = boxes.contentRect;
        this.borderBoxSize = freeze.freeze([boxes.borderBoxSize]);
        this.contentBoxSize = freeze.freeze([boxes.contentBoxSize]);
        this.devicePixelContentBoxSize = freeze.freeze([boxes.devicePixelContentBoxSize]);
    }
    return ResizeObserverEntry;
}());

exports.ResizeObserverEntry = ResizeObserverEntry;
//# sourceMappingURL=ResizeObserverEntry.cjs.map
