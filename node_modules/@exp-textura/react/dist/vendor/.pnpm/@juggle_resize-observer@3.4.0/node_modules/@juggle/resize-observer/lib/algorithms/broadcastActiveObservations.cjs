"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const resizeObservers = require('../utils/resizeObservers.cjs');
const ResizeObserverEntry = require('../ResizeObserverEntry.cjs');
const calculateDepthForNode = require('./calculateDepthForNode.cjs');
const calculateBoxSize = require('./calculateBoxSize.cjs');

var broadcastActiveObservations = function () {
    var shallowestDepth = Infinity;
    var callbacks = [];
    resizeObservers.resizeObservers.forEach(function processObserver(ro) {
        if (ro.activeTargets.length === 0) {
            return;
        }
        var entries = [];
        ro.activeTargets.forEach(function processTarget(ot) {
            var entry = new ResizeObserverEntry.ResizeObserverEntry(ot.target);
            var targetDepth = calculateDepthForNode.calculateDepthForNode(ot.target);
            entries.push(entry);
            ot.lastReportedSize = calculateBoxSize.calculateBoxSize(ot.target, ot.observedBox);
            if (targetDepth < shallowestDepth) {
                shallowestDepth = targetDepth;
            }
        });
        callbacks.push(function resizeObserverCallback() {
            ro.callback.call(ro.observer, entries, ro.observer);
        });
        ro.activeTargets.splice(0, ro.activeTargets.length);
    });
    for (var _i = 0, callbacks_1 = callbacks; _i < callbacks_1.length; _i++) {
        var callback = callbacks_1[_i];
        callback();
    }
    return shallowestDepth;
};

exports.broadcastActiveObservations = broadcastActiveObservations;
//# sourceMappingURL=broadcastActiveObservations.cjs.map
