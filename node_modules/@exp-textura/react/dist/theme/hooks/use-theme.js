"use client";
import { useTheme } from '@emotion/react';
import defaultTheme from '../bundled-themes/default.js';

const useTexturaTheme = () => {
  const theme = useTheme();
  if (Object.keys(theme).length === 0) {
    return defaultTheme;
  } else {
    return theme;
  }
};

export { useTexturaTheme as default, useTexturaTheme };
//# sourceMappingURL=use-theme.js.map
