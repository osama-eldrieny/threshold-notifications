import { jsx, jsxs } from 'react/jsx-runtime';
import { CacheProvider, ThemeProvider, Global, css, ClassNames } from '@emotion/react';
import React, { useMemo, useState, useEffect } from 'react';
import { DirectionProvider } from '@radix-ui/react-direction';
import defaultTheme from '../bundled-themes/default.js';
import { createTexturaCache } from '../utils/create-emotion-cache.js';

const useHydrated = () => {
  const [hydrated, setHydrated] = useState(false);
  useEffect(() => {
    const timeoutId = setTimeout(() => {
      setHydrated(true);
    }, 0);
    return () => clearTimeout(timeoutId);
  }, []);
  return hydrated;
};
const TexturaThemeProvider = ({
  applyThemeScopedStyles = true,
  as: Component,
  dir: propDir,
  emotionCache: propCache,
  theme = defaultTheme,
  children,
  hoistCSSVars = false,
  hoistThemeScopedStyles = false
}) => {
  const themeScopedStyles = useMemo(() => theme.themeScopedStyles, [theme]), isThemeScopedStylesString = typeof themeScopedStyles === "string", cssVars = theme.cssVariables;
  const hydrated = useHydrated();
  const effectiveDir = useMemo(() => {
    if (propDir) {
      return propDir;
    }
    if (hydrated && typeof window !== "undefined") {
      const documentDir = document?.documentElement.dir;
      if (documentDir === "ltr" || documentDir === "rtl") {
        return documentDir;
      }
    }
    return "ltr";
  }, [propDir, hydrated]);
  const emotionCache = useMemo(() => propCache || createTexturaCache(), [propCache]);
  Component = !Component ? "div" : Component;
  return /* @__PURE__ */ jsx(DirectionProvider, { dir: effectiveDir, children: /* @__PURE__ */ jsx(CacheProvider, { value: emotionCache, children: /* @__PURE__ */ jsxs(ThemeProvider, { theme, children: [
    hoistThemeScopedStyles && themeScopedStyles && applyThemeScopedStyles && /* @__PURE__ */ jsx(
      Global,
      {
        styles: isThemeScopedStylesString ? css`
                      ${themeScopedStyles}
                    ` : css({
          ":root": { ...themeScopedStyles }
        })
      }
    ),
    hoistCSSVars && cssVars && /* @__PURE__ */ jsx(
      Global,
      {
        styles: css`
                :root {
                  ${cssVars}
                }
              `
      }
    ),
    /* @__PURE__ */ jsx(ClassNames, { children: ({ css: inlineCss, cx }) => {
      const cssVarsCss = inlineCss`
            ${cssVars}
          `;
      const themeScopedStylesCss = isThemeScopedStylesString ? inlineCss`
              ${themeScopedStyles}
            ` : inlineCss({ ...themeScopedStyles });
      if (Component === React.Fragment) {
        return /* @__PURE__ */ jsx(Component, { children });
      }
      return /* @__PURE__ */ jsx(
        Component,
        {
          className: cx(cssVarsCss, applyThemeScopedStyles && themeScopedStylesCss),
          "data-textura-theme-provider": "true",
          dir: effectiveDir,
          children
        }
      );
    } })
  ] }) }) });
};
TexturaThemeProvider.displayName = "ThemeProvider";

export { TexturaThemeProvider as default };
//# sourceMappingURL=theme-provider.js.map
