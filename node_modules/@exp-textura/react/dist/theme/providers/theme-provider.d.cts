import { Theme } from '../../types';
import { EmotionCache } from '@emotion/react';
import { default as React } from 'react';
export interface ThemeProviderProps {
    applyThemeScopedStyles?: boolean;
    as?: React.ElementType;
    children?: React.ReactNode;
    dir?: "ltr" | "rtl";
    emotionCache?: EmotionCache;
    hoistCSSVars?: boolean;
    hoistThemeScopedStyles?: boolean;
    theme?: Theme;
}
declare const TexturaThemeProvider: {
    ({ applyThemeScopedStyles, as: Component, dir: propDir, emotionCache: propCache, theme, children, hoistCSSVars, hoistThemeScopedStyles, }: ThemeProviderProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
export default TexturaThemeProvider;
//# sourceMappingURL=theme-provider.d.ts.map