"use client";
import defaultTheme from '../bundled-themes/default.js';
import { handleThemeMerge } from './create-theme-without-default.js';

function createTheme(themeOrConfig, newConfig) {
  if (newConfig !== void 0) {
    const baseTheme = themeOrConfig;
    const customTheme2 = newConfig;
    return handleThemeMerge(baseTheme, customTheme2);
  }
  const customTheme = themeOrConfig;
  return handleThemeMerge(defaultTheme, customTheme);
}

export { createTheme };
//# sourceMappingURL=create-theme.js.map
