"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const _default = require('../bundled-themes/default.cjs');
const createThemeWithoutDefault = require('./create-theme-without-default.cjs');

function createTheme(themeOrConfig, newConfig) {
  if (newConfig !== void 0) {
    const baseTheme = themeOrConfig;
    const customTheme2 = newConfig;
    return createThemeWithoutDefault.handleThemeMerge(baseTheme, customTheme2);
  }
  const customTheme = themeOrConfig;
  return createThemeWithoutDefault.handleThemeMerge(_default.default, customTheme);
}

exports.createTheme = createTheme;
//# sourceMappingURL=create-theme.cjs.map
