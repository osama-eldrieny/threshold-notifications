"use client";
import { themes } from '../bundled-themes/index.js';

const getTheme = (themeName) => {
  const selectedTheme = themes[themeName] || themes.default;
  return selectedTheme();
};

export { getTheme as default, getTheme };
//# sourceMappingURL=get-theme.js.map
