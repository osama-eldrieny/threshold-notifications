"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { IconButtonClasses, StyledIconButton } from './icon-button.styles.js';

const IconButton = forwardRef(
  ({
    className,
    variant = "default",
    buttonType = "primary",
    size = "default",
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(
      IconButtonClasses.root,
      {
        [IconButtonClasses.secondary]: buttonType === "secondary",
        [IconButtonClasses.sm]: size === "sm"
      },
      className
    );
    return /* @__PURE__ */ jsx(
      StyledIconButton,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props
      }
    );
  }
);
IconButton.displayName = "IconButton";

export { IconButton as default };
//# sourceMappingURL=icon-button.js.map
