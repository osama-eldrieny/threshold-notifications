"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const icons = require('@exp-textura/icons');
const iconButton = require('../icon-button/icon-button.cjs');
const alert_styles = require('./alert.styles.cjs');
const useAlert = require('./hooks/use-alert.cjs');

const variantIconMap = {
  default: icons.Comment,
  critical: icons.Attention,
  info: icons.Info,
  neutral: icons.Star,
  success: icons.Success,
  warning: icons.Warning
};
const AlertRoot = React.forwardRef(
  ({
    className,
    variant = "default",
    treatment = "bold",
    type = "edge",
    onClose,
    show: showProp = true,
    children,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const [show, setShow] = React.useState(showProp);
    const rootClasses = clsx$1.clsx(
      alert_styles.AlertClasses.root,
      alert_styles.AlertClasses[type],
      {
        [alert_styles.AlertClasses.subtle]: treatment === "subtle"
      },
      className
    );
    const handleClose = React.useCallback(() => {
      setShow(false);
      onClose?.();
    }, [onClose]);
    React.useEffect(() => {
      setShow(showProp);
      if (!showProp) {
        onClose?.();
      }
    }, [onClose, showProp]);
    const contextValue = React.useMemo(
      () => ({ handleClose, variant }),
      [handleClose, variant]
    );
    if (!show) return null;
    return /* @__PURE__ */ jsxRuntime.jsx(useAlert.AlertContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsxRuntime.jsx(
      alert_styles.StyledAlert,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children
      }
    ) });
  }
);
AlertRoot.displayName = "Alert";
const Title = ({ children, className }) => /* @__PURE__ */ jsxRuntime.jsx("p", { className: clsx$1.clsx(alert_styles.AlertClasses.title, className), children });
Title.displayName = "Alert.Title";
const Description = ({
  children,
  className
}) => /* @__PURE__ */ jsxRuntime.jsx("p", { className: clsx$1.clsx(alert_styles.AlertClasses.description, className), children });
Description.displayName = "Alert.Description";
const Content = ({ children, className }) => /* @__PURE__ */ jsxRuntime.jsx("div", { "aria-atomic": "true", className: clsx$1.clsx(alert_styles.AlertClasses.content, className), role: "alert", children });
Content.displayName = "Alert.Content";
const Icon = ({ children, className }) => {
  const { variant } = useAlert.useAlert();
  const iconClasses = clsx$1.clsx(alert_styles.AlertClasses.icon, className);
  if (children && React.isValidElement(children)) {
    return React.cloneElement(children, {
      className: clsx$1.clsx(iconClasses, children.props.className)
    });
  }
  const VariantIcon = variantIconMap[variant] ?? icons.Info;
  return /* @__PURE__ */ jsxRuntime.jsx(VariantIcon, { className: iconClasses });
};
Icon.displayName = "Alert.Icon";
const CloseIconButton = ({
  children,
  className,
  "aria-label": customAriaLabel
}) => {
  const { handleClose } = useAlert.useAlert();
  const buttonClasses = clsx$1.clsx(alert_styles.AlertClasses.closeIconButton, className);
  const commonProps = {
    "aria-label": customAriaLabel || "close current alert"
  };
  return /* @__PURE__ */ jsxRuntime.jsx(
    iconButton.default,
    {
      buttonType: "secondary",
      className: buttonClasses,
      size: "sm",
      variant: "inverse",
      onClick: handleClose,
      ...commonProps,
      children: children ? children : /* @__PURE__ */ jsxRuntime.jsx(icons.Close, {})
    }
  );
};
CloseIconButton.displayName = "Alert.CloseIconButton";
const Alert = Object.assign(AlertRoot, {
  Title,
  Description,
  Content,
  Icon,
  CloseIconButton
});

exports.default = Alert;
//# sourceMappingURL=alert.cjs.map
