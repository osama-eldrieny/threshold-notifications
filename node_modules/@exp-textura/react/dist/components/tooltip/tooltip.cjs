"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const react = require('@floating-ui/react');
const tooltip_styles = require('./tooltip.styles.cjs');
const useTooltip = require('./hooks/use-tooltip.cjs');

const TooltipRoot = ({
  children,
  arrow = true,
  position = "top",
  enterDelay = 0,
  leaveDelay = 0,
  open: controlledOpen,
  onOpenChange,
  offset = 8,
  showOnHover = true,
  variant = "default",
  styleSlots
}) => {
  const hookOptions = {
    position,
    enterDelay,
    leaveDelay,
    open: controlledOpen,
    onOpenChange,
    showOnHover,
    arrow,
    variant,
    offset,
    styleSlots
  };
  const contextValue = useTooltip.useTooltip(hookOptions);
  return /* @__PURE__ */ jsxRuntime.jsx(useTooltip.TooltipContext.Provider, { value: contextValue, children });
};
TooltipRoot.displayName = "Tooltip";
const Trigger = React.forwardRef(
  ({ children, className, ...triggerProps }, forwardedRef) => {
    const theme = useTheme.default();
    const {
      getReferenceProps,
      refs,
      currentOpenState,
      styleSlots: contextStyleSlots,
      variant
    } = useTooltip.useTooltipContext();
    const childrenArray = React.Children.toArray(children);
    if (childrenArray.length !== 1 || !React.isValidElement(childrenArray[0])) {
      throw new Error("Tooltip.Trigger requires a single valid React element as its child.");
    }
    const childElement = childrenArray[0];
    const setRef = React.useCallback(
      (node) => {
        refs.setReference(node);
        if (typeof forwardedRef === "function") {
          forwardedRef(node);
        } else if (forwardedRef) {
          forwardedRef.current = node;
        }
      },
      [refs, forwardedRef]
    );
    const referenceProps = getReferenceProps({
      ref: setRef
    });
    const ariaDescribedBy = referenceProps["aria-describedby"];
    referenceProps["aria-describedby"] = void 0;
    return /* @__PURE__ */ jsxRuntime.jsx(
      tooltip_styles.StyledTooltip,
      {
        className: tooltip_styles.TooltipClasses.triggerWrapper,
        currentTheme: theme,
        styleSlots: contextStyleSlots,
        variant,
        ...referenceProps,
        children: React.cloneElement(childElement, {
          ...childElement.props,
          className: clsx$1.clsx(childElement.props.className, className),
          "data-tooltip-open": currentOpenState ? "true" : "false",
          ["aria-describedby"]: ariaDescribedBy || void 0,
          ...triggerProps
        })
      }
    );
  }
);
Trigger.displayName = "Tooltip.Trigger";
const Content = React.forwardRef(
  ({ children, className, style, ...contentProps }, forwardedRef) => {
    const {
      refs,
      floatingStyles,
      floatingContext,
      getFloatingProps,
      isMounted,
      showArrow,
      arrowRef,
      actualPlacement,
      styles,
      middlewareData,
      variant,
      styleSlots: contextStyleSlots
    } = useTooltip.useTooltipContext();
    const theme = useTheme.default();
    if (!isMounted) return null;
    const { referenceHidden } = middlewareData.hide || {};
    return /* @__PURE__ */ jsxRuntime.jsx(react.FloatingPortal, { children: /* @__PURE__ */ jsxRuntime.jsx(
      "div",
      {
        ref: refs.setFloating,
        style: {
          zIndex: theme?.zIndex?.dynamic,
          ...floatingStyles,
          ...styles,
          ...style || {},
          visibility: referenceHidden ? "hidden" : "visible"
        },
        ...getFloatingProps(contentProps),
        children: /* @__PURE__ */ jsxRuntime.jsxs(
          tooltip_styles.StyledTooltip,
          {
            ref: forwardedRef,
            className: clsx$1.clsx(tooltip_styles.TooltipClasses.root, className),
            currentTheme: theme,
            styleSlots: contextStyleSlots,
            variant,
            children: [
              showArrow && /* @__PURE__ */ jsxRuntime.jsx(
                react.FloatingArrow,
                {
                  ref: arrowRef,
                  className: clsx$1.clsx(tooltip_styles.TooltipClasses.arrow, contextStyleSlots?.arrow, {
                    [tooltip_styles.TooltipClasses.arrowTop]: actualPlacement?.includes("top"),
                    [tooltip_styles.TooltipClasses.arrowBottom]: actualPlacement?.includes("bottom"),
                    [tooltip_styles.TooltipClasses.arrowLeft]: actualPlacement?.includes("left"),
                    [tooltip_styles.TooltipClasses.arrowRight]: actualPlacement?.includes("right")
                  }),
                  context: floatingContext,
                  "data-testid": "arrow"
                }
              ),
              /* @__PURE__ */ jsxRuntime.jsx("span", { className: clsx$1.clsx(tooltip_styles.TooltipClasses.content, contextStyleSlots?.content), children })
            ]
          }
        )
      }
    ) });
  }
);
Content.displayName = "Tooltip.Content";
const Hotkey = React.forwardRef(
  ({ children, className, ...props }, forwardedRef) => /* @__PURE__ */ jsxRuntime.jsx("span", { ref: forwardedRef, className: clsx$1.clsx(tooltip_styles.TooltipClasses.hotkey, className), ...props, children })
);
Hotkey.displayName = "Tooltip.Hotkey";
const Tooltip = Object.assign(TooltipRoot, {
  Trigger,
  Content,
  Hotkey
});

exports.default = Tooltip;
//# sourceMappingURL=tooltip.cjs.map
