"use client";
import { jsx, jsxs } from 'react/jsx-runtime';
import React, { forwardRef, useCallback, cloneElement } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { FloatingPortal, FloatingArrow } from '@floating-ui/react';
import { TooltipClasses, StyledTooltip } from './tooltip.styles.js';
import { useTooltip, TooltipContext, useTooltipContext } from './hooks/use-tooltip.js';

const TooltipRoot = ({
  children,
  arrow = true,
  position = "top",
  enterDelay = 0,
  leaveDelay = 0,
  open: controlledOpen,
  onOpenChange,
  offset = 8,
  showOnHover = true,
  variant = "default",
  styleSlots
}) => {
  const hookOptions = {
    position,
    enterDelay,
    leaveDelay,
    open: controlledOpen,
    onOpenChange,
    showOnHover,
    arrow,
    variant,
    offset,
    styleSlots
  };
  const contextValue = useTooltip(hookOptions);
  return /* @__PURE__ */ jsx(TooltipContext.Provider, { value: contextValue, children });
};
TooltipRoot.displayName = "Tooltip";
const Trigger = forwardRef(
  ({ children, className, ...triggerProps }, forwardedRef) => {
    const theme = useTexturaTheme();
    const {
      getReferenceProps,
      refs,
      currentOpenState,
      styleSlots: contextStyleSlots,
      variant
    } = useTooltipContext();
    const childrenArray = React.Children.toArray(children);
    if (childrenArray.length !== 1 || !React.isValidElement(childrenArray[0])) {
      throw new Error("Tooltip.Trigger requires a single valid React element as its child.");
    }
    const childElement = childrenArray[0];
    const setRef = useCallback(
      (node) => {
        refs.setReference(node);
        if (typeof forwardedRef === "function") {
          forwardedRef(node);
        } else if (forwardedRef) {
          forwardedRef.current = node;
        }
      },
      [refs, forwardedRef]
    );
    const referenceProps = getReferenceProps({
      ref: setRef
    });
    const ariaDescribedBy = referenceProps["aria-describedby"];
    referenceProps["aria-describedby"] = void 0;
    return /* @__PURE__ */ jsx(
      StyledTooltip,
      {
        className: TooltipClasses.triggerWrapper,
        currentTheme: theme,
        styleSlots: contextStyleSlots,
        variant,
        ...referenceProps,
        children: cloneElement(childElement, {
          ...childElement.props,
          className: clsx(childElement.props.className, className),
          "data-tooltip-open": currentOpenState ? "true" : "false",
          ["aria-describedby"]: ariaDescribedBy || void 0,
          ...triggerProps
        })
      }
    );
  }
);
Trigger.displayName = "Tooltip.Trigger";
const Content = forwardRef(
  ({ children, className, style, ...contentProps }, forwardedRef) => {
    const {
      refs,
      floatingStyles,
      floatingContext,
      getFloatingProps,
      isMounted,
      showArrow,
      arrowRef,
      actualPlacement,
      styles,
      middlewareData,
      variant,
      styleSlots: contextStyleSlots
    } = useTooltipContext();
    const theme = useTexturaTheme();
    if (!isMounted) return null;
    const { referenceHidden } = middlewareData.hide || {};
    return /* @__PURE__ */ jsx(FloatingPortal, { children: /* @__PURE__ */ jsx(
      "div",
      {
        ref: refs.setFloating,
        style: {
          zIndex: theme?.zIndex?.dynamic,
          ...floatingStyles,
          ...styles,
          ...style || {},
          visibility: referenceHidden ? "hidden" : "visible"
        },
        ...getFloatingProps(contentProps),
        children: /* @__PURE__ */ jsxs(
          StyledTooltip,
          {
            ref: forwardedRef,
            className: clsx(TooltipClasses.root, className),
            currentTheme: theme,
            styleSlots: contextStyleSlots,
            variant,
            children: [
              showArrow && /* @__PURE__ */ jsx(
                FloatingArrow,
                {
                  ref: arrowRef,
                  className: clsx(TooltipClasses.arrow, contextStyleSlots?.arrow, {
                    [TooltipClasses.arrowTop]: actualPlacement?.includes("top"),
                    [TooltipClasses.arrowBottom]: actualPlacement?.includes("bottom"),
                    [TooltipClasses.arrowLeft]: actualPlacement?.includes("left"),
                    [TooltipClasses.arrowRight]: actualPlacement?.includes("right")
                  }),
                  context: floatingContext,
                  "data-testid": "arrow"
                }
              ),
              /* @__PURE__ */ jsx("span", { className: clsx(TooltipClasses.content, contextStyleSlots?.content), children })
            ]
          }
        )
      }
    ) });
  }
);
Content.displayName = "Tooltip.Content";
const Hotkey = forwardRef(
  ({ children, className, ...props }, forwardedRef) => /* @__PURE__ */ jsx("span", { ref: forwardedRef, className: clsx(TooltipClasses.hotkey, className), ...props, children })
);
Hotkey.displayName = "Tooltip.Hotkey";
const Tooltip = Object.assign(TooltipRoot, {
  Trigger,
  Content,
  Hotkey
});

export { Tooltip as default };
//# sourceMappingURL=tooltip.js.map
