"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const combobox = require('../combobox/combobox.cjs');
const iconButton = require('../icon-button/icon-button.cjs');
const icons = require('@exp-textura/icons');
const search_styles = require('./search.styles.cjs');

const SearchRoot = React.forwardRef(
  ({
    className,
    variant = "default",
    styleSlots,
    children,
    showSelectedIndicator = false,
    openOnClick = false,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(search_styles.SearchClasses.root, className);
    return /* @__PURE__ */ jsxRuntime.jsx(
      search_styles.StyledSearch,
      {
        ref,
        as: search_styles.StyledSearch,
        className: rootClasses,
        currentTheme: theme,
        openOnClick,
        showSelectedIndicator,
        styleSlots,
        variant,
        ...props,
        children
      }
    );
  }
);
SearchRoot.displayName = "Search";
const SearchInput = React.forwardRef(
  (props, ref) => {
    const { className, ...rest } = props;
    const inputClasses = clsx$1.clsx(search_styles.SearchClasses.input, className);
    return /* @__PURE__ */ jsxRuntime.jsx(combobox.default.Input, { ref, className: inputClasses, ...rest });
  }
);
SearchInput.displayName = "Search.Input";
const SearchInputPrefix = React.forwardRef(
  (props, ref) => {
    const { className, ...rest } = props;
    const prefixClasses = clsx$1.clsx(search_styles.SearchClasses.prefix, className);
    return /* @__PURE__ */ jsxRuntime.jsx(combobox.default.InputPrefix, { ref, className: prefixClasses, ...rest });
  }
);
SearchInputPrefix.displayName = "Search.InputPrefix";
const SearchInputSuffix = React.forwardRef(
  (props, ref) => {
    const { className, ...rest } = props;
    const suffixClasses = clsx$1.clsx(search_styles.SearchClasses.suffix, className);
    return /* @__PURE__ */ jsxRuntime.jsx(combobox.default.InputSuffix, { ref, className: suffixClasses, ...rest });
  }
);
SearchInputSuffix.displayName = "Search.InputSuffix";
const SearchContent = React.forwardRef(
  (props, ref) => {
    const { className, children, ...rest } = props;
    const contentClasses = clsx$1.clsx(search_styles.SearchClasses.content, className);
    return /* @__PURE__ */ jsxRuntime.jsx(combobox.default.Content, { ref, className: contentClasses, ...rest, children });
  }
);
SearchContent.displayName = "Search.Content";
const SearchItemContext = React.createContext({ isHovered: false });
const SearchItem = React.forwardRef((props, ref) => {
  const { className, children, ...rest } = props;
  const [isHovered, setIsHovered] = React.useState(false);
  const itemClasses = clsx$1.clsx(search_styles.SearchClasses.item, className);
  return /* @__PURE__ */ jsxRuntime.jsx(SearchItemContext.Provider, { value: { isHovered }, children: /* @__PURE__ */ jsxRuntime.jsx(
    combobox.default.Item,
    {
      ref,
      className: itemClasses,
      onMouseEnter: () => setIsHovered(true),
      onMouseLeave: () => setIsHovered(false),
      ...rest,
      children
    }
  ) });
});
SearchItem.displayName = "Search.Item";
const SearchItemRemoveIconButton = React.forwardRef(
  (props, ref) => {
    const { children, style, className, variant, onClick, ...rest } = props;
    const { isHovered } = React.useContext(SearchItemContext);
    return /* @__PURE__ */ jsxRuntime.jsx(
      iconButton.default,
      {
        ref,
        buttonType: "secondary",
        className: clsx$1.clsx(search_styles.SearchClasses.removeItemButton, className),
        size: "sm",
        style: { marginInlineStart: "auto", ...style },
        variant: isHovered ? "inverse" : variant,
        onClick: (e) => {
          e.preventDefault();
          e.stopPropagation();
          onClick?.(e);
        },
        onPointerDown: (e) => e.preventDefault(),
        ...rest,
        children: children || /* @__PURE__ */ jsxRuntime.jsx(icons.Clear, {})
      }
    );
  }
);
SearchItemRemoveIconButton.displayName = "Search.ItemRemoveIconButton";
const SearchGroup = React.forwardRef(
  (props, ref) => {
    const { className, ...rest } = props;
    return /* @__PURE__ */ jsxRuntime.jsx(combobox.default.Group, { ref, className: clsx$1.clsx(search_styles.SearchClasses.group, className), ...rest });
  }
);
SearchGroup.displayName = "Search.Group";
const SearchLabel = React.forwardRef(
  (props, ref) => {
    const { className, ...rest } = props;
    return /* @__PURE__ */ jsxRuntime.jsx(combobox.default.Label, { ref, className: clsx$1.clsx(search_styles.SearchClasses.groupLabel, className), ...rest });
  }
);
SearchLabel.displayName = "Search.Label";
const SearchDivider = React.forwardRef(
  (props, ref) => {
    const { className, ...rest } = props;
    return /* @__PURE__ */ jsxRuntime.jsx(combobox.default.Divider, { ref, className: clsx$1.clsx(search_styles.SearchClasses.divider, className), ...rest });
  }
);
SearchDivider.displayName = "Search.Divider";
const SearchClearIconButton = ({ className, ...props }) => {
  return /* @__PURE__ */ jsxRuntime.jsx(
    combobox.default.ClearIconButton,
    {
      className: clsx$1.clsx(search_styles.SearchClasses.clearIconButton, className),
      ...props
    }
  );
};
SearchClearIconButton.displayName = "Search.ClearIconButton";
const Search = Object.assign(SearchRoot, {
  Input: SearchInput,
  InputPrefix: SearchInputPrefix,
  InputSuffix: SearchInputSuffix,
  Content: SearchContent,
  Item: SearchItem,
  ItemRemoveIconButton: SearchItemRemoveIconButton,
  Group: SearchGroup,
  Label: SearchLabel,
  Divider: SearchDivider,
  ClearIconButton: SearchClearIconButton
});

exports.default = Search;
//# sourceMappingURL=search.cjs.map
