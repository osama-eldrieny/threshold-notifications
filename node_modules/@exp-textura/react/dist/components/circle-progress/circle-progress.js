"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { CircleProgressClasses, StyledCircleProgress } from './circle-progress.styles.js';

const CircleProgress = forwardRef(
  ({ className, variant = "default", max = 100, value, ...props }, ref) => {
    const theme = useTexturaTheme();
    const isValueDefined = value != null;
    const rootClasses = clsx(CircleProgressClasses.root, className);
    const radius = 20;
    const circumference = 2 * Math.PI * radius;
    const dashOffset = isValueDefined ? circumference - value / max * circumference : void 0;
    return /* @__PURE__ */ jsx(
      StyledCircleProgress,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        "aria-label": void 0,
        children: /* @__PURE__ */ jsx(
          "div",
          {
            "aria-label": props["aria-label"],
            "aria-valuemax": max,
            "aria-valuemin": 0,
            className: clsx({
              [CircleProgressClasses.indeterminate]: !isValueDefined,
              [CircleProgressClasses.determinate]: isValueDefined
            }),
            role: "progressbar",
            ...isValueDefined && { "aria-valuenow": value },
            children: /* @__PURE__ */ jsx("svg", { viewBox: "22 22 44 44", children: /* @__PURE__ */ jsx(
              "circle",
              {
                cx: "44",
                cy: "44",
                r: radius,
                strokeDasharray: circumference,
                strokeDashoffset: dashOffset
              }
            ) })
          }
        )
      }
    );
  }
);
CircleProgress.displayName = "CircleProgress";

export { CircleProgress as default };
//# sourceMappingURL=circle-progress.js.map
