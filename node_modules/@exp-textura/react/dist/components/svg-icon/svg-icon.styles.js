"use client";
import { getExcludedDOMProps, getStyleSlots } from '../../system/utils/component-utils.js';
import { isNullOrEmptyString } from '../../system/utils/type-validation.js';
import styled from '@emotion/styled';
import { useMemo } from 'react';

const componentName = "SvgIcon";
const SvgIconClasses = {
  root: "exp-textura_svg-icon"
};
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = useMemo(
    () => getStyleSlots(
      componentName,
      theme,
      variant,
      styleSlotsProp
      // getVariantConfig(),
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "block",
    height: !isNullOrEmptyString(props.fontSize || props.height) ? void 0 : "1rem",
    width: !isNullOrEmptyString(props.fontSize || props.width) ? void 0 : "1rem",
    color: theme.color.content.icon,
    ...styleSlots?.root
  };
};
const excludedDOMProps = getExcludedDOMProps();
const StyledSvgIcon = styled("svg", {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));

export { StyledSvgIcon, SvgIconClasses };
//# sourceMappingURL=svg-icon.styles.js.map
