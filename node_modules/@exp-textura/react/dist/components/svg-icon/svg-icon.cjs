"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const svgIcon_styles = require('./svg-icon.styles.cjs');

const SvgIcon = React.forwardRef(
  ({
    className,
    children,
    title,
    height,
    width,
    fontSize,
    desc: description,
    color,
    style,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(svgIcon_styles.SvgIconClasses.root, className);
    const ariaHidden = props["aria-labelledby"] ? void 0 : "true";
    const hasSvgAsChild = React.isValidElement(children) && children.type === "svg";
    const child = !hasSvgAsChild ? children.type?.() : children;
    return /* @__PURE__ */ jsxRuntime.jsxs(
      svgIcon_styles.StyledSvgIcon,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        ...child.props,
        "aria-hidden": ariaHidden,
        fontSize,
        height: fontSize ?? height,
        role: title ? "img" : void 0,
        style: { color, ...style },
        width: fontSize ?? width,
        ...props,
        children: [
          title && /* @__PURE__ */ jsxRuntime.jsx("title", { children: title }),
          description && /* @__PURE__ */ jsxRuntime.jsx("desc", { children: description }),
          child.props.children
        ]
      }
    );
  }
);
SvgIcon.displayName = "SvgIcon";

exports.default = SvgIcon;
//# sourceMappingURL=svg-icon.cjs.map
