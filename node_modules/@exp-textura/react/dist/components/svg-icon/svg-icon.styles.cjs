"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const componentUtils = require('../../system/utils/component-utils.cjs');
const typeValidation = require('../../system/utils/type-validation.cjs');
const styled = require('@emotion/styled');
const React = require('react');

const componentName = "SvgIcon";
const SvgIconClasses = {
  root: "exp-textura_svg-icon"
};
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = React.useMemo(
    () => componentUtils.getStyleSlots(
      componentName,
      theme,
      variant,
      styleSlotsProp
      // getVariantConfig(),
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "block",
    height: !typeValidation.isNullOrEmptyString(props.fontSize || props.height) ? void 0 : "1rem",
    width: !typeValidation.isNullOrEmptyString(props.fontSize || props.width) ? void 0 : "1rem",
    color: theme.color.content.icon,
    ...styleSlots?.root
  };
};
const excludedDOMProps = componentUtils.getExcludedDOMProps();
const StyledSvgIcon = styled("svg", {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));

exports.StyledSvgIcon = StyledSvgIcon;
exports.SvgIconClasses = SvgIconClasses;
//# sourceMappingURL=svg-icon.styles.cjs.map
