"use client";
import { jsxs, jsx } from 'react/jsx-runtime';
import React, { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { SvgIconClasses, StyledSvgIcon } from './svg-icon.styles.js';

const SvgIcon = forwardRef(
  ({
    className,
    children,
    title,
    height,
    width,
    fontSize,
    desc: description,
    color,
    style,
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(SvgIconClasses.root, className);
    const ariaHidden = props["aria-labelledby"] ? void 0 : "true";
    const hasSvgAsChild = React.isValidElement(children) && children.type === "svg";
    const child = !hasSvgAsChild ? children.type?.() : children;
    return /* @__PURE__ */ jsxs(
      StyledSvgIcon,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        ...child.props,
        "aria-hidden": ariaHidden,
        fontSize,
        height: fontSize ?? height,
        role: title ? "img" : void 0,
        style: { color, ...style },
        width: fontSize ?? width,
        ...props,
        children: [
          title && /* @__PURE__ */ jsx("title", { children: title }),
          description && /* @__PURE__ */ jsx("desc", { children: description }),
          child.props.children
        ]
      }
    );
  }
);
SvgIcon.displayName = "SvgIcon";

export { SvgIcon as default };
//# sourceMappingURL=svg-icon.js.map
