import { DeepPartial, Theme } from '../../../types';
/**
 * Converts a typography value to a number, resolving CSS variables if necessary
 */
export declare const resolveTypographyValue: (value: string | number, theme: DeepPartial<Theme>) => number;
/**
 * Resolves a font family value, handling CSS variables
 */
export declare const resolveFontFamily: (value: string, theme: DeepPartial<Theme>) => string;
/**
 * Resolves typography values for Monaco editor, converting CSS variables to actual values
 */
export declare const resolveTypographyForMonaco: (typography: {
    fontSize: string | number;
    fontFamily: string;
    fontWeight: string | number;
    lineHeight: string | number;
    letterSpacing: string | number;
}, theme: DeepPartial<Theme>) => {
    fontSize: number;
    fontFamily: string;
    fontWeight: string;
    lineHeight: number;
    letterSpacing: number;
};
//# sourceMappingURL=resolve-typography.d.ts.map