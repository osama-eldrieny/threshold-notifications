import { editor } from 'monaco-editor';
import { OnMount } from '@monaco-editor/react';
import { ForwardedRef } from 'react';
export declare const useCodeBlockState: ({ defaultHeight, defaultWidth, ref, onMount, id, }: {
    defaultHeight: string;
    defaultWidth: string;
    ref: ForwardedRef<editor.IStandaloneCodeEditor>;
    onMount?: OnMount;
    id: string;
}) => {
    handleEditorDidMount: (editor: editor.IStandaloneCodeEditor, monaco: typeof import("monaco-editor")) => void;
    height: string;
    width: string;
};
//# sourceMappingURL=use-code-block-state.d.ts.map