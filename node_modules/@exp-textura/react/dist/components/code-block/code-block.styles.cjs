"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const styled = require('@emotion/styled');
const componentUtils = require('../../system/utils/component-utils.cjs');
const React = require('react');

const componentName = "CodeBlock";
const CodeBlockClasses = {
  root: "exp-textura_code-block"
};
const getVariantConfig = (theme) => ({
  critical: {
    root: {
      borderColor: theme.color.content.utility.critical
    }
  }
});
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = React.useMemo(
    () => componentUtils.getStyleSlots(
      componentName,
      theme,
      variant,
      styleSlotsProp,
      getVariantConfig(theme)
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "block",
    padding: "0",
    border: `${theme.toRem(theme.border.width.sm)} solid ${theme.color.border.actionHover}`,
    borderRadius: theme.toRem(theme.border.radius.sm),
    overflow: "hidden",
    "&[aria-disabled]": {
      pointerEvents: "none",
      opacity: 0.65,
      ...styleSlots?.disabled
    },
    // the focused class as applied by the monaco editor
    "&:has(.view-overlays.focused)": {
      ...theme.focusRing,
      ...styleSlots?.focus
    },
    ...styleSlots?.root
  };
};
const excludedDOMProps = componentUtils.getExcludedDOMProps();
const StyledCodeBlock = styled("div", {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop) && prop !== "size"
})((props) => generateStyles(props));

exports.CodeBlockClasses = CodeBlockClasses;
exports.StyledCodeBlock = StyledCodeBlock;
//# sourceMappingURL=code-block.styles.cjs.map
