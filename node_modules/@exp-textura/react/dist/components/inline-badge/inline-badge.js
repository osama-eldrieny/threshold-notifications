"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { InlineBadgeClasses, StyledInlineBadge } from './inline-badge.styles.js';

const InlineBadge = forwardRef(
  ({ className, variant = "default", size = "default", children, ...props }, ref) => {
    const theme = useTexturaTheme(), rootClasses = clsx(InlineBadgeClasses.root, className, {
      [InlineBadgeClasses.sm]: size === "sm"
    });
    return /* @__PURE__ */ jsx(
      StyledInlineBadge,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        size,
        variant,
        ...props,
        children
      }
    );
  }
);
InlineBadge.displayName = "InlineBadge";

export { InlineBadge as default };
//# sourceMappingURL=inline-badge.js.map
