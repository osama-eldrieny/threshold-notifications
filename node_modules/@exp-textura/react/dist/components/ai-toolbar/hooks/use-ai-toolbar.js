"use client";
import { createContext, useContext } from 'react';

const AIToolbarContext = createContext(void 0);
const useAIToolbar = () => {
  const context = useContext(AIToolbarContext);
  if (!context) {
    throw new Error("useAIToolbar must be used within an AIToolbar component");
  }
  return context;
};

export { AIToolbarContext, useAIToolbar };
//# sourceMappingURL=use-ai-toolbar.js.map
