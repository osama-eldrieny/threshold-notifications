"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const React = require('react');

const AIToolbarContext = React.createContext(void 0);
const useAIToolbar = () => {
  const context = React.useContext(AIToolbarContext);
  if (!context) {
    throw new Error("useAIToolbar must be used within an AIToolbar component");
  }
  return context;
};

exports.AIToolbarContext = AIToolbarContext;
exports.useAIToolbar = useAIToolbar;
//# sourceMappingURL=use-ai-toolbar.cjs.map
