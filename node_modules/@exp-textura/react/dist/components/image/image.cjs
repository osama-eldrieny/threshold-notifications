"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const image_styles = require('./image.styles.cjs');

const Image = React.forwardRef(
  ({ className, variant = "default", src, fallbackSrc, onSrcError, ...props }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(image_styles.ImageClasses.root, className);
    const [imgSrc, setImgSrc] = React.useState(fallbackSrc || src);
    const [attemptedSrc, setAttemptedSrc] = React.useState(false);
    React.useEffect(() => {
      setImgSrc(fallbackSrc || src);
      setAttemptedSrc(false);
    }, [src, fallbackSrc]);
    const handleLoad = React.useCallback(() => {
      if (!attemptedSrc) {
        setImgSrc(src);
        setAttemptedSrc(true);
      }
    }, [attemptedSrc, src]);
    const handleError = React.useCallback(
      (e) => {
        if (fallbackSrc) {
          setImgSrc(fallbackSrc);
        } else {
          setImgSrc(void 0);
        }
        if (imgSrc === fallbackSrc || !fallbackSrc) {
          onSrcError?.(e);
        }
      },
      [fallbackSrc, imgSrc, onSrcError]
    );
    return /* @__PURE__ */ jsxRuntime.jsx(
      image_styles.StyledImage,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        src: imgSrc,
        variant,
        onError: handleError,
        onLoad: handleLoad,
        ...props
      }
    );
  }
);
Image.displayName = "Image";

exports.default = Image;
//# sourceMappingURL=image.cjs.map
