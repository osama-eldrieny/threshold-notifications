"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const styled = require('@emotion/styled');
const componentUtils = require('../../system/utils/component-utils.cjs');
const React = require('react');

const componentName = "Image";
const ImageClasses = {
  root: "exp-textura_image"
};
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = React.useMemo(
    () => componentUtils.getStyleSlots(
      componentName,
      theme,
      variant,
      styleSlotsProp
      // getVariantConfig(),
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "block",
    objectFit: props.objectFit,
    ...styleSlots?.root
  };
};
const excludedDOMProps = componentUtils.getExcludedDOMProps("objectFit");
const StyledImage = styled("img", {
  shouldForwardProp: (prop) => prop !== "children" && !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));

exports.ImageClasses = ImageClasses;
exports.StyledImage = StyledImage;
//# sourceMappingURL=image.styles.cjs.map
