import { PageItem, PaginationContextType } from '../pagination.types';
import { Context } from 'react';
export declare const PaginationContext: Context<PaginationContextType | undefined>;
export declare const usePaginationContext: () => PaginationContextType;
export declare const usePagination: (start: number, finish: number, spread: number, selected: number, onSelectedChange?: (selected: number) => void) => {
    items: PageItem[];
    selectedIndex: number;
    selectIndex: (index: number) => void;
    start: number;
    finish: number;
    spread: number;
};
//# sourceMappingURL=use-pagination.d.ts.map