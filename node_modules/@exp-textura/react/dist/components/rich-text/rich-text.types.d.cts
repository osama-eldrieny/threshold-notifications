import { ComponentProps } from '../../types';
import { CSSObject } from '@emotion/react';
import { PlateEditor } from 'platejs/react';
import { Value } from 'platejs';
import { ReactNode } from 'react';
export type RichTextProps = Omit<ComponentProps, "onChange"> & {
    editor?: PlateEditor;
    onChange?: (value: Value) => void;
    readOnly?: boolean;
    placeholder?: string;
    variant?: "default" | string;
    styleSlots?: {
        root?: CSSObject;
        content?: CSSObject;
        floatingToolbar?: CSSObject;
        linkInsertToolbar?: CSSObject;
        linkEditToolbar?: CSSObject;
    };
};
/** A single font option for RichText.FontFamilyMenu */
export type FontOption = {
    /** CSS font-family value written to the fontFamily mark, e.g. "arial" */
    value: string;
    /** Human-readable label shown in the dropdown, e.g. "Arial" */
    label: string;
};
/** A single option for RichText.TurnIntoMenu */
export type TurnIntoOption = {
    /** Plate block type key. */
    blockType: string;
    /** Label shown in the menu item and the trigger button when this block is active */
    label: string;
    /** Icon rendered as Menu.ItemLeadingIcon */
    icon: ReactNode;
};
/** A single option for RichText.InsertMenu */
export type InsertOption = {
    /** Plate block type key. */
    blockType: string;
    /** Label shown in the menu item */
    label: string;
    /** Icon rendered as Menu.ItemLeadingIcon */
    icon: ReactNode;
    /**
     * "toggle" (default) calls editor.tf.toggleBlock(blockType).
     * "insert" calls editor.tf.insertNodes — use this for void/leaf elements like HR.
     */
    insertMode?: "toggle" | "insert";
};
//# sourceMappingURL=rich-text.types.d.ts.map