"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef, useEffect } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { RichTextClasses, StyledRichText } from './rich-text.styles.js';
import { RichTextContent } from './subcomponents/rich-text-content.js';
import { RichTextFloatingToolbar } from './subcomponents/rich-text-floating-toolbar.js';
import { RichTextToolbar } from './subcomponents/rich-text-toolbar.js';
import { RichTextFloatingLinkToolbar, RichTextLinkEditToolbar, RichTextLinkInsertToolbar } from './subcomponents/rich-text-link-toolbar.js';
import { usePlateEditor, Plate } from '../../vendor/.pnpm/@platejs_core@52.3.21_@types_react@18.3.28_immer@11.1.4_react-dom@19.2.5_react@19.2.5___59cf038edad8f9a3096e68ad5ff89915/node_modules/@platejs/core/dist/react/index.js';

const RichTextRoot = forwardRef(
  ({
    className,
    variant = "default",
    children,
    editor: externalEditor,
    onChange,
    readOnly = false,
    styleSlots,
    ...props
  }, ref) => {
    const theme = useTexturaTheme(), rootClasses = clsx(RichTextClasses.root, className);
    const internalEditor = usePlateEditor({ enabled: !externalEditor });
    useEffect(() => {
      if (!externalEditor && process.env.NODE_ENV !== "production") {
        console.warn(
          "RichText: 'editor' prop was omitted. A bare editor is being used internally without any plugins. In production, you should provide a fully configured PlateEditor."
        );
      }
    }, [externalEditor]);
    const activeEditor = externalEditor || internalEditor;
    return /* @__PURE__ */ jsx(
      Plate,
      {
        editor: activeEditor,
        readOnly,
        onChange: ({ value: newValue }) => {
          if (onChange) {
            onChange(newValue);
          }
        },
        children: /* @__PURE__ */ jsx(
          StyledRichText,
          {
            ref,
            className: rootClasses,
            currentTheme: theme,
            "data-readonly": readOnly || void 0,
            styleSlots,
            variant,
            ...props,
            children
          }
        )
      }
    );
  }
);
RichTextRoot.displayName = "RichText";
const RichText = Object.assign(RichTextRoot, {
  Toolbar: RichTextToolbar,
  FloatingToolbar: RichTextFloatingToolbar,
  Content: RichTextContent,
  LinkInsertToolbar: RichTextLinkInsertToolbar,
  LinkEditToolbar: RichTextLinkEditToolbar,
  FloatingLinkToolbar: RichTextFloatingLinkToolbar
});

export { RichText as default };
//# sourceMappingURL=rich-text.js.map
