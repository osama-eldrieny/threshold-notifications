"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const richText_styles = require('./rich-text.styles.cjs');
const richTextContent = require('./subcomponents/rich-text-content.cjs');
const richTextFloatingToolbar = require('./subcomponents/rich-text-floating-toolbar.cjs');
const richTextToolbar = require('./subcomponents/rich-text-toolbar.cjs');
const richTextLinkToolbar = require('./subcomponents/rich-text-link-toolbar.cjs');
const index = require('../../vendor/.pnpm/@platejs_core@52.3.21_@types_react@18.3.28_immer@11.1.4_react-dom@19.2.5_react@19.2.5___59cf038edad8f9a3096e68ad5ff89915/node_modules/@platejs/core/dist/react/index.cjs');

const RichTextRoot = React.forwardRef(
  ({
    className,
    variant = "default",
    children,
    editor: externalEditor,
    onChange,
    readOnly = false,
    styleSlots,
    ...props
  }, ref) => {
    const theme = useTheme.default(), rootClasses = clsx$1.clsx(richText_styles.RichTextClasses.root, className);
    const internalEditor = index.usePlateEditor({ enabled: !externalEditor });
    React.useEffect(() => {
      if (!externalEditor && process.env.NODE_ENV !== "production") {
        console.warn(
          "RichText: 'editor' prop was omitted. A bare editor is being used internally without any plugins. In production, you should provide a fully configured PlateEditor."
        );
      }
    }, [externalEditor]);
    const activeEditor = externalEditor || internalEditor;
    return /* @__PURE__ */ jsxRuntime.jsx(
      index.Plate,
      {
        editor: activeEditor,
        readOnly,
        onChange: ({ value: newValue }) => {
          if (onChange) {
            onChange(newValue);
          }
        },
        children: /* @__PURE__ */ jsxRuntime.jsx(
          richText_styles.StyledRichText,
          {
            ref,
            className: rootClasses,
            currentTheme: theme,
            "data-readonly": readOnly || void 0,
            styleSlots,
            variant,
            ...props,
            children
          }
        )
      }
    );
  }
);
RichTextRoot.displayName = "RichText";
const RichText = Object.assign(RichTextRoot, {
  Toolbar: richTextToolbar.RichTextToolbar,
  FloatingToolbar: richTextFloatingToolbar.RichTextFloatingToolbar,
  Content: richTextContent.RichTextContent,
  LinkInsertToolbar: richTextLinkToolbar.RichTextLinkInsertToolbar,
  LinkEditToolbar: richTextLinkToolbar.RichTextLinkEditToolbar,
  FloatingLinkToolbar: richTextLinkToolbar.RichTextFloatingLinkToolbar
});

exports.default = RichText;
//# sourceMappingURL=rich-text.cjs.map
