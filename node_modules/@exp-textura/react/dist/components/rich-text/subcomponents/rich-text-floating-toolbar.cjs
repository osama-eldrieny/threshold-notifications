"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
const react = require('@floating-ui/react');
const floating = require('@platejs/floating');
const richText_styles = require('../rich-text.styles.cjs');
const index = require('../../../vendor/.pnpm/@platejs_core@52.3.21_@types_react@18.3.28_immer@11.1.4_react-dom@19.2.5_react@19.2.5___59cf038edad8f9a3096e68ad5ff89915/node_modules/@platejs/core/dist/react/index.cjs');

const RichTextFloatingToolbar = React.forwardRef(
  ({ className, children, ...rest }, forwardedRef) => {
    const editorId = index.useEditorId();
    const focusedEditorId = index.useEventEditorValue("focus");
    const state = floating.useFloatingToolbarState({
      editorId,
      focusedEditorId,
      floatingOptions: {
        placement: "top",
        middleware: [react.offset(8), react.flip(), react.shift({ padding: 8 })]
      }
    });
    const { ref, props: floatingProps, hidden } = floating.useFloatingToolbar(state);
    if (hidden) return null;
    return /* @__PURE__ */ jsxRuntime.jsx(
      "div",
      {
        ref: (el) => {
          ref(el);
          if (typeof forwardedRef === "function") forwardedRef(el);
          else if (forwardedRef) forwardedRef.current = el;
        },
        className: clsx$1.clsx(richText_styles.RichTextClasses.floatingToolbar, className),
        ...floatingProps,
        ...rest,
        children
      }
    );
  }
);
RichTextFloatingToolbar.displayName = "RichText.FloatingToolbar";

exports.RichTextFloatingToolbar = RichTextFloatingToolbar;
//# sourceMappingURL=rich-text-floating-toolbar.cjs.map
