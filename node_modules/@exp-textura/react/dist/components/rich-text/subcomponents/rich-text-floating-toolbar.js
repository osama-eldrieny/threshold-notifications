"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import { offset, flip, shift } from '@floating-ui/react';
import { useFloatingToolbarState, useFloatingToolbar } from '@platejs/floating';
import { RichTextClasses } from '../rich-text.styles.js';
import { useEditorId, useEventEditorValue } from '../../../vendor/.pnpm/@platejs_core@52.3.21_@types_react@18.3.28_immer@11.1.4_react-dom@19.2.5_react@19.2.5___59cf038edad8f9a3096e68ad5ff89915/node_modules/@platejs/core/dist/react/index.js';

const RichTextFloatingToolbar = forwardRef(
  ({ className, children, ...rest }, forwardedRef) => {
    const editorId = useEditorId();
    const focusedEditorId = useEventEditorValue("focus");
    const state = useFloatingToolbarState({
      editorId,
      focusedEditorId,
      floatingOptions: {
        placement: "top",
        middleware: [offset(8), flip(), shift({ padding: 8 })]
      }
    });
    const { ref, props: floatingProps, hidden } = useFloatingToolbar(state);
    if (hidden) return null;
    return /* @__PURE__ */ jsx(
      "div",
      {
        ref: (el) => {
          ref(el);
          if (typeof forwardedRef === "function") forwardedRef(el);
          else if (forwardedRef) forwardedRef.current = el;
        },
        className: clsx(RichTextClasses.floatingToolbar, className),
        ...floatingProps,
        ...rest,
        children
      }
    );
  }
);
RichTextFloatingToolbar.displayName = "RichText.FloatingToolbar";

export { RichTextFloatingToolbar };
//# sourceMappingURL=rich-text-floating-toolbar.js.map
