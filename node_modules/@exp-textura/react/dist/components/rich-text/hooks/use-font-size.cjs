"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const index = require('../../../vendor/.pnpm/@platejs_core@52.3.21_@types_react@18.3.28_immer@11.1.4_react-dom@19.2.5_react@19.2.5___59cf038edad8f9a3096e68ad5ff89915/node_modules/@platejs/core/dist/react/index.cjs');

function useFontSize({ defaults, fallback, editorId }) {
  const resolvedFallback = fallback ?? 14;
  return index.useEditorSelector(
    (ed) => {
      if (!ed) return resolvedFallback;
      const marks = ed?.api?.marks?.() || ed?.marks;
      const size = marks?.fontSize;
      if (size) {
        const parsed = parseInt(size, 10);
        if (!isNaN(parsed)) return parsed;
      }
      if (defaults) {
        const blockType = ed?.api?.block()?.[0]?.type;
        if (blockType && blockType in defaults) {
          return defaults[blockType] ?? resolvedFallback;
        }
      }
      return resolvedFallback;
    },
    [],
    ...editorId ? [{ id: editorId }] : []
  );
}

exports.useFontSize = useFontSize;
//# sourceMappingURL=use-font-size.cjs.map
