"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const React = require('react');
const index = require('../../../vendor/.pnpm/@platejs_core@52.3.21_@types_react@18.3.28_immer@11.1.4_react-dom@19.2.5_react@19.2.5___59cf038edad8f9a3096e68ad5ff89915/node_modules/@platejs/core/dist/react/index.cjs');

function useHistoryState(editorId) {
  const editor = index.useEditorRef();
  const selectorOptions = editorId ? { id: editorId } : void 0;
  const canUndo = index.useEditorSelector(
    (ed) => !!ed?.history?.undos?.length,
    [],
    ...selectorOptions ? [selectorOptions] : []
  );
  const canRedo = index.useEditorSelector(
    (ed) => !!ed?.history?.redos?.length,
    [],
    ...selectorOptions ? [selectorOptions] : []
  );
  const undo = React.useCallback(() => {
    editor.tf.undo();
    editor.tf.focus();
  }, [editor]);
  const redo = React.useCallback(() => {
    editor.tf.redo();
    editor.tf.focus();
  }, [editor]);
  return { canUndo, canRedo, undo, redo };
}

exports.useHistoryState = useHistoryState;
//# sourceMappingURL=use-history-state.cjs.map
