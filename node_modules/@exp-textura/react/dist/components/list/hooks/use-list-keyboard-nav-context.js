"use client";
import { useContext, createContext } from 'react';

const ListKeyboardNavContext = createContext(
  void 0
);
const useListKeyboardNavContext = () => {
  const context = useContext(ListKeyboardNavContext);
  if (!context) {
    throw new Error("useListKeyboardNavContext must be used within a ListKeyboardNavProvider");
  }
  return context;
};

export { ListKeyboardNavContext, useListKeyboardNavContext };
//# sourceMappingURL=use-list-keyboard-nav-context.js.map
