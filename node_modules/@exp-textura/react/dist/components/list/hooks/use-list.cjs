"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const React = require('react');

const ListContext = React.createContext(null);
const ListStateContext = React.createContext(null);
const useList = () => {
  const list = React.useContext(ListContext);
  const state = React.useContext(ListStateContext);
  if (!list || !state) {
    throw new Error("useList must be used within a <List> component");
  }
  return { ...list, state };
};

exports.ListContext = ListContext;
exports.ListStateContext = ListStateContext;
exports.useList = useList;
//# sourceMappingURL=use-list.cjs.map
