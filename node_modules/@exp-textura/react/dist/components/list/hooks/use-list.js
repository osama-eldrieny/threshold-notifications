"use client";
import { useContext, createContext } from 'react';

const ListContext = createContext(null);
const ListStateContext = createContext(null);
const useList = () => {
  const list = useContext(ListContext);
  const state = useContext(ListStateContext);
  if (!list || !state) {
    throw new Error("useList must be used within a <List> component");
  }
  return { ...list, state };
};

export { ListContext, ListStateContext, useList };
//# sourceMappingURL=use-list.js.map
