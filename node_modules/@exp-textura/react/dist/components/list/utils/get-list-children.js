"use client";
import React from 'react';

const getListChildren = (children) => {
  const toolbar = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === "List.Toolbar"
  );
  const header = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === "List.Header"
  );
  const body = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === "List.Body"
  );
  const footer = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === "List.Footer"
  );
  const filterChipBars = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === "List.FilterChipBars"
  );
  const emptyState = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === "List.EmptyState"
  );
  return {
    toolbar,
    header,
    body,
    footer,
    filterChipBars,
    emptyState
  };
};

export { getListChildren };
//# sourceMappingURL=get-list-children.js.map
