"use client";
import { jsxs, jsx } from 'react/jsx-runtime';
import React, { forwardRef, Children } from 'react';
import { clsx } from 'clsx';
import IconButton from '../../icon-button/icon-button.js';
import { Filter } from '@exp-textura/icons';
import Float from '../../float/float.js';
import { ListClasses } from '../list.styles.js';

const ListFilterIconButton = forwardRef(
  ({ floatProps, className, children, ...props }, ref) => {
    const childrenArray = Children.toArray(children);
    const badge = childrenArray.find(
      (child) => React.isValidElement(child) && child.type.displayName === "List.FilterIconButtonBadge"
    );
    const otherChildren = childrenArray.filter((child) => child !== badge);
    return /* @__PURE__ */ jsxs(Float, { offSetX: 4, offSetY: 4, placement: "top-end", ...floatProps, children: [
      badge && /* @__PURE__ */ jsx(Float.Content, { children: badge }),
      /* @__PURE__ */ jsx(Float.Anchor, { children: /* @__PURE__ */ jsx(
        IconButton,
        {
          ref,
          buttonType: "secondary",
          ...props,
          className: clsx(ListClasses.filterIconButton, className),
          children: otherChildren.length > 0 ? otherChildren : /* @__PURE__ */ jsx(Filter, {})
        }
      ) })
    ] });
  }
);
ListFilterIconButton.displayName = "List.FilterIconButton";

export { ListFilterIconButton };
//# sourceMappingURL=list-filter-icon-button.js.map
