"use client";
import { jsx, jsxs } from 'react/jsx-runtime';
import Search from '../../search/search.js';
import { clsx } from 'clsx';
import { useList } from '../hooks/use-list.js';
import { ListClasses } from '../list.styles.js';

const ListSearch = ({
  className,
  onQueryChange,
  value,
  children,
  "aria-label": ariaLabel = "Search list",
  ...props
}) => {
  const list = useList();
  const globalFilter = list.getState().globalFilter;
  return /* @__PURE__ */ jsx(
    Search,
    {
      className: clsx(ListClasses.searchInput, className),
      openOnClick: false,
      ...props,
      value: value !== void 0 ? value : globalFilter || "",
      onQueryChange: (query) => {
        list.setGlobalFilter(query);
        onQueryChange?.(query);
      },
      children: children || /* @__PURE__ */ jsxs(Search.Input, { "aria-label": ariaLabel, children: [
        /* @__PURE__ */ jsx(Search.InputPrefix, {}),
        /* @__PURE__ */ jsx(Search.InputSuffix, { children: /* @__PURE__ */ jsx(Search.ClearIconButton, {}) })
      ] })
    }
  );
};
ListSearch.displayName = "List.Search";

export { ListSearch };
//# sourceMappingURL=list-search.js.map
