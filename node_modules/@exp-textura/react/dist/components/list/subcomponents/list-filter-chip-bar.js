"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import { ListClasses } from '../list.styles.js';

const ListFilterChipBar = forwardRef(
  ({ children, className, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(ListClasses.filterChipBars, className), ...props, children });
  }
);
ListFilterChipBar.displayName = "List.FilterChipBars";

export { ListFilterChipBar };
//# sourceMappingURL=list-filter-chip-bar.js.map
