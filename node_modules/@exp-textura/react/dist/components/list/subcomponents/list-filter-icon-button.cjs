"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
const iconButton = require('../../icon-button/icon-button.cjs');
const icons = require('@exp-textura/icons');
const float = require('../../float/float.cjs');
const list_styles = require('../list.styles.cjs');

const ListFilterIconButton = React.forwardRef(
  ({ floatProps, className, children, ...props }, ref) => {
    const childrenArray = React.Children.toArray(children);
    const badge = childrenArray.find(
      (child) => React.isValidElement(child) && child.type.displayName === "List.FilterIconButtonBadge"
    );
    const otherChildren = childrenArray.filter((child) => child !== badge);
    return /* @__PURE__ */ jsxRuntime.jsxs(float.default, { offSetX: 4, offSetY: 4, placement: "top-end", ...floatProps, children: [
      badge && /* @__PURE__ */ jsxRuntime.jsx(float.default.Content, { children: badge }),
      /* @__PURE__ */ jsxRuntime.jsx(float.default.Anchor, { children: /* @__PURE__ */ jsxRuntime.jsx(
        iconButton.default,
        {
          ref,
          buttonType: "secondary",
          ...props,
          className: clsx$1.clsx(list_styles.ListClasses.filterIconButton, className),
          children: otherChildren.length > 0 ? otherChildren : /* @__PURE__ */ jsxRuntime.jsx(icons.Filter, {})
        }
      ) })
    ] });
  }
);
ListFilterIconButton.displayName = "List.FilterIconButton";

exports.ListFilterIconButton = ListFilterIconButton;
//# sourceMappingURL=list-filter-icon-button.cjs.map
