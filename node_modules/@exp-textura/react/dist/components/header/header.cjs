"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const header_styles = require('./header.styles.cjs');

const HeaderRoot = React.forwardRef(
  ({ className, variant = "default", children, ...props }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(header_styles.HeaderClasses.root, className);
    const breadcrumbs = React.Children.map(
      children,
      (child) => React.isValidElement(child) && child.type === Breadcrumbs ? child : null
    );
    const content = React.Children.map(
      children,
      (child) => React.isValidElement(child) && child.type === Content ? child : null
    );
    const actions = React.Children.map(
      children,
      (child) => React.isValidElement(child) && child.type === Actions ? child : null
    );
    return /* @__PURE__ */ jsxRuntime.jsxs(
      header_styles.StyledHeader,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children: [
          breadcrumbs,
          /* @__PURE__ */ jsxRuntime.jsxs("div", { className: header_styles.HeaderClasses.main, children: [
            content,
            actions
          ] })
        ]
      }
    );
  }
);
HeaderRoot.displayName = "Header";
const Title = ({ className, children, headingLevel = 1, ...props }) => {
  const HeadingTag = `h${headingLevel}`;
  return React.createElement(
    HeadingTag,
    { className: clsx$1.clsx(header_styles.HeaderClasses.title, className), tabIndex: -1, ...props },
    children
  );
};
Title.displayName = "Header.Title";
const Subtitle = ({
  className,
  children,
  headingLevel = 2,
  ...props
}) => {
  const HeadingTag = `h${headingLevel}`;
  return React.createElement(
    HeadingTag,
    { className: clsx$1.clsx(header_styles.HeaderClasses.subtitle, className), tabIndex: -1, ...props },
    children
  );
};
Subtitle.displayName = "Header.Subtitle";
const Description = ({ className, children, ...props }) => {
  return React.createElement(
    "div",
    { className: clsx$1.clsx(header_styles.HeaderClasses.description, className), tabIndex: -1, ...props },
    children
  );
};
Description.displayName = "Header.Description";
const Content = ({ className, children }) => /* @__PURE__ */ jsxRuntime.jsx("div", { className: clsx$1.clsx(header_styles.HeaderClasses.content, className), children });
Content.displayName = "Header.Content";
const Actions = ({ className, children }) => /* @__PURE__ */ jsxRuntime.jsx("div", { className: clsx$1.clsx(header_styles.HeaderClasses.actions, className), children });
Actions.displayName = "Header.Actions";
const Breadcrumbs = ({ className, children }) => /* @__PURE__ */ jsxRuntime.jsx("div", { className: clsx$1.clsx(header_styles.HeaderClasses.breadcrumbs, className), children });
Breadcrumbs.displayName = "Header.Breadcrumbs";
const Header = Object.assign(HeaderRoot, {
  Title,
  Subtitle,
  Description,
  Content,
  Actions,
  Breadcrumbs
});

exports.default = Header;
//# sourceMappingURL=header.cjs.map
