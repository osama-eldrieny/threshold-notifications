"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const skeleton_styles = require('./skeleton.styles.cjs');

const Skeleton = React.forwardRef(
  ({ children, variant, className, style, shape = "square", size, loading = true, ...props }, ref) => {
    const childRef = React.useRef(null);
    const appliedSize = size ?? "lg";
    const [borderRadius, setBorderRadius] = React.useState();
    const [childWidth, setChildWidth] = React.useState();
    const [childHeight, setChildHeight] = React.useState();
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(
      skeleton_styles.SkeletonClasses.root,
      {
        [skeleton_styles.SkeletonClasses[shape]]: shape,
        [skeleton_styles.SkeletonClasses[appliedSize]]: appliedSize
      },
      className
    );
    React.useLayoutEffect(() => {
      if (childRef.current && !size) {
        const childComputedStyle = window.getComputedStyle(childRef.current);
        const { borderRadius: br, width: w, height: h } = childComputedStyle;
        if (br && br !== "0px") setBorderRadius(br);
        if (w && w !== "0px" && w !== "auto") setChildWidth(w);
        if (h && h !== "0px" && h !== "auto") setChildHeight(h);
      }
    }, [children, size]);
    const childWithRef = React.isValidElement(children) ? React.cloneElement(children, {
      ref: childRef
    }) : children;
    if (!loading) return /* @__PURE__ */ jsxRuntime.jsx(jsxRuntime.Fragment, { children });
    return /* @__PURE__ */ jsxRuntime.jsx(
      skeleton_styles.StyledSkeleton,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        style: {
          borderRadius,
          // Explicit style prop values take precedence over measured values
          height: style?.height ?? childHeight,
          width: style?.width ?? childWidth,
          ...style
        },
        variant,
        ...props,
        children: /* @__PURE__ */ jsxRuntime.jsx(
          "span",
          {
            "aria-hidden": "true",
            inert: "true",
            children: childWithRef
          }
        )
      }
    );
  }
);
Skeleton.displayName = "Skeleton";

exports.default = Skeleton;
//# sourceMappingURL=skeleton.cjs.map
