"use client";
import { jsxs, jsx } from 'react/jsx-runtime';
import React, { forwardRef, useRef, useMemo, Children, isValidElement, cloneElement } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { Check } from '@exp-textura/icons';
import { useMergeRefs } from '../../hooks/use-merge-refs.js';
import Popover from '../popover/popover.js';
import Chip from '../chip/chip.js';
import { ChipBarClasses, StyledChipBar } from './chip-bar.styles.js';
import { useChipBar } from './hooks/use-chip-bar.js';
import { useVisibleChipBar } from './hooks/use-visible-chip-bar.js';

const ChipBarOverflow = ({ overflowChips, moreButtonRef, size }) => {
  const count = overflowChips.length;
  return /* @__PURE__ */ jsxs(Popover, { dismissable: true, children: [
    /* @__PURE__ */ jsx(Popover.Trigger, { children: /* @__PURE__ */ jsxs(Chip, { ref: moreButtonRef, isOverflow: true, size, children: [
      "+",
      count
    ] }) }),
    /* @__PURE__ */ jsx(Popover.Content, { children: /* @__PURE__ */ jsx("div", { className: ChipBarClasses.overflowContent, children: overflowChips }) })
  ] });
};
const ChipBar = forwardRef(
  ({
    className,
    variant = "default",
    selectionMode = "none",
    value: valueProp,
    defaultValue,
    onChange,
    selectedIcon = /* @__PURE__ */ jsx(Check, {}),
    enableOverflow = true,
    size,
    children,
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(ChipBarClasses.root, className);
    const containerRef = useRef(null);
    const moreButtonRef = useRef(null);
    const mergedRef = useMergeRefs(ref, containerRef);
    const [closedValues, setClosedValues] = React.useState([]);
    const { selectedValue, handleChipClick, chipBarRole, chipWrapperRole } = useChipBar({
      value: valueProp,
      defaultValue,
      onChange,
      selectionMode
    });
    const childrenWithIds = useMemo(() => {
      return Children.toArray(children).map((child, index) => ({
        child,
        id: (isValidElement(child) ? child.props.value : void 0) || `generated-chip-${index}`
      }));
    }, [children]);
    const validChildrenItems = useMemo(() => {
      return childrenWithIds.filter(
        (item) => isValidElement(item.child) && item.child.props.show !== false && !closedValues.includes(item.id)
      );
    }, [childrenWithIds, closedValues]);
    const allValues = useMemo(
      () => validChildrenItems.map((item) => item.id),
      [validChildrenItems]
    );
    const { visibleValues: hookVisibleValues, overflowValues: hookOverflowValues } = useVisibleChipBar(containerRef, enableOverflow ? allValues : [], moreButtonRef);
    const visibleValues = enableOverflow ? hookVisibleValues : allValues;
    const overflowValues = enableOverflow ? hookOverflowValues : [];
    const renderChip = (item, isOverflow = false) => {
      const { child, id: chipValue } = item;
      const isSelected = selectionMode === "multiple" ? (selectedValue || []).includes(chipValue) : selectedValue === chipValue;
      const wrapperClasses = clsx(ChipBarClasses.chipWrapper, {
        [ChipBarClasses.chipWrapperSelected]: isSelected,
        [ChipBarClasses.chipWrapperSelectable]: selectionMode !== "none"
      });
      return cloneElement(
        child,
        {
          ...child.props,
          className: clsx(child.props.className, wrapperClasses),
          variant: child.props.variant || "neutral",
          type: child.props.type || "outline",
          size: child.props.size || size,
          // Cascade size if not overridden
          role: chipWrapperRole,
          "aria-checked": selectionMode !== "none" ? isSelected : void 0,
          "data-value": !isOverflow ? chipValue : void 0,
          style: {
            ...child.props.style,
            ...!isOverflow && enableOverflow && !visibleValues.includes(chipValue) ? { visibility: "hidden", position: "absolute", zIndex: -1 } : void 0
          },
          tabIndex: selectionMode !== "none" ? 0 : void 0,
          onClick: (e) => {
            child.props.onClick?.(e);
            handleChipClick(child.props.value || chipValue);
          },
          onClose: () => {
            child.props.onClose?.();
            setClosedValues((prev) => [...prev, chipValue]);
          },
          onKeyDown: (e) => {
            child.props.onKeyDown?.(e);
            if (e.key === "Enter" || e.key === " ") {
              e.preventDefault();
              handleChipClick(child.props.value || chipValue);
            }
          }
        },
        isSelected ? selectedIcon : null,
        child.props.children
      );
    };
    const overflowChips = validChildrenItems.filter((item) => overflowValues.includes(item.id)).map((item) => renderChip(item, true));
    return /* @__PURE__ */ jsxs(
      StyledChipBar,
      {
        ref: mergedRef,
        className: rootClasses,
        currentTheme: theme,
        role: chipBarRole,
        variant,
        ...props,
        children: [
          validChildrenItems.map((item) => renderChip(item)),
          overflowValues.length > 0 && /* @__PURE__ */ jsx(
            ChipBarOverflow,
            {
              moreButtonRef,
              overflowChips,
              size
            }
          )
        ]
      }
    );
  }
);
ChipBar.displayName = "ChipBar";

export { ChipBar as default };
//# sourceMappingURL=chip-bar.js.map
