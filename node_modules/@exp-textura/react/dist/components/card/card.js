"use client";
import { jsx } from 'react/jsx-runtime';
import Divider$1 from '../divider/divider.js';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { CardClasses, StyledCard } from './card.styles.js';

const CardRoot = forwardRef(
  ({ className, variant = "default", spacing = "default", children, ...props }, ref) => {
    const theme = useTexturaTheme();
    const spacingClass = {
      default: CardClasses.spacingDefault,
      roomy: CardClasses.spacingRoomy,
      tight: CardClasses.spacingTight
    };
    const rootClasses = clsx(CardClasses.root, spacingClass[spacing], className);
    return /* @__PURE__ */ jsx(
      StyledCard,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children
      }
    );
  }
);
CardRoot.displayName = "Card";
const Header = ({
  as: Component = "h3",
  children,
  className,
  ...props
}) => {
  return /* @__PURE__ */ jsx(Component, { className: clsx(CardClasses.header, className), ...props, children });
};
Header.displayName = "Card.Header";
const Content = ({ children, className, ...props }) => {
  return /* @__PURE__ */ jsx("div", { className: clsx(CardClasses.content, className), ...props, children });
};
Content.displayName = "Card.Content";
const Divider = ({ className, ...props }) => {
  return /* @__PURE__ */ jsx(Divider$1, { className: clsx(CardClasses.divider, className), ...props });
};
Divider.displayName = "Card.Divider";
const Card = CardRoot;
Card.Header = Header;
Card.Content = Content;
Card.Divider = Divider;

export { Card as default };
//# sourceMappingURL=card.js.map
