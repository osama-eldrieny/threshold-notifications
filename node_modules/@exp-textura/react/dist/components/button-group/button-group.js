"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { ButtonGroupClasses, StyledButtonGroup } from './button-group.styles.js';

const ButtonGroup = forwardRef(
  ({ className, variant = "default", children, ...props }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(ButtonGroupClasses.root, className);
    return /* @__PURE__ */ jsx(
      StyledButtonGroup,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        role: "group",
        variant,
        ...props,
        children
      }
    );
  }
);
ButtonGroup.displayName = "ButtonGroup";

export { ButtonGroup as default };
//# sourceMappingURL=button-group.js.map
