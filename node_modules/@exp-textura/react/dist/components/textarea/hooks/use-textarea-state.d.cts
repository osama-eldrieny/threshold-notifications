import { TextareaProps } from '../textarea.types';
export declare const useTextareaState: ({ id, value: controlledValue, defaultValue, maxLength, onChange, restrictCharacterCount, ["aria-describedby"]: ariaDescribedBy, disabled, }: TextareaProps) => {
    textareaId: string;
    currentValue: string;
    describedBy: string[];
    handleChange: (event: React.ChangeEvent<HTMLTextAreaElement>) => void;
    addAriaDescribedBy: (id: string) => void;
    removeAriaDescribedBy: (id: string) => void;
    getAriaDescribedBy: string;
};
//# sourceMappingURL=use-textarea-state.d.ts.map