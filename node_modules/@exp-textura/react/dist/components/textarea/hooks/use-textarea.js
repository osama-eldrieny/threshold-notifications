"use client";
import { createContext, useContext } from 'react';

const TextareaContext = createContext(null);
const useTextarea = () => {
  const context = useContext(TextareaContext);
  if (!context) {
    throw new Error(
      "Textarea compound components cannot be rendered outside the Textarea component"
    );
  }
  return context;
};

export { TextareaContext, useTextarea };
//# sourceMappingURL=use-textarea.js.map
