"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const React = require('react');

const TextareaContext = React.createContext(null);
const useTextarea = () => {
  const context = React.useContext(TextareaContext);
  if (!context) {
    throw new Error(
      "Textarea compound components cannot be rendered outside the Textarea component"
    );
  }
  return context;
};

exports.TextareaContext = TextareaContext;
exports.useTextarea = useTextarea;
//# sourceMappingURL=use-textarea.cjs.map
