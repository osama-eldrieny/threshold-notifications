"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const useTextarea = require('./hooks/use-textarea.cjs');
const useAriaDescribedBy = require('./hooks/use-aria-described-by.cjs');
const useTextareaState = require('./hooks/use-textarea-state.cjs');
const textarea_styles = require('./textarea.styles.cjs');

const defaultGetItemAriaLabel = (currentLength, maxLength) => `${currentLength} out of ${maxLength} characters used`;
const TextareaRoot = React.forwardRef((props, ref) => {
  const {
    variant = "default",
    size = "default",
    resize = "both",
    placeholder,
    rows,
    maxLength,
    className,
    restrictCharacterCount,
    children,
    id,
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    defaultValue,
    ...rest
  } = props;
  const theme = useTheme.default();
  const {
    textareaId: generatedTextareaId,
    currentValue,
    handleChange,
    addAriaDescribedBy,
    removeAriaDescribedBy,
    getAriaDescribedBy
  } = useTextareaState.useTextareaState(props);
  const textareaId = id || generatedTextareaId;
  const contextValue = {
    textareaId,
    value: currentValue,
    maxLength,
    restrictCharacterCount,
    addAriaDescribedBy,
    removeAriaDescribedBy
  };
  const textareaElementClasses = clsx$1.clsx(textarea_styles.TextareaClasses.input, className, {
    [textarea_styles.TextareaClasses.sm]: size === "sm",
    [textarea_styles.TextareaClasses.resizeHorizontal]: resize === "horizontal",
    [textarea_styles.TextareaClasses.resizeVertical]: resize === "vertical",
    [textarea_styles.TextareaClasses.resizeNone]: resize === "none"
  });
  return /* @__PURE__ */ jsxRuntime.jsx(useTextarea.TextareaContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsxRuntime.jsxs(
    textarea_styles.StyledTextarea,
    {
      className: textarea_styles.TextareaClasses.root,
      currentTheme: theme,
      styleSlots: props.styleSlots,
      variant,
      children: [
        /* @__PURE__ */ jsxRuntime.jsx(
          "textarea",
          {
            ...rest,
            ref,
            "aria-describedby": getAriaDescribedBy,
            className: textareaElementClasses,
            id: textareaId,
            maxLength: restrictCharacterCount ? maxLength : void 0,
            placeholder,
            rows,
            value: currentValue,
            onChange: handleChange
          }
        ),
        children
      ]
    }
  ) });
});
TextareaRoot.displayName = "Textarea";
const CharCount = ({
  getItemAriaLabel: customGetItemAriaLabel,
  "aria-hidden": ariaHidden,
  render,
  ...props
}) => {
  const { value, maxLength, textareaId } = useTextarea.useTextarea();
  const charCountId = `${textareaId}-character-count`;
  const labelGetter = customGetItemAriaLabel || defaultGetItemAriaLabel;
  useAriaDescribedBy.useAriaDescribedBy({ id: charCountId, "aria-hidden": ariaHidden });
  if (!maxLength) {
    return null;
  }
  const charCountProps = {
    "aria-label": labelGetter(value.length, maxLength),
    "aria-live": "polite",
    className: clsx$1.clsx(textarea_styles.TextareaClasses.characterCount, {
      [textarea_styles.TextareaClasses.characterCountWarning]: value.length >= Number(maxLength)
    }),
    id: charCountId,
    ...props
  };
  if (render) {
    const rendered = render(value.length, maxLength);
    if (React.isValidElement(rendered)) {
      return React.cloneElement(rendered, {
        ...charCountProps,
        className: clsx$1.clsx(charCountProps.className, rendered.props.className)
      });
    } else {
      throw new Error("Textarea.CharCount render prop must return a valid React element");
    }
  }
  return /* @__PURE__ */ jsxRuntime.jsxs("div", { ...charCountProps, children: [
    value.length,
    "/",
    maxLength
  ] });
};
CharCount.displayName = "Textarea.CharCount";
const Textarea = Object.assign(TextareaRoot, {
  CharCount
});

exports.default = Textarea;
//# sourceMappingURL=textarea.cjs.map
