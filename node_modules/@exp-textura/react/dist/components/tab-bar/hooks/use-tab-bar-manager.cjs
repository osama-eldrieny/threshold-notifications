"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const React = require('react');

const useTabBarManager = (defaultValue, rememberActiveTab, uniqueId, onValueChange, addTabId) => {
  const [activeTab, setActiveTabState] = React.useState(defaultValue ?? "");
  React.useEffect(() => {
    if (rememberActiveTab && uniqueId) {
      const storedActiveTab = localStorage.getItem(`${uniqueId}-activeTab`);
      if (storedActiveTab) {
        setActiveTabState(storedActiveTab);
      }
    } else if (uniqueId) {
      localStorage.removeItem(`${uniqueId}-activeTab`);
    }
  }, [rememberActiveTab, uniqueId]);
  const setActiveTab = (value) => {
    if (!value || value === addTabId) return;
    setActiveTabState(value);
    if (rememberActiveTab && uniqueId) {
      localStorage.setItem(`${uniqueId}-activeTab`, value);
    }
    onValueChange?.(value);
  };
  return { activeTab, setActiveTab };
};

exports.useTabBarManager = useTabBarManager;
//# sourceMappingURL=use-tab-bar-manager.cjs.map
