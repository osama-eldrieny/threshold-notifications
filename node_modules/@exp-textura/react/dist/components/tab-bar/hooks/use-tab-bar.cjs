"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const React = require('react');

const TabBarContext = React.createContext(null);
const useTabBar = () => {
  const ctx = React.useContext(TabBarContext);
  if (!ctx) throw new Error("useTabBar must be used within <TabBar>");
  return ctx;
};

exports.TabBarContext = TabBarContext;
exports.useTabBar = useTabBar;
//# sourceMappingURL=use-tab-bar.cjs.map
