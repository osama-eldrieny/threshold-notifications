"use client";
import { useContext, createContext } from 'react';

const TabBarContext = createContext(null);
const useTabBar = () => {
  const ctx = useContext(TabBarContext);
  if (!ctx) throw new Error("useTabBar must be used within <TabBar>");
  return ctx;
};

export { TabBarContext, useTabBar };
//# sourceMappingURL=use-tab-bar.js.map
