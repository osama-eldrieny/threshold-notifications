"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import InlineBadge from '../../inline-badge/inline-badge.js';
import { clsx } from 'clsx';
import { TabClasses } from './tab/tab.styles.js';

const TabInlineBadge = forwardRef(
  ({ className, variant = "new", ...props }, ref) => {
    const inlineBadgeClasses = clsx(TabClasses.inlineBadge, className);
    return /* @__PURE__ */ jsx(InlineBadge, { ref, className: inlineBadgeClasses, variant, ...props });
  }
);
TabInlineBadge.displayName = "Tab.InlineBadge";

export { TabInlineBadge };
//# sourceMappingURL=tab-inline-badge.js.map
