"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import * as RadixTabs from '@radix-ui/react-tabs';
import { clsx } from 'clsx';
import { TabBarClasses } from '../tab-bar.styles.js';

const TabBarPanel = forwardRef(({ className, value = "", children, ...props }, ref) => /* @__PURE__ */ jsx(
  RadixTabs.Content,
  {
    ref,
    className: clsx(TabBarClasses.panel, className),
    value,
    ...props,
    children
  }
));
TabBarPanel.displayName = "TabBar.Panel";

export { TabBarPanel };
//# sourceMappingURL=tab-bar-panel.js.map
