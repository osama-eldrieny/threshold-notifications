"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const RadixTabs = require('@radix-ui/react-tabs');
const clsx$1 = require('clsx');
const tabBar_styles = require('../tab-bar.styles.cjs');
const useVisibleTabBar = require('../hooks/use-visible-tab-bar.cjs');
const tabBarOverflow = require('./tab-bar-overflow.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixTabs__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixTabs);

const TabBarList = React.forwardRef(
  ({ className, children, overflowMenuTriggerLabel, ...props }, ref) => {
    const listRef = React.useRef(null);
    const moreButtonRef = React.useRef(null);
    const { addTab, tabs } = React.useMemo(() => {
      const addTab2 = [];
      const tabs2 = [];
      React.Children.toArray(children).forEach((child) => {
        if (React.isValidElement(child)) {
          if (child.type.displayName === "TabBar.AddTab") {
            addTab2.push(child);
          } else if (child.type.displayName === "Tab") {
            tabs2.push(child);
          }
        }
      });
      return { addTab: addTab2, tabs: tabs2 };
    }, [children]);
    const allTriggers = tabs;
    const allValues = React.useMemo(() => allTriggers.map((tab) => tab.props.value), [allTriggers]);
    const { visibleValues, overflowValues } = useVisibleTabBar.useVisibleTabBar(listRef, allValues, moreButtonRef);
    return /* @__PURE__ */ jsxRuntime.jsx("div", { className: clsx$1.clsx(tabBar_styles.TabBarClasses.listWrapper, className), children: /* @__PURE__ */ jsxRuntime.jsxs(
      RadixTabs__namespace.List,
      {
        ref: (node) => {
          listRef.current = node;
          if (typeof ref === "function") ref(node);
          else if (ref) ref.current = node;
        },
        className: tabBar_styles.TabBarClasses.list,
        ...props,
        children: [
          allTriggers.map((child) => {
            const value = child.props.value;
            const isVisible = visibleValues.includes(value);
            return React.cloneElement(child, {
              "data-value": value,
              style: !isVisible ? { visibility: "hidden", position: "absolute", zIndex: -1 } : child.props.style
            });
          }),
          overflowValues.length > 0 && /* @__PURE__ */ jsxRuntime.jsx(
            tabBarOverflow.OverflowMenu,
            {
              allTriggers,
              moreButtonRef,
              overflowMenuTriggerLabel,
              overflowValues
            }
          ),
          addTab
        ]
      }
    ) });
  }
);
TabBarList.displayName = "TabBar.List";

exports.TabBarList = TabBarList;
//# sourceMappingURL=tab-bar-list.cjs.map
