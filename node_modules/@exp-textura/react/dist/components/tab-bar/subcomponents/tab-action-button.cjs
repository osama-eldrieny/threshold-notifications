"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const icons = require('@exp-textura/icons');
const clsx$1 = require('clsx');
const iconButton = require('../../icon-button/icon-button.cjs');
const tab_styles = require('./tab/tab.styles.cjs');

const TabActionButton = React.forwardRef(
  ({ action, className, children, ...props }, ref) => {
    const Icon = action === "close" ? icons.Close : icons.ChevronDown;
    const buttonClasses = clsx$1.clsx(tab_styles.TabClasses.actionButton, className);
    return /* @__PURE__ */ jsxRuntime.jsx(
      iconButton.default,
      {
        ref,
        "aria-hidden": "true",
        "aria-label": props["aria-label"] ?? (action === "close" ? "Close tab" : "Tab actions"),
        as: "div",
        buttonType: "secondary",
        className: buttonClasses,
        size: "sm",
        tabIndex: -1,
        onPointerDown: (e) => {
          e.preventDefault();
          e.stopPropagation();
        },
        ...props,
        children: children || /* @__PURE__ */ jsxRuntime.jsx(Icon, {})
      }
    );
  }
);
TabActionButton.displayName = "Tab.ActionButton";

exports.TabActionButton = TabActionButton;
//# sourceMappingURL=tab-action-button.cjs.map
