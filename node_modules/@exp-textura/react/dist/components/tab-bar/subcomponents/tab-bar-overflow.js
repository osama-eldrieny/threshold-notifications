"use client";
import { jsxs, jsx } from 'react/jsx-runtime';
import { Check } from '@exp-textura/icons';
import Menu from '../../menu/menu.js';
import { useTabBar } from '../hooks/use-tab-bar.js';
import Tab from './tab/tab.js';

const OverflowMenu = ({ allTriggers, overflowValues, moreButtonRef, overflowMenuTriggerLabel = "More" }) => {
  const { activeTab, setActiveTab } = useTabBar();
  return /* @__PURE__ */ jsxs(Menu, { children: [
    /* @__PURE__ */ jsx(Menu.Trigger, { children: /* @__PURE__ */ jsxs(Tab, { ref: moreButtonRef, children: [
      overflowMenuTriggerLabel,
      /* @__PURE__ */ jsx(Tab.ActionButton, { action: "menu" })
    ] }) }),
    /* @__PURE__ */ jsx(Menu.Content, { children: allTriggers.filter((child) => overflowValues.includes(child.props.value)).map((child) => {
      const value = child.props.value;
      const isActive = value === activeTab;
      return /* @__PURE__ */ jsx(Menu.Item, { onSelect: () => setActiveTab(value), children: /* @__PURE__ */ jsxs("span", { style: { display: "flex", justifyContent: "space-between", width: "100%" }, children: [
        child.props.children,
        isActive && /* @__PURE__ */ jsx(Check, {})
      ] }) }, value);
    }) })
  ] });
};

export { OverflowMenu };
//# sourceMappingURL=tab-bar-overflow.js.map
