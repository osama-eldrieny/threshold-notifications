"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { Add } from '@exp-textura/icons';
import { useTabBar } from '../hooks/use-tab-bar.js';
import Tab from './tab/tab.js';

const TabBarAddTab = forwardRef((props, ref) => {
  const { addTabId } = useTabBar();
  return /* @__PURE__ */ jsx(Tab, { ref, "aria-label": props["aria-label"] ?? "Add tab", value: addTabId, ...props, children: props.children || /* @__PURE__ */ jsx(Add, {}) });
});
TabBarAddTab.displayName = "TabBar.AddTab";

export { TabBarAddTab };
//# sourceMappingURL=tab-bar-add-tab.js.map
