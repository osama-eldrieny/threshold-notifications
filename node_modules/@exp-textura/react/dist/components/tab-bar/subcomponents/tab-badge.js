"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import Badge from '../../badge/badge.js';
import { clsx } from 'clsx';
import { TabClasses } from './tab/tab.styles.js';

const TabBadge = forwardRef(
  ({ className, ...props }, ref) => {
    const badgeClasses = clsx(TabClasses.badge, className);
    return /* @__PURE__ */ jsx(Badge, { ref, className: badgeClasses, variant: "info", ...props });
  }
);
TabBadge.displayName = "Tab.Badge";

export { TabBadge };
//# sourceMappingURL=tab-badge.js.map
