"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const componentUtils = require('../../system/utils/component-utils.cjs');
const tabBar_styles = require('./tab-bar.styles.cjs');
const useTabBar = require('./hooks/use-tab-bar.cjs');
const useTabBarManager = require('./hooks/use-tab-bar-manager.cjs');
const tabBarList = require('./subcomponents/tab-bar-list.cjs');
const tabBarPanel = require('./subcomponents/tab-bar-panel.cjs');
const tabBarAddTab = require('./subcomponents/tab-bar-add-tab.cjs');

const TabBar = React.forwardRef(
  ({
    className,
    variant = "default",
    size = "default",
    children,
    defaultValue,
    rememberActiveTab = false,
    uniqueId,
    onValueChange,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(tabBar_styles.TabBarClasses.root, size === "sm" && tabBar_styles.TabBarClasses.sm, className);
    const addTabId = componentUtils.useUniqueId("add-tab");
    const { activeTab, setActiveTab } = useTabBarManager.useTabBarManager(
      defaultValue,
      rememberActiveTab,
      uniqueId,
      onValueChange,
      addTabId
    );
    return /* @__PURE__ */ jsxRuntime.jsx(useTabBar.TabBarContext.Provider, { value: { activeTab, setActiveTab, variant, size, addTabId }, children: /* @__PURE__ */ jsxRuntime.jsx(
      tabBar_styles.StyledTabBar,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        defaultValue,
        value: activeTab,
        variant,
        onValueChange: setActiveTab,
        ...props,
        children
      }
    ) });
  }
);
TabBar.displayName = "TabBar";
TabBar.List = tabBarList.TabBarList;
TabBar.Panel = tabBarPanel.TabBarPanel;
TabBar.AddTab = tabBarAddTab.TabBarAddTab;

exports.default = TabBar;
//# sourceMappingURL=tab-bar.cjs.map
