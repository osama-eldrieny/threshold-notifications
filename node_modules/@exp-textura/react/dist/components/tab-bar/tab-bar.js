"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { useUniqueId } from '../../system/utils/component-utils.js';
import { TabBarClasses, StyledTabBar } from './tab-bar.styles.js';
import { TabBarContext } from './hooks/use-tab-bar.js';
import { useTabBarManager } from './hooks/use-tab-bar-manager.js';
import { TabBarList } from './subcomponents/tab-bar-list.js';
import { TabBarPanel } from './subcomponents/tab-bar-panel.js';
import { TabBarAddTab } from './subcomponents/tab-bar-add-tab.js';

const TabBar = forwardRef(
  ({
    className,
    variant = "default",
    size = "default",
    children,
    defaultValue,
    rememberActiveTab = false,
    uniqueId,
    onValueChange,
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(TabBarClasses.root, size === "sm" && TabBarClasses.sm, className);
    const addTabId = useUniqueId("add-tab");
    const { activeTab, setActiveTab } = useTabBarManager(
      defaultValue,
      rememberActiveTab,
      uniqueId,
      onValueChange,
      addTabId
    );
    return /* @__PURE__ */ jsx(TabBarContext.Provider, { value: { activeTab, setActiveTab, variant, size, addTabId }, children: /* @__PURE__ */ jsx(
      StyledTabBar,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        defaultValue,
        value: activeTab,
        variant,
        onValueChange: setActiveTab,
        ...props,
        children
      }
    ) });
  }
);
TabBar.displayName = "TabBar";
TabBar.List = TabBarList;
TabBar.Panel = TabBarPanel;
TabBar.AddTab = TabBarAddTab;

export { TabBar as default };
//# sourceMappingURL=tab-bar.js.map
