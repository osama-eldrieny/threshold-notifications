"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { ToggleButtonClasses, StyledToggleButton } from './toggle-button.styles.js';
import { useToggleButton } from './hooks/use-toggle-button.js';

const ToggleButton = forwardRef(
  ({
    className,
    variant = "default",
    children,
    selected: controlledSelected,
    defaultSelected,
    onValueChange,
    disabled = false,
    size,
    required = false,
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(
      ToggleButtonClasses.root,
      {
        [ToggleButtonClasses.sm]: size === "sm"
      },
      className
    );
    const { role, handleKeyDown, renderChildren } = useToggleButton({
      disabled,
      selected: controlledSelected,
      defaultSelected,
      onValueChange,
      children,
      variant,
      size});
    return /* @__PURE__ */ jsx(
      StyledToggleButton,
      {
        ref,
        "aria-orientation": "horizontal",
        className: rootClasses,
        currentTheme: theme,
        role,
        variant,
        onKeyDown: handleKeyDown,
        ...props,
        children: renderChildren()
      }
    );
  }
);
ToggleButton.displayName = "ToggleButton";

export { ToggleButton as default };
//# sourceMappingURL=toggle-button.js.map
