"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const toggleButton_styles = require('./toggle-button.styles.cjs');
const useToggleButton = require('./hooks/use-toggle-button.cjs');

const ToggleButton = React.forwardRef(
  ({
    className,
    variant = "default",
    children,
    selected: controlledSelected,
    defaultSelected,
    onValueChange,
    disabled = false,
    size,
    required = false,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(
      toggleButton_styles.ToggleButtonClasses.root,
      {
        [toggleButton_styles.ToggleButtonClasses.sm]: size === "sm"
      },
      className
    );
    const { role, handleKeyDown, renderChildren } = useToggleButton.useToggleButton({
      disabled,
      selected: controlledSelected,
      defaultSelected,
      onValueChange,
      children,
      variant,
      size});
    return /* @__PURE__ */ jsxRuntime.jsx(
      toggleButton_styles.StyledToggleButton,
      {
        ref,
        "aria-orientation": "horizontal",
        className: rootClasses,
        currentTheme: theme,
        role,
        variant,
        onKeyDown: handleKeyDown,
        ...props,
        children: renderChildren()
      }
    );
  }
);
ToggleButton.displayName = "ToggleButton";

exports.default = ToggleButton;
//# sourceMappingURL=toggle-button.cjs.map
