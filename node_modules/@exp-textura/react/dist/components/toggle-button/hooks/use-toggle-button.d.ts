import { ToggleButtonProps } from '../toggle-button.types';
import { default as React } from 'react';
export declare const useToggleButton: ({ disabled, selected: controlledSelected, defaultSelected, onValueChange, children, variant: buttonVariant, size: buttonSize, }: Partial<ToggleButtonProps>) => {
    role: string;
    handleKeyDown: (event: React.KeyboardEvent) => void;
    renderChildren: () => React.ReactNode;
};
//# sourceMappingURL=use-toggle-button.d.ts.map