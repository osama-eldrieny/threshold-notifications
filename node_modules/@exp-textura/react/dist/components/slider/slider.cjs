"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const RadixSlider = require('@radix-ui/react-slider');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const slider_types = require('./slider.types.cjs');
const slider_styles = require('./slider.styles.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixSlider__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixSlider);

const defaultGetItemAriaLabel = (label) => {
  switch (label) {
    case slider_types.SliderAriaLabel.Thumb:
      return "Value";
    case slider_types.SliderAriaLabel.ThumbMin:
      return "Minimum value";
    case slider_types.SliderAriaLabel.ThumbMax:
      return "Maximum value";
  }
};
const Slider = React.forwardRef(
  ({
    className,
    variant = "default",
    behavior = "single",
    rangeMin = 0,
    rangeMax = 100,
    step = 1,
    defaultValue,
    defaultMinValue,
    defaultMaxValue,
    disabled = false,
    dir,
    value,
    onValueChange,
    getItemAriaLabel = defaultGetItemAriaLabel,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(slider_styles.SliderClasses.root, className);
    const initialRange = React.useMemo(() => {
      if (behavior === "range") {
        return [Number(defaultMinValue ?? rangeMin), Number(defaultMaxValue ?? rangeMax)];
      }
      return [Number(defaultValue ?? rangeMin)];
    }, [behavior, defaultMinValue, defaultMaxValue, defaultValue, rangeMin, rangeMax]);
    const isControlled = value !== void 0;
    const [internalValue, setInternalValue] = React.useState(initialRange);
    const currentValue = isControlled ? value : internalValue;
    const handleChange = React.useCallback(
      (newValue) => {
        if (!isControlled) {
          setInternalValue(newValue);
        }
        onValueChange?.(newValue);
      },
      [isControlled, onValueChange]
    );
    React.useEffect(() => {
      if (isControlled && value !== void 0) {
        if (JSON.stringify(value) !== JSON.stringify(internalValue)) {
          setInternalValue(value);
        }
      }
    }, [value, isControlled, internalValue]);
    return /* @__PURE__ */ jsxRuntime.jsxs(
      slider_styles.StyledSlider,
      {
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        onChange: void 0,
        children: [
          /* @__PURE__ */ jsxRuntime.jsxs("div", { className: slider_styles.SliderClasses.labelContainer, children: [
            /* @__PURE__ */ jsxRuntime.jsx("span", { className: slider_styles.SliderClasses.labelMin, children: behavior === "single" ? rangeMin : currentValue[0] }),
            /* @__PURE__ */ jsxRuntime.jsx("span", { className: slider_styles.SliderClasses.labelMax, children: behavior === "single" ? currentValue[0] : currentValue[1] })
          ] }),
          /* @__PURE__ */ jsxRuntime.jsxs(
            RadixSlider__namespace.Root,
            {
              ref,
              className: slider_styles.SliderClasses.sliderRoot,
              dir,
              disabled,
              max: rangeMax,
              min: rangeMin,
              minStepsBetweenThumbs: 1,
              step,
              value: currentValue,
              onValueChange: handleChange,
              children: [
                /* @__PURE__ */ jsxRuntime.jsx(RadixSlider__namespace.Track, { className: slider_styles.SliderClasses.track, children: /* @__PURE__ */ jsxRuntime.jsx(RadixSlider__namespace.Range, { className: slider_styles.SliderClasses.range }) }),
                currentValue.map((_, index) => /* @__PURE__ */ jsxRuntime.jsx(
                  RadixSlider__namespace.Thumb,
                  {
                    "aria-label": getItemAriaLabel(
                      behavior === "range" ? index === 0 ? slider_types.SliderAriaLabel.ThumbMin : slider_types.SliderAriaLabel.ThumbMax : slider_types.SliderAriaLabel.Thumb
                    ),
                    className: slider_styles.SliderClasses.thumb
                  },
                  index
                ))
              ]
            }
          )
        ]
      }
    );
  }
);
Slider.displayName = "Slider";

exports.default = Slider;
//# sourceMappingURL=slider.cjs.map
