"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const clsx$1 = require('clsx');
const icons = require('@exp-textura/icons');
const iconButton = require('../../icon-button/icon-button.cjs');
const useComboboxState = require('../hooks/use-combobox-state.cjs');
const useComboboxInteraction = require('../hooks/use-combobox-interaction.cjs');
const combobox_styles = require('../combobox.styles.cjs');

const ComboboxClearIconButton = ({
  children,
  className,
  "aria-label": customAriaLabel,
  hideWhenEmpty = true
}) => {
  const { value, disabled, inputValue, inputRef } = useComboboxState.useComboboxState();
  const { clearValue } = useComboboxInteraction.useComboboxInteraction();
  const buttonClasses = clsx$1.clsx(combobox_styles.ComboboxClasses.clearIconButton, className);
  const commonProps = {
    "aria-label": customAriaLabel || "clear selected value"
  };
  if (!value && !inputValue && hideWhenEmpty) {
    return null;
  }
  const handleClear = () => {
    clearValue();
    queueMicrotask(() => {
      inputRef.current?.focus();
    });
  };
  return /* @__PURE__ */ jsxRuntime.jsx(
    iconButton.default,
    {
      buttonType: "secondary",
      className: buttonClasses,
      disabled,
      size: "sm",
      onClick: (e) => {
        e.stopPropagation();
        e.preventDefault();
        handleClear();
      },
      onKeyDown: (e) => e.stopPropagation(),
      onPointerDown: (e) => e.preventDefault(),
      ...commonProps,
      children: children || /* @__PURE__ */ jsxRuntime.jsx(icons.Clear, {})
    }
  );
};
ComboboxClearIconButton.displayName = "Combobox.ClearIconButton";

exports.ComboboxClearIconButton = ComboboxClearIconButton;
//# sourceMappingURL=combobox-clear-icon-button.cjs.map
