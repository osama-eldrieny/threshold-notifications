"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { ComboboxButton as ComboboxButton$1 } from '@headlessui/react';
import { clsx } from 'clsx';
import { ChevronDown } from '@exp-textura/icons';
import { ComboboxClasses } from '../combobox.styles.js';
import { useComboboxState } from '../hooks/use-combobox-state.js';
import { useComboboxFloating } from '../hooks/use-combobox-floating.js';

const ComboboxButton = forwardRef(
  ({ children, className, ...props }, ref) => {
    const { disabled } = useComboboxState();
    const { setOpen, currentOpenState } = useComboboxFloating();
    return /* @__PURE__ */ jsx(
      ComboboxButton$1,
      {
        ref,
        "aria-label": "toggle combobox options",
        className: clsx(ComboboxClasses.button, className),
        disabled,
        onClick: () => setOpen(!currentOpenState),
        ...props,
        children: children || /* @__PURE__ */ jsx(ChevronDown, {})
      }
    );
  }
);
ComboboxButton.displayName = "Combobox.Button";

export { ComboboxButton };
//# sourceMappingURL=combobox-button.js.map
