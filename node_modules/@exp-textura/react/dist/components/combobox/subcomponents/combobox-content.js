"use client";
import { jsx } from 'react/jsx-runtime';
import React, { forwardRef } from 'react';
import { FloatingPortal } from '@floating-ui/react';
import '@emotion/react';
import useTexturaTheme from '../../../theme/hooks/use-theme.js';
import { clsx } from 'clsx';
import { flattenChildren } from '../../../system/utils/component-utils.js';
import { assignRef } from '../../../hooks/use-merge-refs.js';
import { useComboboxState } from '../hooks/use-combobox-state.js';
import { useComboboxFloating } from '../hooks/use-combobox-floating.js';
import { StyledCombobox, ComboboxClasses } from '../combobox.styles.js';
import { ComboboxGroup } from './combobox-group.js';
import { ComboboxItem } from './combobox-item.js';

const ComboboxContent = forwardRef(
  ({ children, className, style, ...props }, ref) => {
    const { styleSlots, variant, noResults, as } = useComboboxState();
    const { refs, floatingStyles, getFloatingProps, isMounted, listRef } = useComboboxFloating();
    const theme = useTexturaTheme();
    const childrenArray = flattenChildren(children);
    const groupedChildren = [];
    let currentGroup = [];
    const hasChildren = childrenArray.length > 0;
    const isGroup = (child) => React.isValidElement(child) && child.type?.displayName?.endsWith("Group");
    const isDivider = (child) => React.isValidElement(child) && child.type?.displayName?.endsWith("Divider");
    const isItem = (child) => React.isValidElement(child) && child.type?.displayName?.endsWith("Item");
    const flushGroup = () => {
      if (currentGroup.length > 0) {
        groupedChildren.push(
          /* @__PURE__ */ jsx(ComboboxGroup, { children: currentGroup }, groupedChildren.length)
        );
        currentGroup = [];
      }
    };
    childrenArray.forEach((child, index) => {
      if (isGroup(child) || isDivider(child)) {
        flushGroup();
        groupedChildren.push(child);
      } else if (isItem(child)) {
        currentGroup.push(
          React.cloneElement(child, {
            key: index,
            ref: (node) => {
              listRef.current[index] = node;
              assignRef(child.props.ref, node);
            }
          })
        );
      } else {
        currentGroup.push(
          /* @__PURE__ */ jsx(
            ComboboxItem,
            {
              ref: (node) => {
                listRef.current[index] = node;
              },
              ...child.props
            },
            index
          )
        );
      }
    });
    flushGroup();
    if (!isMounted) {
      return null;
    }
    return /* @__PURE__ */ jsx(FloatingPortal, { children: /* @__PURE__ */ jsx(
      "div",
      {
        ref: refs.setFloating,
        style: { zIndex: theme?.zIndex?.dynamic, ...floatingStyles },
        ...getFloatingProps(props),
        children: /* @__PURE__ */ jsx(
          StyledCombobox,
          {
            ref,
            as,
            className: clsx(ComboboxClasses.content, className),
            currentTheme: theme,
            role: "listbox",
            style,
            styleSlots,
            variant,
            children: hasChildren ? groupedChildren : /* @__PURE__ */ jsx("div", { className: ComboboxClasses.noResults, children: noResults })
          }
        )
      }
    ) });
  }
);
ComboboxContent.displayName = "Combobox.Content";

export { ComboboxContent };
//# sourceMappingURL=combobox-content.js.map
