"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import { ComboboxClasses } from '../combobox.styles.js';

const ComboboxDivider = forwardRef(
  ({ className, ...props }, ref) => {
    return /* @__PURE__ */ jsx(
      "div",
      {
        ref,
        className: clsx(ComboboxClasses.divider, className),
        role: "separator",
        ...props
      }
    );
  }
);
ComboboxDivider.displayName = "Combobox.Divider";

export { ComboboxDivider };
//# sourceMappingURL=combobox-divider.js.map
