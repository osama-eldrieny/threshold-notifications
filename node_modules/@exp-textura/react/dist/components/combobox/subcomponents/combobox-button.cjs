"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const react = require('@headlessui/react');
const clsx$1 = require('clsx');
const icons = require('@exp-textura/icons');
const combobox_styles = require('../combobox.styles.cjs');
const useComboboxState = require('../hooks/use-combobox-state.cjs');
const useComboboxFloating = require('../hooks/use-combobox-floating.cjs');

const ComboboxButton = React.forwardRef(
  ({ children, className, ...props }, ref) => {
    const { disabled } = useComboboxState.useComboboxState();
    const { setOpen, currentOpenState } = useComboboxFloating.useComboboxFloating();
    return /* @__PURE__ */ jsxRuntime.jsx(
      react.ComboboxButton,
      {
        ref,
        "aria-label": "toggle combobox options",
        className: clsx$1.clsx(combobox_styles.ComboboxClasses.button, className),
        disabled,
        onClick: () => setOpen(!currentOpenState),
        ...props,
        children: children || /* @__PURE__ */ jsxRuntime.jsx(icons.ChevronDown, {})
      }
    );
  }
);
ComboboxButton.displayName = "Combobox.Button";

exports.ComboboxButton = ComboboxButton;
//# sourceMappingURL=combobox-button.cjs.map
