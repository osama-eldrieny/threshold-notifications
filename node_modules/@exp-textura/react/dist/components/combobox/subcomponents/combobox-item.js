"use client";
import { jsx, jsxs, Fragment } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { ComboboxOption } from '@headlessui/react';
import { clsx } from 'clsx';
import { Check } from '@exp-textura/icons';
import { useComboboxState } from '../hooks/use-combobox-state.js';
import { ComboboxClasses } from '../combobox.styles.js';

const ComboboxItem = forwardRef(
  ({ children, value, className, ...restProps }, ref) => {
    const { showSelectedIndicator } = useComboboxState();
    return /* @__PURE__ */ jsx(
      ComboboxOption,
      {
        ref,
        className: ({ focus, selected }) => clsx(ComboboxClasses.item, className, {
          [ComboboxClasses.itemActive]: focus,
          [ComboboxClasses.itemSelected]: selected
        }),
        value,
        ...restProps,
        children: ({ selected }) => /* @__PURE__ */ jsxs(Fragment, { children: [
          showSelectedIndicator && selected && /* @__PURE__ */ jsx(Check, {}),
          children
        ] })
      }
    );
  }
);
ComboboxItem.displayName = "Combobox.Item";

export { ComboboxItem };
//# sourceMappingURL=combobox-item.js.map
