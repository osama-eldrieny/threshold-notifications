"use client";
import { useContext, createContext } from 'react';

const ComboboxStateContext = createContext(void 0);
const useComboboxState = () => {
  const context = useContext(ComboboxStateContext);
  if (!context) {
    throw new Error("useComboboxState must be used within a Combobox component");
  }
  return context;
};

export { ComboboxStateContext, useComboboxState };
//# sourceMappingURL=use-combobox-state.js.map
