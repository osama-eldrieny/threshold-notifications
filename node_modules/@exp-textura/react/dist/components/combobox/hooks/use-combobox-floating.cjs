"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const React = require('react');

const ComboboxFloatingContext = React.createContext(
  void 0
);
const useComboboxFloating = () => {
  const context = React.useContext(ComboboxFloatingContext);
  if (!context) {
    throw new Error("useComboboxFloating must be used within a Combobox component");
  }
  return context;
};

exports.ComboboxFloatingContext = ComboboxFloatingContext;
exports.useComboboxFloating = useComboboxFloating;
//# sourceMappingURL=use-combobox-floating.cjs.map
