"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const React = require('react');

const ComboboxStateContext = React.createContext(void 0);
const useComboboxState = () => {
  const context = React.useContext(ComboboxStateContext);
  if (!context) {
    throw new Error("useComboboxState must be used within a Combobox component");
  }
  return context;
};

exports.ComboboxStateContext = ComboboxStateContext;
exports.useComboboxState = useComboboxState;
//# sourceMappingURL=use-combobox-state.cjs.map
