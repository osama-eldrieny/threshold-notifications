"use client";
import { useContext, createContext } from 'react';

const ComboboxFloatingContext = createContext(
  void 0
);
const useComboboxFloating = () => {
  const context = useContext(ComboboxFloatingContext);
  if (!context) {
    throw new Error("useComboboxFloating must be used within a Combobox component");
  }
  return context;
};

export { ComboboxFloatingContext, useComboboxFloating };
//# sourceMappingURL=use-combobox-floating.js.map
