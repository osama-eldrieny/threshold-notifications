"use client";
import { useContext, createContext } from 'react';

const ComboboxInteractionContext = createContext(
  void 0
);
const useComboboxInteraction = () => {
  const context = useContext(ComboboxInteractionContext);
  if (!context) {
    throw new Error("useComboboxInteraction must be used within a Combobox component");
  }
  return context;
};

export { ComboboxInteractionContext, useComboboxInteraction };
//# sourceMappingURL=use-combobox-interaction.js.map
