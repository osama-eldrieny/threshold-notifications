"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef, useMemo } from 'react';
import '@emotion/react';
import TexturaThemeProvider from '../../theme/providers/theme-provider.js';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import Menu from '../menu/menu.js';
import { clsx } from 'clsx';
import { createMainNavTheme } from '../main-nav/utils/create-main-nav-theme.js';
import { MainNavMenuItem } from './subcomponents/main-nav-menu-item.js';
import { MainNavMenuInlineBadge } from './subcomponents/main-nav-menu-inline-badge.js';
import { MainNavMenuClasses } from './main-nav-menu.styles.js';
import { MainNavMenuContext } from './hooks/use-main-nav-menu.js';

const MainNavMenuRoot = forwardRef(
  ({ variant = "default", className, children, ...props }, ref) => {
    const theme = useTexturaTheme();
    const mainNavTheme = useMemo(() => theme && createMainNavTheme(theme), [theme]);
    const variantProp = variant === "default" ? "mainNavMenu" : variant;
    const contextValue = useMemo(() => ({ styleSlots: props.styleSlots }), [props.styleSlots]);
    return /* @__PURE__ */ jsx(TexturaThemeProvider, { theme: mainNavTheme, children: /* @__PURE__ */ jsx(MainNavMenuContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsx(
      Menu,
      {
        ref,
        className: clsx(MainNavMenuClasses.root, className),
        variant: variantProp,
        ...props,
        children
      }
    ) }) });
  }
);
MainNavMenuRoot.displayName = "MainNavMenu";
const MainNavMenu = Object.assign(MainNavMenuRoot, {
  Trigger: Menu.Trigger,
  Content: Menu.Content,
  Group: Menu.Group,
  Divider: Menu.Divider,
  Item: MainNavMenuItem,
  InlineBadge: MainNavMenuInlineBadge
});

export { MainNavMenu, MainNavMenu as default };
//# sourceMappingURL=main-nav-menu.js.map
