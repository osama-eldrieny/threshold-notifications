"use client";
import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../../theme/hooks/use-theme.js';
import Menu from '../../menu/menu.js';
import { Slot } from '@radix-ui/react-slot';
import { MainNavMenuClasses, StyledMainNavMenuItem } from '../main-nav-menu.styles.js';
import { useMainNavMenu } from '../hooks/use-main-nav-menu.js';

const MainNavMenuItem = forwardRef(
  ({
    className,
    variant = "default",
    children,
    icon,
    hoverIcon,
    as = "a",
    asChild,
    ...props
  }, ref) => {
    const theme = useTexturaTheme(), rootClasses = clsx(MainNavMenuClasses.item, className), variantProp = variant === "default" ? "mainNavMenu" : variant;
    const context = useMainNavMenu();
    const mergedStyleSlots = { ...context?.styleSlots, ...props.styleSlots };
    const content = /* @__PURE__ */ jsxs(Fragment, { children: [
      icon && /* @__PURE__ */ jsx("span", { className: MainNavMenuClasses.itemIcon, children: hoverIcon ? /* @__PURE__ */ jsxs("span", { className: MainNavMenuClasses.iconSwap, children: [
        /* @__PURE__ */ jsx("span", { className: MainNavMenuClasses.iconSwapDefault, children: icon }),
        /* @__PURE__ */ jsx("span", { "aria-hidden": true, className: MainNavMenuClasses.iconSwapHover, children: hoverIcon })
      ] }) : icon }),
      /* @__PURE__ */ jsx("span", { className: MainNavMenuClasses.itemLabel, children })
    ] });
    return /* @__PURE__ */ jsx(Menu.Item, { asChild: true, children: /* @__PURE__ */ jsx(
      StyledMainNavMenuItem,
      {
        ref,
        as: asChild ? Slot : as,
        className: rootClasses,
        currentTheme: theme,
        styleSlots: mergedStyleSlots,
        variant: variantProp,
        ...props,
        children: asChild ? children : content
      }
    ) });
  }
);
MainNavMenuItem.displayName = "MainNavMenu.Item";

export { MainNavMenuItem };
//# sourceMappingURL=main-nav-menu-item.js.map
