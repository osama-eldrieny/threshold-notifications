"use client";
import { jsx } from 'react/jsx-runtime';
import InlineBadge from '../../inline-badge/inline-badge.js';
import { clsx } from 'clsx';
import { MainNavMenuClasses } from '../main-nav-menu.styles.js';
import { useMainNavMenu } from '../hooks/use-main-nav-menu.js';

const MainNavMenuInlineBadge = ({ children, className, ...props }) => {
  const context = useMainNavMenu();
  const contextStyleSlots = context?.styleSlots;
  return /* @__PURE__ */ jsx(
    InlineBadge,
    {
      className: clsx(MainNavMenuClasses.itemInlineBadge, className),
      size: "sm",
      styleSlots: {
        root: {
          ...contextStyleSlots?.itemInlineBadge
        }
      },
      variant: "newInverse",
      ...props,
      children
    }
  );
};
MainNavMenuInlineBadge.displayName = "MainNavMenu.InlineBadge";

export { MainNavMenuInlineBadge };
//# sourceMappingURL=main-nav-menu-inline-badge.js.map
