"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const clsx$1 = require('clsx');
const icons = require('@exp-textura/icons');
const useAccordionSection = require('../hooks/use-accordion-section.cjs');
const accordion_styles = require('../accordion.styles.cjs');

const DEFAULT_LEADING_ICON = /* @__PURE__ */ jsxRuntime.jsx(icons.ChevronRight, {});
const DEFAULT_TRAILING_ICON = /* @__PURE__ */ jsxRuntime.jsx(icons.ChevronDown, {});
const AccordionIndicator = ({
  icon,
  collapseIcon,
  className,
  isLeading = false,
  ...props
}) => {
  const { isExpanded } = useAccordionSection.useAccordionSection();
  const DefaultIcon = isLeading ? DEFAULT_LEADING_ICON : DEFAULT_TRAILING_ICON;
  const collapsedIconToShow = icon === void 0 ? DefaultIcon : icon;
  const expandedIconToShow = collapseIcon === void 0 ? collapsedIconToShow : collapseIcon;
  const applyRotation = collapseIcon === void 0;
  const iconToRender = isExpanded ? expandedIconToShow : collapsedIconToShow;
  return /* @__PURE__ */ jsxRuntime.jsx(
    "span",
    {
      className: clsx$1.clsx(accordion_styles.AccordionClasses.indicator, className),
      "data-expanded": isExpanded && applyRotation,
      "data-leading": isLeading,
      ...props,
      children: iconToRender
    }
  );
};
AccordionIndicator.displayName = "Accordion.Indicator";

exports.AccordionIndicator = AccordionIndicator;
//# sourceMappingURL=accordion-indicator.cjs.map
