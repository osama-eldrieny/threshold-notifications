"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
const useAccordionSection = require('../hooks/use-accordion-section.cjs');
const accordion_styles = require('../accordion.styles.cjs');

const AccordionHeader = React.forwardRef(
  ({ children, className, headingLevel = 3, ...props }, ref) => {
    const { isExpanded, handleToggle, sectionId, buttonId } = useAccordionSection.useAccordionSection();
    const HeadingTag = props.as || `h${headingLevel}`;
    return /* @__PURE__ */ jsxRuntime.jsx(HeadingTag, { className: accordion_styles.AccordionClasses.header, children: /* @__PURE__ */ jsxRuntime.jsx(
      "button",
      {
        ref,
        "aria-controls": sectionId,
        "aria-expanded": isExpanded,
        className: clsx$1.clsx(accordion_styles.AccordionClasses.button, className),
        "data-expanded": isExpanded,
        id: buttonId,
        onClick: handleToggle,
        ...props,
        children
      }
    ) });
  }
);
AccordionHeader.displayName = "Accordion.Header";

exports.AccordionHeader = AccordionHeader;
//# sourceMappingURL=accordion-header.cjs.map
