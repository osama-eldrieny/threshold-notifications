"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
const componentUtils = require('../../../system/utils/component-utils.cjs');
const useAccordion = require('../hooks/use-accordion.cjs');
const accordion_styles = require('../accordion.styles.cjs');
const useAccordionSection = require('../hooks/use-accordion-section.cjs');

const AccordionSection = React.forwardRef(
  ({
    children,
    isInitialOpen = false,
    className,
    onValueChange,
    isExpanded: controlledExpanded,
    ...props
  }, ref) => {
    const { openIndex, setOpenIndex, onePanelAtATime, registerSection } = useAccordion.useAccordion();
    const sectionClasses = clsx$1.clsx(accordion_styles.AccordionClasses.section, className);
    const sectionIndex = React.useMemo(() => registerSection(isInitialOpen), [isInitialOpen]);
    const sectionId = componentUtils.useUniqueId("accordion-section");
    const buttonId = componentUtils.useUniqueId("accordion-section-button");
    const isExpanded = React.useMemo(() => {
      return (controlledExpanded || openIndex?.includes(sectionIndex)) ?? false;
    }, [openIndex, sectionIndex, controlledExpanded]);
    const handleToggle = React.useCallback(() => {
      setOpenIndex((prevOpenIndex) => {
        const currentOpenIndexes = prevOpenIndex ?? [];
        const isOpen = currentOpenIndexes.includes(sectionIndex);
        onValueChange?.(isOpen);
        if (onePanelAtATime) {
          return isOpen ? null : [sectionIndex];
        } else {
          return isOpen ? currentOpenIndexes.filter((i) => i !== sectionIndex) : [...currentOpenIndexes, sectionIndex];
        }
      });
    }, [setOpenIndex, sectionIndex, onePanelAtATime, onValueChange]);
    const contextValue = React.useMemo(
      () => ({
        isExpanded,
        sectionId,
        buttonId,
        handleToggle,
        sectionIndex
      }),
      [isExpanded, sectionId, buttonId, handleToggle, sectionIndex]
    );
    return /* @__PURE__ */ jsxRuntime.jsx(useAccordionSection.AccordionSectionContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsxRuntime.jsx("div", { ref, className: sectionClasses, "data-expanded": isExpanded, ...props, children }) });
  }
);
AccordionSection.displayName = "Accordion.Section";

exports.AccordionSection = AccordionSection;
//# sourceMappingURL=accordion-section.cjs.map
