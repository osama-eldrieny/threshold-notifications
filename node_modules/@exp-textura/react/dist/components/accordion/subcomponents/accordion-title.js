"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import { AccordionClasses } from '../accordion.styles.js';

const AccordionTitle = forwardRef(
  ({ children, className, ...props }, ref) => {
    return /* @__PURE__ */ jsx("span", { ref, className: clsx(AccordionClasses.title, className), ...props, children });
  }
);
AccordionTitle.displayName = "Accordion.Title";

export { AccordionTitle };
//# sourceMappingURL=accordion-title.js.map
