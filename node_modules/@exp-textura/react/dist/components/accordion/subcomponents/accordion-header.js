"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import { useAccordionSection } from '../hooks/use-accordion-section.js';
import { AccordionClasses } from '../accordion.styles.js';

const AccordionHeader = forwardRef(
  ({ children, className, headingLevel = 3, ...props }, ref) => {
    const { isExpanded, handleToggle, sectionId, buttonId } = useAccordionSection();
    const HeadingTag = props.as || `h${headingLevel}`;
    return /* @__PURE__ */ jsx(HeadingTag, { className: AccordionClasses.header, children: /* @__PURE__ */ jsx(
      "button",
      {
        ref,
        "aria-controls": sectionId,
        "aria-expanded": isExpanded,
        className: clsx(AccordionClasses.button, className),
        "data-expanded": isExpanded,
        id: buttonId,
        onClick: handleToggle,
        ...props,
        children
      }
    ) });
  }
);
AccordionHeader.displayName = "Accordion.Header";

export { AccordionHeader };
//# sourceMappingURL=accordion-header.js.map
