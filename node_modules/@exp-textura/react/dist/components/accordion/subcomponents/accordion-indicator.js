"use client";
import { jsx } from 'react/jsx-runtime';
import { clsx } from 'clsx';
import { ChevronRight, ChevronDown } from '@exp-textura/icons';
import { useAccordionSection } from '../hooks/use-accordion-section.js';
import { AccordionClasses } from '../accordion.styles.js';

const DEFAULT_LEADING_ICON = /* @__PURE__ */ jsx(ChevronRight, {});
const DEFAULT_TRAILING_ICON = /* @__PURE__ */ jsx(ChevronDown, {});
const AccordionIndicator = ({
  icon,
  collapseIcon,
  className,
  isLeading = false,
  ...props
}) => {
  const { isExpanded } = useAccordionSection();
  const DefaultIcon = isLeading ? DEFAULT_LEADING_ICON : DEFAULT_TRAILING_ICON;
  const collapsedIconToShow = icon === void 0 ? DefaultIcon : icon;
  const expandedIconToShow = collapseIcon === void 0 ? collapsedIconToShow : collapseIcon;
  const applyRotation = collapseIcon === void 0;
  const iconToRender = isExpanded ? expandedIconToShow : collapsedIconToShow;
  return /* @__PURE__ */ jsx(
    "span",
    {
      className: clsx(AccordionClasses.indicator, className),
      "data-expanded": isExpanded && applyRotation,
      "data-leading": isLeading,
      ...props,
      children: iconToRender
    }
  );
};
AccordionIndicator.displayName = "Accordion.Indicator";

export { AccordionIndicator };
//# sourceMappingURL=accordion-indicator.js.map
