"use client";
import { useContext, createContext } from 'react';

const AccordionSectionContext = createContext(
  void 0
);
const useAccordionSection = () => {
  const context = useContext(AccordionSectionContext);
  if (context === void 0) {
    throw new Error("useAccordionSection must be used within an AccordionSectionContext.Provider");
  }
  return context;
};

export { AccordionSectionContext, useAccordionSection };
//# sourceMappingURL=use-accordion-section.js.map
