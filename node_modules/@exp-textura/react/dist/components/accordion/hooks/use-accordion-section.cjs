"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const React = require('react');

const AccordionSectionContext = React.createContext(
  void 0
);
const useAccordionSection = () => {
  const context = React.useContext(AccordionSectionContext);
  if (context === void 0) {
    throw new Error("useAccordionSection must be used within an AccordionSectionContext.Provider");
  }
  return context;
};

exports.AccordionSectionContext = AccordionSectionContext;
exports.useAccordionSection = useAccordionSection;
//# sourceMappingURL=use-accordion-section.cjs.map
