"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const linkButton_styles = require('./link-button.styles.cjs');

const LinkButton = React.forwardRef(function LinkButton2({ as, className, variant = "default", children, disabled, size, ...props }, ref) {
  const theme = useTheme.default();
  const rootClasses = clsx$1.clsx(linkButton_styles.LinkButtonClasses.root, className, {
    [linkButton_styles.LinkButtonClasses.sm]: size === "sm"
  });
  const handleClick = (e) => {
    if (disabled) {
      e.preventDefault();
      return;
    }
    if (props.onClick) {
      props.onClick(e);
    }
  };
  const inferredAs = as || ("href" in props ? "a" : "button");
  return /* @__PURE__ */ jsxRuntime.jsx(
    linkButton_styles.StyledLinkButton,
    {
      ref,
      "aria-disabled": disabled,
      as: inferredAs,
      className: rootClasses,
      currentTheme: theme,
      "data-disabled": disabled,
      disabled,
      variant,
      ...props,
      onClick: handleClick,
      children
    }
  );
});
LinkButton.displayName = "LinkButton";

exports.default = LinkButton;
//# sourceMappingURL=link-button.cjs.map
