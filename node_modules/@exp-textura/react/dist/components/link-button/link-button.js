"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { LinkButtonClasses, StyledLinkButton } from './link-button.styles.js';

const LinkButton = forwardRef(function LinkButton2({ as, className, variant = "default", children, disabled, size, ...props }, ref) {
  const theme = useTexturaTheme();
  const rootClasses = clsx(LinkButtonClasses.root, className, {
    [LinkButtonClasses.sm]: size === "sm"
  });
  const handleClick = (e) => {
    if (disabled) {
      e.preventDefault();
      return;
    }
    if (props.onClick) {
      props.onClick(e);
    }
  };
  const inferredAs = as || ("href" in props ? "a" : "button");
  return /* @__PURE__ */ jsx(
    StyledLinkButton,
    {
      ref,
      "aria-disabled": disabled,
      as: inferredAs,
      className: rootClasses,
      currentTheme: theme,
      "data-disabled": disabled,
      disabled,
      variant,
      ...props,
      onClick: handleClick,
      children
    }
  );
});
LinkButton.displayName = "LinkButton";

export { LinkButton as default };
//# sourceMappingURL=link-button.js.map
