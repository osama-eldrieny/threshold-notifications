"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const styled = require('@emotion/styled');
const componentUtils = require('../../system/utils/component-utils.cjs');
const React = require('react');

const componentName = "LinkButton";
const LinkButtonClasses = {
  root: "exp-textura_link-button",
  sm: "exp-textura_link-button_sm"
};
const getVariantConfig = (theme) => ({
  secondary: {
    root: {
      color: theme.buttonLink.secondary.color.content.enabled,
      textDecoration: "underline"
    },
    hover: {
      color: theme.buttonLink.secondary.color.content.hover,
      textDecoration: "none"
    },
    pressed: {
      color: theme.buttonLink.secondary.color.content.pressed,
      textDecoration: "underline"
    },
    disabled: {
      color: theme.buttonLink.secondary.color.content.disabled,
      textDecoration: "underline"
    }
  }
});
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = React.useMemo(
    () => componentUtils.getStyleSlots(
      componentName,
      theme,
      variant,
      styleSlotsProp,
      getVariantConfig(theme)
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "inline-flex",
    background: "none",
    border: "none",
    cursor: "pointer",
    height: theme.toRem(20),
    alignItems: "center",
    justifyContent: "center",
    textAlign: "center",
    gap: theme.toRem(theme.spacing[8]),
    textDecoration: "none",
    padding: "0px",
    color: theme.buttonLink.primary.color.content.enabled,
    ...theme.typography.label.md,
    "&:hover:not([data-disabled='true'])": {
      color: theme.buttonLink.primary.color.content.hover,
      textDecoration: "underline",
      ...styleSlots?.hover
    },
    "&:active:not([data-disabled='true'])": {
      color: theme.buttonLink.primary.color.content.pressed,
      ...styleSlots?.pressed
    },
    "&:disabled, &:disabled:hover, &[data-disabled='true']": {
      cursor: "not-allowed",
      color: theme.buttonLink.primary.color.content.disabled,
      textDecoration: "none",
      ...styleSlots?.disabled
    },
    "&:focus-visible": {
      ...theme.focusRing,
      ...styleSlots?.focus
    },
    [`&.${LinkButtonClasses.sm}`]: {
      height: theme.toRem(16),
      fontSize: theme.typography.label.sm.fontSize,
      ...styleSlots?.sm
    },
    ...styleSlots?.root
  };
};
const excludedDOMProps = componentUtils.getExcludedDOMProps();
const StyledLinkButton = styled("button", {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));

exports.LinkButtonClasses = LinkButtonClasses;
exports.StyledLinkButton = StyledLinkButton;
//# sourceMappingURL=link-button.styles.cjs.map
