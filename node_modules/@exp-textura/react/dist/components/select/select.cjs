"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const RadixSelect = require('@radix-ui/react-select');
const icons = require('@exp-textura/icons');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const clsx$1 = require('clsx');
const select_styles = require('./select.styles.cjs');
const useSelect = require('./hooks/use-select.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixSelect__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixSelect);

const SelectRoot = React.forwardRef(
  ({
    children,
    value: controlledValue,
    defaultValue,
    onValueChange: userOnValueChange,
    open: controlledOpen,
    defaultOpen = false,
    onOpenChange: userOnOpenChange,
    disabled = false,
    variant,
    styleSlots,
    placeholder,
    placeholderIcon,
    startAdornment,
    position = "popper",
    className,
    align,
    side,
    sideOffset = 4,
    alignOffset,
    arrowPadding,
    collisionPadding = 10,
    showSelectedIndicator = true,
    inputSize = "default",
    id,
    required,
    "aria-describedby": ariaDescribedBy,
    "aria-invalid": ariaInvalid,
    "aria-required": ariaRequired,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const [hasSelection, setHasSelection] = React.useState(Boolean(controlledValue || defaultValue));
    const [isInternalOpen, setInternalOpen] = React.useState(defaultOpen);
    const open = controlledOpen ?? isInternalOpen;
    const onOpenChange = (newOpen) => {
      setInternalOpen(newOpen);
      userOnOpenChange?.(newOpen);
    };
    const resolvedPlaceholder = /* @__PURE__ */ jsxRuntime.jsxs("span", { className: select_styles.SelectClasses.placeholderWrapper, children: [
      placeholderIcon,
      placeholder
    ] });
    React.useEffect(() => {
      if (controlledValue !== void 0) {
        setHasSelection(Boolean(controlledValue));
      }
    }, [controlledValue]);
    React.useEffect(() => {
      if (controlledValue === void 0 && defaultValue !== void 0) {
        setHasSelection(Boolean(defaultValue));
      }
    }, [controlledValue, defaultValue]);
    const handleRadixValueChange = (newValue) => {
      setHasSelection(Boolean(newValue));
      userOnValueChange?.(newValue);
    };
    const radixRootProps = {
      value: controlledValue,
      defaultValue,
      onValueChange: handleRadixValueChange,
      open,
      onOpenChange,
      disabled,
      required
    };
    const contextValue = {
      theme,
      variant,
      styleSlots,
      placeholder: resolvedPlaceholder,
      startAdornment,
      hasSelection,
      position,
      align,
      side,
      sideOffset,
      alignOffset,
      arrowPadding,
      collisionPadding,
      value: controlledValue,
      defaultValue,
      onValueChange: userOnValueChange,
      open,
      defaultOpen,
      onOpenChange,
      disabled,
      showSelectedIndicator,
      inputSize,
      id,
      "aria-describedby": ariaDescribedBy,
      "aria-invalid": ariaInvalid,
      "aria-required": ariaRequired,
      required
    };
    return /* @__PURE__ */ jsxRuntime.jsx(useSelect.SelectContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsxRuntime.jsx(
      select_styles.StyledSelect,
      {
        ref,
        className: clsx$1.clsx(select_styles.SelectClasses.root, className, {
          [select_styles.SelectClasses.sm]: inputSize === "sm",
          [select_styles.SelectClasses.hasSelection]: hasSelection
        }),
        currentTheme: theme,
        styleSlots,
        variant,
        ...props,
        children: /* @__PURE__ */ jsxRuntime.jsx(RadixSelect__namespace.Root, { ...radixRootProps, children })
      }
    ) });
  }
);
SelectRoot.displayName = "Select";
const Trigger = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    const {
      id,
      "aria-describedby": ariaDescribedBy,
      "aria-invalid": ariaInvalid,
      "aria-required": required,
      open,
      onOpenChange,
      startAdornment
    } = useSelect.useSelectContext();
    const handleKeyDown = (event) => {
      if (event.key.length === 1 && !event.ctrlKey && !event.metaKey && !open) {
        onOpenChange?.(true);
      }
    };
    return /* @__PURE__ */ jsxRuntime.jsxs(
      RadixSelect__namespace.Trigger,
      {
        ref,
        "aria-describedby": ariaDescribedBy,
        "aria-invalid": ariaInvalid,
        "aria-required": required,
        className: clsx$1.clsx(select_styles.SelectClasses.trigger, className),
        id,
        onKeyDown: handleKeyDown,
        ...props,
        children: [
          startAdornment && /* @__PURE__ */ jsxRuntime.jsx("span", { className: select_styles.SelectClasses.startAdornment, children: startAdornment }),
          children
        ]
      }
    );
  }
);
Trigger.displayName = "Select.Trigger";
const Value = ({ className, children, ...props }) => {
  const { placeholder } = useSelect.useSelectContext();
  return /* @__PURE__ */ jsxRuntime.jsx(
    RadixSelect__namespace.Value,
    {
      className: clsx$1.clsx(select_styles.SelectClasses.value, className),
      placeholder,
      ...props,
      children
    }
  );
};
Value.displayName = "Select.Value";
const Icon = React.forwardRef(
  ({ className, children = /* @__PURE__ */ jsxRuntime.jsx(icons.ChevronDown, {}), ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx(
      RadixSelect__namespace.Icon,
      {
        ref,
        className: clsx$1.clsx(select_styles.SelectClasses.triggerIcon, className),
        ...props,
        asChild: true,
        children
      }
    );
  }
);
Icon.displayName = "Select.Icon";
const Content = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    const {
      theme,
      variant,
      styleSlots,
      align,
      position,
      side,
      sideOffset,
      alignOffset,
      arrowPadding,
      collisionPadding
    } = useSelect.useSelectContext();
    const radixContentProps = {
      align,
      position,
      side,
      sideOffset,
      alignOffset,
      arrowPadding,
      collisionPadding,
      ...props
    };
    return /* @__PURE__ */ jsxRuntime.jsx(RadixSelect__namespace.Portal, { children: /* @__PURE__ */ jsxRuntime.jsx(
      select_styles.StyledSelect,
      {
        currentTheme: theme,
        "data-radix-popper-wrapper": "true",
        styleSlots,
        variant,
        children: /* @__PURE__ */ jsxRuntime.jsx(
          RadixSelect__namespace.Content,
          {
            ref,
            className: clsx$1.clsx(select_styles.SelectClasses.content, className),
            ...radixContentProps,
            children: /* @__PURE__ */ jsxRuntime.jsx(RadixSelect__namespace.Viewport, { className: select_styles.SelectClasses.viewport, children })
          }
        )
      }
    ) });
  }
);
Content.displayName = "Select.Content";
const SelectItemLabel = React.forwardRef(
  ({ children, className, ...props }, ref) => /* @__PURE__ */ jsxRuntime.jsx("span", { ref, className: clsx$1.clsx(select_styles.SelectClasses.label, className), ...props, children })
);
SelectItemLabel.displayName = "Select.ItemLabel";
const SelectItemSubtext = React.forwardRef(
  ({ children, className, ...props }, ref) => /* @__PURE__ */ jsxRuntime.jsx("span", { ref, className: clsx$1.clsx(select_styles.SelectClasses.subtext, className), ...props, children })
);
SelectItemSubtext.displayName = "Select.ItemSubtext";
const SelectItemLeadingIcon = React.forwardRef(
  ({ children, className, ...props }, ref) => /* @__PURE__ */ jsxRuntime.jsx("span", { ref, className: clsx$1.clsx(select_styles.SelectClasses.leadingIcon, className), ...props, children })
);
SelectItemLeadingIcon.displayName = "Select.ItemLeadingIcon";
const SelectItemTrailingIcon = React.forwardRef(
  ({ children, className, ...props }, ref) => /* @__PURE__ */ jsxRuntime.jsx("span", { ref, className: clsx$1.clsx(select_styles.SelectClasses.trailingIcon, className), ...props, children })
);
SelectItemTrailingIcon.displayName = "Select.ItemTrailingIcon";
const Item = React.forwardRef(
  ({ className, children, value, disabled, ...props }, ref) => {
    const { hasSelection, showSelectedIndicator, inputSize } = useSelect.useSelectContext();
    const content = React.Children.map(
      children,
      (child) => typeof child === "string" ? /* @__PURE__ */ jsxRuntime.jsx(SelectItemLabel, { children: child }) : child
    );
    const leadingIcon = React.Children.toArray(content).find(
      (child) => React.isValidElement(child) && child.type.displayName === SelectItemLeadingIcon.displayName
    );
    const trailingIcon = React.Children.toArray(content).find(
      (child) => React.isValidElement(child) && child.type.displayName === SelectItemTrailingIcon.displayName
    );
    const subtext = React.Children.toArray(content).find(
      (child) => React.isValidElement(child) && child.type.displayName === SelectItemSubtext.displayName
    );
    const labelContent = React.Children.toArray(content).filter(
      (child) => React.isValidElement(child) && child.type.displayName !== SelectItemLeadingIcon.displayName && child.type.displayName !== SelectItemTrailingIcon.displayName && child.type.displayName !== SelectItemSubtext.displayName
    );
    const hasSubtext = Boolean(subtext);
    return /* @__PURE__ */ jsxRuntime.jsxs(
      RadixSelect__namespace.Item,
      {
        ref,
        className: clsx$1.clsx(
          select_styles.SelectClasses.item,
          {
            [select_styles.SelectClasses.hasSelectedItemPadding]: hasSelection && showSelectedIndicator,
            [select_styles.SelectClasses.itemSm]: inputSize === "sm"
          },
          className
        ),
        "data-has-subtext": hasSubtext ? true : void 0,
        disabled,
        value,
        ...props,
        children: [
          showSelectedIndicator && /* @__PURE__ */ jsxRuntime.jsx(RadixSelect__namespace.ItemIndicator, { className: select_styles.SelectClasses.itemIndicator, children: /* @__PURE__ */ jsxRuntime.jsx(icons.Check, {}) }),
          /* @__PURE__ */ jsxRuntime.jsxs("div", { className: select_styles.SelectClasses.textWrapper, children: [
            /* @__PURE__ */ jsxRuntime.jsx(RadixSelect__namespace.ItemText, { asChild: true, children: /* @__PURE__ */ jsxRuntime.jsxs("span", { className: select_styles.SelectClasses.itemText, children: [
              leadingIcon,
              labelContent.length === 1 && React.isValidElement(labelContent[0]) && labelContent[0].type.displayName === SelectItemLabel.displayName ? labelContent[0] : /* @__PURE__ */ jsxRuntime.jsx("span", { className: select_styles.SelectClasses.label, children: labelContent })
            ] }) }),
            subtext
          ] }),
          trailingIcon
        ]
      }
    );
  }
);
Item.displayName = "Select.Item";
const Group = ({ className, children, ...props }) => {
  return /* @__PURE__ */ jsxRuntime.jsx(RadixSelect__namespace.Group, { className, ...props, children });
};
Group.displayName = "Select.Group";
const Label = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx(RadixSelect__namespace.Label, { ref, className: clsx$1.clsx(select_styles.SelectClasses.groupLabel, className), ...props, children });
  }
);
Label.displayName = "Select.Label";
const Divider = React.forwardRef(({ children, className, ...props }, ref) => /* @__PURE__ */ jsxRuntime.jsx(RadixSelect__namespace.Separator, { ref, className: clsx$1.clsx(select_styles.SelectClasses.divider, className), ...props, children }));
Divider.displayName = "Select.Divider";
const Select = Object.assign(SelectRoot, {
  Trigger,
  Value,
  Icon,
  Content,
  Item,
  Group,
  Label,
  Divider,
  ItemLabel: SelectItemLabel,
  ItemSubtext: SelectItemSubtext,
  ItemLeadingIcon: SelectItemLeadingIcon,
  ItemTrailingIcon: SelectItemTrailingIcon
});

exports.default = Select;
//# sourceMappingURL=select.cjs.map
