"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { useUniqueId } from '../../system/utils/component-utils.js';
import { TabBarContext } from '../tab-bar/hooks/use-tab-bar.js';
import { useTabBarManager } from '../tab-bar/hooks/use-tab-bar-manager.js';
import { VerticalTabBarClasses, StyledVerticalTabBar } from './vertical-tab-bar.styles.js';
import { VerticalTabBarList } from './subcomponents/vertical-tab-bar-list.js';
import { VerticalTabBarPanel } from './subcomponents/vertical-tab-bar-panel.js';
import { VerticalTabBarSection } from './subcomponents/vertical-tab-bar-section.js';
import { VerticalTabBarHeader } from './subcomponents/vertical-tab-bar-header.js';
import { VerticalTabBarMain } from './subcomponents/vertical-tab-bar-main.js';
import { VerticalTabBarTitle } from './subcomponents/vertical-tab-bar-title.js';

const VerticalTabBar = forwardRef(
  ({
    className,
    variant = "default",
    children,
    defaultValue,
    rememberActiveTab = false,
    uniqueId,
    onValueChange,
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(VerticalTabBarClasses.root, className);
    const addTabId = useUniqueId("add-tab");
    const { activeTab, setActiveTab } = useTabBarManager(
      defaultValue,
      rememberActiveTab,
      uniqueId,
      onValueChange,
      addTabId
    );
    return /* @__PURE__ */ jsx(
      TabBarContext.Provider,
      {
        value: { activeTab, setActiveTab, variant, addTabId, orientation: "vertical" },
        children: /* @__PURE__ */ jsx(
          StyledVerticalTabBar,
          {
            ref,
            className: rootClasses,
            currentTheme: theme,
            defaultValue,
            orientation: "vertical",
            value: activeTab,
            variant,
            onValueChange: setActiveTab,
            ...props,
            children
          }
        )
      }
    );
  }
);
VerticalTabBar.displayName = "VerticalTabBar";
VerticalTabBar.Header = VerticalTabBarHeader;
VerticalTabBar.List = VerticalTabBarList;
VerticalTabBar.Main = VerticalTabBarMain;
VerticalTabBar.Panel = VerticalTabBarPanel;
VerticalTabBar.Section = VerticalTabBarSection;
VerticalTabBar.Title = VerticalTabBarTitle;

export { VerticalTabBar as default };
//# sourceMappingURL=vertical-tab-bar.js.map
