"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import { VerticalTabBarClasses } from '../vertical-tab-bar.styles.js';

const VerticalTabBarTitle = forwardRef(
  ({ className, children, ...props }, ref) => {
    const classes = clsx(VerticalTabBarClasses.title, className);
    return /* @__PURE__ */ jsx("div", { ref, className: classes, ...props, children });
  }
);
VerticalTabBarTitle.displayName = "VerticalTabBar.Title";

export { VerticalTabBarTitle };
//# sourceMappingURL=vertical-tab-bar-title.js.map
