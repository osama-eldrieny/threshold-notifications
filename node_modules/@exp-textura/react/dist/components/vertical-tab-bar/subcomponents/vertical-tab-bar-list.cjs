"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const RadixTabs = require('@radix-ui/react-tabs');
const clsx$1 = require('clsx');
const verticalTabBar_styles = require('../vertical-tab-bar.styles.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixTabs__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixTabs);

const cloneTabsRecursively = (children) => {
  return React.Children.map(children, (child) => {
    if (!React.isValidElement(child)) {
      return child;
    }
    const newProps = {};
    if (child.type?.displayName === "Tab") {
      newProps.variant = child.props.variant || "vertical";
    }
    if (child.props.children) {
      return React.cloneElement(child, newProps, cloneTabsRecursively(child.props.children));
    }
    return child;
  });
};
const VerticalTabBarList = React.forwardRef(({ className, children, ...props }, ref) => {
  const clonedChildren = cloneTabsRecursively(children);
  const hasSections = React.Children.toArray(children).some(
    (child) => React.isValidElement(child) && child.type?.displayName === "VerticalTabBar.Section"
  );
  const conditionalProps = hasSections ? {
    "aria-orientation": void 0,
    role: void 0
  } : {};
  return /* @__PURE__ */ jsxRuntime.jsx(
    RadixTabs__namespace.List,
    {
      ref,
      className: clsx$1.clsx(verticalTabBar_styles.VerticalTabBarClasses.list, className),
      ...conditionalProps,
      ...props,
      children: clonedChildren
    }
  );
});
VerticalTabBarList.displayName = "VerticalTabBar.List";

exports.VerticalTabBarList = VerticalTabBarList;
//# sourceMappingURL=vertical-tab-bar-list.cjs.map
