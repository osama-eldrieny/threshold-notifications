"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import * as RadixTabs from '@radix-ui/react-tabs';
import { clsx } from 'clsx';
import { VerticalTabBarClasses } from '../vertical-tab-bar.styles.js';

const VerticalTabBarPanel = forwardRef(({ className, children, value, ...props }, ref) => {
  return /* @__PURE__ */ jsx(
    RadixTabs.Content,
    {
      ref,
      className: clsx(VerticalTabBarClasses.panel, className),
      value,
      ...props,
      children
    }
  );
});
VerticalTabBarPanel.displayName = "VerticalTabBar.Panel";

export { VerticalTabBarPanel };
//# sourceMappingURL=vertical-tab-bar-panel.js.map
