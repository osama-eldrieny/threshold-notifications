"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const styled = require('@emotion/styled');
const componentUtils = require('../../system/utils/component-utils.cjs');
const React = require('react');
const iconButton_styles = require('../icon-button/icon-button.styles.cjs');

const componentName = "SplitIconButton";
const SplitIconButtonClasses = {
  root: "exp-textura_split-icon-button",
  leading: "exp-textura_split-icon-button_leading",
  trailing: "exp-textura_split-icon-button_trailing",
  sm: "exp-textura_split-icon-button_sm"
};
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = React.useMemo(
    () => componentUtils.getStyleSlots(
      componentName,
      theme,
      variant,
      styleSlotsProp
      // getVariantConfig(theme),
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "inline-flex",
    position: "relative",
    gap: 0,
    [`.${SplitIconButtonClasses.leading}`]: {
      position: "relative",
      marginInlineEnd: theme.toRem(-1),
      ":dir(ltr)": {
        borderTopRightRadius: 0,
        borderBottomRightRadius: 0
      },
      ":dir(rtl)": {
        borderTopLeftRadius: 0,
        borderBottomLeftRadius: 0
      },
      "&:hover": {
        zIndex: 1,
        ...styleSlots?.leadingButtonHover
      },
      "&:active": {
        ...styleSlots?.leadingButtonPressed
      },
      "&:disabled, &:disabled:hover": {
        ...styleSlots?.leadingButtonDisabled
      },
      [`&.${iconButton_styles.IconButtonClasses.sm}`]: {
        height: "100%"
      },
      ...styleSlots?.leadingButton
    },
    [`.${SplitIconButtonClasses.trailing}`]: {
      position: "relative",
      width: theme.toRem(16),
      maxWidth: theme.toRem(16),
      ":dir(ltr)": {
        borderTopLeftRadius: 0,
        borderBottomLeftRadius: 0
      },
      ":dir(rtl)": {
        borderTopRightRadius: 0,
        borderBottomRightRadius: 0
      },
      [`&.${iconButton_styles.IconButtonClasses.sm}`]: {
        height: "100%"
      },
      "&:hover": {
        zIndex: 1,
        ...styleSlots?.trailingButtonHover
      },
      "&:active": {
        ...styleSlots?.trailingButtonPressed
      },
      "&:disabled, &:disabled:hover": {
        ...styleSlots?.trailingButtonDisabled
      },
      ...styleSlots?.trailingButton
    },
    [`&.${SplitIconButtonClasses.sm}`]: {
      [`.${SplitIconButtonClasses.leading}`]: {
        height: theme.toRem(24),
        ...styleSlots?.leadingButtonSm
      },
      [`.${SplitIconButtonClasses.trailing}`]: {
        height: theme.toRem(24),
        ...styleSlots?.trailingButtonSm
      }
    },
    ...styleSlots?.root
  };
};
const excludedDOMProps = componentUtils.getExcludedDOMProps();
const StyledSplitIconButton = styled("div", {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));

exports.SplitIconButtonClasses = SplitIconButtonClasses;
exports.StyledSplitIconButton = StyledSplitIconButton;
//# sourceMappingURL=split-icon-button.styles.cjs.map
