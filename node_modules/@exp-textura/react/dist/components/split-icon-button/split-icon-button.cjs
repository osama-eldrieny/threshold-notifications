"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const icons = require('@exp-textura/icons');
const menu = require('../menu/menu.cjs');
const iconButton = require('../icon-button/icon-button.cjs');
const splitIconButton_styles = require('./split-icon-button.styles.cjs');

const getSplitIconButtonChildren = (children) => {
  let leading = null;
  let trailing = null;
  let menuContent = null;
  React.Children.forEach(children, (child) => {
    if (React.isValidElement(child)) {
      const displayName = child.type.displayName;
      if (displayName === SplitIconButtonLeading.displayName) {
        leading = child;
      } else if (displayName === SplitIconButtonTrailing.displayName) {
        trailing = child;
      } else if (displayName === SplitIconButtonMenuContent.displayName) {
        menuContent = child;
      }
    }
  });
  return { leading, trailing, menuContent };
};
const SplitIconButtonRoot = React.forwardRef(
  ({
    children,
    className,
    variant = "default",
    size = "default",
    disabled = false,
    side = "bottom",
    sideOffset = 4,
    align = "end",
    alignOffset = 0,
    menuVariant,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(splitIconButton_styles.SplitIconButtonClasses.root, className, {
      [splitIconButton_styles.SplitIconButtonClasses.sm]: size === "sm"
    });
    const { leading, trailing, menuContent } = getSplitIconButtonChildren(children);
    if (!leading && !trailing && !menuContent) {
      return null;
    }
    const leadingWithProps = leading && React.cloneElement(leading, {
      variant,
      buttonType: leading.props.buttonType || "secondary",
      size: leading.props.size || size,
      disabled: disabled || leading.props.disabled,
      className: clsx$1.clsx(splitIconButton_styles.SplitIconButtonClasses.leading, leading.props.className)
    });
    const trailingWithProps = trailing && React.cloneElement(trailing, {
      variant,
      buttonType: trailing.props.buttonType || "secondary",
      size: trailing.props.size || "sm",
      disabled: disabled || trailing.props.disabled,
      className: clsx$1.clsx(splitIconButton_styles.SplitIconButtonClasses.trailing, trailing.props.className)
    });
    const menuContentWithProps = menuContent && /* @__PURE__ */ jsxRuntime.jsx(menu.default.Content, { ...menuContent.props });
    return /* @__PURE__ */ jsxRuntime.jsxs(
      splitIconButton_styles.StyledSplitIconButton,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children: [
          leadingWithProps,
          /* @__PURE__ */ jsxRuntime.jsxs(
            menu.default,
            {
              align,
              alignOffset,
              side,
              sideOffset,
              variant: menuVariant || "icons-only",
              children: [
                /* @__PURE__ */ jsxRuntime.jsx(menu.default.Trigger, { children: trailingWithProps }),
                menuContentWithProps
              ]
            }
          )
        ]
      }
    );
  }
);
SplitIconButtonRoot.displayName = "SplitIconButton";
const SplitIconButtonLeading = React.forwardRef(
  ({ children, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx(iconButton.default, { ref, ...props, children });
  }
);
SplitIconButtonLeading.displayName = "SplitIconButton.Leading";
const SplitIconButtonTrailing = React.forwardRef(
  ({ children, "aria-label": ariaLabel, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx(iconButton.default, { ref, "aria-label": ariaLabel || "More actions", size: "sm", ...props, children: children || /* @__PURE__ */ jsxRuntime.jsx(icons.ChevronDownSm, {}) });
  }
);
SplitIconButtonTrailing.displayName = "SplitIconButton.Trailing";
const SplitIconButtonMenuContent = (props) => {
  return /* @__PURE__ */ jsxRuntime.jsx(jsxRuntime.Fragment, { children: props.children });
};
SplitIconButtonMenuContent.displayName = "SplitIconButton.MenuContent";
const SplitIconButtonMenuItem = React.forwardRef(
  ({ children, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx(menu.default.Item, { ref, ...props, children });
  }
);
SplitIconButtonMenuItem.displayName = "SplitIconButton.MenuItem";
const SplitIconButton = Object.assign(SplitIconButtonRoot, {
  Leading: SplitIconButtonLeading,
  Trailing: SplitIconButtonTrailing,
  MenuContent: SplitIconButtonMenuContent,
  MenuItem: SplitIconButtonMenuItem
});

exports.default = SplitIconButton;
//# sourceMappingURL=split-icon-button.cjs.map
