"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const recharts = require('recharts');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const clsx$1 = require('clsx');
const chart_styles = require('./chart.styles.cjs');
const useChart = require('./hooks/use-chart.cjs');
const useDonutChart = require('./hooks/use-donut-chart.cjs');

const DonutChart = React.forwardRef(
  ({
    children,
    className,
    style,
    styleSlots,
    variant = "default",
    innerRadius = "75%",
    outerRadius = "100%",
    showActivePieLabels,
    margin = { top: 10, right: 10, left: 10, bottom: 10 },
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const [activeIndex, setActiveIndex] = React.useState();
    const { chartChildren, numCategoricalColors } = useDonutChart.useDonutChart(children, {
      innerRadius,
      outerRadius,
      ...showActivePieLabels && {
        activeIndex,
        onMouseLeave: () => setActiveIndex(void 0),
        onMouseEnter: (_, index) => setActiveIndex(index)
      }
    });
    return /* @__PURE__ */ jsxRuntime.jsx(
      useChart.ChartContext.Provider,
      {
        value: {
          numCategoricalColors,
          showActivePieLabels
        },
        children: /* @__PURE__ */ jsxRuntime.jsx(
          chart_styles.StyledChart,
          {
            ref,
            className: clsx$1.clsx(chart_styles.ChartClasses.root, className),
            currentTheme: theme,
            style: { width: "100%", height: "100%", ...style },
            styleSlots,
            variant,
            ...props,
            children: /* @__PURE__ */ jsxRuntime.jsx(
              recharts.ResponsiveContainer,
              {
                className: chart_styles.ChartClasses.responsiveContainer,
                height: "100%",
                width: "100%",
                children: /* @__PURE__ */ jsxRuntime.jsx(
                  recharts.PieChart,
                  {
                    className: chart_styles.ChartClasses.donutChart,
                    innerRadius,
                    margin,
                    outerRadius,
                    children: chartChildren
                  }
                )
              }
            )
          }
        )
      }
    );
  }
);
DonutChart.displayName = "DonutChart";

exports.default = DonutChart;
//# sourceMappingURL=donut-chart.cjs.map
