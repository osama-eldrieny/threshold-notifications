"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const React = require('react');

const ChartContext = React.createContext(void 0);
const useChart = () => {
  const context = React.useContext(ChartContext);
  if (!context) {
    throw new Error("useChart must be used within a <BarChart> component");
  }
  return context;
};

exports.ChartContext = ChartContext;
exports.useChart = useChart;
//# sourceMappingURL=use-chart.cjs.map
