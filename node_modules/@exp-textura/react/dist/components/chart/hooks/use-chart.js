"use client";
import { useContext, createContext } from 'react';

const ChartContext = createContext(void 0);
const useChart = () => {
  const context = useContext(ChartContext);
  if (!context) {
    throw new Error("useChart must be used within a <BarChart> component");
  }
  return context;
};

export { ChartContext, useChart };
//# sourceMappingURL=use-chart.js.map
