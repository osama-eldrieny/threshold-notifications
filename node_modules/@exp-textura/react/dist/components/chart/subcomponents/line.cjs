"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const recharts = require('recharts');
require('@emotion/react');
const useTheme = require('../../../theme/hooks/use-theme.cjs');
const clsx$1 = require('clsx');
const useChart = require('../hooks/use-chart.cjs');
const chart_styles = require('../chart.styles.cjs');

const IndividualDot = (props) => {
  const theme = useTheme.default();
  const { cx, cy, payload = {} } = props;
  const { label, value } = payload;
  if (!label || !cx || !cy) {
    return false;
  }
  const { text, position = "top" } = label;
  let x = cx;
  let y = cy;
  let textAnchor = "middle";
  let dominantBaseline = "auto";
  switch (position) {
    case "bottom":
      y += 8;
      dominantBaseline = "hanging";
      break;
    case "left":
      x -= 8;
      textAnchor = "end";
      dominantBaseline = "middle";
      break;
    case "right":
      x += 8;
      textAnchor = "start";
      dominantBaseline = "middle";
      break;
    case "top":
      y -= 8;
      dominantBaseline = "auto";
      break;
  }
  return /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
    /* @__PURE__ */ jsxRuntime.jsx(recharts.Dot, { ...props }),
    /* @__PURE__ */ jsxRuntime.jsx(
      "text",
      {
        className: chart_styles.ChartClasses.lineLabel,
        dominantBaseline,
        fill: theme.color.content.default,
        textAnchor,
        x,
        y,
        children: text || value
      }
    )
  ] });
};
const Line = ({ lineIndex = 0, showLineLabel: showLineLabelProp, ...props }) => {
  const theme = useTheme.default();
  const { showLineLabels: showLineLabelsFromContext, numCategoricalColors } = useChart.useChart();
  const showLineLabels = showLineLabelProp ?? showLineLabelsFromContext;
  const lineProps = { ...props };
  if (!props.stroke) {
    const colorKey = `${lineIndex % numCategoricalColors + 1}Default`;
    lineProps.stroke = theme.color.data.categorical[colorKey];
  }
  if (!props.fill) {
    const colorKey = `${lineIndex % numCategoricalColors + 1}Default`;
    lineProps.fill = theme.color.data.categorical[colorKey];
  }
  let labelChild;
  if (showLineLabels && showLineLabels !== "individual") {
    const position = typeof showLineLabels === "string" ? showLineLabels : "top";
    labelChild = /* @__PURE__ */ jsxRuntime.jsx(
      recharts.LabelList,
      {
        className: chart_styles.ChartClasses.lineLabel,
        fill: theme.color.content.default,
        offset: 8,
        position
      }
    );
  }
  return /* @__PURE__ */ jsxRuntime.jsx(
    recharts.Line,
    {
      dot: showLineLabels === "individual" ? /* @__PURE__ */ jsxRuntime.jsx(IndividualDot, {}) : !!showLineLabels,
      strokeWidth: 3,
      ...lineProps,
      className: clsx$1.clsx(chart_styles.ChartClasses.line, lineProps.className),
      children: labelChild
    }
  );
};
Line.displayName = "Line";

exports.default = Line;
//# sourceMappingURL=line.cjs.map
