"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const recharts = require('recharts');
require('@emotion/react');
const useTheme = require('../../../theme/hooks/use-theme.cjs');
const clsx$1 = require('clsx');
const useChart = require('../hooks/use-chart.cjs');
const chart_styles = require('../chart.styles.cjs');

const Area = ({ areaIndex = 0, variant = "default", ...props }) => {
  const theme = useTheme.default();
  const { numCategoricalColors } = useChart.useChart();
  const areaProps = { ...props };
  if (variant === "dashed") {
    areaProps.fillOpacity = props.fillOpacity ?? 0;
    areaProps.strokeDasharray = props.strokeDasharray ?? "5 5";
  }
  if (!props.stroke) {
    const colorKey = `${areaIndex % numCategoricalColors + 1}Default`;
    areaProps.stroke = theme.color.data.categorical[colorKey];
  }
  if (!props.fill) {
    const colorKey = `${areaIndex % numCategoricalColors + 1}Default`;
    areaProps.fill = theme.color.data.categorical[colorKey];
  }
  return /* @__PURE__ */ jsxRuntime.jsx(
    recharts.Area,
    {
      strokeWidth: 2,
      ...areaProps,
      className: clsx$1.clsx(chart_styles.ChartClasses.area, areaProps.className)
    }
  );
};
Area.displayName = "Area";

exports.default = Area;
//# sourceMappingURL=area.cjs.map
