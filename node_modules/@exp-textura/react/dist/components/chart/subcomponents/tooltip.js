"use client";
import { jsx } from 'react/jsx-runtime';
import { Tooltip as Tooltip$1 } from 'recharts';
import '@emotion/react';
import useTexturaTheme from '../../../theme/hooks/use-theme.js';

const Tooltip = (props) => {
  const theme = useTexturaTheme();
  return /* @__PURE__ */ jsx(
    Tooltip$1,
    {
      contentStyle: {
        borderRadius: theme.toRem(theme.border.radius.sm),
        ...props.contentStyle
      },
      cursor: { fill: theme.color.background.defaultHover },
      ...props
    }
  );
};
Tooltip.displayName = "Tooltip";

export { Tooltip as default };
//# sourceMappingURL=tooltip.js.map
