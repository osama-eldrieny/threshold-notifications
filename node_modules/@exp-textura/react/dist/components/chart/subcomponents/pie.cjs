"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const recharts = require('recharts');
require('@emotion/react');
const useTheme = require('../../../theme/hooks/use-theme.cjs');
const useChart = require('../hooks/use-chart.cjs');
const chart_styles = require('../chart.styles.cjs');

const RenderActiveShape = (props) => {
  const { showActivePieLabels } = useChart.useChart();
  const RADIAN = Math.PI / 180;
  const {
    cx,
    cy,
    midAngle = 0,
    innerRadius,
    outerRadius,
    startAngle,
    endAngle,
    fill,
    payload,
    percent,
    value
  } = props;
  const sin = Math.sin(-RADIAN * midAngle);
  const cos = Math.cos(-RADIAN * midAngle);
  const sx = cx + outerRadius * cos;
  const sy = cy + outerRadius * sin;
  const mx = cx + (outerRadius + 8) * cos;
  const my = cy + (outerRadius + 8) * sin;
  const ex = mx + (cos >= 0 ? 1 : -1) * 22;
  const ey = my;
  const textAnchor = cos >= 0 ? "start" : "end";
  return /* @__PURE__ */ jsxRuntime.jsxs("g", { children: [
    /* @__PURE__ */ jsxRuntime.jsx(
      recharts.Sector,
      {
        cx,
        cy,
        endAngle,
        fill,
        innerRadius,
        outerRadius,
        startAngle
      }
    ),
    /* @__PURE__ */ jsxRuntime.jsx("path", { d: `M${sx},${sy}L${mx},${my}L${ex},${ey}`, fill: "none", stroke: fill }),
    /* @__PURE__ */ jsxRuntime.jsx("circle", { cx: ex, cy: ey, fill, r: 2, stroke: "none" }),
    /* @__PURE__ */ jsxRuntime.jsx(
      "text",
      {
        className: chart_styles.ChartClasses.pieActiveLabel,
        textAnchor,
        x: ex + (cos >= 0 ? 1 : -1) * 12,
        y: ey,
        children: payload.name
      }
    ),
    /* @__PURE__ */ jsxRuntime.jsxs(
      "text",
      {
        className: chart_styles.ChartClasses.pieActiveLabel,
        dy: 18,
        textAnchor,
        x: ex + (cos >= 0 ? 1 : -1) * 12,
        y: ey,
        children: [
          showActivePieLabels === "percentage" && `${percent * 100}%`,
          showActivePieLabels === "value" && value
        ]
      }
    )
  ] });
};
const Pie = (props) => {
  const { colorStartIndex = 0, data, ...rest } = props;
  const theme = useTheme.default();
  const { numCategoricalColors, showActivePieLabels } = useChart.useChart();
  if (!theme) {
    return /* @__PURE__ */ jsxRuntime.jsx(recharts.Pie, { data, ...rest });
  }
  return /* @__PURE__ */ jsxRuntime.jsx(
    recharts.Pie,
    {
      data,
      ...rest,
      activeShape: showActivePieLabels ? RenderActiveShape : void 0,
      children: data?.map((_entry, index) => {
        const colorIndex = index % numCategoricalColors + colorStartIndex;
        const colorKey = `${colorIndex + 1}Default`;
        const color = theme.color.data.categorical[colorKey];
        return /* @__PURE__ */ jsxRuntime.jsx(recharts.Cell, { fill: color }, `cell-${index}`);
      })
    }
  );
};
Pie.displayName = "Pie";

exports.Pie = Pie;
exports.RenderActiveShape = RenderActiveShape;
exports.default = Pie;
//# sourceMappingURL=pie.cjs.map
