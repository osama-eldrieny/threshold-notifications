"use client";
import { jsx, jsxs } from 'react/jsx-runtime';
import { Pie as Pie$1, Cell, Sector } from 'recharts';
import '@emotion/react';
import useTexturaTheme from '../../../theme/hooks/use-theme.js';
import { useChart } from '../hooks/use-chart.js';
import { ChartClasses } from '../chart.styles.js';

const RenderActiveShape = (props) => {
  const { showActivePieLabels } = useChart();
  const RADIAN = Math.PI / 180;
  const {
    cx,
    cy,
    midAngle = 0,
    innerRadius,
    outerRadius,
    startAngle,
    endAngle,
    fill,
    payload,
    percent,
    value
  } = props;
  const sin = Math.sin(-RADIAN * midAngle);
  const cos = Math.cos(-RADIAN * midAngle);
  const sx = cx + outerRadius * cos;
  const sy = cy + outerRadius * sin;
  const mx = cx + (outerRadius + 8) * cos;
  const my = cy + (outerRadius + 8) * sin;
  const ex = mx + (cos >= 0 ? 1 : -1) * 22;
  const ey = my;
  const textAnchor = cos >= 0 ? "start" : "end";
  return /* @__PURE__ */ jsxs("g", { children: [
    /* @__PURE__ */ jsx(
      Sector,
      {
        cx,
        cy,
        endAngle,
        fill,
        innerRadius,
        outerRadius,
        startAngle
      }
    ),
    /* @__PURE__ */ jsx("path", { d: `M${sx},${sy}L${mx},${my}L${ex},${ey}`, fill: "none", stroke: fill }),
    /* @__PURE__ */ jsx("circle", { cx: ex, cy: ey, fill, r: 2, stroke: "none" }),
    /* @__PURE__ */ jsx(
      "text",
      {
        className: ChartClasses.pieActiveLabel,
        textAnchor,
        x: ex + (cos >= 0 ? 1 : -1) * 12,
        y: ey,
        children: payload.name
      }
    ),
    /* @__PURE__ */ jsxs(
      "text",
      {
        className: ChartClasses.pieActiveLabel,
        dy: 18,
        textAnchor,
        x: ex + (cos >= 0 ? 1 : -1) * 12,
        y: ey,
        children: [
          showActivePieLabels === "percentage" && `${percent * 100}%`,
          showActivePieLabels === "value" && value
        ]
      }
    )
  ] });
};
const Pie = (props) => {
  const { colorStartIndex = 0, data, ...rest } = props;
  const theme = useTexturaTheme();
  const { numCategoricalColors, showActivePieLabels } = useChart();
  if (!theme) {
    return /* @__PURE__ */ jsx(Pie$1, { data, ...rest });
  }
  return /* @__PURE__ */ jsx(
    Pie$1,
    {
      data,
      ...rest,
      activeShape: showActivePieLabels ? RenderActiveShape : void 0,
      children: data?.map((_entry, index) => {
        const colorIndex = index % numCategoricalColors + colorStartIndex;
        const colorKey = `${colorIndex + 1}Default`;
        const color = theme.color.data.categorical[colorKey];
        return /* @__PURE__ */ jsx(Cell, { fill: color }, `cell-${index}`);
      })
    }
  );
};
Pie.displayName = "Pie";

export { Pie, RenderActiveShape, Pie as default };
//# sourceMappingURL=pie.js.map
