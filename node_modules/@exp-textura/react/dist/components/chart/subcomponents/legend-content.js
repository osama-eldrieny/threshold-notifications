"use client";
import { jsx, jsxs } from 'react/jsx-runtime';
import { clsx } from 'clsx';
import { ChartClasses } from '../chart.styles.js';

const LegendContent = ({
  payload,
  layout,
  align,
  className,
  style,
  subLabelFormatter,
  formatter,
  showIcon = true
}) => {
  if (!payload || payload.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: clsx(
        {
          [ChartClasses.legend]: true,
          [ChartClasses.legendDashboard]: layout === "dashboard",
          [ChartClasses.legendCenterAlign]: !align || align === "center"
        },
        className
      ),
      style,
      children: /* @__PURE__ */ jsx(
        "ul",
        {
          className: clsx(
            {
              [ChartClasses.legendList]: true,
              [ChartClasses.legendVertical]: layout === "vertical",
              [ChartClasses.legendDashboard]: layout === "dashboard"
            },
            className
          ),
          children: payload.map((entry, index) => {
            const isLine = entry.type === "plainline" || entry.type === "line";
            const isDashed = entry.isDashed || !!entry.strokeDasharray;
            const entryShowIcon = showIcon && entry.type !== "none";
            const iconColor = entry.color || entry.fill || entry.stroke;
            return /* @__PURE__ */ jsx("li", { className: ChartClasses.legendItem, children: /* @__PURE__ */ jsxs("div", { style: { display: "flex", flexDirection: "column" }, children: [
              /* @__PURE__ */ jsxs("div", { style: { display: "flex", alignItems: "center", gap: "8px" }, children: [
                layout === "dashboard" && /* @__PURE__ */ jsx("span", { className: ChartClasses.legendLabel, children: entry.value }),
                entryShowIcon && /* @__PURE__ */ jsx("div", { style: { display: "flex", alignItems: "center" }, children: entry.type === "line" && !isDashed ? /* @__PURE__ */ jsx(
                  "span",
                  {
                    className: clsx({
                      [ChartClasses.legendColor]: true
                    }),
                    style: {
                      borderColor: iconColor,
                      borderStyle: "solid",
                      backgroundColor: iconColor
                    }
                  }
                ) : /* @__PURE__ */ jsx(
                  "span",
                  {
                    className: clsx({
                      [ChartClasses.legendColor]: true,
                      [ChartClasses.legendDashedColor]: isDashed
                    }),
                    style: isLine ? {
                      borderColor: iconColor,
                      backgroundColor: "transparent",
                      borderStyle: isDashed ? "dashed" : "solid",
                      borderWidth: "2px"
                    } : { backgroundColor: iconColor }
                  }
                ) }),
                layout !== "dashboard" && /* @__PURE__ */ jsx("span", { className: ChartClasses.legendLabel, children: formatter ? formatter(entry.value, entry, index) : entry.value })
              ] }),
              subLabelFormatter && /* @__PURE__ */ jsx("span", { className: ChartClasses.legendSubLabel, children: subLabelFormatter(entry.value, entry, index) })
            ] }) }, `item-${index}`);
          })
        }
      )
    }
  );
};
LegendContent.displayName = "LegendContent";

export { LegendContent as default };
//# sourceMappingURL=legend-content.js.map
