"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const recharts = require('recharts');
require('@emotion/react');
const useTheme = require('../../../theme/hooks/use-theme.cjs');
const useChart = require('../hooks/use-chart.cjs');
const chart_styles = require('../chart.styles.cjs');

const YAxis = (props) => {
  const theme = useTheme.default();
  const { handleSort } = useChart.useChart();
  const { orientation, sortable, dataKey, label: labelProp, angle = -90, ...rest } = props;
  const label = /* @__PURE__ */ jsxRuntime.jsx(
    recharts.Label,
    {
      angle,
      className: chart_styles.ChartClasses.yAxisLabel,
      offset: Number(props.offset) || (orientation === "right" ? 18 : 12),
      position: orientation === "right" ? "insideRight" : "insideLeft",
      style: {
        cursor: sortable ? "pointer" : void 0,
        fill: theme.color.content.default,
        textAnchor: "middle"
      },
      value: String(labelProp ?? ""),
      onClick: sortable ? (e) => {
        e.preventDefault();
        e.stopPropagation();
        handleSort?.(sortable, dataKey);
      } : void 0
    }
  );
  return /* @__PURE__ */ jsxRuntime.jsx(
    recharts.YAxis,
    {
      orientation,
      stroke: theme.color.border.strong,
      tick: { fill: theme.color.content.default },
      tickLine: false,
      ...rest,
      dataKey,
      label
    }
  );
};
YAxis.displayName = "YAxis";

exports.default = YAxis;
//# sourceMappingURL=y-axis.cjs.map
