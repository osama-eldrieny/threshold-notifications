"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const recharts = require('recharts');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const clsx$1 = require('clsx');
const chart_styles = require('./chart.styles.cjs');
const useChart = require('./hooks/use-chart.cjs');
const useAreaChart = require('./hooks/use-area-chart.cjs');

const AreaChart = React.forwardRef(
  ({
    data: initialData,
    children,
    className,
    style,
    styleSlots,
    variant = "default",
    type = "default",
    margin: marginProp,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const { chartData, numCategoricalColors, numAreas, childrenWithProps, gradientDefs, colorMap } = useAreaChart.useAreaChart({
      data: initialData,
      children,
      type
    });
    const defaultMargin = React.useMemo(() => {
      const hasReferenceLine = React.Children.toArray(children).some(
        (child) => React.isValidElement(child) && child.type?.displayName === "ReferenceLine"
      );
      return {
        top: 10,
        right: hasReferenceLine ? 60 : 10,
        left: 10,
        bottom: 10
      };
    }, [children]);
    const margin = { ...defaultMargin, ...marginProp };
    return /* @__PURE__ */ jsxRuntime.jsx(
      useChart.ChartContext.Provider,
      {
        value: {
          data: initialData,
          chartData,
          type,
          numCategoricalColors,
          numAreas,
          colorMap
        },
        children: /* @__PURE__ */ jsxRuntime.jsx(
          chart_styles.StyledChart,
          {
            ref,
            className: clsx$1.clsx(chart_styles.ChartClasses.root, className),
            currentTheme: theme,
            style: { width: "100%", height: "100%", ...style },
            styleSlots,
            variant,
            ...props,
            children: /* @__PURE__ */ jsxRuntime.jsx(
              recharts.ResponsiveContainer,
              {
                className: chart_styles.ChartClasses.responsiveContainer,
                height: "100%",
                width: "100%",
                children: /* @__PURE__ */ jsxRuntime.jsxs(recharts.AreaChart, { className: chart_styles.ChartClasses.areaChart, data: chartData, margin, children: [
                  gradientDefs && gradientDefs.length > 0 && /* @__PURE__ */ jsxRuntime.jsx("defs", { children: gradientDefs }),
                  childrenWithProps
                ] })
              }
            )
          }
        )
      }
    );
  }
);
AreaChart.displayName = "AreaChart";

exports.default = AreaChart;
//# sourceMappingURL=area-chart.cjs.map
