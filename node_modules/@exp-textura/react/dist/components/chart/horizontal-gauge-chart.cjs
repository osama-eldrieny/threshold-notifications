"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const barChart = require('./bar-chart.cjs');
const xAxis = require('./subcomponents/x-axis.cjs');
const yAxis = require('./subcomponents/y-axis.cjs');

const HorizontalGaugeChart = React.forwardRef(
  ({
    children,
    roundedCorners = true,
    margin = { top: 0, right: 10, bottom: 0, left: 10 },
    styleSlots: styleSlotsProp,
    height: heightProp,
    data,
    ...props
  }, ref) => {
    const { defaultHeight } = React.useMemo(() => {
      const legendChild = React.Children.toArray(children).find(
        (child) => React.isValidElement(child) && child.type?.displayName === "Legend"
      );
      const hasLegend = !!legendChild;
      return {
        // For a gauge with a legend, 120px is a safe bet. Without, 60px is plenty for a 40px bar.
        defaultHeight: hasLegend ? 90 : 60
      };
    }, [children]);
    const height = heightProp ?? defaultHeight;
    const childrenWithDefaults = React.Children.map(children, (child) => {
      if (React.isValidElement(child)) {
        const displayName = child.type?.displayName;
        if (displayName === "Bar") {
          return React.cloneElement(child, {
            barSize: child.props.barSize ?? 40,
            showInternalLabel: child.props.showInternalLabel ?? true,
            stackId: child.props.stackId ?? "1"
          });
        }
        if (displayName === "Legend") {
          return React.cloneElement(child, {
            layout: child.props.layout ?? "dashboard"
          });
        }
      }
      return child;
    });
    return /* @__PURE__ */ jsxRuntime.jsxs(
      barChart.default,
      {
        ref,
        data: [data],
        height,
        layout: "vertical",
        margin,
        roundedCorners,
        styleSlots: {
          surface: { padding: 0 },
          ...styleSlotsProp
        },
        type: "stacked",
        ...props,
        children: [
          /* @__PURE__ */ jsxRuntime.jsx(
            xAxis.default,
            {
              hide: true,
              domain: [0, "dataMax"],
              height: 0,
              padding: { left: 0, right: 0 },
              type: "number"
            }
          ),
          /* @__PURE__ */ jsxRuntime.jsx(
            yAxis.default,
            {
              hide: true,
              axisLine: false,
              dataKey: "name",
              padding: { top: 0, bottom: 0 },
              tickLine: false,
              type: "category",
              width: 0
            }
          ),
          childrenWithDefaults
        ]
      }
    );
  }
);
HorizontalGaugeChart.displayName = "HorizontalGaugeChart";

exports.default = HorizontalGaugeChart;
//# sourceMappingURL=horizontal-gauge-chart.cjs.map
