"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { ResponsiveContainer, LineChart as LineChart$1 } from 'recharts';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { clsx } from 'clsx';
import { StyledChart, ChartClasses } from './chart.styles.js';
import { ChartContext } from './hooks/use-chart.js';
import { useLineChart } from './hooks/use-line-chart.js';

const LineChart = forwardRef(
  ({
    data: initialData,
    children,
    className,
    style,
    styleSlots,
    variant = "default",
    showLineLabels,
    margin = { top: 10, right: 10, left: 10, bottom: 10 },
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const { chartData, numCategoricalColors, numLines, childrenWithProps } = useLineChart({
      data: initialData,
      children
    });
    return /* @__PURE__ */ jsx(
      ChartContext.Provider,
      {
        value: {
          data: initialData,
          chartData,
          numCategoricalColors,
          numLines,
          showLineLabels
        },
        children: /* @__PURE__ */ jsx(
          StyledChart,
          {
            ref,
            className: clsx(ChartClasses.root, className),
            currentTheme: theme,
            style: { width: "100%", height: "100%", ...style },
            styleSlots,
            variant,
            ...props,
            children: /* @__PURE__ */ jsx(
              ResponsiveContainer,
              {
                className: ChartClasses.responsiveContainer,
                height: "100%",
                width: "100%",
                children: /* @__PURE__ */ jsx(LineChart$1, { className: ChartClasses.lineChart, data: chartData, margin, children: childrenWithProps })
              }
            )
          }
        )
      }
    );
  }
);
LineChart.displayName = "LineChart";

export { LineChart as default };
//# sourceMappingURL=line-chart.js.map
