"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { ResponsiveContainer, BarChart as BarChart$1 } from 'recharts';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { clsx } from 'clsx';
import { StyledChart, ChartClasses } from './chart.styles.js';
import { ChartContext } from './hooks/use-chart.js';
import { useBarChart } from './hooks/use-bar-chart.js';

const BarChart = forwardRef(
  ({
    data: initialData,
    children,
    className,
    style,
    styleSlots,
    variant = "default",
    type = "default",
    layout = "horizontal",
    roundedCorners = false,
    showBarLabels = false,
    margin = { top: 10, right: 10, left: 10, bottom: 10 },
    height = "100%",
    width = "100%",
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const {
      chartData,
      sortOrder,
      handleSort,
      numCategoricalColors,
      numBars,
      childrenWithProps,
      colorMap
    } = useBarChart({
      data: initialData,
      children});
    return /* @__PURE__ */ jsx(
      ChartContext.Provider,
      {
        value: {
          data: initialData,
          chartData,
          type,
          layout,
          roundedCorners,
          showBarLabels,
          handleSort,
          sortOrder,
          numCategoricalColors,
          numBars,
          colorMap
        },
        children: /* @__PURE__ */ jsx(
          StyledChart,
          {
            ref,
            className: clsx(ChartClasses.root, className),
            currentTheme: theme,
            style: { width, height, ...style },
            styleSlots,
            variant,
            ...props,
            children: /* @__PURE__ */ jsx(
              ResponsiveContainer,
              {
                className: ChartClasses.responsiveContainer,
                height: "100%",
                width: "100%",
                children: /* @__PURE__ */ jsx(
                  BarChart$1,
                  {
                    className: ChartClasses.barChart,
                    data: chartData,
                    layout,
                    margin,
                    children: childrenWithProps
                  }
                )
              }
            )
          }
        )
      }
    );
  }
);
BarChart.displayName = "BarChart";

export { BarChart as default };
//# sourceMappingURL=bar-chart.js.map
