"use client";
import { createContext, useContext } from 'react';

const ToastLocalContext = createContext(null);
const useToastContext = () => {
  const context = useContext(ToastLocalContext);
  if (!context) {
    throw new Error("Toast compound components cannot be rendered outside the Toast component");
  }
  return context;
};

export { ToastLocalContext, useToastContext };
//# sourceMappingURL=use-local-toast.js.map
