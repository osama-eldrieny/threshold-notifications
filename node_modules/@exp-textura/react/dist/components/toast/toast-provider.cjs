"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const componentUtils = require('../../system/utils/component-utils.cjs');
const useToast = require('./hooks/use-toast.cjs');
const toast = require('./toast.cjs');

const ToastProvider = ({ children, containerElement }) => {
  const [currentToast, setCurrentToast] = React.useState(null);
  const fallbackId = componentUtils.useUniqueId("toast");
  const showToast = React.useCallback(
    (options) => {
      const id = options.id ?? fallbackId;
      const { content, ...restOptions } = options;
      const newToastState = {
        containerElement,
        // Default from provider
        ...restOptions,
        // Override from options if present
        id,
        children: /* @__PURE__ */ jsxRuntime.jsx(jsxRuntime.Fragment, { children: content }),
        show: true
      };
      setCurrentToast(newToastState);
      options.onOpenChange?.(true);
      return id;
    },
    [containerElement, fallbackId]
  );
  const removeToast = React.useCallback((id) => {
    setCurrentToast((activeToast) => {
      if (activeToast && activeToast.id === id) {
        const userOnOpenChange = activeToast.onOpenChange;
        userOnOpenChange?.(false);
        return null;
      }
      return activeToast;
    });
  }, []);
  const contextValue = {
    showToast,
    removeToast
  };
  return /* @__PURE__ */ jsxRuntime.jsxs(useToast.ToastContext.Provider, { value: contextValue, children: [
    children,
    currentToast && /* @__PURE__ */ jsxRuntime.jsx(
      toast.default,
      {
        ...currentToast,
        show: true,
        onOpenChange: (isOpen) => {
          if (!isOpen) {
            removeToast(currentToast.id);
          }
        }
      },
      currentToast.id
    )
  ] });
};

exports.ToastProvider = ToastProvider;
//# sourceMappingURL=toast-provider.cjs.map
