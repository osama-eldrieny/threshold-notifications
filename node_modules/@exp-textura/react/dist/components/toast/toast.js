"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef, useState, useCallback, useEffect, useRef } from 'react';
import { createPortal } from 'react-dom';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { Close, Info } from '@exp-textura/icons';
import IconButton from '../icon-button/icon-button.js';
import { usePortalContainer } from '../../hooks/use-portal-container.js';
import { ToastClasses, StyledToast } from './toast.styles.js';
import { ToastLocalContext, useToastContext } from './hooks/use-local-toast.js';

const ToastRoot = forwardRef(
  ({
    autoHideDuration,
    className,
    clickToDismiss,
    onOpenChange,
    onClose,
    variant = "default",
    children,
    show: showProp,
    position = "bottom-start",
    disablePortal = false,
    containerElement,
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const [show, setShow] = useState(showProp);
    const [mounted, setMounted] = useState(false);
    const { resolvedContainer, isScoped } = usePortalContainer(containerElement);
    const rootClasses = clsx(ToastClasses.root, className, {
      [ToastClasses.root]: true,
      [ToastClasses.slideIn]: show,
      [ToastClasses.topStart]: position === "top-start",
      [ToastClasses.topCenter]: position === "top-center",
      [ToastClasses.topEnd]: position === "top-end",
      [ToastClasses.bottomStart]: position === "bottom-start",
      [ToastClasses.bottomCenter]: position === "bottom-center",
      [ToastClasses.bottomEnd]: position === "bottom-end",
      [ToastClasses.clickToDismiss]: clickToDismiss
    });
    const handleClose = useCallback(() => {
      setShow(false);
      onOpenChange?.(false);
      onClose?.();
    }, [onOpenChange, onClose]);
    const handleKeyDown = useCallback(
      (event) => {
        if (clickToDismiss && (event.key === "Enter" || event.key === " ")) {
          event.preventDefault();
          handleClose();
        }
      },
      [clickToDismiss, handleClose]
    );
    useEffect(() => {
      setMounted(true);
      return () => setMounted(false);
    }, []);
    useEffect(() => {
      if (typeof showProp === "boolean" && showProp !== show) {
        setShow(showProp);
        onOpenChange?.(showProp);
      }
    }, [showProp, show, onOpenChange]);
    const handleCloseRef = useRef(handleClose);
    useEffect(() => {
      handleCloseRef.current = handleClose;
    }, [handleClose]);
    useEffect(() => {
      let timerId;
      if (autoHideDuration && show) {
        timerId = setTimeout(() => handleCloseRef.current(), autoHideDuration);
      }
      return () => {
        if (timerId) {
          clearTimeout(timerId);
        }
      };
    }, [autoHideDuration, show]);
    if (!show) return null;
    const contextValue = {
      handleClose
    };
    const content = /* @__PURE__ */ jsx(ToastLocalContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsx(
      StyledToast,
      {
        ref,
        "aria-atomic": "true",
        "aria-hidden": !show,
        className: rootClasses,
        currentTheme: theme,
        "data-textura-scoped": isScoped,
        role: "alert",
        tabIndex: show && clickToDismiss ? 0 : void 0,
        variant,
        onClick: show && clickToDismiss ? handleClose : void 0,
        onKeyDown: show && clickToDismiss ? handleKeyDown : void 0,
        ...props,
        children: show && children
      }
    ) });
    if (disablePortal) {
      return content;
    }
    return mounted && resolvedContainer ? createPortal(content, resolvedContainer) : null;
  }
);
ToastRoot.displayName = "Toast";
const Icon = ({
  children,
  className,
  ...props
}) => {
  const iconClasses = clsx(ToastClasses.icon, className);
  return /* @__PURE__ */ jsx("span", { className: iconClasses, ...props, children: children || /* @__PURE__ */ jsx(Info, {}) });
};
Icon.displayName = "Toast.Icon";
const Message = ({
  children,
  className,
  ...props
}) => {
  const messageClasses = clsx(ToastClasses.message, className);
  return /* @__PURE__ */ jsx("div", { className: messageClasses, ...props, children });
};
Message.displayName = "Toast.Message";
const CloseIconButton = ({
  children,
  className,
  "aria-label": customAriaLabel
}) => {
  const { handleClose } = useToastContext();
  const buttonClasses = clsx(ToastClasses.closeIconButton, className);
  const commonProps = {
    "aria-label": customAriaLabel || "close current toast"
  };
  return /* @__PURE__ */ jsx(
    IconButton,
    {
      buttonType: "secondary",
      className: buttonClasses,
      size: "sm",
      variant: "inverse",
      onClick: handleClose,
      ...commonProps,
      children: children ? children : /* @__PURE__ */ jsx(Close, {})
    }
  );
};
CloseIconButton.displayName = "Toast.CloseIconButton";
const Toast = Object.assign(ToastRoot, {
  Icon,
  Message,
  CloseIconButton
});

export { Toast as default };
//# sourceMappingURL=toast.js.map
