"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const RadixDialog = require('@radix-ui/react-dialog');
const icons = require('@exp-textura/icons');
const iconButton = require('../icon-button/icon-button.cjs');
const divider = require('../divider/divider.cjs');
const usePortalContainer = require('../../hooks/use-portal-container.cjs');
const modal_styles = require('./modal.styles.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixDialog__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixDialog);

const ModalRoot = React.forwardRef(
  ({
    className,
    children,
    onOpenChange,
    dismissible = true,
    open,
    defaultOpen = false,
    containerElement,
    variant = "default",
    size = "sm",
    onEscapeKeyDown,
    onInteractOutside,
    onPointerDownOutside,
    onOpenAutoFocus,
    onCloseAutoFocus,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(modal_styles.ModalClasses.root, className);
    const { resolvedContainer, isScoped } = usePortalContainer.usePortalContainer(containerElement);
    const { trigger, content, closeIconButton } = getValidModalChildren(children);
    return /* @__PURE__ */ jsxRuntime.jsxs(
      RadixDialog__namespace.Root,
      {
        defaultOpen,
        modal: true,
        open,
        onOpenChange,
        children: [
          trigger,
          /* @__PURE__ */ jsxRuntime.jsxs(RadixDialog__namespace.Portal, { container: resolvedContainer, children: [
            /* @__PURE__ */ jsxRuntime.jsx(
              modal_styles.StyledModalOverlay,
              {
                className: modal_styles.ModalClasses.overlay,
                currentTheme: theme,
                "data-textura-scoped": isScoped,
                styleSlots: props.styleSlots,
                variant
              }
            ),
            /* @__PURE__ */ jsxRuntime.jsxs(
              modal_styles.StyledModal,
              {
                ref,
                "aria-describedby": void 0,
                className: clsx$1.clsx(rootClasses, modal_styles.ModalClasses.body, {
                  [modal_styles.ModalClasses.sm]: size === "sm",
                  [modal_styles.ModalClasses.md]: size === "md"
                }),
                currentTheme: theme,
                "data-textura-scoped": isScoped,
                variant,
                onCloseAutoFocus,
                onEscapeKeyDown: (e) => {
                  if (!dismissible) {
                    e.preventDefault();
                  }
                  onEscapeKeyDown?.(e);
                },
                onInteractOutside: (e) => {
                  if (!dismissible) {
                    e.preventDefault();
                  }
                  onInteractOutside?.(e);
                },
                onOpenAutoFocus,
                onPointerDownOutside: (e) => {
                  if (!dismissible) {
                    e.preventDefault();
                  }
                  onPointerDownOutside?.(e);
                },
                ...props,
                children: [
                  content,
                  closeIconButton
                ]
              }
            )
          ] })
        ]
      }
    );
  }
);
ModalRoot.displayName = "Modal";
const Title = ({ className, children, ...props }) => /* @__PURE__ */ jsxRuntime.jsx(
  RadixDialog__namespace.Title,
  {
    className: clsx$1.clsx(modal_styles.ModalClasses.title, className),
    "data-dialog-type": "title",
    ...props,
    children
  }
);
Title.displayName = "Modal.Title";
const Trigger = ({ children, ...props }) => {
  return /* @__PURE__ */ jsxRuntime.jsx(RadixDialog__namespace.Trigger, { "data-dialog-type": "trigger", ...props, asChild: true, children });
};
Trigger.displayName = "Modal.Trigger";
const Close = ({ children, ...props }) => /* @__PURE__ */ jsxRuntime.jsx(RadixDialog__namespace.Close, { "data-dialog-type": "close", ...props, asChild: true, children });
Close.displayName = "Modal.Close";
const CloseIconButton = ({
  className,
  children,
  ...props
}) => /* @__PURE__ */ jsxRuntime.jsx(
  RadixDialog__namespace.Close,
  {
    asChild: true,
    "aria-label": props["aria-label"] || "Close dialog",
    className: clsx$1.clsx(modal_styles.ModalClasses.closeIconButton, className),
    "data-dialog-type": "close-button",
    ...props,
    children: /* @__PURE__ */ jsxRuntime.jsx(iconButton.default, { buttonType: "secondary", children: children || /* @__PURE__ */ jsxRuntime.jsx(icons.Close, {}) })
  }
);
CloseIconButton.displayName = "Modal.CloseIconButton";
const Content = ({ className, children, ...props }) => /* @__PURE__ */ jsxRuntime.jsx("div", { className: clsx$1.clsx(modal_styles.ModalClasses.content, className), "data-dialog-type": "content", ...props, children });
Content.displayName = "Modal.Content";
const Divider = ({ className, ...props }) => {
  return /* @__PURE__ */ jsxRuntime.jsx(divider.default, { className: clsx$1.clsx(modal_styles.ModalClasses.divider, className), ...props });
};
Divider.displayName = "Modal.Divider";
const Footer = ({ className, children, ...props }) => /* @__PURE__ */ jsxRuntime.jsx("footer", { className: clsx$1.clsx(modal_styles.ModalClasses.footer, className), "data-dialog-type": "footer", ...props, children });
Footer.displayName = "Modal.Footer";
const Modal = Object.assign(ModalRoot, {
  Trigger,
  Close,
  CloseIconButton,
  Title,
  Content,
  Divider,
  Footer
});
const getValidModalChildren = (children) => {
  let titleCount = 0;
  let closeIconButtonCount = 0;
  let triggerCount = 0;
  let contentCount = 0;
  let dividerCount = 0;
  let footerCount = 0;
  const trigger = [];
  const content = [];
  const closeIconButton = [];
  React.Children.forEach(children, (child) => {
    if (React.isValidElement(child)) {
      const displayName = child.type.displayName;
      if (displayName !== "Modal.Trigger" && displayName !== "Modal.CloseIconButton") {
        content.push(child);
      }
      if (displayName === "Modal.Content") {
        contentCount++;
      } else if (displayName === "Modal.Divider") {
        dividerCount++;
      } else if (displayName === "Modal.Footer") {
        footerCount++;
      } else if (displayName === "Modal.Title") {
        titleCount++;
      } else if (displayName === "Modal.Trigger") {
        trigger.push(child);
        triggerCount++;
      } else if (displayName === "Modal.CloseIconButton") {
        closeIconButton.push(child);
        closeIconButtonCount++;
      } else {
        throw new Error(
          "<Modal> only accepts <Modal.Title>, <Modal.CloseIconButton>, <Modal.Trigger>, <Modal.Content>, <Modal.Divider> and <Modal.Footer> as children."
        );
      }
    }
  });
  if (contentCount > 1)
    throw new Error(`<Modal> can only have one <Modal.Content>. Found ${contentCount}.`);
  if (dividerCount > 2)
    throw new Error(`<Modal> can have up to two <Modal.Divider>. Found ${dividerCount}.`);
  if (closeIconButtonCount > 1)
    throw new Error(
      `<Modal> can only have one <Modal.CloseIconButton>. Found ${closeIconButtonCount}.`
    );
  if (footerCount > 1)
    throw new Error(`<Modal> can only have one <Modal.Footer>. Found ${footerCount}.`);
  if (titleCount > 1)
    throw new Error(`<Modal> can only have one <Modal.Title>. Found ${titleCount}.`);
  if (triggerCount > 1)
    throw new Error(`<Modal> can only have one <Modal.Trigger>. Found ${triggerCount}.`);
  return { trigger, content, closeIconButton };
};

exports.default = Modal;
//# sourceMappingURL=modal.cjs.map
