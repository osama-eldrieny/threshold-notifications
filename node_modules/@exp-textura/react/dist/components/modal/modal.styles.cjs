"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const styled = require('@emotion/styled');
const react = require('@emotion/react');
const componentUtils = require('../../system/utils/component-utils.cjs');
const RadixDialog = require('@radix-ui/react-dialog');
const React = require('react');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixDialog__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixDialog);

const componentName = "Modal";
const ModalClasses = {
  root: "exp-textura_modal",
  sm: "exp-textura_modal_sm",
  md: "exp-textura_modal_md",
  body: "exp-textura_modal_body",
  title: "exp-textura_modal_title",
  content: "exp-textura_modal_content",
  divider: "exp-textura_modal_divider",
  footer: "exp-textura_modal_footer",
  overlay: "exp-textura_modal_overlay",
  closeIconButton: "exp-textura_modal_close_icon_button"
};
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = React.useMemo(
    () => componentUtils.getStyleSlots(
      componentName,
      theme,
      variant,
      styleSlotsProp
      // getVariantConfig(),
    ),
    [theme, variant, styleSlotsProp]
  );
  const fadeInOverlay = react.keyframes`from { opacity: 0; } to { opacity: ${styleSlots?.overlay?.opacity}; }`;
  const fadeOutOverlay = react.keyframes`from { opacity: ${styleSlots?.overlay?.opacity}; } to { opacity: 0; }`;
  const fadeInModal = react.keyframes`from { opacity: 0; } to { opacity: 1; }`;
  const fadeOutModal = react.keyframes`from { opacity: 1; } to { opacity: 0; }`;
  return {
    [`&.${ModalClasses.overlay}`]: {
      position: "fixed",
      inset: 0,
      animationTimingFunction: "ease",
      animationFillMode: "forwards",
      backgroundColor: theme.overlay.background,
      zIndex: theme.zIndex.dynamic,
      animationDuration: "100ms",
      '&[data-state="open"]': {
        animationName: fadeInOverlay,
        opacity: styleSlots?.overlay?.opacity
      },
      '&[data-state="closed"]': {
        animationName: fadeOutOverlay,
        opacity: 0
      },
      '&[data-textura-scoped="true"]': {
        position: "absolute"
      },
      ...styleSlots?.overlay
    },
    [`&.${ModalClasses.sm}`]: {
      width: theme.toRem(576)
    },
    [`&.${ModalClasses.md}`]: {
      width: theme.toRem(752)
    },
    [`&.${ModalClasses.body}`]: {
      display: "flex",
      flexDirection: "column",
      position: "fixed",
      top: "50%",
      left: "50%",
      transform: "translate(-50%, -50%)",
      opacity: 1,
      animationTimingFunction: "ease",
      animationFillMode: "forwards",
      animationDuration: "100ms",
      backgroundColor: theme.dialog.color.background,
      color: theme.dialog.color.content,
      boxShadow: theme.dropShadow.lg,
      borderRadius: theme.toRem(theme.border.radius.sm),
      overflowY: "auto",
      padding: `${theme.toRem(theme.spacing[16])} 0`,
      maxWidth: `calc(100vw - ${theme.toRem(80)})`,
      maxHeight: `calc(100% - ${theme.toRem(80)})`,
      zIndex: theme.zIndex.dynamic,
      ...theme.typography.body.md,
      '&[data-state="open"]': {
        animationName: fadeInModal,
        opacity: 1
      },
      [`&[data-state="closed"]`]: {
        animationName: fadeOutModal
      },
      '&[data-textura-scoped="true"]': {
        position: "absolute"
      },
      ...styleSlots?.body
    },
    [`& .${ModalClasses.content}`]: {
      flex: "1 1 auto",
      overflowY: "auto",
      padding: theme.toRem(theme.spacing[24]),
      ...theme.typography.body.md,
      ...styleSlots?.content
    },
    [`& .${ModalClasses.footer}`]: {
      padding: `${theme.toRem(theme.spacing[16])} ${theme.toRem(theme.spacing[24])} 0 ${theme.toRem(theme.spacing[24])}`,
      ...styleSlots?.footer
    },
    [`& .${ModalClasses.title}`]: {
      ...theme.typography.heading.sm,
      margin: 0,
      color: theme.dialog.color.content,
      padding: `0 ${theme.toRem(theme.spacing[24])} ${theme.toRem(theme.spacing[16])}`,
      ...styleSlots?.title
    },
    [`& .${ModalClasses.divider}`]: {
      backgroundColor: theme.divider.default.color.default,
      ...styleSlots?.divider
    },
    [`& .${ModalClasses.closeIconButton}`]: {
      position: "absolute",
      top: theme.toRem(theme.spacing[16]),
      right: theme.toRem(theme.spacing[16]),
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      padding: theme.toRem(theme.spacing[8]),
      fontSize: theme.typography.fontSize[16],
      ":dir(rtl)": {
        right: "unset",
        left: theme.toRem(theme.spacing[16])
      },
      "&:focus-visible": {
        ...styleSlots?.closeIconButtonFocus
      },
      ...styleSlots?.closeIconButton
    },
    ...styleSlots?.root
  };
};
const excludedDOMProps = componentUtils.getExcludedDOMProps();
const StyledModal = styled(RadixDialog__namespace.Content, {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));
const StyledModalOverlay = styled(RadixDialog__namespace.Overlay, {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));

exports.ModalClasses = ModalClasses;
exports.StyledModal = StyledModal;
exports.StyledModalOverlay = StyledModalOverlay;
//# sourceMappingURL=modal.styles.cjs.map
