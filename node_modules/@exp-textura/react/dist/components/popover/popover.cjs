"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const react = require('@floating-ui/react');
const button = require('../button/button.cjs');
const divider = require('../divider/divider.cjs');
const popover_styles = require('./popover.styles.cjs');
const usePopover = require('./hooks/use-popover.cjs');

const PopoverRoot = ({
  children,
  arrow = true,
  position = "top",
  open: controlledOpen,
  onOpenChange,
  offset = 8,
  variant = "default",
  styleSlots,
  dismissable = false
}) => {
  const hookOptions = {
    position,
    open: controlledOpen,
    dismissable,
    onOpenChange,
    arrow,
    variant,
    offset,
    styleSlots
  };
  const contextValue = usePopover.usePopover(hookOptions);
  return /* @__PURE__ */ jsxRuntime.jsx(usePopover.PopoverContext.Provider, { value: contextValue, children });
};
PopoverRoot.displayName = "Popover";
const Trigger = React.forwardRef(
  ({ children, className, ...triggerProps }, forwardedRef) => {
    const theme = useTheme.default();
    const {
      getReferenceProps,
      refs,
      currentOpenState,
      styleSlots: contextStyleSlots,
      variant
    } = usePopover.usePopoverContext();
    const childrenArray = React.Children.toArray(children);
    if (childrenArray.length !== 1 || !React.isValidElement(childrenArray[0])) {
      throw new Error("Popover.Trigger requires a single valid React element as its child.");
    }
    const childElement = childrenArray[0];
    const setRef = React.useCallback(
      (node) => {
        refs.setReference(node);
        if (typeof forwardedRef === "function") {
          forwardedRef(node);
        } else if (forwardedRef) {
          forwardedRef.current = node;
        }
      },
      [refs, forwardedRef]
    );
    const referenceProps = getReferenceProps({
      ref: setRef,
      disabled: triggerProps.disabled
    });
    const ariaDescribedBy = referenceProps["aria-describedby"];
    const ariaHasPopup = referenceProps["aria-haspopup"];
    const ariaControls = referenceProps["aria-controls"];
    referenceProps["aria-describedby"] = void 0;
    referenceProps["aria-haspopup"] = void 0;
    referenceProps["aria-controls"] = void 0;
    return /* @__PURE__ */ jsxRuntime.jsx(
      popover_styles.StyledPopover,
      {
        className: popover_styles.PopoverClasses.triggerWrapper,
        currentTheme: theme,
        styleSlots: contextStyleSlots,
        variant,
        ...referenceProps,
        children: React.cloneElement(childElement, {
          ...childElement.props,
          className: clsx$1.clsx(childElement.props.className, className),
          "data-popover-open": currentOpenState ? "true" : "false",
          ["aria-describedby"]: ariaDescribedBy || void 0,
          ["aria-haspopup"]: ariaHasPopup,
          ["aria-controls"]: ariaControls,
          ...triggerProps
        })
      }
    );
  }
);
Trigger.displayName = "Popover.Trigger";
const Content = React.forwardRef(
  ({ children, className, style, ...contentProps }, forwardedRef) => {
    const {
      refs,
      floatingStyles,
      floatingContext,
      getFloatingProps,
      isMounted,
      showArrow,
      arrowRef,
      actualPlacement,
      styles,
      variant,
      styleSlots: contextStyleSlots
    } = usePopover.usePopoverContext();
    const theme = useTheme.default();
    if (!isMounted) return null;
    return /* @__PURE__ */ jsxRuntime.jsx(
      "div",
      {
        ref: refs.setFloating,
        style: { zIndex: theme?.zIndex?.dynamic, ...floatingStyles, ...styles, ...style || {} },
        ...getFloatingProps(contentProps),
        children: /* @__PURE__ */ jsxRuntime.jsxs(
          popover_styles.StyledPopover,
          {
            ref: forwardedRef,
            className: clsx$1.clsx(popover_styles.PopoverClasses.root, className),
            currentTheme: theme,
            styleSlots: contextStyleSlots,
            variant,
            children: [
              showArrow && /* @__PURE__ */ jsxRuntime.jsx(
                react.FloatingArrow,
                {
                  ref: arrowRef,
                  className: clsx$1.clsx(popover_styles.PopoverClasses.arrow, {
                    [popover_styles.PopoverClasses.arrowTop]: actualPlacement?.includes("top"),
                    [popover_styles.PopoverClasses.arrowBottom]: actualPlacement?.includes("bottom"),
                    [popover_styles.PopoverClasses.arrowLeft]: actualPlacement?.includes("left"),
                    [popover_styles.PopoverClasses.arrowRight]: actualPlacement?.includes("right")
                  }),
                  context: floatingContext,
                  "data-testid": "arrow"
                }
              ),
              /* @__PURE__ */ jsxRuntime.jsx("div", { className: popover_styles.PopoverClasses.content, children })
            ]
          }
        )
      }
    );
  }
);
Content.displayName = "Popover.Content";
const PopoverDivider = React.forwardRef(
  ({ children, className, ...props }, forwardedRef) => /* @__PURE__ */ jsxRuntime.jsx(divider.default, { ref: forwardedRef, className: clsx$1.clsx(popover_styles.PopoverClasses.divider, className), ...props, children })
);
PopoverDivider.displayName = "Popover.Divider";
const FooterButton = React.forwardRef(
  ({ children, className, ...props }, forwardedRef) => /* @__PURE__ */ jsxRuntime.jsx(
    button.default,
    {
      ref: forwardedRef,
      buttonType: "tertiary",
      className: clsx$1.clsx(popover_styles.PopoverClasses.footerButton, className),
      ...props,
      children
    }
  )
);
FooterButton.displayName = "Popover.FooterButton";
const Popover = Object.assign(PopoverRoot, {
  Trigger,
  Content,
  Divider: PopoverDivider,
  FooterButton
});

exports.default = Popover;
//# sourceMappingURL=popover.cjs.map
