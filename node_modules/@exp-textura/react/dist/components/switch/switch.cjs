"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const RadixSwitch = require('@radix-ui/react-switch');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const icons = require('@exp-textura/icons');
const switch_styles = require('./switch.styles.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixSwitch__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixSwitch);

const SwitchRoot = React.forwardRef(
  ({ className, variant = "default", children, ...props }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(switch_styles.SwitchClasses.root, className);
    return /* @__PURE__ */ jsxRuntime.jsxs(
      switch_styles.StyledSwitch,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children: [
          /* @__PURE__ */ jsxRuntime.jsx("span", { className: switch_styles.SwitchClasses.track }),
          /* @__PURE__ */ jsxRuntime.jsx(RadixSwitch__namespace.Thumb, { className: switch_styles.SwitchClasses.thumb }),
          children
        ]
      }
    );
  }
);
SwitchRoot.displayName = "Switch";
const OnIcon = ({ children, className, style }) => {
  const onIconClasses = clsx$1.clsx(switch_styles.SwitchClasses.onIcon, className);
  if (children && React.isValidElement(children)) {
    return React.cloneElement(children, {
      className: clsx$1.clsx(onIconClasses, children.props.className),
      style: { ...style, ...children.props.style }
    });
  }
  return /* @__PURE__ */ jsxRuntime.jsx(icons.CheckSolid, { className: onIconClasses, style });
};
OnIcon.displayName = "Switch.OnIcon";
const OffIcon = ({ children, className, style }) => {
  const offIconClasses = clsx$1.clsx(switch_styles.SwitchClasses.offIcon, className);
  if (children && React.isValidElement(children)) {
    return React.cloneElement(children, {
      className: clsx$1.clsx(offIconClasses, children.props.className),
      style: { ...style, ...children.props.style }
    });
  }
  return /* @__PURE__ */ jsxRuntime.jsx(icons.Close, { className: offIconClasses, style });
};
OffIcon.displayName = "Switch.OffIcon";
const Switch = Object.assign(SwitchRoot, {
  OnIcon,
  OffIcon
});

exports.default = Switch;
//# sourceMappingURL=switch.cjs.map
