"use client";
import { jsxs, jsx } from 'react/jsx-runtime';
import * as RadixSwitch from '@radix-ui/react-switch';
import React, { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { Close, CheckSolid } from '@exp-textura/icons';
import { SwitchClasses, StyledSwitch } from './switch.styles.js';

const SwitchRoot = forwardRef(
  ({ className, variant = "default", children, ...props }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(SwitchClasses.root, className);
    return /* @__PURE__ */ jsxs(
      StyledSwitch,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children: [
          /* @__PURE__ */ jsx("span", { className: SwitchClasses.track }),
          /* @__PURE__ */ jsx(RadixSwitch.Thumb, { className: SwitchClasses.thumb }),
          children
        ]
      }
    );
  }
);
SwitchRoot.displayName = "Switch";
const OnIcon = ({ children, className, style }) => {
  const onIconClasses = clsx(SwitchClasses.onIcon, className);
  if (children && React.isValidElement(children)) {
    return React.cloneElement(children, {
      className: clsx(onIconClasses, children.props.className),
      style: { ...style, ...children.props.style }
    });
  }
  return /* @__PURE__ */ jsx(CheckSolid, { className: onIconClasses, style });
};
OnIcon.displayName = "Switch.OnIcon";
const OffIcon = ({ children, className, style }) => {
  const offIconClasses = clsx(SwitchClasses.offIcon, className);
  if (children && React.isValidElement(children)) {
    return React.cloneElement(children, {
      className: clsx(offIconClasses, children.props.className),
      style: { ...style, ...children.props.style }
    });
  }
  return /* @__PURE__ */ jsx(Close, { className: offIconClasses, style });
};
OffIcon.displayName = "Switch.OffIcon";
const Switch = Object.assign(SwitchRoot, {
  OnIcon,
  OffIcon
});

export { Switch as default };
//# sourceMappingURL=switch.js.map
