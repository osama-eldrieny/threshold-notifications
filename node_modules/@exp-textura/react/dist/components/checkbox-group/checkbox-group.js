"use client";
import { jsx, jsxs } from 'react/jsx-runtime';
import { forwardRef, useState, useMemo, useCallback, Children, isValidElement, cloneElement } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import Checkbox from '../checkbox/checkbox.js';
import { useUniqueId } from '../../system/utils/component-utils.js';
import { CheckboxGroupClasses, StyledCheckboxGroup } from './checkbox-group.styles.js';

const CheckboxGroupRoot = forwardRef(
  ({
    className,
    defaultValue = [],
    value,
    onValueChange,
    variant = "default",
    required = false,
    disabled = false,
    orientation = "vertical",
    size = "default",
    children,
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const isControlled = value !== void 0;
    const [internalValues, setInternalValues] = useState(defaultValue);
    const selectedValues = useMemo(
      () => isControlled ? Array.isArray(value) ? value : [] : internalValues,
      [isControlled, value, internalValues]
    );
    const toggleValue = useCallback(
      (val) => {
        const nextValues = selectedValues.includes(val) ? selectedValues.filter((v) => v !== val) : [...selectedValues, val];
        if (!isControlled) {
          setInternalValues(nextValues);
        }
        onValueChange?.(nextValues);
      },
      [selectedValues, isControlled, onValueChange]
    );
    const childrenWithProps = Children.map(children, (child) => {
      if (!isValidElement(child)) return child;
      const { value: childValue, disabled: itemDisabled, variant: childVariant } = child.props;
      const isChecked = selectedValues.includes(childValue);
      const isDisabled = disabled || itemDisabled;
      const handleCheckedChange = (checked) => {
        if (checked === true && !isChecked) {
          toggleValue(childValue);
        } else if (checked === false && isChecked) {
          toggleValue(childValue);
        }
      };
      return cloneElement(child, {
        checked: isChecked,
        disabled: isDisabled,
        // Respect individual item variant if set, otherwise use group variant
        variant: childVariant || variant,
        onCheckedChange: handleCheckedChange
      });
    });
    const rootClasses = clsx(CheckboxGroupClasses.root, className, {
      [CheckboxGroupClasses.sm]: size === "sm"
    });
    return /* @__PURE__ */ jsx(
      StyledCheckboxGroup,
      {
        ref,
        "aria-required": required,
        className: rootClasses,
        currentTheme: theme,
        "data-orientation": orientation,
        disabled,
        role: "group",
        variant,
        ...props,
        onChange: void 0,
        children: childrenWithProps
      }
    );
  }
);
CheckboxGroupRoot.displayName = "CheckboxGroup";
const Item = forwardRef(
  ({ label, subtext, checked = false, onCheckedChange, disabled, value, variant, ...props }, ref) => {
    const handleChange = (checked2) => {
      onCheckedChange?.(checked2);
    };
    const generatedId = useUniqueId("checkbox-group-item");
    const id = props.id ?? generatedId;
    return /* @__PURE__ */ jsxs("div", { ref, className: clsx(CheckboxGroupClasses.item), children: [
      /* @__PURE__ */ jsx(
        Checkbox,
        {
          checked,
          disabled,
          id,
          value,
          variant,
          onCheckedChange: handleChange,
          ...props
        }
      ),
      /* @__PURE__ */ jsxs("label", { className: CheckboxGroupClasses.itemLabel, "data-disabled": disabled, htmlFor: id, children: [
        /* @__PURE__ */ jsx("span", { className: CheckboxGroupClasses.itemLabelText, children: label }),
        subtext && /* @__PURE__ */ jsx("span", { className: CheckboxGroupClasses.itemSubtext, children: subtext })
      ] })
    ] });
  }
);
Item.displayName = "CheckboxGroup.Item";
const CheckboxGroup = Object.assign(CheckboxGroupRoot, {
  Item
});

export { CheckboxGroup as default };
//# sourceMappingURL=checkbox-group.js.map
