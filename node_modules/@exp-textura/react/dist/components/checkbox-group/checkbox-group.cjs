"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const checkbox = require('../checkbox/checkbox.cjs');
const componentUtils = require('../../system/utils/component-utils.cjs');
const checkboxGroup_styles = require('./checkbox-group.styles.cjs');

const CheckboxGroupRoot = React.forwardRef(
  ({
    className,
    defaultValue = [],
    value,
    onValueChange,
    variant = "default",
    required = false,
    disabled = false,
    orientation = "vertical",
    size = "default",
    children,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const isControlled = value !== void 0;
    const [internalValues, setInternalValues] = React.useState(defaultValue);
    const selectedValues = React.useMemo(
      () => isControlled ? Array.isArray(value) ? value : [] : internalValues,
      [isControlled, value, internalValues]
    );
    const toggleValue = React.useCallback(
      (val) => {
        const nextValues = selectedValues.includes(val) ? selectedValues.filter((v) => v !== val) : [...selectedValues, val];
        if (!isControlled) {
          setInternalValues(nextValues);
        }
        onValueChange?.(nextValues);
      },
      [selectedValues, isControlled, onValueChange]
    );
    const childrenWithProps = React.Children.map(children, (child) => {
      if (!React.isValidElement(child)) return child;
      const { value: childValue, disabled: itemDisabled, variant: childVariant } = child.props;
      const isChecked = selectedValues.includes(childValue);
      const isDisabled = disabled || itemDisabled;
      const handleCheckedChange = (checked) => {
        if (checked === true && !isChecked) {
          toggleValue(childValue);
        } else if (checked === false && isChecked) {
          toggleValue(childValue);
        }
      };
      return React.cloneElement(child, {
        checked: isChecked,
        disabled: isDisabled,
        // Respect individual item variant if set, otherwise use group variant
        variant: childVariant || variant,
        onCheckedChange: handleCheckedChange
      });
    });
    const rootClasses = clsx$1.clsx(checkboxGroup_styles.CheckboxGroupClasses.root, className, {
      [checkboxGroup_styles.CheckboxGroupClasses.sm]: size === "sm"
    });
    return /* @__PURE__ */ jsxRuntime.jsx(
      checkboxGroup_styles.StyledCheckboxGroup,
      {
        ref,
        "aria-required": required,
        className: rootClasses,
        currentTheme: theme,
        "data-orientation": orientation,
        disabled,
        role: "group",
        variant,
        ...props,
        onChange: void 0,
        children: childrenWithProps
      }
    );
  }
);
CheckboxGroupRoot.displayName = "CheckboxGroup";
const Item = React.forwardRef(
  ({ label, subtext, checked = false, onCheckedChange, disabled, value, variant, ...props }, ref) => {
    const handleChange = (checked2) => {
      onCheckedChange?.(checked2);
    };
    const generatedId = componentUtils.useUniqueId("checkbox-group-item");
    const id = props.id ?? generatedId;
    return /* @__PURE__ */ jsxRuntime.jsxs("div", { ref, className: clsx$1.clsx(checkboxGroup_styles.CheckboxGroupClasses.item), children: [
      /* @__PURE__ */ jsxRuntime.jsx(
        checkbox.default,
        {
          checked,
          disabled,
          id,
          value,
          variant,
          onCheckedChange: handleChange,
          ...props
        }
      ),
      /* @__PURE__ */ jsxRuntime.jsxs("label", { className: checkboxGroup_styles.CheckboxGroupClasses.itemLabel, "data-disabled": disabled, htmlFor: id, children: [
        /* @__PURE__ */ jsxRuntime.jsx("span", { className: checkboxGroup_styles.CheckboxGroupClasses.itemLabelText, children: label }),
        subtext && /* @__PURE__ */ jsxRuntime.jsx("span", { className: checkboxGroup_styles.CheckboxGroupClasses.itemSubtext, children: subtext })
      ] })
    ] });
  }
);
Item.displayName = "CheckboxGroup.Item";
const CheckboxGroup = Object.assign(CheckboxGroupRoot, {
  Item
});

exports.default = CheckboxGroup;
//# sourceMappingURL=checkbox-group.cjs.map
