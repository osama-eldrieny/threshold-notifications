"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const anchorOnlyProps = /* @__PURE__ */ new Set([
  "download",
  "href",
  "hrefLang",
  "media",
  "ping",
  "rel",
  "target",
  "type",
  "referrerPolicy"
]);
const splitAnchorProps = (props) => {
  const anchorProps = {};
  const divProps = {};
  for (const key in props) {
    if (Object.prototype.hasOwnProperty.call(props, key)) {
      if (anchorOnlyProps.has(key)) {
        anchorProps[key] = props[key];
      } else {
        divProps[key] = props[key];
      }
    }
  }
  return [anchorProps, divProps];
};

exports.splitAnchorProps = splitAnchorProps;
//# sourceMappingURL=splitAnchorProps.cjs.map
