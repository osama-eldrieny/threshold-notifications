"use client";
import { jsx, jsxs } from 'react/jsx-runtime';
import { useReactTable, getGroupedRowModel, getSortedRowModel, getPaginationRowModel, getFilteredRowModel, getExpandedRowModel, getCoreRowModel } from '@tanstack/react-table';
import React, { forwardRef, useState, useMemo, useEffect } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import Checkbox from '../checkbox/checkbox.js';
import Cell from '../cell/cell.js';
import { getTableChildren } from './utils/get-table-children.js';
import { TableHeader } from './subcomponents/table-header.js';
import { TableBody } from './subcomponents/table-body.js';
import { TableToolbarTrailing, TableToolbar } from './subcomponents/table-toolbar.js';
import { TableFooter } from './subcomponents/table-footer.js';
import { TableFilterIconButton } from './subcomponents/table-filter-icon-button.js';
import { TableFilterIconButtonBadge } from './subcomponents/table-filter-icon-button-badge.js';
import { TablePageSizeSelect } from './subcomponents/table-page-size-select.js';
import { TablePagination } from './subcomponents/table-pagination.js';
import { TableFilterChipBar } from './subcomponents/table-filter-chip-bar.js';
import { TableSearch } from './subcomponents/table-search.js';
import { TableEmptyState } from './subcomponents/table-empty-state.js';
import { TableContext, TableStateContext } from './hooks/use-table.js';
import { useTableKeyboardNav } from './hooks/use-table-keyboard-nav.js';
import { TableKeyboardNavContext } from './hooks/use-table-keyboard-nav-context.js';
import { StyledTable, TableClasses } from './table.styles.js';

const TableRoot = forwardRef(
  ({
    styleSlots,
    variant = "default",
    data,
    columns,
    children,
    className,
    enableRowSelection = false,
    enableRowExpansion = false,
    enableKeyboardShortcuts = true,
    initialState = {
      sorting: [],
      pagination: { pageIndex: 0, pageSize: 10 },
      rowSelection: {},
      columnFilters: [],
      globalFilter: ""
    },
    state: controlledState,
    onGlobalFilterChange,
    onSortingChange,
    onPaginationChange,
    onColumnFiltersChange,
    onExpandedChange,
    onRowSelectionChange,
    pageCount: controlledPageCount,
    rowSelectionAriaLabels = {
      selectAll: "Select all rows",
      selectRow: "Select row"
    },
    expandRowAriaLabels = {
      expand: "Expand row",
      collapse: "Collapse row"
    },
    expandRowIcons,
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const [tableHeight, setTableHeight] = useState(0);
    const tableContainerRef = React.useRef(null);
    const [globalFilter, setGlobalFilter] = useState(initialState?.globalFilter ?? "");
    const [sorting, setSorting] = useState(initialState?.sorting ?? []);
    const [pagination, setPagination] = useState({
      pageIndex: initialState?.pagination?.pageIndex ?? 0,
      pageSize: initialState?.pagination?.pageSize ?? 10
    });
    const [columnFilters, setColumnFilters] = useState(
      initialState?.columnFilters ?? []
    );
    const [rowSelection, setRowSelection] = useState(
      initialState?.rowSelection ?? {}
    );
    const [expanded, setExpanded] = useState(initialState?.expanded ?? {});
    const state = useMemo(
      () => ({
        ...controlledState,
        globalFilter: controlledState?.globalFilter ?? globalFilter,
        sorting: controlledState?.sorting ?? sorting,
        pagination: controlledState?.pagination ?? pagination,
        columnFilters: controlledState?.columnFilters ?? columnFilters,
        rowSelection: controlledState?.rowSelection ?? rowSelection,
        expanded: controlledState?.expanded ?? expanded
      }),
      [controlledState, globalFilter, sorting, pagination, columnFilters, rowSelection, expanded]
    );
    const handleGlobalFilterChange = (updater) => {
      onGlobalFilterChange?.(updater);
      if (!controlledState?.globalFilter) {
        setGlobalFilter(updater);
      }
    };
    const handleSortingChange = (updater) => {
      onSortingChange?.(updater);
      if (!controlledState?.sorting) {
        setSorting(updater);
      }
    };
    const handlePaginationChange = (updater) => {
      onPaginationChange?.(updater);
      if (!controlledState?.pagination) {
        setPagination(updater);
      }
    };
    const handleColumnFiltersChange = (updater) => {
      onColumnFiltersChange?.(updater);
      if (!controlledState?.columnFilters) {
        setColumnFilters(updater);
      }
    };
    const handleRowSelectionChange = (updater) => {
      onRowSelectionChange?.(updater);
      if (!controlledState?.rowSelection) {
        setRowSelection(updater);
      }
    };
    const handleExpandedChange = (updater) => {
      onExpandedChange?.(updater);
      if (!controlledState?.expanded) {
        setExpanded(updater);
      }
    };
    const table = useReactTable({
      data,
      columns: [
        ...enableRowSelection ? [
          {
            id: "select",
            header: ({ table: table2 }) => /* @__PURE__ */ jsx(
              Checkbox,
              {
                "aria-label": rowSelectionAriaLabels.selectAll,
                checked: table2.getIsAllRowsSelected() ? true : table2.getIsSomeRowsSelected() ? "indeterminate" : false,
                onCheckedChange: (value) => table2.toggleAllRowsSelected(!!value)
              }
            ),
            cell: ({ row }) => /* @__PURE__ */ jsx(
              Checkbox,
              {
                "aria-label": rowSelectionAriaLabels.selectRow,
                checked: row.getIsSelected(),
                onCheckedChange: (value) => row.toggleSelected(!!value)
              }
            ),
            enableSorting: false,
            enableColumnFilter: false,
            size: 40,
            minSize: 40,
            maxSize: 40
          }
        ] : [],
        ...enableRowExpansion ? [
          {
            id: "expand",
            header: () => null,
            cell: ({ row }) => /* @__PURE__ */ jsx(
              Cell.ExpandToggle,
              {
                canExpand: row.getCanExpand(),
                collapseAriaLabel: expandRowAriaLabels.collapse,
                collapsedIcon: expandRowIcons?.collapsed,
                expandAriaLabel: expandRowAriaLabels.expand,
                expandedIcon: expandRowIcons?.expanded,
                isExpanded: row.getIsExpanded(),
                onToggle: row.getToggleExpandedHandler()
              }
            ),
            enableSorting: false,
            enableColumnFilter: false,
            size: 24,
            minSize: 24,
            maxSize: 24
          }
        ] : [],
        ...columns
      ],
      state,
      pageCount: controlledPageCount,
      enableRowSelection,
      onGlobalFilterChange: handleGlobalFilterChange,
      onSortingChange: handleSortingChange,
      onPaginationChange: handlePaginationChange,
      onColumnFiltersChange: handleColumnFiltersChange,
      onRowSelectionChange: handleRowSelectionChange,
      onExpandedChange: handleExpandedChange,
      getCoreRowModel: getCoreRowModel(),
      getExpandedRowModel: getExpandedRowModel(),
      getFilteredRowModel: getFilteredRowModel(),
      getPaginationRowModel: getPaginationRowModel(),
      getSortedRowModel: getSortedRowModel(),
      getGroupedRowModel: getGroupedRowModel(),
      ...props
    });
    const { tableRef, handleKeyDown } = useTableKeyboardNav();
    const { toolbar, header, body, footer, filterChipBars, emptyState } = getTableChildren(children);
    useEffect(() => {
      if (tableContainerRef.current) {
        setTableHeight(tableContainerRef.current.offsetHeight);
      }
    }, [tableContainerRef]);
    const { rows } = table.getRowModel();
    const pageSize = state.pagination?.pageSize ?? 10;
    const emptyRows = rows.length < pageSize ? pageSize - rows.length : 0;
    return /* @__PURE__ */ jsx(TableContext.Provider, { value: table, children: /* @__PURE__ */ jsx(TableStateContext.Provider, { value: table.getState(), children: /* @__PURE__ */ jsx(TableKeyboardNavContext.Provider, { value: { enableKeyboardShortcuts, handleKeyDown }, children: /* @__PURE__ */ jsxs(
      StyledTable,
      {
        ref,
        className: clsx(TableClasses.root, className),
        currentTheme: theme,
        styleSlots,
        variant,
        children: [
          toolbar,
          filterChipBars,
          /* @__PURE__ */ jsx("div", { className: TableClasses.tableContainer, children: emptyRows !== pageSize || !emptyState ? /* @__PURE__ */ jsxs("div", { ref: tableContainerRef, children: [
            /* @__PURE__ */ jsxs("table", { ref: tableRef, className: TableClasses.table, children: [
              header,
              body
            ] }),
            footer
          ] }) : /* @__PURE__ */ jsx("div", { ref: tableContainerRef, style: { display: "flex", height: tableHeight }, children: emptyState }) })
        ]
      }
    ) }) }) });
  }
);
TableRoot.displayName = "Table";
const Table = Object.assign(TableRoot, {
  // ofType provides a typed view of the generic Table component for a specific row type TData.
  // Usage: const FileTable = Table.ofType<File>(); then <FileTable data={...} columns={...} />
  ofType: () => TableRoot,
  Toolbar: TableToolbar,
  ToolbarTrailing: TableToolbarTrailing,
  Header: TableHeader,
  Body: TableBody,
  Footer: TableFooter,
  Search: TableSearch,
  FilterIconButton: TableFilterIconButton,
  FilterIconButtonBadge: TableFilterIconButtonBadge,
  FilterChipBars: TableFilterChipBar,
  Pagination: TablePagination,
  PageSizeSelect: TablePageSizeSelect,
  EmptyState: TableEmptyState
});

export { Table as default };
//# sourceMappingURL=table.js.map
