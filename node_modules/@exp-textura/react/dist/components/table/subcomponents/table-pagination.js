"use client";
import { jsx, jsxs, Fragment } from 'react/jsx-runtime';
import Pagination from '../../pagination/pagination.js';
import { useTable } from '../hooks/use-table.js';

const TablePagination = (props) => {
  const { state, getPageCount, setPageIndex } = useTable();
  const { pagination } = state;
  return /* @__PURE__ */ jsx(
    Pagination,
    {
      finish: getPageCount(),
      selected: pagination.pageIndex + 1,
      onSelectedChange: (page) => setPageIndex(page - 1),
      ...props,
      children: props.children || /* @__PURE__ */ jsxs(Fragment, { children: [
        /* @__PURE__ */ jsx(Pagination.Prev, {}),
        /* @__PURE__ */ jsx(Pagination.Items, {}),
        /* @__PURE__ */ jsx(Pagination.Next, {})
      ] })
    }
  );
};
TablePagination.displayName = "Table.Pagination";

export { TablePagination };
//# sourceMappingURL=table-pagination.js.map
