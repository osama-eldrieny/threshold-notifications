"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const React = require('react');

const getTableChildren = (children) => {
  const toolbar = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === "Table.Toolbar"
  );
  const header = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === "Table.Header"
  );
  const body = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === "Table.Body"
  );
  const footer = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === "Table.Footer"
  );
  const filterChipBars = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === "Table.FilterChipBars"
  );
  const emptyState = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === "Table.EmptyState"
  );
  return {
    toolbar,
    header,
    body,
    footer,
    filterChipBars,
    emptyState
  };
};

exports.getTableChildren = getTableChildren;
//# sourceMappingURL=get-table-children.cjs.map
