"use client";
import { useContext, createContext } from 'react';

const TableKeyboardNavContext = createContext(
  void 0
);
const useTableKeyboardNavContext = () => {
  const context = useContext(TableKeyboardNavContext);
  if (!context) {
    throw new Error("useTableKeyboardNavContext must be used within a TableKeyboardNavProvider");
  }
  return context;
};

export { TableKeyboardNavContext, useTableKeyboardNavContext };
//# sourceMappingURL=use-table-keyboard-nav-context.js.map
