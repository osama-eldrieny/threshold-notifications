"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef, Children, isValidElement, cloneElement } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import Divider from '../divider/divider.js';
import IconButton from '../icon-button/icon-button.js';
import { Menu } from '@exp-textura/icons';
import { ActionHeaderClasses, StyledActionHeader } from './action-header.styles.js';

const recursiveCloneChildren = (children) => {
  return Children.map(children, (child) => {
    if (!isValidElement(child)) {
      return child;
    }
    const displayName = child.type.displayName;
    const sizeToApply = "sm";
    if ((displayName === "Button" || displayName === "IconButton" || displayName === "ToggleButton") && !child.props.size) {
      return cloneElement(child, {
        size: sizeToApply,
        buttonType: displayName === "IconButton" ? child.props.buttonType || "secondary" : child.props.buttonType
      });
    }
    if (child.props.children) {
      const clonedChildren = recursiveCloneChildren(child.props.children);
      return cloneElement(child, { children: clonedChildren });
    }
    return child;
  });
};
const ActionHeaderRoot = forwardRef(
  ({ className, variant = "default", children, ...props }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(ActionHeaderClasses.root, className);
    return /* @__PURE__ */ jsx(
      StyledActionHeader,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children
      }
    );
  }
);
ActionHeaderRoot.displayName = "ActionHeader";
const ActionHeaderLeading = forwardRef(
  ({ className, children, ...props }, ref) => {
    const childrenWithProps = recursiveCloneChildren(children);
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(ActionHeaderClasses.leading, className), ...props, children: childrenWithProps });
  }
);
ActionHeaderLeading.displayName = "ActionHeader.Leading";
const ActionHeaderCenter = forwardRef(
  ({ className, children, ...props }, ref) => {
    const childrenWithProps = recursiveCloneChildren(children);
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(ActionHeaderClasses.center, className), ...props, children: childrenWithProps });
  }
);
ActionHeaderCenter.displayName = "ActionHeader.Center";
const ActionHeaderTrailing = forwardRef(
  ({ className, children, ...props }, ref) => {
    const childrenWithProps = recursiveCloneChildren(children);
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(ActionHeaderClasses.trailing, className), ...props, children: childrenWithProps });
  }
);
ActionHeaderTrailing.displayName = "ActionHeader.Trailing";
const ActionHeaderDivider = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(ActionHeaderClasses.divider, className), ...props, children: children ?? /* @__PURE__ */ jsx(Divider, { orientation: "vertical", size: "sm" }) });
  }
);
ActionHeaderDivider.displayName = "ActionHeader.Divider";
const ActionHeaderStatus = forwardRef(
  ({ className, ...props }, ref) => {
    return /* @__PURE__ */ jsx("span", { ref, className: clsx(ActionHeaderClasses.status, className), ...props });
  }
);
ActionHeaderStatus.displayName = "ActionHeader.Status";
const ActionHeaderMenuIconButton = forwardRef(
  ({ size = "sm", children, ...props }, ref) => {
    return /* @__PURE__ */ jsx(IconButton, { ref, buttonType: "secondary", size, ...props, children: children || /* @__PURE__ */ jsx(Menu, {}) });
  }
);
ActionHeaderMenuIconButton.displayName = "ActionHeader.MenuIconButton";
const ActionHeader = Object.assign(ActionHeaderRoot, {
  Leading: ActionHeaderLeading,
  Center: ActionHeaderCenter,
  Trailing: ActionHeaderTrailing,
  Divider: ActionHeaderDivider,
  Status: ActionHeaderStatus,
  MenuIconButton: ActionHeaderMenuIconButton
});

export { ActionHeader as default };
//# sourceMappingURL=action-header.js.map
