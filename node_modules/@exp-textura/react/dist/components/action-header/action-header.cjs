"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const divider = require('../divider/divider.cjs');
const iconButton = require('../icon-button/icon-button.cjs');
const icons = require('@exp-textura/icons');
const actionHeader_styles = require('./action-header.styles.cjs');

const recursiveCloneChildren = (children) => {
  return React.Children.map(children, (child) => {
    if (!React.isValidElement(child)) {
      return child;
    }
    const displayName = child.type.displayName;
    const sizeToApply = "sm";
    if ((displayName === "Button" || displayName === "IconButton" || displayName === "ToggleButton") && !child.props.size) {
      return React.cloneElement(child, {
        size: sizeToApply,
        buttonType: displayName === "IconButton" ? child.props.buttonType || "secondary" : child.props.buttonType
      });
    }
    if (child.props.children) {
      const clonedChildren = recursiveCloneChildren(child.props.children);
      return React.cloneElement(child, { children: clonedChildren });
    }
    return child;
  });
};
const ActionHeaderRoot = React.forwardRef(
  ({ className, variant = "default", children, ...props }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(actionHeader_styles.ActionHeaderClasses.root, className);
    return /* @__PURE__ */ jsxRuntime.jsx(
      actionHeader_styles.StyledActionHeader,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children
      }
    );
  }
);
ActionHeaderRoot.displayName = "ActionHeader";
const ActionHeaderLeading = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    const childrenWithProps = recursiveCloneChildren(children);
    return /* @__PURE__ */ jsxRuntime.jsx("div", { ref, className: clsx$1.clsx(actionHeader_styles.ActionHeaderClasses.leading, className), ...props, children: childrenWithProps });
  }
);
ActionHeaderLeading.displayName = "ActionHeader.Leading";
const ActionHeaderCenter = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    const childrenWithProps = recursiveCloneChildren(children);
    return /* @__PURE__ */ jsxRuntime.jsx("div", { ref, className: clsx$1.clsx(actionHeader_styles.ActionHeaderClasses.center, className), ...props, children: childrenWithProps });
  }
);
ActionHeaderCenter.displayName = "ActionHeader.Center";
const ActionHeaderTrailing = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    const childrenWithProps = recursiveCloneChildren(children);
    return /* @__PURE__ */ jsxRuntime.jsx("div", { ref, className: clsx$1.clsx(actionHeader_styles.ActionHeaderClasses.trailing, className), ...props, children: childrenWithProps });
  }
);
ActionHeaderTrailing.displayName = "ActionHeader.Trailing";
const ActionHeaderDivider = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx("div", { ref, className: clsx$1.clsx(actionHeader_styles.ActionHeaderClasses.divider, className), ...props, children: children ?? /* @__PURE__ */ jsxRuntime.jsx(divider.default, { orientation: "vertical", size: "sm" }) });
  }
);
ActionHeaderDivider.displayName = "ActionHeader.Divider";
const ActionHeaderStatus = React.forwardRef(
  ({ className, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx("span", { ref, className: clsx$1.clsx(actionHeader_styles.ActionHeaderClasses.status, className), ...props });
  }
);
ActionHeaderStatus.displayName = "ActionHeader.Status";
const ActionHeaderMenuIconButton = React.forwardRef(
  ({ size = "sm", children, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx(iconButton.default, { ref, buttonType: "secondary", size, ...props, children: children || /* @__PURE__ */ jsxRuntime.jsx(icons.Menu, {}) });
  }
);
ActionHeaderMenuIconButton.displayName = "ActionHeader.MenuIconButton";
const ActionHeader = Object.assign(ActionHeaderRoot, {
  Leading: ActionHeaderLeading,
  Center: ActionHeaderCenter,
  Trailing: ActionHeaderTrailing,
  Divider: ActionHeaderDivider,
  Status: ActionHeaderStatus,
  MenuIconButton: ActionHeaderMenuIconButton
});

exports.default = ActionHeader;
//# sourceMappingURL=action-header.cjs.map
