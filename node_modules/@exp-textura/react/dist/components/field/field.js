"use client";
import { jsx, jsxs } from 'react/jsx-runtime';
import React, { forwardRef } from 'react';
import { Help, Attention } from '@exp-textura/icons';
import Tooltip from '../tooltip/tooltip.js';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { useFormContext, useFormState, Controller } from 'react-hook-form';
import { useFormSize } from '../form/hooks/use-form-size.js';
import Popover from '../popover/popover.js';
import { StyledField, FieldClasses } from './field.styles.js';
import { FieldContext, useField } from './hooks/use-field.js';
import { useFieldSharedLogic } from './hooks/use-field-shared-logic.js';

const FieldRenderer = forwardRef((props, ref) => {
  const { children, name, contextValue, rootClasses, controlPlacement, ...rest } = props;
  const theme = useTexturaTheme();
  const childrenArray = React.Children.toArray(children);
  const labelChildren = childrenArray.filter(
    (child) => React.isValidElement(child) && child.type.displayName === FieldLabel.displayName
  );
  const subtextChildren = childrenArray.filter(
    (child) => React.isValidElement(child) && child.type.displayName === Subtext.displayName
  );
  const controlChildren = childrenArray.filter(
    (child) => React.isValidElement(child) && child.type.displayName === Control.displayName
  );
  const otherChildren = childrenArray.filter(
    (child) => !React.isValidElement(child) || labelChildren.indexOf(child) === -1 && controlChildren.indexOf(child) === -1 && subtextChildren.indexOf(child) === -1
  );
  return /* @__PURE__ */ jsx(FieldContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsxs(StyledField, { ref, className: rootClasses, currentTheme: theme, name, ...rest, children: [
    /* @__PURE__ */ jsxs(
      "div",
      {
        className: clsx(FieldClasses.main, {
          [FieldClasses.controlPlacementInline]: controlPlacement === "inline",
          [FieldClasses.controlPlacementInlineTrailing]: controlPlacement === "inlineTrailing"
        }),
        children: [
          labelChildren,
          subtextChildren,
          controlChildren
        ]
      }
    ),
    otherChildren
  ] }) });
});
FieldRenderer.displayName = "FieldRenderer";
const FieldConnected = forwardRef((props, ref) => {
  const formState = useFormState({ name: props.name });
  const logic = useFieldSharedLogic(props, formState);
  return /* @__PURE__ */ jsx(FieldRenderer, { ...props, ...logic, ref });
});
FieldConnected.displayName = "FieldConnected";
const FieldStandalone = forwardRef((props, ref) => {
  const logic = useFieldSharedLogic(props, null);
  return /* @__PURE__ */ jsx(FieldRenderer, { ...props, ...logic, ref });
});
FieldStandalone.displayName = "FieldStandalone";
const Field = forwardRef((props, ref) => {
  const formContext = useFormContext();
  const formSizeHook = useFormSize();
  const formSize = props.formSize ?? formSizeHook?.formSize;
  const newProps = { ...props, formSize };
  return formContext ? /* @__PURE__ */ jsx(FieldConnected, { ...newProps, ref }) : /* @__PURE__ */ jsx(FieldStandalone, { ...newProps, ref });
});
Field.displayName = "Field";
const fieldsWithoutRequired = ["Button", "CheckboxGroup", "IconButton", "Slider"];
const fieldsLabelledBy = ["MultiSelect"];
const Control = ({ children, className, style, ...props }) => {
  const {
    name,
    rules,
    inputId,
    labelId,
    ariaDescribedBy,
    isInvalid,
    formSize,
    required,
    disabled
  } = useField();
  const formContext = useFormContext();
  const renderControl = (field) => {
    const element = children(field);
    const displayName = element.type.displayName;
    const sizeProp = displayName === "Input" || displayName === "Select" ? { inputSize: formSize } : { size: formSize };
    const ariaRequired = required && !fieldsWithoutRequired.includes(String(displayName)) ? "true" : void 0;
    const ariaLabelledBy = fieldsLabelledBy.includes(String(displayName)) ? labelId : void 0;
    return /* @__PURE__ */ jsx("div", { className: FieldClasses.controlContainer, children: React.cloneElement(element, {
      ...props,
      ...field,
      required,
      disabled,
      id: inputId,
      ...sizeProp,
      "aria-describedby": ariaDescribedBy,
      "aria-required": ariaRequired,
      "aria-labelledby": ariaLabelledBy,
      "aria-invalid": isInvalid ? "true" : void 0,
      variant: isInvalid ? "critical" : element.props.variant,
      className: clsx(element.props.className, className),
      style: { ...element.props.style, ...style }
    }) });
  };
  if (!formContext) {
    const fieldLike = {
      name,
      value: void 0,
      onChange: () => {
      },
      onBlur: () => {
      },
      ref: () => {
      }
    };
    return renderControl(fieldLike);
  }
  return /* @__PURE__ */ jsx(
    Controller,
    {
      control: formContext.control,
      name,
      render: ({ field }) => renderControl(field),
      rules
    }
  );
};
Control.displayName = "Field.Control";
const FieldLabel = ({ className, ...props }) => {
  const { inputId, labelId, required, disabled } = useField();
  const childrenArray = React.Children.toArray(props.children);
  const helpTooltip = childrenArray.find(
    (child) => React.isValidElement(child) && child.type.displayName === HelpTextTooltip.displayName
  );
  const otherChildren = childrenArray.filter(
    (child) => !React.isValidElement(child) || child.type.displayName !== HelpTextTooltip.displayName
  );
  return /* @__PURE__ */ jsxs(
    "label",
    {
      className: clsx(FieldClasses.label, { [FieldClasses.labelDisabled]: disabled }, className),
      htmlFor: inputId,
      id: labelId,
      ...props,
      children: [
        otherChildren,
        required && /* @__PURE__ */ jsx("span", { className: FieldClasses.requiredIndicator, children: "*" }),
        helpTooltip
      ]
    }
  );
};
FieldLabel.displayName = "Field.Label";
const Error = ({ className, children, showIcon = true, icon, ...props }) => {
  const { isInvalid, error, errorId } = useField();
  if (!isInvalid) {
    return null;
  }
  return /* @__PURE__ */ jsxs("span", { className: clsx(FieldClasses.error, className), id: errorId, ...props, children: [
    showIcon && (icon || /* @__PURE__ */ jsx(Attention, {})),
    children || error?.message
  ] });
};
Error.displayName = "Field.Error";
const HelpText = ({ children, className, ...props }) => {
  const { helpTextId } = useField();
  return /* @__PURE__ */ jsx("span", { className: clsx(FieldClasses.helpText, className), id: helpTextId, ...props, children });
};
HelpText.displayName = "Field.HelpText";
const Subtext = ({ children, className, ...props }) => {
  const { subtextId } = useField();
  return /* @__PURE__ */ jsx("span", { className: clsx(FieldClasses.subtext, className), id: subtextId, ...props, children });
};
Subtext.displayName = "Field.Subtext";
const HelpTextTooltip = ({ children, icon, ...props }) => {
  return /* @__PURE__ */ jsxs(Tooltip, { ...props, children: [
    /* @__PURE__ */ jsx(Tooltip.Trigger, { children: /* @__PURE__ */ jsx("span", { className: FieldClasses.helpTextTooltipTrigger, children: icon || /* @__PURE__ */ jsx(Help, {}) }) }),
    /* @__PURE__ */ jsx(Tooltip.Content, { children })
  ] });
};
HelpTextTooltip.displayName = "Field.HelpTextTooltip";
const HelpTextPopover = ({ children, icon, ...props }) => {
  return /* @__PURE__ */ jsxs(Popover, { dismissable: true, ...props, children: [
    /* @__PURE__ */ jsx(Popover.Trigger, { children: /* @__PURE__ */ jsx("span", { className: FieldClasses.helpTextPopoverTrigger, children: icon || /* @__PURE__ */ jsx(Help, {}) }) }),
    /* @__PURE__ */ jsx(Popover.Content, { children })
  ] });
};
HelpTextTooltip.displayName = "Field.HelpTextTooltip";
const FieldRoot = Field;
FieldRoot.Control = Control;
FieldRoot.Label = FieldLabel;
FieldRoot.Error = Error;
FieldRoot.HelpText = HelpText;
FieldRoot.HelpTextTooltip = HelpTextTooltip;
FieldRoot.HelpTextPopover = HelpTextPopover;
FieldRoot.Subtext = Subtext;

export { FieldRoot as default };
//# sourceMappingURL=field.js.map
