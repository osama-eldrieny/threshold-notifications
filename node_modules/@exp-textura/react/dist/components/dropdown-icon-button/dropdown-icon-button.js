"use client";
import { jsx, jsxs, Fragment } from 'react/jsx-runtime';
import React, { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { ChevronDownSm } from '@exp-textura/icons';
import Menu from '../menu/menu.js';
import { DropdownIconButtonClasses, StyledDropdownIconButton } from './dropdown-icon-button.styles.js';

const getDropdownIconButtonChildren = (children) => {
  let dropdownIcon = null;
  let menuContent = null;
  const iconContent = [];
  React.Children.forEach(children, (child) => {
    if (React.isValidElement(child)) {
      const displayName = child.type.displayName;
      if (displayName === DropdownIconButtonDropdownIcon.displayName) {
        dropdownIcon = child;
      } else if (displayName === DropdownIconButtonMenuContent.displayName) {
        menuContent = child;
      } else {
        iconContent.push(child);
      }
    } else if (child !== null && child !== void 0 && child !== false && child !== true) {
      iconContent.push(child);
    }
  });
  return { dropdownIcon, menuContent, iconContent };
};
const DropdownIconButtonRoot = forwardRef(
  ({
    children,
    className,
    variant = "default",
    size = "default",
    disabled = false,
    selected = false,
    side = "bottom",
    sideOffset = 4,
    align = "end",
    alignOffset = 0,
    menuVariant = "icons-only",
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(DropdownIconButtonClasses.root, className, {
      [DropdownIconButtonClasses.sm]: size === "sm"
    });
    const { dropdownIcon, menuContent, iconContent } = getDropdownIconButtonChildren(children);
    const dropdownIndicator = dropdownIcon ? React.cloneElement(dropdownIcon, {
      className: clsx(
        dropdownIcon.props.className,
        DropdownIconButtonClasses.dropdownIcon
      )
    }) : /* @__PURE__ */ jsx(ChevronDownSm, { "aria-hidden": "true", className: DropdownIconButtonClasses.dropdownIcon });
    const iconContentWithClasses = React.Children.map(iconContent, (child) => {
      if (React.isValidElement(child)) {
        return React.cloneElement(child, {
          className: clsx(
            child.props.className,
            DropdownIconButtonClasses.leadingIcon
          )
        });
      }
      return child;
    });
    const menuContentWithProps = menuContent && /* @__PURE__ */ jsx(Menu.Content, { ...menuContent.props });
    return /* @__PURE__ */ jsxs(
      Menu,
      {
        align,
        alignOffset,
        side,
        sideOffset,
        variant: menuVariant,
        children: [
          /* @__PURE__ */ jsx(Menu.Trigger, { children: /* @__PURE__ */ jsxs(
            StyledDropdownIconButton,
            {
              ref,
              className: rootClasses,
              currentTheme: theme,
              "data-selected": selected ? "true" : void 0,
              disabled,
              variant,
              ...props,
              children: [
                iconContentWithClasses,
                dropdownIndicator
              ]
            }
          ) }),
          menuContentWithProps
        ]
      }
    );
  }
);
DropdownIconButtonRoot.displayName = "DropdownIconButton";
const DropdownIconButtonDropdownIcon = ({ children }) => {
  return /* @__PURE__ */ jsx(Fragment, { children });
};
DropdownIconButtonDropdownIcon.displayName = "DropdownIconButton.DropdownIcon";
const DropdownIconButtonMenuContent = (props) => {
  return /* @__PURE__ */ jsx(Fragment, { children: props.children });
};
DropdownIconButtonMenuContent.displayName = "DropdownIconButton.MenuContent";
const DropdownIconButtonMenuItem = forwardRef(
  ({ children, ...props }, ref) => {
    return /* @__PURE__ */ jsx(Menu.Item, { ref, ...props, children });
  }
);
DropdownIconButtonMenuItem.displayName = "DropdownIconButton.MenuItem";
const DropdownIconButton = Object.assign(DropdownIconButtonRoot, {
  DropdownIcon: DropdownIconButtonDropdownIcon,
  MenuContent: DropdownIconButtonMenuContent,
  MenuItem: DropdownIconButtonMenuItem
});

export { DropdownIconButton as default };
//# sourceMappingURL=dropdown-icon-button.js.map
