"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const RadixDialog = require('@radix-ui/react-dialog');
const icons = require('@exp-textura/icons');
const iconButton = require('../icon-button/icon-button.cjs');
const dialog_styles = require('./dialog.styles.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixDialog__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixDialog);

const DialogRoot = React.forwardRef(
  ({
    className,
    children,
    onOpenChange,
    dismissible = true,
    open,
    defaultOpen = false,
    containerElement,
    modal = false,
    variant = "default",
    onEscapeKeyDown,
    onInteractOutside,
    onPointerDownOutside,
    onOpenAutoFocus,
    onCloseAutoFocus,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(dialog_styles.DialogClasses.root, className);
    const [portalContainer, setPortalContainer] = React.useState(null);
    React.useEffect(() => {
      const el = document.body.querySelector(`[data-textura-theme-provider="true"]`);
      setPortalContainer(containerElement || el || document.body);
    }, [containerElement]);
    const { trigger, content, closeIconButton } = getValidDialogChildren(children);
    return /* @__PURE__ */ jsxRuntime.jsxs(
      RadixDialog__namespace.Root,
      {
        defaultOpen,
        modal,
        open,
        onOpenChange,
        children: [
          trigger,
          /* @__PURE__ */ jsxRuntime.jsxs(RadixDialog__namespace.Portal, { container: portalContainer, children: [
            /* @__PURE__ */ jsxRuntime.jsx(
              dialog_styles.StyledDialogOverlay,
              {
                className: dialog_styles.DialogClasses.overlay,
                currentTheme: theme,
                styleSlots: props.styleSlots,
                variant
              }
            ),
            /* @__PURE__ */ jsxRuntime.jsxs(
              dialog_styles.StyledDialog,
              {
                ref,
                "aria-describedby": void 0,
                className: clsx$1.clsx(rootClasses, dialog_styles.DialogClasses.body),
                currentTheme: theme,
                variant,
                onCloseAutoFocus,
                onEscapeKeyDown: (e) => {
                  if (!dismissible) {
                    e.preventDefault();
                  }
                  onEscapeKeyDown?.(e);
                },
                onInteractOutside: (e) => {
                  if (!dismissible) {
                    e.preventDefault();
                  }
                  onInteractOutside?.(e);
                },
                onOpenAutoFocus,
                onPointerDownOutside: (e) => {
                  if (!dismissible) {
                    e.preventDefault();
                  }
                  onPointerDownOutside?.(e);
                },
                ...props,
                children: [
                  content,
                  closeIconButton
                ]
              }
            )
          ] })
        ]
      }
    );
  }
);
DialogRoot.displayName = "Dialog";
const Title = ({ className, children, ...props }) => /* @__PURE__ */ jsxRuntime.jsx(
  RadixDialog__namespace.Title,
  {
    className: clsx$1.clsx(dialog_styles.DialogClasses.title, className),
    "data-dialog-type": "title",
    ...props,
    children
  }
);
Title.displayName = "Dialog.Title";
const Trigger = ({ children, ...props }) => {
  return /* @__PURE__ */ jsxRuntime.jsx(RadixDialog__namespace.Trigger, { "data-dialog-type": "trigger", ...props, asChild: true, children });
};
Trigger.displayName = "Dialog.Trigger";
const Close = ({ children, ...props }) => /* @__PURE__ */ jsxRuntime.jsx(RadixDialog__namespace.Close, { "data-dialog-type": "close", ...props, asChild: true, children });
Close.displayName = "Dialog.Close";
const CloseIconButton = ({
  className,
  children,
  ...props
}) => /* @__PURE__ */ jsxRuntime.jsx(
  RadixDialog__namespace.Close,
  {
    asChild: true,
    "aria-label": props["aria-label"] || "Close dialog",
    className: clsx$1.clsx(dialog_styles.DialogClasses.closeIconButton, className),
    "data-dialog-type": "close-button",
    ...props,
    children: /* @__PURE__ */ jsxRuntime.jsx(iconButton.default, { buttonType: "secondary", children: children || /* @__PURE__ */ jsxRuntime.jsx(icons.Close, {}) })
  }
);
CloseIconButton.displayName = "Dialog.CloseIconButton";
const Content = ({ className, children, ...props }) => /* @__PURE__ */ jsxRuntime.jsx("div", { className: clsx$1.clsx(dialog_styles.DialogClasses.content, className), "data-dialog-type": "content", ...props, children });
Content.displayName = "Dialog.Content";
const Footer = ({ className, children, ...props }) => /* @__PURE__ */ jsxRuntime.jsx("footer", { className: clsx$1.clsx(dialog_styles.DialogClasses.footer, className), "data-dialog-type": "footer", ...props, children });
Footer.displayName = "Dialog.Footer";
const Dialog = Object.assign(DialogRoot, {
  Trigger,
  Close,
  CloseIconButton,
  Title,
  Content,
  Footer
});
const getValidDialogChildren = (children) => {
  let titleCount = 0;
  let closeIconButtonCount = 0;
  let triggerCount = 0;
  let contentCount = 0;
  let footerCount = 0;
  const trigger = [];
  const content = [];
  const closeIconButton = [];
  React.Children.forEach(children, (child) => {
    if (React.isValidElement(child)) {
      const displayName = child.type.displayName;
      if (displayName !== "Dialog.Trigger" && displayName !== "Dialog.CloseIconButton") {
        content.push(child);
      }
      if (displayName === "Dialog.Content") {
        contentCount++;
      } else if (displayName === "Dialog.Footer") {
        footerCount++;
      } else if (displayName === "Dialog.Title") {
        titleCount++;
      } else if (displayName === "Dialog.Trigger") {
        trigger.push(child);
        triggerCount++;
      } else if (displayName === "Dialog.CloseIconButton") {
        closeIconButton.push(child);
        closeIconButtonCount++;
      } else {
        throw new Error(
          "<Dialog> only accepts <Dialog.Title>, <Dialog.CloseIconButton>, <Dialog.Trigger>, <Dialog.Content>, and <Dialog.Footer> as children."
        );
      }
    }
  });
  if (contentCount > 1)
    throw new Error(`<Dialog> can only have one <Dialog.Content>. Found ${contentCount}.`);
  if (closeIconButtonCount > 1)
    throw new Error(
      `<Dialog> can only have one <Dialog.CloseIconButton>. Found ${closeIconButtonCount}.`
    );
  if (footerCount > 1)
    throw new Error(`<Dialog> can only have one <Dialog.Footer>. Found ${footerCount}.`);
  if (titleCount > 1)
    throw new Error(`<Dialog> can only have one <Dialog.Title>. Found ${titleCount}.`);
  if (triggerCount > 1)
    throw new Error(`<Dialog> can only have one <Dialog.Trigger>. Found ${triggerCount}.`);
  return { trigger, content, closeIconButton };
};

exports.default = Dialog;
//# sourceMappingURL=dialog.cjs.map
