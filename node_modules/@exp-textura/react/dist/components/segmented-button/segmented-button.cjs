"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const segmentedButton_styles = require('./segmented-button.styles.cjs');
const useSegmentedButton = require('./hooks/use-segmented-button.cjs');

const SegmentedButton = React.forwardRef(
  ({
    className,
    variant = "default",
    children,
    behavior = "button",
    selected: controlledSelected,
    defaultSelected,
    onValueChange,
    disabled = false,
    size,
    required = false,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(
      segmentedButton_styles.SegmentedButtonClasses.root,
      {
        [segmentedButton_styles.SegmentedButtonClasses.sm]: size === "sm"
      },
      className
    );
    const { role, handleKeyDown, renderChildren } = useSegmentedButton.useSegmentedButton({
      disabled,
      behavior,
      selected: controlledSelected,
      defaultSelected,
      onValueChange,
      children,
      variant,
      size,
      required
    });
    return /* @__PURE__ */ jsxRuntime.jsx(
      segmentedButton_styles.StyledSegmentedButton,
      {
        ref,
        "aria-orientation": behavior === "radio" ? "horizontal" : void 0,
        className: rootClasses,
        currentTheme: theme,
        role,
        variant,
        onKeyDown: handleKeyDown,
        ...props,
        children: renderChildren()
      }
    );
  }
);
SegmentedButton.displayName = "SegmentedButton";

exports.default = SegmentedButton;
//# sourceMappingURL=segmented-button.cjs.map
