"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { SegmentedButtonClasses, StyledSegmentedButton } from './segmented-button.styles.js';
import { useSegmentedButton } from './hooks/use-segmented-button.js';

const SegmentedButton = forwardRef(
  ({
    className,
    variant = "default",
    children,
    behavior = "button",
    selected: controlledSelected,
    defaultSelected,
    onValueChange,
    disabled = false,
    size,
    required = false,
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(
      SegmentedButtonClasses.root,
      {
        [SegmentedButtonClasses.sm]: size === "sm"
      },
      className
    );
    const { role, handleKeyDown, renderChildren } = useSegmentedButton({
      disabled,
      behavior,
      selected: controlledSelected,
      defaultSelected,
      onValueChange,
      children,
      variant,
      size,
      required
    });
    return /* @__PURE__ */ jsx(
      StyledSegmentedButton,
      {
        ref,
        "aria-orientation": behavior === "radio" ? "horizontal" : void 0,
        className: rootClasses,
        currentTheme: theme,
        role,
        variant,
        onKeyDown: handleKeyDown,
        ...props,
        children: renderChildren()
      }
    );
  }
);
SegmentedButton.displayName = "SegmentedButton";

export { SegmentedButton as default };
//# sourceMappingURL=segmented-button.js.map
