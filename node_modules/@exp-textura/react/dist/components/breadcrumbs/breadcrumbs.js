"use client";
import { jsx, jsxs, Fragment } from 'react/jsx-runtime';
import React, { forwardRef, useRef, useImperativeHandle, useMemo } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { VerticalScroll } from '@exp-textura/icons';
import LinkButton from '../link-button/link-button.js';
import IconButton from '../icon-button/icon-button.js';
import Menu from '../menu/menu.js';
import { BreadcrumbsClasses, StyledBreadcrumbs } from './breadcrumbs.styles.js';
import { BreadcrumbsContext, useBreadcrumbs } from './hooks/use-breadcrumbs.js';
import { useOverflowBreadcrumbs } from './hooks/use-overflow-breadcrumbs.js';

const Item = forwardRef(function BreadcrumbsItem({
  as,
  children,
  disabled,
  value,
  isCurrent,
  className,
  onClick,
  ...props
}, ref) {
  const { disabled: allDisabled, onAction } = useBreadcrumbs();
  const isDisabled = disabled || allDisabled;
  const handleClick = (e) => {
    if (value !== void 0) {
      onAction?.(value);
    }
    onClick?.(e);
  };
  if (isCurrent) {
    return /* @__PURE__ */ jsx(
      "span",
      {
        ref,
        "aria-current": "page",
        "aria-disabled": isDisabled || void 0,
        className: clsx(BreadcrumbsClasses.item, className),
        "data-current": isCurrent,
        ...props,
        children
      }
    );
  }
  return /* @__PURE__ */ jsx(
    LinkButton,
    {
      ref,
      as: as || "a",
      className: clsx(BreadcrumbsClasses.item, className),
      "data-current": isCurrent,
      disabled: isDisabled,
      size: "sm",
      tabIndex: isDisabled ? -1 : 0,
      variant: "secondary",
      onClick: !isDisabled ? handleClick : void 0,
      ...props,
      children
    }
  );
});
Item.displayName = "Breadcrumbs.Item";
const Divider = ({ children, className, ...props }) => {
  const { disabled: allDisabled } = useBreadcrumbs();
  return /* @__PURE__ */ jsx(
    "span",
    {
      "aria-hidden": "true",
      className: clsx(BreadcrumbsClasses.divider, className),
      "data-disabled": allDisabled,
      role: "presentation",
      ...props,
      children: children ?? "/"
    }
  );
};
Divider.displayName = "Breadcrumbs.Divider";
const Ellipsis = ({ children, className, ...props }) => {
  const { disabled: allDisabled } = useBreadcrumbs();
  return /* @__PURE__ */ jsx(
    "span",
    {
      "aria-hidden": "true",
      className: clsx(BreadcrumbsClasses.ellipsis, className),
      "data-disabled": allDisabled,
      "data-ellipsis": "true",
      role: "presentation",
      ...props,
      children: children ?? "..."
    }
  );
};
Ellipsis.displayName = "Breadcrumbs.Ellipsis";
const DropdownIconButton = forwardRef(
  ({
    className,
    size = "sm",
    buttonType = "secondary",
    disabled,
    value,
    onClick,
    children,
    ...props
  }, ref) => {
    const { disabled: allDisabled, onAction } = useBreadcrumbs();
    const isDisabled = disabled || allDisabled;
    const handleClick = (e) => {
      if (value !== void 0) {
        onAction?.(value);
      }
      onClick?.(e);
    };
    return /* @__PURE__ */ jsx(
      IconButton,
      {
        ref,
        buttonType,
        className: clsx(BreadcrumbsClasses.item, BreadcrumbsClasses.dropdownIconButton, className),
        disabled: isDisabled,
        size,
        tabIndex: isDisabled ? -1 : 0,
        onClick: !isDisabled ? handleClick : void 0,
        ...props,
        children: children ?? /* @__PURE__ */ jsx(VerticalScroll, {})
      }
    );
  }
);
DropdownIconButton.displayName = "Breadcrumbs.DropdownIconButton";
const isDivider = (child) => React.isValidElement(child) && child.type === Divider;
const isEllipsis = (child) => React.isValidElement(child) && child.type === Ellipsis;
const isBreadcrumbsItem = (child) => React.isValidElement(child) && child.type === Item;
const applyImplicitCurrent = (childrenArray) => {
  const hasExplicitCurrent = childrenArray.some(
    (child) => isBreadcrumbsItem(child) && child.props.isCurrent
  );
  if (hasExplicitCurrent) return childrenArray;
  const lastIndex = childrenArray.length - 1;
  return childrenArray.map((child, index) => {
    if (index === lastIndex && isBreadcrumbsItem(child)) {
      return React.cloneElement(child, { isCurrent: true });
    }
    return child;
  });
};
const BreadcrumbsRoot = forwardRef(
  ({
    className,
    variant = "default",
    onAction,
    disabled,
    children,
    ariaLabel = "Breadcrumb",
    autoOverflow,
    overflowMenuAriaLabel = "More pages",
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(BreadcrumbsClasses.root, className);
    const rootRef = useRef(null);
    const listRef = useRef(null);
    const overflowButtonRef = useRef(null);
    useImperativeHandle(ref, () => rootRef.current);
    const childrenArray = React.Children.toArray(children);
    const processedChildren = applyImplicitCurrent(childrenArray);
    const items = useMemo(() => {
      if (!autoOverflow) return [];
      return childrenArray.filter(isBreadcrumbsItem);
    }, [childrenArray, autoOverflow]);
    const { visibleMiddleCount } = useOverflowBreadcrumbs(
      listRef,
      overflowButtonRef,
      items.length > 2 ? items.length - 2 : 0,
      autoOverflow || "gradual",
      rootRef
    );
    const renderList = (nodes) => {
      return nodes.map((child, idx) => {
        const isDecorative = isDivider(child) || isEllipsis(child);
        return /* @__PURE__ */ jsx(
          "li",
          {
            "aria-hidden": isDecorative ? "true" : void 0,
            className: BreadcrumbsClasses.listItem,
            role: isDecorative ? "presentation" : void 0,
            children: child
          },
          idx
        );
      });
    };
    const renderAutoOverflowList = () => {
      const first = items[0];
      const last = items.length > 1 ? items[items.length - 1] : null;
      const middle = items.slice(1, -1);
      const isCollapsed = visibleMiddleCount !== Infinity;
      const overflowMiddle = isCollapsed ? middle.slice(visibleMiddleCount) : [];
      const hasOverflow = overflowMiddle.length > 0;
      return /* @__PURE__ */ jsxs(Fragment, { children: [
        /* @__PURE__ */ jsx("li", { className: BreadcrumbsClasses.listItem, "data-breadcrumb-index": "first", children: first }, "first"),
        items.length > 1 && /* @__PURE__ */ jsx(
          "li",
          {
            className: BreadcrumbsClasses.listItem,
            "data-breadcrumb-divider": "true",
            children: /* @__PURE__ */ jsx(Divider, {})
          },
          "div-first"
        ),
        middle.map((item, idx) => {
          const isVisible = idx < visibleMiddleCount;
          return /* @__PURE__ */ jsxs(React.Fragment, { children: [
            /* @__PURE__ */ jsx(
              "li",
              {
                "aria-hidden": !isVisible ? "true" : void 0,
                className: BreadcrumbsClasses.listItem,
                "data-breadcrumb-hidden": !isVisible ? "true" : void 0,
                "data-breadcrumb-index": idx + 1,
                children: item
              }
            ),
            /* @__PURE__ */ jsx(
              "li",
              {
                "aria-hidden": !isVisible ? "true" : void 0,
                className: BreadcrumbsClasses.listItem,
                "data-breadcrumb-divider": "true",
                "data-breadcrumb-hidden": !isVisible ? "true" : void 0,
                children: /* @__PURE__ */ jsx(Divider, {})
              }
            )
          ] }, `middle-${idx}`);
        }),
        /* @__PURE__ */ jsxs(
          "li",
          {
            ref: overflowButtonRef,
            "aria-hidden": !hasOverflow ? "true" : void 0,
            className: clsx(BreadcrumbsClasses.listItem, BreadcrumbsClasses.overflowItem),
            "data-breadcrumb-hidden": !hasOverflow ? "true" : void 0,
            children: [
              /* @__PURE__ */ jsx(Ellipsis, {}),
              /* @__PURE__ */ jsxs(Menu, { children: [
                /* @__PURE__ */ jsx(Menu.Trigger, { children: /* @__PURE__ */ jsx(DropdownIconButton, { "aria-label": overflowMenuAriaLabel }) }),
                /* @__PURE__ */ jsx(Menu.Content, { children: overflowMiddle.map((item, idx) => {
                  const { children: children2, value, disabled: disabled2, onClick, ...restProps } = item.props;
                  const isInteractive = Object.keys(restProps).length > 0;
                  const handleSelect = (e) => {
                    if (value !== void 0) {
                      onAction?.(value);
                    }
                    onClick?.(e);
                  };
                  return /* @__PURE__ */ jsx(
                    Menu.Item,
                    {
                      asChild: isInteractive,
                      disabled: disabled2,
                      onSelect: handleSelect,
                      children: isInteractive ? React.createElement(restProps.as || "a", restProps, children2) : /* @__PURE__ */ jsx("span", { children: children2 })
                    },
                    idx
                  );
                }) })
              ] })
            ]
          }
        ),
        hasOverflow && /* @__PURE__ */ jsx(
          "li",
          {
            className: BreadcrumbsClasses.listItem,
            "data-breadcrumb-divider": "true",
            children: /* @__PURE__ */ jsx(Divider, {})
          },
          "div-overflow"
        ),
        last && /* @__PURE__ */ jsx("li", { className: BreadcrumbsClasses.listItem, "data-breadcrumb-index": "last", children: React.cloneElement(last, { isCurrent: true }) }, "last")
      ] });
    };
    return /* @__PURE__ */ jsx(BreadcrumbsContext.Provider, { value: { disabled, onAction }, children: /* @__PURE__ */ jsx(
      StyledBreadcrumbs,
      {
        ref: rootRef,
        "aria-label": ariaLabel,
        as: "nav",
        autoOverflow,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children: /* @__PURE__ */ jsx("ol", { ref: listRef, className: BreadcrumbsClasses.list, children: autoOverflow ? renderAutoOverflowList() : renderList(processedChildren) })
      }
    ) });
  }
);
BreadcrumbsRoot.displayName = "Breadcrumbs";
const Breadcrumbs = Object.assign(BreadcrumbsRoot, {
  Item,
  DropdownIconButton,
  Divider,
  Ellipsis
});

export { Breadcrumbs as default };
//# sourceMappingURL=breadcrumbs.js.map
