"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../../theme/hooks/use-theme.cjs');
const checkbox = require('../../checkbox/checkbox.cjs');
const useMenu = require('../hooks/use-menu.cjs');
const menuItem_styles = require('./menu-item/menu-item.styles.cjs');
const menuItem = require('./menu-item/menu-item.cjs');

const MenuCheckboxItem = React.forwardRef(
  ({
    children,
    className,
    onSelect: propOnSelect,
    variant: variantProp,
    styleSlots,
    style,
    checked,
    onCheckedChange,
    closeOnSelect = false,
    size: sizeProp,
    ...props
  }, ref) => {
    const { onSelect: contextOnSelect, RadixComponent, variant, size: contextSize } = useMenu.useMenu();
    const theme = useTheme.default();
    const size = sizeProp || contextSize;
    const menuItemClasses = clsx$1.clsx(
      menuItem_styles.MenuItemClasses.root,
      {
        [menuItem_styles.MenuItemClasses.sm]: size === "sm"
      },
      className
    );
    const handleSelect = (event) => {
      const nativeEvent = event;
      if (!closeOnSelect) {
        nativeEvent.preventDefault();
      }
      propOnSelect?.(nativeEvent);
      contextOnSelect?.(nativeEvent);
    };
    const content = React.Children.map(
      children,
      (child) => typeof child === "string" ? /* @__PURE__ */ jsxRuntime.jsx(menuItem.MenuItemLabel, { children: child }) : child
    );
    const hasSubtext = React.Children.toArray(content).some(
      (child) => React.isValidElement(child) && child.type.displayName === "Menu.ItemSubtext"
    );
    const contentWithTextWrapper = hasSubtext ? /* @__PURE__ */ jsxRuntime.jsx(menuItem.MenuItemText, { children: React.Children.toArray(content).filter(
      (child) => React.isValidElement(child) && child.type.displayName !== "Menu.ItemLeadingIcon" && child.type.displayName !== "Menu.ItemTrailingIcon" && child.type.displayName !== "Menu.ItemHotkey"
    ) }, "menu-item-text") : content;
    return /* @__PURE__ */ jsxRuntime.jsxs(
      menuItem_styles.StyledMenuItem,
      {
        ref,
        as: RadixComponent.CheckboxItem,
        checked,
        className: menuItemClasses,
        currentTheme: theme,
        "data-has-subtext": hasSubtext ? true : void 0,
        style,
        styleSlots,
        variant: variantProp || variant,
        onCheckedChange,
        onSelect: handleSelect,
        ...props,
        children: [
          /* @__PURE__ */ jsxRuntime.jsx("div", { className: menuItem_styles.MenuItemClasses.leadingIcon, children: /* @__PURE__ */ jsxRuntime.jsx(checkbox.default, { checked }) }),
          hasSubtext ? [
            React.Children.toArray(content).find(
              (child) => React.isValidElement(child) && child.type.displayName === "Menu.ItemLeadingIcon"
            ),
            contentWithTextWrapper,
            React.Children.toArray(content).find(
              (child) => React.isValidElement(child) && child.type.displayName === "Menu.ItemTrailingIcon"
            ),
            React.Children.toArray(content).find(
              (child) => React.isValidElement(child) && child.type.displayName === "Menu.ItemHotkey"
            )
          ] : content
        ]
      }
    );
  }
);
MenuCheckboxItem.displayName = "Menu.CheckboxItem";

exports.MenuCheckboxItem = MenuCheckboxItem;
//# sourceMappingURL=menu-checkbox-item.cjs.map
