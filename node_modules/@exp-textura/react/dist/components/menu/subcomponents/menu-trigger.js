"use client";
import { jsx } from 'react/jsx-runtime';
import React, { forwardRef } from 'react';
import { clsx } from 'clsx';
import { useMenu } from '../hooks/use-menu.js';
import { MenuClasses } from '../menu.styles.js';

const MenuTrigger = forwardRef(({ children, className, ...props }, ref) => {
  const { RadixComponent } = useMenu();
  const disabled = React.isValidElement(children) && children.props.disabled;
  return /* @__PURE__ */ jsx(
    RadixComponent.Trigger,
    {
      ref,
      asChild: true,
      className: clsx(MenuClasses.trigger, className),
      disabled,
      ...props,
      children
    }
  );
});
MenuTrigger.displayName = "Menu.Trigger";

export { MenuTrigger };
//# sourceMappingURL=menu-trigger.js.map
