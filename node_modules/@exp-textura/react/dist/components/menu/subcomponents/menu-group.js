"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import { useMenu } from '../hooks/use-menu.js';
import { MenuClasses } from '../menu.styles.js';

const MenuGroup = forwardRef(
  ({ children, className, ...props }, ref) => {
    const { RadixComponent } = useMenu();
    return /* @__PURE__ */ jsx(RadixComponent.Group, { ref, className: clsx(MenuClasses.group, className), ...props, children });
  }
);
MenuGroup.displayName = "Menu.Group";

export { MenuGroup };
//# sourceMappingURL=menu-group.js.map
