"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../../theme/hooks/use-theme.cjs');
const useMenu = require('../hooks/use-menu.cjs');
const menu_styles = require('../menu.styles.cjs');
const menuItem_styles = require('./menu-item/menu-item.styles.cjs');
const menuGroup = require('./menu-group.cjs');
const menuDivider = require('./menu-divider.cjs');

const MenuSub = ({
  children,
  ...props
}) => {
  const { RadixComponent } = useMenu.useMenu();
  return /* @__PURE__ */ jsxRuntime.jsx(RadixComponent.Sub, { ...props, children });
};
MenuSub.displayName = "Menu.Sub";
const MenuSubTrigger = React.forwardRef(
  ({ children, className, variant: variantProp, styleSlots, style, size: sizeProp, ...props }, ref) => {
    const { RadixComponent, variant, size: contextSize } = useMenu.useMenu();
    const theme = useTheme.default();
    const size = sizeProp || contextSize;
    const menuItemClasses = clsx$1.clsx(
      menuItem_styles.MenuItemClasses.root,
      {
        [menuItem_styles.MenuItemClasses.sm]: size === "sm"
      },
      className
    );
    return /* @__PURE__ */ jsxRuntime.jsx(RadixComponent.SubTrigger, { ref, asChild: true, ...props, children: /* @__PURE__ */ jsxRuntime.jsx(
      menuItem_styles.StyledMenuItem,
      {
        className: menuItemClasses,
        currentTheme: theme,
        style,
        styleSlots,
        variant: variantProp || variant,
        children
      }
    ) });
  }
);
MenuSubTrigger.displayName = "Menu.SubTrigger";
const MenuSubContent = React.forwardRef(
  ({ children, className, ...props }, ref) => {
    const { loop, RadixComponent } = useMenu.useMenu();
    const childrenArray = React.Children.toArray(children);
    const groupedChildren = [];
    let currentGroup = [];
    const isGroup = (child) => React.isValidElement(child) && child.type?.displayName === menuGroup.MenuGroup.displayName;
    const isDivider = (child) => React.isValidElement(child) && child.type?.displayName === menuDivider.MenuDivider.displayName;
    const flushGroup = () => {
      if (currentGroup.length > 0) {
        groupedChildren.push(/* @__PURE__ */ jsxRuntime.jsx(menuGroup.MenuGroup, { children: currentGroup }, groupedChildren.length));
        currentGroup = [];
      }
    };
    childrenArray.forEach((child) => {
      if (isGroup(child) || isDivider(child)) {
        flushGroup();
        groupedChildren.push(child);
      } else {
        currentGroup.push(child);
      }
    });
    flushGroup();
    return /* @__PURE__ */ jsxRuntime.jsx(
      RadixComponent.SubContent,
      {
        ref,
        className: clsx$1.clsx(menu_styles.MenuClasses.content, menu_styles.MenuClasses.subContent, className),
        loop,
        sideOffset: 14,
        ...props,
        children: groupedChildren
      }
    );
  }
);
MenuSubContent.displayName = "Menu.SubContent";

exports.MenuSub = MenuSub;
exports.MenuSubContent = MenuSubContent;
exports.MenuSubTrigger = MenuSubTrigger;
//# sourceMappingURL=menu-sub.cjs.map
