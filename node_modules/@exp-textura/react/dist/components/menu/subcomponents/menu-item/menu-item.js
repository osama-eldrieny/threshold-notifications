"use client";
import { jsx } from 'react/jsx-runtime';
import React, { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../../../theme/hooks/use-theme.js';
import { useMenu } from '../../hooks/use-menu.js';
import { MenuItemClasses, StyledMenuItem } from './menu-item.styles.js';

const MenuItemLabel = forwardRef(
  ({ children, className, ...props }, ref) => /* @__PURE__ */ jsx("span", { ref, className: clsx(MenuItemClasses.label, className), ...props, children })
);
MenuItemLabel.displayName = "Menu.ItemLabel";
const MenuItemText = ({ children }) => {
  return /* @__PURE__ */ jsx("div", { className: MenuItemClasses.text, children });
};
MenuItemText.displayName = "Menu.ItemText";
const MenuItemSubtext = forwardRef(
  ({ children, className, ...props }, ref) => /* @__PURE__ */ jsx("span", { ref, className: clsx(MenuItemClasses.subtext, className), ...props, children })
);
MenuItemSubtext.displayName = "Menu.ItemSubtext";
const MenuItemHotkey = forwardRef(
  ({ children, className, ...props }, ref) => /* @__PURE__ */ jsx("span", { ref, className: clsx(MenuItemClasses.hotkey, className), ...props, children })
);
MenuItemHotkey.displayName = "Menu.ItemHotkey";
const MenuItemLeadingIcon = forwardRef(
  ({ children, className, ...props }, ref) => /* @__PURE__ */ jsx("span", { ref, className: clsx(MenuItemClasses.leadingIcon, className), ...props, children })
);
MenuItemLeadingIcon.displayName = "Menu.ItemLeadingIcon";
const MenuItemTrailingIcon = forwardRef(
  ({ children, className, ...props }, ref) => /* @__PURE__ */ jsx("span", { ref, className: clsx(MenuItemClasses.trailingIcon, className), ...props, children })
);
MenuItemTrailingIcon.displayName = "Menu.ItemTrailingIcon";
const MenuItem = forwardRef(
  ({
    children,
    className,
    onSelect: propOnSelect,
    variant: variantProp,
    styleSlots,
    style,
    size: sizeProp,
    asChild,
    selected,
    ...props
  }, ref) => {
    const { onSelect: contextOnSelect, RadixComponent, variant, size: contextSize } = useMenu();
    const theme = useTexturaTheme();
    const size = sizeProp || contextSize;
    const menuItemClasses = clsx(
      MenuItemClasses.root,
      {
        [MenuItemClasses.sm]: size === "sm"
      },
      className
    );
    const handleSelect = (event) => {
      const nativeEvent = event;
      propOnSelect?.(nativeEvent);
      contextOnSelect?.(nativeEvent);
    };
    const content = React.Children.map(
      children,
      (child) => typeof child === "string" ? /* @__PURE__ */ jsx(MenuItemLabel, { children: child }) : child
    );
    const hasSubtext = React.Children.toArray(content).some(
      (child) => React.isValidElement(child) && child.type.displayName === MenuItemSubtext.displayName
    );
    const contentWithTextWrapper = hasSubtext ? /* @__PURE__ */ jsx(MenuItemText, { children: React.Children.toArray(content).filter(
      (child) => React.isValidElement(child) && child.type.displayName !== MenuItemLeadingIcon.displayName && child.type.displayName !== MenuItemTrailingIcon.displayName && child.type.displayName !== MenuItemHotkey.displayName
    ) }, "menu-item-text") : content;
    return /* @__PURE__ */ jsx(
      StyledMenuItem,
      {
        ref,
        as: RadixComponent.Item,
        asChild,
        className: menuItemClasses,
        currentTheme: theme,
        "data-has-subtext": hasSubtext ? true : void 0,
        "data-selected": selected ? "true" : void 0,
        style,
        styleSlots,
        variant: variantProp || variant,
        onSelect: handleSelect,
        ...props,
        children: asChild ? children : hasSubtext ? [
          React.Children.toArray(content).find(
            (child) => React.isValidElement(child) && child.type.displayName === MenuItemLeadingIcon.displayName
          ),
          contentWithTextWrapper,
          React.Children.toArray(content).find(
            (child) => React.isValidElement(child) && child.type.displayName === MenuItemTrailingIcon.displayName
          ),
          React.Children.toArray(content).find(
            (child) => React.isValidElement(child) && child.type.displayName === MenuItemHotkey.displayName
          )
        ] : content
      }
    );
  }
);
MenuItem.displayName = "Menu.Item";

export { MenuItem, MenuItemHotkey, MenuItemLabel, MenuItemLeadingIcon, MenuItemSubtext, MenuItemText, MenuItemTrailingIcon };
//# sourceMappingURL=menu-item.js.map
