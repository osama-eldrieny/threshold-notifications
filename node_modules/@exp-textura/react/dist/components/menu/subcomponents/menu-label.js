"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import { useMenu } from '../hooks/use-menu.js';
import { MenuClasses } from '../menu.styles.js';

const MenuLabel = forwardRef(
  ({ children, className, ...props }, ref) => {
    const { RadixComponent } = useMenu();
    return /* @__PURE__ */ jsx(
      RadixComponent.Label,
      {
        ref,
        className: clsx(MenuClasses.groupLabel, className),
        ...props,
        children
      }
    );
  }
);
MenuLabel.displayName = "Menu.Label";

export { MenuLabel };
//# sourceMappingURL=menu-label.js.map
