"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
const useMenu = require('../hooks/use-menu.cjs');
const menu_styles = require('../menu.styles.cjs');

const MenuLabel = React.forwardRef(
  ({ children, className, ...props }, ref) => {
    const { RadixComponent } = useMenu.useMenu();
    return /* @__PURE__ */ jsxRuntime.jsx(
      RadixComponent.Label,
      {
        ref,
        className: clsx$1.clsx(menu_styles.MenuClasses.groupLabel, className),
        ...props,
        children
      }
    );
  }
);
MenuLabel.displayName = "Menu.Label";

exports.MenuLabel = MenuLabel;
//# sourceMappingURL=menu-label.cjs.map
