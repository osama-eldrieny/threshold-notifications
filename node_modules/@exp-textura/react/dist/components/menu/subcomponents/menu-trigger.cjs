"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
const useMenu = require('../hooks/use-menu.cjs');
const menu_styles = require('../menu.styles.cjs');

const MenuTrigger = React.forwardRef(({ children, className, ...props }, ref) => {
  const { RadixComponent } = useMenu.useMenu();
  const disabled = React.isValidElement(children) && children.props.disabled;
  return /* @__PURE__ */ jsxRuntime.jsx(
    RadixComponent.Trigger,
    {
      ref,
      asChild: true,
      className: clsx$1.clsx(menu_styles.MenuClasses.trigger, className),
      disabled,
      ...props,
      children
    }
  );
});
MenuTrigger.displayName = "Menu.Trigger";

exports.MenuTrigger = MenuTrigger;
//# sourceMappingURL=menu-trigger.cjs.map
