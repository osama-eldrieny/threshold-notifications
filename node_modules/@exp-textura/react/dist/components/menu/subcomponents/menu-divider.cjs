"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
const useMenu = require('../hooks/use-menu.cjs');
const menu_styles = require('../menu.styles.cjs');

const MenuDivider = React.forwardRef(
  ({ children, className, ...props }, ref) => {
    const { RadixComponent } = useMenu.useMenu();
    return /* @__PURE__ */ jsxRuntime.jsx(
      RadixComponent.Separator,
      {
        ref,
        className: clsx$1.clsx(menu_styles.MenuClasses.divider, className),
        ...props,
        children
      }
    );
  }
);
MenuDivider.displayName = "Menu.Divider";

exports.MenuDivider = MenuDivider;
//# sourceMappingURL=menu-divider.cjs.map
