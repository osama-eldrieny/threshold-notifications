"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const RadixDropdownMenu = require('@radix-ui/react-dropdown-menu');
const RadixContextMenu = require('@radix-ui/react-context-menu');
const menuItem = require('./subcomponents/menu-item/menu-item.cjs');
const menuCheckboxItem = require('./subcomponents/menu-checkbox-item.cjs');
const menuSub = require('./subcomponents/menu-sub.cjs');
const menuGroup = require('./subcomponents/menu-group.cjs');
const menuDivider = require('./subcomponents/menu-divider.cjs');
const menuTrigger = require('./subcomponents/menu-trigger.cjs');
const menuContent = require('./subcomponents/menu-content.cjs');
const menuLabel = require('./subcomponents/menu-label.cjs');
const menu_styles = require('./menu.styles.cjs');
const useMenu = require('./hooks/use-menu.cjs');
const getMenuChildren = require('./utils/get-menu-children.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixDropdownMenu__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixDropdownMenu);
const RadixContextMenu__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixContextMenu);

const getRadix = (type) => type === "context" ? RadixContextMenu__namespace : RadixDropdownMenu__namespace;
const MenuRoot = React.forwardRef(
  ({
    variant = "default",
    children,
    className,
    defaultOpen = false,
    open,
    onOpenChange,
    modal = false,
    dismissible = true,
    containerElement,
    loop = true,
    side = "bottom",
    sideOffset = 4,
    align = "start",
    alignOffset = 0,
    onSelect,
    size = "default",
    type = "dropdown",
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(menu_styles.MenuClasses.root, className);
    const [portalContainer, setPortalContainer] = React.useState(null);
    React.useEffect(() => {
      const el = document.body.querySelector(`[data-textura-theme-provider="true"]`);
      setPortalContainer(containerElement || el || document.body);
    }, [containerElement]);
    const { trigger, content } = getMenuChildren.getMenuChildren(children);
    const radixRootProps = {
      open,
      defaultOpen,
      onOpenChange,
      modal
    };
    const RadixComponent = React.useMemo(() => getRadix(type), [type]);
    const contextValue = {
      dismissible,
      loop,
      side: side === "start" ? "left" : side === "end" ? "right" : side,
      sideOffset,
      align,
      alignOffset,
      onSelect,
      size,
      variant,
      RadixComponent
    };
    return /* @__PURE__ */ jsxRuntime.jsx(useMenu.MenuContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsxRuntime.jsx(RadixComponent.Root, { ...radixRootProps, children: /* @__PURE__ */ jsxRuntime.jsxs(
      menu_styles.StyledMenu,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children: [
          trigger,
          /* @__PURE__ */ jsxRuntime.jsx(RadixComponent.Portal, { container: portalContainer, children: /* @__PURE__ */ jsxRuntime.jsx(
            menu_styles.StyledMenuContent,
            {
              currentTheme: theme,
              styleSlots: props.styleSlots,
              variant,
              children: content
            }
          ) })
        ]
      }
    ) }) });
  }
);
MenuRoot.displayName = "Menu";
const Menu = Object.assign(MenuRoot, {
  Trigger: menuTrigger.MenuTrigger,
  Content: menuContent.MenuContent,
  Group: menuGroup.MenuGroup,
  Label: menuLabel.MenuLabel,
  Item: menuItem.MenuItem,
  ItemLabel: menuItem.MenuItemLabel,
  ItemSubtext: menuItem.MenuItemSubtext,
  ItemHotkey: menuItem.MenuItemHotkey,
  ItemLeadingIcon: menuItem.MenuItemLeadingIcon,
  ItemTrailingIcon: menuItem.MenuItemTrailingIcon,
  CheckboxItem: menuCheckboxItem.MenuCheckboxItem,
  Divider: menuDivider.MenuDivider,
  Sub: menuSub.MenuSub,
  SubTrigger: menuSub.MenuSubTrigger,
  SubContent: menuSub.MenuSubContent
});

exports.default = Menu;
//# sourceMappingURL=menu.cjs.map
