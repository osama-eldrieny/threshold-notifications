"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const RadixToolbar = require('@radix-ui/react-toolbar');
const divider = require('../divider/divider.cjs');
const buttonGroup = require('../button-group/button-group.cjs');
const toggleButton_styles = require('../toggle-button/toggle-button.styles.cjs');
const toolbar_styles = require('./toolbar.styles.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixToolbar__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixToolbar);

const ToolbarRoot = React.forwardRef(
  ({ className, variant = "default", asChild, children, ...props }, ref) => {
    const theme = useTheme.default(), rootClasses = clsx$1.clsx(toolbar_styles.ToolbarClasses.root, className);
    return /* @__PURE__ */ jsxRuntime.jsx(
      toolbar_styles.StyledToolbar,
      {
        ref,
        asChild,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children
      }
    );
  }
);
ToolbarRoot.displayName = "Toolbar";
const ToolbarLeading = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx("div", { ref, className: clsx$1.clsx(toolbar_styles.ToolbarClasses.leading, className), ...props, children });
  }
);
ToolbarLeading.displayName = "Toolbar.Leading";
const ToolbarTrailing = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx("div", { ref, className: clsx$1.clsx(toolbar_styles.ToolbarClasses.trailing, className), ...props, children });
  }
);
ToolbarTrailing.displayName = "Toolbar.Trailing";
const ToolbarItem = React.forwardRef(
  ({ className, asChild = true, children, ...props }, ref) => {
    const modifiedChildren = React.Children.map(children, (child) => {
      if (React.isValidElement(child)) {
        const displayName = child.type?.displayName;
        if (displayName === "Button") {
          return React.cloneElement(child, {
            variant: child.props.variant || "desaturated",
            buttonType: child.props.buttonType || "secondary"
          });
        } else if (displayName === "IconButton") {
          return React.cloneElement(child, {
            buttonType: child.props.buttonType || "secondary"
          });
        }
      }
      return child;
    });
    const childToRender = Array.isArray(modifiedChildren) && modifiedChildren.length === 1 ? modifiedChildren[0] : modifiedChildren;
    return /* @__PURE__ */ jsxRuntime.jsx(
      RadixToolbar__namespace.Button,
      {
        ref,
        asChild,
        className: clsx$1.clsx(toolbar_styles.ToolbarClasses.item, className),
        ...props,
        children: childToRender
      }
    );
  }
);
ToolbarItem.displayName = "Toolbar.Item";
const ToolbarDivider = React.forwardRef(
  ({ className, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx(
      divider.default,
      {
        ref,
        className: clsx$1.clsx(toolbar_styles.ToolbarClasses.divider, className),
        orientation: "vertical",
        ...props
      }
    );
  }
);
ToolbarDivider.displayName = "Toolbar.Divider";
const ToolbarToggleGroup = React.forwardRef(
  ({ className, children, value: controlledValue, onValueChange, type, defaultValue, ...props }, ref) => {
    const theme = useTheme.default();
    const [uncontrolledValue, setUncontrolledValue] = React.useState(defaultValue);
    const isControlled = controlledValue !== void 0;
    const value = isControlled ? controlledValue : uncontrolledValue;
    const handleValueChange = React.useCallback(
      (newValue) => {
        if (type === "single" && newValue === "") {
          return;
        }
        if (!isControlled) {
          setUncontrolledValue(newValue);
        }
        onValueChange?.(newValue);
      },
      [isControlled, onValueChange, type]
    );
    return /* @__PURE__ */ jsxRuntime.jsx(
      RadixToolbar__namespace.ToggleGroup,
      {
        ref,
        asChild: true,
        className: clsx$1.clsx(toolbar_styles.ToolbarClasses.toggleGroup, className),
        type,
        value,
        onValueChange: handleValueChange,
        ...props,
        children: /* @__PURE__ */ jsxRuntime.jsx(toggleButton_styles.StyledToggleButton, { currentTheme: theme, children })
      }
    );
  }
);
ToolbarToggleGroup.displayName = "Toolbar.ToggleGroup";
const ToolbarToggleItem = React.forwardRef(
  ({ className, asChild = true, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx(
      RadixToolbar__namespace.ToggleItem,
      {
        ref,
        asChild,
        className: clsx$1.clsx(toolbar_styles.ToolbarClasses.toggleItem, className),
        ...props
      }
    );
  }
);
ToolbarToggleItem.displayName = "Toolbar.ToggleItem";
const ToolbarGroup = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx(buttonGroup.default, { ref, className: clsx$1.clsx(toolbar_styles.ToolbarClasses.group, className), ...props, children });
  }
);
ToolbarGroup.displayName = "Toolbar.Group";
const Toolbar = Object.assign(ToolbarRoot, {
  Leading: ToolbarLeading,
  Trailing: ToolbarTrailing,
  Item: ToolbarItem,
  Divider: ToolbarDivider,
  ToggleGroup: ToolbarToggleGroup,
  ToggleItem: ToolbarToggleItem,
  Group: ToolbarGroup
});

exports.default = Toolbar;
//# sourceMappingURL=toolbar.cjs.map
