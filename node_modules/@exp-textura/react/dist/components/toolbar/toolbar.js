"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef, useState, useCallback, Children, isValidElement, cloneElement } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import * as RadixToolbar from '@radix-ui/react-toolbar';
import Divider from '../divider/divider.js';
import ButtonGroup from '../button-group/button-group.js';
import { StyledToggleButton } from '../toggle-button/toggle-button.styles.js';
import { ToolbarClasses, StyledToolbar } from './toolbar.styles.js';

const ToolbarRoot = forwardRef(
  ({ className, variant = "default", asChild, children, ...props }, ref) => {
    const theme = useTexturaTheme(), rootClasses = clsx(ToolbarClasses.root, className);
    return /* @__PURE__ */ jsx(
      StyledToolbar,
      {
        ref,
        asChild,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children
      }
    );
  }
);
ToolbarRoot.displayName = "Toolbar";
const ToolbarLeading = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(ToolbarClasses.leading, className), ...props, children });
  }
);
ToolbarLeading.displayName = "Toolbar.Leading";
const ToolbarTrailing = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(ToolbarClasses.trailing, className), ...props, children });
  }
);
ToolbarTrailing.displayName = "Toolbar.Trailing";
const ToolbarItem = forwardRef(
  ({ className, asChild = true, children, ...props }, ref) => {
    const modifiedChildren = Children.map(children, (child) => {
      if (isValidElement(child)) {
        const displayName = child.type?.displayName;
        if (displayName === "Button") {
          return cloneElement(child, {
            variant: child.props.variant || "desaturated",
            buttonType: child.props.buttonType || "secondary"
          });
        } else if (displayName === "IconButton") {
          return cloneElement(child, {
            buttonType: child.props.buttonType || "secondary"
          });
        }
      }
      return child;
    });
    const childToRender = Array.isArray(modifiedChildren) && modifiedChildren.length === 1 ? modifiedChildren[0] : modifiedChildren;
    return /* @__PURE__ */ jsx(
      RadixToolbar.Button,
      {
        ref,
        asChild,
        className: clsx(ToolbarClasses.item, className),
        ...props,
        children: childToRender
      }
    );
  }
);
ToolbarItem.displayName = "Toolbar.Item";
const ToolbarDivider = forwardRef(
  ({ className, ...props }, ref) => {
    return /* @__PURE__ */ jsx(
      Divider,
      {
        ref,
        className: clsx(ToolbarClasses.divider, className),
        orientation: "vertical",
        ...props
      }
    );
  }
);
ToolbarDivider.displayName = "Toolbar.Divider";
const ToolbarToggleGroup = forwardRef(
  ({ className, children, value: controlledValue, onValueChange, type, defaultValue, ...props }, ref) => {
    const theme = useTexturaTheme();
    const [uncontrolledValue, setUncontrolledValue] = useState(defaultValue);
    const isControlled = controlledValue !== void 0;
    const value = isControlled ? controlledValue : uncontrolledValue;
    const handleValueChange = useCallback(
      (newValue) => {
        if (type === "single" && newValue === "") {
          return;
        }
        if (!isControlled) {
          setUncontrolledValue(newValue);
        }
        onValueChange?.(newValue);
      },
      [isControlled, onValueChange, type]
    );
    return /* @__PURE__ */ jsx(
      RadixToolbar.ToggleGroup,
      {
        ref,
        asChild: true,
        className: clsx(ToolbarClasses.toggleGroup, className),
        type,
        value,
        onValueChange: handleValueChange,
        ...props,
        children: /* @__PURE__ */ jsx(StyledToggleButton, { currentTheme: theme, children })
      }
    );
  }
);
ToolbarToggleGroup.displayName = "Toolbar.ToggleGroup";
const ToolbarToggleItem = forwardRef(
  ({ className, asChild = true, ...props }, ref) => {
    return /* @__PURE__ */ jsx(
      RadixToolbar.ToggleItem,
      {
        ref,
        asChild,
        className: clsx(ToolbarClasses.toggleItem, className),
        ...props
      }
    );
  }
);
ToolbarToggleItem.displayName = "Toolbar.ToggleItem";
const ToolbarGroup = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx(ButtonGroup, { ref, className: clsx(ToolbarClasses.group, className), ...props, children });
  }
);
ToolbarGroup.displayName = "Toolbar.Group";
const Toolbar = Object.assign(ToolbarRoot, {
  Leading: ToolbarLeading,
  Trailing: ToolbarTrailing,
  Item: ToolbarItem,
  Divider: ToolbarDivider,
  ToggleGroup: ToolbarToggleGroup,
  ToggleItem: ToolbarToggleItem,
  Group: ToolbarGroup
});

export { Toolbar as default };
//# sourceMappingURL=toolbar.js.map
