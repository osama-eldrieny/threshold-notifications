"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const styled = require('@emotion/styled');
const react = require('@emotion/react');
const componentUtils = require('../../system/utils/component-utils.cjs');
const RadixDialog = require('@radix-ui/react-dialog');
const React = require('react');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixDialog__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixDialog);

const componentName = "Overlay";
const OverlayClasses = {
  root: "exp-textura_overlay",
  content: "exp-textura_overlay_content"
};
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = React.useMemo(
    () => componentUtils.getStyleSlots(
      componentName,
      theme,
      variant,
      styleSlotsProp
      // getVariantConfig(theme),
    ),
    [theme, variant, styleSlotsProp]
  );
  const fadeIn = react.keyframes`from { opacity: 0; } to { opacity: 1; }`;
  const fadeOut = react.keyframes`from { opacity: 1; } to { opacity: 0; }`;
  return {
    [`&.${OverlayClasses.root}`]: {
      position: "fixed",
      inset: 0,
      animationTimingFunction: "ease",
      animationFillMode: "forwards",
      backgroundColor: theme.overlay.background,
      zIndex: theme.zIndex.dynamic,
      animationDuration: "150ms",
      '&[data-state="open"]': {
        animationName: fadeIn
      },
      '&[data-state="closed"]': {
        animationName: fadeOut
      },
      '&[data-textura-scoped="true"]': {
        position: "absolute"
      },
      ...styleSlots?.root
    },
    [`&.${OverlayClasses.content}`]: {
      position: "fixed",
      top: "50%",
      left: "50%",
      transform: "translate(-50%, -50%)",
      opacity: 1,
      animationTimingFunction: "ease",
      animationFillMode: "forwards",
      animationDuration: "100ms",
      overflowY: "auto",
      zIndex: theme.zIndex.overlay,
      '&[data-state="open"]': {
        animationName: fadeIn,
        opacity: 1
      },
      [`&[data-state="closed"]`]: {
        animationName: fadeOut
      },
      "&:focus-visible": {
        outline: "none",
        boxShadow: "none"
      },
      '&[data-textura-scoped="true"]': {
        position: "absolute"
      },
      ...styleSlots?.content
    }
  };
};
const excludedDOMProps = componentUtils.getExcludedDOMProps();
const StyledOverlay = styled("div", {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));
const StyledContent = styled(RadixDialog__namespace.Content, {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));

exports.OverlayClasses = OverlayClasses;
exports.StyledContent = StyledContent;
exports.StyledOverlay = StyledOverlay;
//# sourceMappingURL=overlay.styles.cjs.map
