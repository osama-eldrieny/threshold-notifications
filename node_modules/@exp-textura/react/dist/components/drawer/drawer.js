"use client";
import { jsx } from 'react/jsx-runtime';
import React, { forwardRef, useState, useRef, useCallback } from 'react';
import { clsx } from 'clsx';
import * as RadixDialog from '@radix-ui/react-dialog';
import { Slot } from '@radix-ui/react-slot';
import { Close as Close$1 } from '@exp-textura/icons';
import IconButton from '../icon-button/icon-button.js';
import { DrawerClasses } from './drawer.styles.js';
import { DrawerContext, useDrawerContext } from './hooks/use-drawer-context.js';
import { DrawerOverlay } from './drawer.overlay.js';
import { DrawerPush } from './drawer.push.js';

const DrawerRoot = forwardRef(
  ({
    children,
    onOpenChange,
    dismissible = true,
    open: openProp,
    defaultOpen = false,
    layoutMode = "overlay",
    modal = false,
    ...props
  }, ref) => {
    const [internalOpen, setInternalOpen] = useState(defaultOpen);
    const triggerRef = useRef(null);
    const isControlled = openProp !== void 0;
    const open = isControlled ? openProp : internalOpen;
    const handleOpenChange = useCallback(
      (nextOpen) => {
        if (!isControlled) {
          setInternalOpen(nextOpen);
        }
        onOpenChange?.(nextOpen);
      },
      [isControlled, onOpenChange]
    );
    const { trigger, content, closeIconButton } = getValidDrawerChildren(children);
    const contextValue = React.useMemo(
      () => ({
        open,
        onOpenChange: handleOpenChange,
        layoutMode,
        modal: layoutMode === "push" ? false : modal,
        dismissible,
        triggerRef
      }),
      [open, handleOpenChange, layoutMode, modal, dismissible]
    );
    const Implementation = layoutMode === "push" ? DrawerPush : DrawerOverlay;
    return /* @__PURE__ */ jsx(DrawerContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsx(
      Implementation,
      {
        ref,
        drawerClose: closeIconButton,
        drawerContent: content,
        drawerTrigger: trigger,
        ...props
      }
    ) });
  }
);
DrawerRoot.displayName = "Drawer";
const Title = ({
  children,
  asChild,
  ...props
}) => {
  const { layoutMode } = useDrawerContext();
  if (layoutMode === "overlay") {
    return /* @__PURE__ */ jsx(
      RadixDialog.Title,
      {
        asChild,
        className: DrawerClasses.title,
        "data-drawer-type": "title",
        ...props,
        children
      }
    );
  }
  const Comp = asChild ? Slot : "h2";
  return /* @__PURE__ */ jsx(Comp, { className: DrawerClasses.title, "data-drawer-type": "title", ...props, children });
};
Title.displayName = "Drawer.Title";
const Trigger = ({
  children,
  ...props
}) => {
  const { layoutMode, onOpenChange, triggerRef } = useDrawerContext();
  if (layoutMode === "overlay") {
    return /* @__PURE__ */ jsx(RadixDialog.Trigger, { "data-drawer-type": "trigger", ...props, asChild: true, children });
  }
  return /* @__PURE__ */ jsx(
    Slot,
    {
      ref: triggerRef,
      "data-drawer-type": "trigger",
      onClick: () => onOpenChange?.(true),
      ...props,
      children
    }
  );
};
Trigger.displayName = "Drawer.Trigger";
const Close = ({
  children,
  ...props
}) => {
  const { layoutMode, onOpenChange } = useDrawerContext();
  if (layoutMode === "overlay") {
    return /* @__PURE__ */ jsx(RadixDialog.Close, { "data-drawer-type": "close", ...props, asChild: true, children });
  }
  return /* @__PURE__ */ jsx(Slot, { "data-drawer-type": "close", onClick: () => onOpenChange?.(false), ...props, children });
};
Close.displayName = "Drawer.Close";
const CloseIconButton = ({
  children,
  className,
  "aria-label": customAriaLabel,
  ...props
}) => {
  const { layoutMode, onOpenChange } = useDrawerContext();
  const buttonClasses = clsx(DrawerClasses.closeIconButton, className);
  const commonProps = {
    "aria-label": customAriaLabel || "Close drawer"
  };
  if (layoutMode === "overlay") {
    return /* @__PURE__ */ jsx(RadixDialog.Close, { asChild: true, "data-drawer-type": "close-button", children: /* @__PURE__ */ jsx(IconButton, { buttonType: "secondary", className: buttonClasses, ...commonProps, ...props, children: children ? children : /* @__PURE__ */ jsx(Close$1, {}) }) });
  }
  return /* @__PURE__ */ jsx(
    IconButton,
    {
      buttonType: "secondary",
      className: buttonClasses,
      "data-drawer-type": "close-button",
      onClick: () => onOpenChange?.(false),
      ...commonProps,
      ...props,
      children: children ? children : /* @__PURE__ */ jsx(Close$1, {})
    }
  );
};
CloseIconButton.displayName = "Drawer.CloseIconButton";
const Content = ({ children, tabIndex = 0, ...props }) => /* @__PURE__ */ jsx("div", { className: DrawerClasses.content, "data-drawer-type": "content", tabIndex, ...props, children });
Content.displayName = "Drawer.Content";
const Footer = ({ children, ...props }) => /* @__PURE__ */ jsx("footer", { className: DrawerClasses.footer, "data-drawer-type": "footer", ...props, children });
Footer.displayName = "Drawer.Footer";
const Drawer = DrawerRoot;
Drawer.Trigger = Trigger;
Drawer.Close = Close;
Drawer.CloseIconButton = CloseIconButton;
Drawer.Title = Title;
Drawer.Content = Content;
Drawer.Footer = Footer;
const getValidDrawerChildren = (children) => {
  let titleCount = 0;
  let closeIconButtonCount = 0;
  let triggerCount = 0;
  let contentCount = 0;
  let footerCount = 0;
  const trigger = [];
  const content = [];
  const closeIconButton = [];
  React.Children.forEach(children, (child) => {
    if (React.isValidElement(child)) {
      const displayName = child.type.displayName;
      if (displayName !== "Drawer.Trigger" && displayName !== "Drawer.CloseIconButton") {
        content.push(child);
      }
      if (displayName === "Drawer.Content") {
        contentCount++;
      } else if (displayName === "Drawer.Footer") {
        footerCount++;
      } else if (displayName === "Drawer.Title") {
        titleCount++;
      } else if (displayName === "Drawer.Trigger") {
        trigger.push(child);
        triggerCount++;
      } else if (displayName === "Drawer.CloseIconButton") {
        closeIconButton.push(child);
        closeIconButtonCount++;
      } else {
        throw new Error(
          "<Drawer> only accepts <Drawer.Title>, <Drawer.CloseIconButton>, <Drawer.Trigger>, <Drawer.Content>, and <Drawer.Footer> as children."
        );
      }
    }
  });
  if (contentCount > 1)
    throw new Error(`<Drawer> can only have one <Drawer.Content>. Found ${contentCount}.`);
  if (closeIconButtonCount > 1)
    throw new Error(
      `<Drawer> can only have one <Drawer.CloseIconButton>. Found ${closeIconButtonCount}.`
    );
  if (footerCount > 1)
    throw new Error(`<Drawer> can only have one <Drawer.Footer>. Found ${footerCount}.`);
  if (titleCount > 1)
    throw new Error(`<Drawer> can only have one <Drawer.Title>. Found ${titleCount}.`);
  if (triggerCount > 1)
    throw new Error(`<Drawer> can only have one <Drawer.Trigger>. Found ${triggerCount}.`);
  return { trigger, content, closeIconButton };
};

export { Drawer as default };
//# sourceMappingURL=drawer.js.map
