"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
const RadixDialog = require('@radix-ui/react-dialog');
const reactSlot = require('@radix-ui/react-slot');
const icons = require('@exp-textura/icons');
const iconButton = require('../icon-button/icon-button.cjs');
const drawer_styles = require('./drawer.styles.cjs');
const useDrawerContext = require('./hooks/use-drawer-context.cjs');
const drawer_overlay = require('./drawer.overlay.cjs');
const drawer_push = require('./drawer.push.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixDialog__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixDialog);

const DrawerRoot = React.forwardRef(
  ({
    children,
    onOpenChange,
    dismissible = true,
    open: openProp,
    defaultOpen = false,
    layoutMode = "overlay",
    modal = false,
    ...props
  }, ref) => {
    const [internalOpen, setInternalOpen] = React.useState(defaultOpen);
    const triggerRef = React.useRef(null);
    const isControlled = openProp !== void 0;
    const open = isControlled ? openProp : internalOpen;
    const handleOpenChange = React.useCallback(
      (nextOpen) => {
        if (!isControlled) {
          setInternalOpen(nextOpen);
        }
        onOpenChange?.(nextOpen);
      },
      [isControlled, onOpenChange]
    );
    const { trigger, content, closeIconButton } = getValidDrawerChildren(children);
    const contextValue = React.useMemo(
      () => ({
        open,
        onOpenChange: handleOpenChange,
        layoutMode,
        modal: layoutMode === "push" ? false : modal,
        dismissible,
        triggerRef
      }),
      [open, handleOpenChange, layoutMode, modal, dismissible]
    );
    const Implementation = layoutMode === "push" ? drawer_push.DrawerPush : drawer_overlay.DrawerOverlay;
    return /* @__PURE__ */ jsxRuntime.jsx(useDrawerContext.DrawerContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsxRuntime.jsx(
      Implementation,
      {
        ref,
        drawerClose: closeIconButton,
        drawerContent: content,
        drawerTrigger: trigger,
        ...props
      }
    ) });
  }
);
DrawerRoot.displayName = "Drawer";
const Title = ({
  children,
  asChild,
  ...props
}) => {
  const { layoutMode } = useDrawerContext.useDrawerContext();
  if (layoutMode === "overlay") {
    return /* @__PURE__ */ jsxRuntime.jsx(
      RadixDialog__namespace.Title,
      {
        asChild,
        className: drawer_styles.DrawerClasses.title,
        "data-drawer-type": "title",
        ...props,
        children
      }
    );
  }
  const Comp = asChild ? reactSlot.Slot : "h2";
  return /* @__PURE__ */ jsxRuntime.jsx(Comp, { className: drawer_styles.DrawerClasses.title, "data-drawer-type": "title", ...props, children });
};
Title.displayName = "Drawer.Title";
const Trigger = ({
  children,
  ...props
}) => {
  const { layoutMode, onOpenChange, triggerRef } = useDrawerContext.useDrawerContext();
  if (layoutMode === "overlay") {
    return /* @__PURE__ */ jsxRuntime.jsx(RadixDialog__namespace.Trigger, { "data-drawer-type": "trigger", ...props, asChild: true, children });
  }
  return /* @__PURE__ */ jsxRuntime.jsx(
    reactSlot.Slot,
    {
      ref: triggerRef,
      "data-drawer-type": "trigger",
      onClick: () => onOpenChange?.(true),
      ...props,
      children
    }
  );
};
Trigger.displayName = "Drawer.Trigger";
const Close = ({
  children,
  ...props
}) => {
  const { layoutMode, onOpenChange } = useDrawerContext.useDrawerContext();
  if (layoutMode === "overlay") {
    return /* @__PURE__ */ jsxRuntime.jsx(RadixDialog__namespace.Close, { "data-drawer-type": "close", ...props, asChild: true, children });
  }
  return /* @__PURE__ */ jsxRuntime.jsx(reactSlot.Slot, { "data-drawer-type": "close", onClick: () => onOpenChange?.(false), ...props, children });
};
Close.displayName = "Drawer.Close";
const CloseIconButton = ({
  children,
  className,
  "aria-label": customAriaLabel,
  ...props
}) => {
  const { layoutMode, onOpenChange } = useDrawerContext.useDrawerContext();
  const buttonClasses = clsx$1.clsx(drawer_styles.DrawerClasses.closeIconButton, className);
  const commonProps = {
    "aria-label": customAriaLabel || "Close drawer"
  };
  if (layoutMode === "overlay") {
    return /* @__PURE__ */ jsxRuntime.jsx(RadixDialog__namespace.Close, { asChild: true, "data-drawer-type": "close-button", children: /* @__PURE__ */ jsxRuntime.jsx(iconButton.default, { buttonType: "secondary", className: buttonClasses, ...commonProps, ...props, children: children ? children : /* @__PURE__ */ jsxRuntime.jsx(icons.Close, {}) }) });
  }
  return /* @__PURE__ */ jsxRuntime.jsx(
    iconButton.default,
    {
      buttonType: "secondary",
      className: buttonClasses,
      "data-drawer-type": "close-button",
      onClick: () => onOpenChange?.(false),
      ...commonProps,
      ...props,
      children: children ? children : /* @__PURE__ */ jsxRuntime.jsx(icons.Close, {})
    }
  );
};
CloseIconButton.displayName = "Drawer.CloseIconButton";
const Content = ({ children, tabIndex = 0, ...props }) => /* @__PURE__ */ jsxRuntime.jsx("div", { className: drawer_styles.DrawerClasses.content, "data-drawer-type": "content", tabIndex, ...props, children });
Content.displayName = "Drawer.Content";
const Footer = ({ children, ...props }) => /* @__PURE__ */ jsxRuntime.jsx("footer", { className: drawer_styles.DrawerClasses.footer, "data-drawer-type": "footer", ...props, children });
Footer.displayName = "Drawer.Footer";
const Drawer = DrawerRoot;
Drawer.Trigger = Trigger;
Drawer.Close = Close;
Drawer.CloseIconButton = CloseIconButton;
Drawer.Title = Title;
Drawer.Content = Content;
Drawer.Footer = Footer;
const getValidDrawerChildren = (children) => {
  let titleCount = 0;
  let closeIconButtonCount = 0;
  let triggerCount = 0;
  let contentCount = 0;
  let footerCount = 0;
  const trigger = [];
  const content = [];
  const closeIconButton = [];
  React.Children.forEach(children, (child) => {
    if (React.isValidElement(child)) {
      const displayName = child.type.displayName;
      if (displayName !== "Drawer.Trigger" && displayName !== "Drawer.CloseIconButton") {
        content.push(child);
      }
      if (displayName === "Drawer.Content") {
        contentCount++;
      } else if (displayName === "Drawer.Footer") {
        footerCount++;
      } else if (displayName === "Drawer.Title") {
        titleCount++;
      } else if (displayName === "Drawer.Trigger") {
        trigger.push(child);
        triggerCount++;
      } else if (displayName === "Drawer.CloseIconButton") {
        closeIconButton.push(child);
        closeIconButtonCount++;
      } else {
        throw new Error(
          "<Drawer> only accepts <Drawer.Title>, <Drawer.CloseIconButton>, <Drawer.Trigger>, <Drawer.Content>, and <Drawer.Footer> as children."
        );
      }
    }
  });
  if (contentCount > 1)
    throw new Error(`<Drawer> can only have one <Drawer.Content>. Found ${contentCount}.`);
  if (closeIconButtonCount > 1)
    throw new Error(
      `<Drawer> can only have one <Drawer.CloseIconButton>. Found ${closeIconButtonCount}.`
    );
  if (footerCount > 1)
    throw new Error(`<Drawer> can only have one <Drawer.Footer>. Found ${footerCount}.`);
  if (titleCount > 1)
    throw new Error(`<Drawer> can only have one <Drawer.Title>. Found ${titleCount}.`);
  if (triggerCount > 1)
    throw new Error(`<Drawer> can only have one <Drawer.Trigger>. Found ${triggerCount}.`);
  return { trigger, content, closeIconButton };
};

exports.default = Drawer;
//# sourceMappingURL=drawer.cjs.map
