"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const RadixDialog = require('@radix-ui/react-dialog');
const usePortalContainer = require('../../hooks/use-portal-container.cjs');
const drawer_styles = require('./drawer.styles.cjs');
const useDrawerContext = require('./hooks/use-drawer-context.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixDialog__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixDialog);

const DrawerOverlay = React.forwardRef(
  ({
    className,
    containerElement,
    position = "start",
    variant = "default",
    padding = true,
    styleSlots,
    drawerTrigger,
    drawerContent,
    drawerClose,
    onEscapeKeyDown,
    onInteractOutside,
    onPointerDownOutside,
    onOpenAutoFocus,
    onCloseAutoFocus,
    ...props
  }, ref) => {
    const { open, onOpenChange, modal, dismissible } = useDrawerContext.useDrawerContext();
    const theme = useTheme.default();
    const { resolvedContainer, isScoped } = usePortalContainer.usePortalContainer(containerElement);
    return /* @__PURE__ */ jsxRuntime.jsxs(RadixDialog__namespace.Root, { modal, open, onOpenChange, children: [
      drawerTrigger,
      /* @__PURE__ */ jsxRuntime.jsxs(RadixDialog__namespace.Portal, { container: resolvedContainer, children: [
        /* @__PURE__ */ jsxRuntime.jsx(
          drawer_styles.StyledDrawerOverlay,
          {
            className: drawer_styles.DrawerClasses.overlay,
            currentTheme: theme,
            "data-textura-scoped": isScoped,
            styleSlots,
            variant
          }
        ),
        /* @__PURE__ */ jsxRuntime.jsxs(
          drawer_styles.StyledDrawer,
          {
            ref,
            "aria-describedby": void 0,
            className: clsx$1.clsx(drawer_styles.DrawerClasses.root, drawer_styles.DrawerClasses.body, className, {
              [drawer_styles.DrawerClasses.start]: position === "start",
              [drawer_styles.DrawerClasses.end]: position === "end",
              [drawer_styles.DrawerClasses.top]: position === "top",
              [drawer_styles.DrawerClasses.bottom]: position === "bottom",
              [drawer_styles.DrawerClasses.padding]: padding
            }),
            currentTheme: theme,
            "data-textura-scoped": isScoped,
            styleSlots,
            variant,
            onCloseAutoFocus,
            onEscapeKeyDown: (e) => {
              if (!dismissible) {
                e.preventDefault();
              }
              onEscapeKeyDown?.(e);
            },
            onInteractOutside: (e) => {
              if (!dismissible) {
                e.preventDefault();
              }
              onInteractOutside?.(e);
            },
            onOpenAutoFocus,
            onPointerDownOutside: (e) => {
              if (!dismissible) {
                e.preventDefault();
              }
              onPointerDownOutside?.(e);
            },
            ...props,
            children: [
              drawerContent,
              drawerClose
            ]
          }
        )
      ] })
    ] });
  }
);
DrawerOverlay.displayName = "DrawerOverlay";

exports.DrawerOverlay = DrawerOverlay;
//# sourceMappingURL=drawer.overlay.cjs.map
