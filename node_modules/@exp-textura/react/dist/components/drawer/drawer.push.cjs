"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const useMergeRefs = require('../../hooks/use-merge-refs.cjs');
const drawer_styles = require('./drawer.styles.cjs');
const useDrawerContext = require('./hooks/use-drawer-context.cjs');

const DrawerPush = React.forwardRef(
  ({
    className,
    position = "start",
    variant = "default",
    padding = true,
    drawerTrigger,
    drawerContent,
    drawerClose,
    // These Radix Dialog.Content event handlers are intentionally destructured
    // and not forwarded. Push mode does not use Radix Portal/Dialog.Content,
    // so these props are no-ops. They are destructured here to prevent unknown
    // prop warnings from being forwarded to the underlying DOM element via {...props}.
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    onEscapeKeyDown,
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    onInteractOutside,
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    onPointerDownOutside,
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    onOpenAutoFocus,
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    onCloseAutoFocus,
    ...props
  }, ref) => {
    const { open, triggerRef } = useDrawerContext.useDrawerContext();
    const theme = useTheme.default();
    const internalRef = React.useRef(null);
    const contentRef = React.useRef(null);
    const handleRef = useMergeRefs.useMergeRefs(ref, internalRef);
    const isMounted = React.useRef(false);
    React.useEffect(() => {
      if (internalRef.current) {
        if (!open) {
          if (internalRef.current.contains(document.activeElement)) {
            triggerRef?.current?.focus();
          }
          internalRef.current.setAttribute("inert", "true");
        } else {
          internalRef.current.removeAttribute("inert");
          if (isMounted.current) {
            const contentElement = internalRef.current.querySelector(
              `.${drawer_styles.DrawerClasses.content}`
            );
            if (contentElement) {
              contentElement.focus({ preventScroll: true });
            } else {
              contentRef.current?.focus({ preventScroll: true });
            }
          }
        }
      }
      isMounted.current = true;
    }, [open, triggerRef]);
    const isVertical = position === "top" || position === "bottom";
    return /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
      drawerTrigger,
      /* @__PURE__ */ jsxRuntime.jsx(
        drawer_styles.StyledDrawerBody,
        {
          ref: handleRef,
          "aria-hidden": !open,
          className: clsx$1.clsx(drawer_styles.DrawerClasses.root, drawer_styles.DrawerClasses.body, drawer_styles.DrawerClasses.push, className, {
            [drawer_styles.DrawerClasses.start]: position === "start",
            [drawer_styles.DrawerClasses.end]: position === "end",
            [drawer_styles.DrawerClasses.top]: position === "top",
            [drawer_styles.DrawerClasses.bottom]: position === "bottom"
          }),
          currentTheme: theme,
          "data-state": open ? "open" : "closed",
          variant,
          ...props,
          children: /* @__PURE__ */ jsxRuntime.jsxs(
            "div",
            {
              ref: contentRef,
              className: clsx$1.clsx(drawer_styles.DrawerClasses.pushContent, {
                [drawer_styles.DrawerClasses.padding]: padding
              }),
              style: {
                minWidth: isVertical ? "100%" : "var(--drawer-width)",
                minHeight: isVertical ? "var(--drawer-height)" : "100%"
              },
              tabIndex: -1,
              children: [
                drawerClose,
                drawerContent
              ]
            }
          )
        }
      )
    ] });
  }
);
DrawerPush.displayName = "DrawerPush";

exports.DrawerPush = DrawerPush;
//# sourceMappingURL=drawer.push.cjs.map
