"use client";
import { jsx } from 'react/jsx-runtime';
import React, { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import Avatar from '../avatar/avatar.js';
import { AvatarStackClasses, StyledAvatarStack } from './avatar-stack.styles.js';

const AvatarStack = forwardRef(
  ({ className, variant = "default", children, ring, size, ...props }, ref) => {
    const appliedSize = size ?? "lg";
    const theme = useTexturaTheme(), rootClasses = clsx(
      AvatarStackClasses.root,
      {
        [AvatarStackClasses[appliedSize]]: appliedSize
      },
      className
    );
    return /* @__PURE__ */ jsx(
      StyledAvatarStack,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        role: "group",
        variant,
        ...props,
        children: React.Children.map(children, (child) => {
          if (!React.isValidElement(child)) return child;
          if (child.type === Avatar) {
            return React.cloneElement(child, {
              ring,
              size,
              variant,
              ...child.props
            });
          }
          return child;
        })
      }
    );
  }
);
AvatarStack.displayName = "AvatarStack";

export { AvatarStack as default };
//# sourceMappingURL=avatar-stack.js.map
