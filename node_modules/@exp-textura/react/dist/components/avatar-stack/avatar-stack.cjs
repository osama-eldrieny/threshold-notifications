"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const avatar = require('../avatar/avatar.cjs');
const avatarStack_styles = require('./avatar-stack.styles.cjs');

const AvatarStack = React.forwardRef(
  ({ className, variant = "default", children, ring, size, ...props }, ref) => {
    const appliedSize = size ?? "lg";
    const theme = useTheme.default(), rootClasses = clsx$1.clsx(
      avatarStack_styles.AvatarStackClasses.root,
      {
        [avatarStack_styles.AvatarStackClasses[appliedSize]]: appliedSize
      },
      className
    );
    return /* @__PURE__ */ jsxRuntime.jsx(
      avatarStack_styles.StyledAvatarStack,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        role: "group",
        variant,
        ...props,
        children: React.Children.map(children, (child) => {
          if (!React.isValidElement(child)) return child;
          if (child.type === avatar.default) {
            return React.cloneElement(child, {
              ring,
              size,
              variant,
              ...child.props
            });
          }
          return child;
        })
      }
    );
  }
);
AvatarStack.displayName = "AvatarStack";

exports.default = AvatarStack;
//# sourceMappingURL=avatar-stack.cjs.map
