"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const icons = require('@exp-textura/icons');
const assetIcon_styles = require('./asset-icon.styles.cjs');

const ICON_MAP = {
  default: { solid: icons.AppsRemix, line: icons.Apps },
  app: { solid: icons.AppsRemix, line: icons.Apps },
  page: { solid: icons.PageRemix, line: icons.Page },
  form: { solid: icons.FormRemix, line: icons.Forms },
  theme: { solid: icons.ThemeRemix, line: icons.Theme },
  workflow: { solid: icons.WorkflowRemix, line: icons.Workflow },
  orchestration: { solid: icons.OrchestrationRemix, line: icons.Orchestration },
  agent: { solid: icons.AgentRemix, line: icons.Agent },
  document: { solid: icons.DocumentsRemix, line: icons.Documents },
  file: { solid: icons.FileRemix, line: icons.File },
  connection: { solid: icons.ConnectionRemix, line: icons.ConnectionRemix },
  table: { solid: icons.DataRemix, line: icons.Data },
  persona: { solid: icons.PersonaRemix, line: icons.Persona },
  "user-story": { solid: icons.UserStoryRemix, line: icons.UserStory },
  process: { solid: icons.ProcessRemix, line: icons.Process }
};
const AssetIcon = React.forwardRef(
  ({
    className,
    variant = "app",
    size = "lg",
    utility = "default",
    children,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const isOutline = utility === "outline";
    const useLineIcon = !isOutline && (size === "sm" || size === "md");
    const Icon = useLineIcon ? ICON_MAP[variant].line : ICON_MAP[variant].solid;
    const rootClasses = clsx$1.clsx(assetIcon_styles.AssetIconClasses.root, className, {
      [assetIcon_styles.AssetIconClasses[size]]: size,
      [assetIcon_styles.AssetIconClasses.outline]: isOutline
    });
    return /* @__PURE__ */ jsxRuntime.jsx(
      assetIcon_styles.StyledAssetIcon,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children: children ? React.cloneElement(children, {
          className: assetIcon_styles.AssetIconClasses.icon
        }) : /* @__PURE__ */ jsxRuntime.jsx(Icon, { className: assetIcon_styles.AssetIconClasses.icon })
      }
    );
  }
);
AssetIcon.displayName = "AssetIcon";

exports.default = AssetIcon;
//# sourceMappingURL=asset-icon.cjs.map
