"use client";
import { createContext, useContext } from 'react';

const FloatContext = createContext(void 0);
const useFloat = () => {
  const context = useContext(FloatContext);
  if (context === void 0) {
    throw new Error("useFloat must be used within a Float");
  }
  return context;
};

export { FloatContext, useFloat };
//# sourceMappingURL=use-float.js.map
