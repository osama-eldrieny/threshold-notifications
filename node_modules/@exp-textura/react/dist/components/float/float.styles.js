"use client";
import styled from '@emotion/styled';
import { getExcludedDOMProps, getStyleSlots } from '../../system/utils/component-utils.js';
import { useMemo } from 'react';

const componentName = "Float";
const FloatClasses = {
  root: "exp-textura_float",
  content: "exp-textura_float_content",
  anchor: "exp-textura_float_anchor"
};
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = useMemo(
    () => getStyleSlots(
      componentName,
      theme,
      variant,
      styleSlotsProp
      // getVariantConfig(),
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    position: "relative",
    display: "flex",
    alignItems: "center",
    width: "fit-content",
    [`.${FloatClasses.content}`]: {
      position: "absolute",
      zIndex: 1,
      "&[data-placement=top-start]": {
        top: props.offSetY,
        insetInlineStart: props.offSetX,
        transform: "translate(-50%, -50%)",
        ":dir(rtl)": {
          transform: "translate(50%, -50%)"
        }
      },
      "&[data-placement=top-center]": {
        top: props.offSetY,
        left: "50%",
        transform: "translate(-50%, -50%)"
      },
      "&[data-placement=top-end]": {
        top: props.offSetY,
        insetInlineEnd: props.offSetX,
        transform: "translate(50%, -50%)",
        ":dir(rtl)": {
          transform: "translate(-50%, -50%)"
        }
      },
      "&[data-placement=bottom-start]": {
        bottom: props.offSetY,
        insetInlineStart: props.offSetX,
        transform: "translate(-50%, 50%)",
        ":dir(rtl)": {
          transform: "translate(50%, 50%)"
        }
      },
      "&[data-placement=bottom-center]": {
        bottom: props.offSetY,
        left: "50%",
        transform: "translate(-50%, 50%)"
      },
      "&[data-placement=bottom-end]": {
        bottom: props.offSetY,
        insetInlineEnd: props.offSetX,
        transform: "translate(50%, 50%)",
        ":dir(rtl)": {
          transform: "translate(-50%, 50%)"
        }
      },
      "&[data-placement=middle-start]": {
        top: "50%",
        insetInlineStart: props.offSetX,
        transform: "translate(-50%, -50%)",
        ":dir(rtl)": {
          transform: "translate(50%, -50%)"
        }
      },
      "&[data-placement=middle-center]": {
        top: "50%",
        left: "50%",
        transform: "translate(-50%, -50%)"
      },
      "&[data-placement=middle-end]": {
        top: "50%",
        insetInlineEnd: props.offSetX,
        transform: "translate(50%, -50%)",
        ":dir(rtl)": {
          transform: "translate(-50%, -50%)"
        }
      },
      ...styleSlots?.content
    },
    ...styleSlots?.root
  };
};
const excludedDOMProps = getExcludedDOMProps("offSetX", "offSetY");
const StyledFloat = styled("div", {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));

export { FloatClasses, StyledFloat };
//# sourceMappingURL=float.styles.js.map
