"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const float_styles = require('./float.styles.cjs');
const useFloat = require('./hooks/use-float.cjs');

const FloatRoot = React.forwardRef(
  ({
    className,
    variant = "default",
    placement = "top-end",
    offSetY = 0,
    offSetX = 0,
    children,
    ...props
  }, ref) => {
    const theme = useTheme.default(), rootClasses = clsx$1.clsx(float_styles.FloatClasses.root, className);
    return /* @__PURE__ */ jsxRuntime.jsx(useFloat.FloatContext.Provider, { value: { placement }, children: /* @__PURE__ */ jsxRuntime.jsx(
      float_styles.StyledFloat,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        offSetX,
        offSetY,
        variant,
        ...props,
        children
      }
    ) });
  }
);
FloatRoot.displayName = "Float";
const FloatContent = ({
  className,
  children,
  ...props
}) => {
  const { placement } = useFloat.useFloat();
  const contentProps = {
    className: clsx$1.clsx(float_styles.FloatClasses.content, className),
    "data-placement": placement,
    ...props
  };
  if (React.isValidElement(children)) {
    return React.cloneElement(children, contentProps);
  } else {
    throw new Error(
      "Float.Content must be used as a child of Float and must be a valid React element."
    );
  }
};
FloatContent.displayName = "Float.Content";
const FloatAnchor = ({
  className,
  children,
  ...props
}) => {
  const contentProps = {
    className: clsx$1.clsx(float_styles.FloatClasses.anchor, className),
    ...props
  };
  if (React.isValidElement(children)) {
    return React.cloneElement(children, contentProps);
  } else {
    throw new Error(
      "Float.Anchor must be used as a child of Float and must be a valid React element."
    );
  }
};
FloatAnchor.displayName = "Float.Anchor";
const Float = Object.assign(FloatRoot, {
  Content: FloatContent,
  Anchor: FloatAnchor
});

exports.default = Float;
//# sourceMappingURL=float.cjs.map
