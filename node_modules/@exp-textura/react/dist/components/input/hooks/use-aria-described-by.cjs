"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const React = require('react');
const useInput = require('./use-input.cjs');

const useAriaDescribedBy = ({
  id,
  "aria-hidden": ariaHidden
}) => {
  const { addAriaDescribedBy, removeAriaDescribedBy } = useInput.useInput();
  React.useEffect(() => {
    if (!ariaHidden) {
      addAriaDescribedBy(id);
    }
    return () => {
      removeAriaDescribedBy(id);
    };
  }, [addAriaDescribedBy, removeAriaDescribedBy, id, ariaHidden]);
};

exports.useAriaDescribedBy = useAriaDescribedBy;
//# sourceMappingURL=use-aria-described-by.cjs.map
