"use client";
import { useEffect } from 'react';
import { useInput } from './use-input.js';

const useAriaDescribedBy = ({
  id,
  "aria-hidden": ariaHidden
}) => {
  const { addAriaDescribedBy, removeAriaDescribedBy } = useInput();
  useEffect(() => {
    if (!ariaHidden) {
      addAriaDescribedBy(id);
    }
    return () => {
      removeAriaDescribedBy(id);
    };
  }, [addAriaDescribedBy, removeAriaDescribedBy, id, ariaHidden]);
};

export { useAriaDescribedBy };
//# sourceMappingURL=use-aria-described-by.js.map
