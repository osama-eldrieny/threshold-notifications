"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const useFormSize = require('../form/hooks/use-form-size.cjs');
const circleProgress = require('../circle-progress/circle-progress.cjs');
const button_styles = require('./button.styles.cjs');

const Button = React.forwardRef(
  ({
    className,
    variant = "default",
    buttonType = "primary",
    size: sizeProp,
    children,
    disabled = false,
    selected: controlledSelected,
    toggle = false,
    onValueChange,
    onClick,
    value,
    isLoading = false,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const formContext = useFormSize.useFormSize();
    const size = sizeProp || formContext?.formSize;
    const [internalSelected, setInternalSelected] = React.useState(false);
    const isControlled = controlledSelected !== void 0;
    const isSelected = isControlled ? controlledSelected : internalSelected;
    const hasMounted = React.useRef(false);
    React.useEffect(() => {
      if (!hasMounted.current) {
        hasMounted.current = true;
        return;
      }
      onValueChange?.(Boolean(controlledSelected));
    }, [isControlled, controlledSelected, onValueChange]);
    const handleClick = React.useCallback(
      (e) => {
        if (isLoading || disabled) return;
        if (toggle) {
          if (!isControlled) {
            setInternalSelected(!internalSelected);
          }
          onValueChange?.(!isSelected);
        }
        onClick?.(e);
      },
      [
        toggle,
        isControlled,
        onValueChange,
        isSelected,
        onClick,
        internalSelected,
        isLoading,
        disabled
      ]
    );
    const rootClasses = clsx$1.clsx(button_styles.ButtonClasses.root, className, {
      [button_styles.ButtonClasses.secondary]: buttonType === "secondary",
      [button_styles.ButtonClasses.tertiary]: buttonType === "tertiary",
      [button_styles.ButtonClasses.sm]: size === "sm",
      [button_styles.ButtonClasses.loading]: isLoading
    });
    return /* @__PURE__ */ jsxRuntime.jsxs(
      button_styles.StyledButton,
      {
        ref,
        "aria-busy": isLoading,
        "aria-disabled": disabled || isLoading,
        "aria-pressed": toggle ? isSelected : void 0,
        className: rootClasses,
        currentTheme: theme,
        "data-selected": isSelected ? "true" : void 0,
        disabled,
        type: "button",
        value,
        variant,
        onClick: handleClick,
        ...props,
        children: [
          /* @__PURE__ */ jsxRuntime.jsx("span", { className: button_styles.ButtonClasses.content, children }),
          isLoading && /* @__PURE__ */ jsxRuntime.jsx("span", { className: button_styles.ButtonClasses.loadingWrapper, children: /* @__PURE__ */ jsxRuntime.jsx(circleProgress.default, { variant: "inherit" }) })
        ]
      }
    );
  }
);
Button.displayName = "Button";

exports.default = Button;
//# sourceMappingURL=button.cjs.map
