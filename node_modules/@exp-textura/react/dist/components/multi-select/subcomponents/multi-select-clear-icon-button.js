"use client";
import { jsx } from 'react/jsx-runtime';
import { clsx } from 'clsx';
import { Close } from '@exp-textura/icons';
import IconButton from '../../icon-button/icon-button.js';
import { MultiSelectClasses } from '../multi-select.styles.js';
import { useMultiSelect } from '../hooks/use-multi-select.js';

const MultiSelectClearIconButton = ({
  children,
  className,
  "aria-label": customAriaLabel,
  hideWhenEmpty = true
}) => {
  const { clearValue, value, triggerRef, disabled } = useMultiSelect();
  const buttonClasses = clsx(MultiSelectClasses.clearIconButton, className);
  const commonProps = {
    "aria-label": customAriaLabel || "clear all selected values"
  };
  if (value.length === 0 && hideWhenEmpty) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    IconButton,
    {
      buttonType: "secondary",
      className: buttonClasses,
      disabled,
      size: "sm",
      onClick: (e) => {
        e.stopPropagation();
        e.preventDefault();
        clearValue();
        triggerRef.current?.focus();
      },
      onKeyDown: (e) => {
        e.stopPropagation();
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          clearValue();
          triggerRef.current?.focus();
        }
      },
      onPointerDown: (e) => e.preventDefault(),
      ...commonProps,
      children: children || /* @__PURE__ */ jsx(Close, {})
    }
  );
};
MultiSelectClearIconButton.displayName = "MultiSelect.ClearIconButton";

export { MultiSelectClearIconButton };
//# sourceMappingURL=multi-select-clear-icon-button.js.map
