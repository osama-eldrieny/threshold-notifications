"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const chip = require('../../chip/chip.cjs');
const useMultiSelect = require('../hooks/use-multi-select.cjs');

const MultiSelectChipCloseButton = ({
  value,
  onClick,
  ...props
}) => {
  const { onSingleValueChange, triggerRef, disabled } = useMultiSelect.useMultiSelect();
  const handleClose = (e) => {
    e.stopPropagation();
    onSingleValueChange(value);
    onClick?.(e);
    triggerRef.current?.focus();
  };
  return /* @__PURE__ */ jsxRuntime.jsx(
    chip.default.CloseButton,
    {
      ...props,
      "aria-disabled": disabled,
      tabIndex: disabled ? -1 : 0,
      onClick: handleClose,
      onPointerDown: (e) => e.preventDefault()
    }
  );
};
MultiSelectChipCloseButton.displayName = "MultiSelect.ChipCloseButton";

exports.MultiSelectChipCloseButton = MultiSelectChipCloseButton;
//# sourceMappingURL=multi-select-chip-close-button.cjs.map
