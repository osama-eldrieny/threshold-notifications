"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef, useRef, useEffect } from 'react';
import Menu from '../../menu/menu.js';
import { MultiSelectClasses } from '../multi-select.styles.js';
import { useMultiSelect } from '../hooks/use-multi-select.js';

const MultiSelectTrigger = forwardRef(
  ({ children, ...props }, ref) => {
    const {
      setTriggerWidth,
      disabled,
      triggerRef,
      contentId,
      open,
      activeDescendant,
      "aria-invalid": ariaInvalid,
      "aria-describedby": ariaDescribedby,
      "aria-labelledby": ariaLabelledBy,
      "aria-required": ariaRequired,
      id
    } = useMultiSelect();
    const buttonRef = useRef(null);
    useEffect(() => {
      const resizeObserver = new ResizeObserver((entries) => {
        for (const entry of entries) {
          setTriggerWidth(entry.target.getBoundingClientRect().width);
        }
      });
      if (buttonRef.current) {
        resizeObserver.observe(buttonRef.current);
      }
      return () => {
        resizeObserver.disconnect();
      };
    }, [setTriggerWidth, buttonRef]);
    return /* @__PURE__ */ jsx(
      Menu.Trigger,
      {
        ...props,
        ref: (node) => {
          if (typeof ref === "function") {
            ref(node);
          } else if (ref) {
            ref.current = node;
          }
          triggerRef.current = node;
        },
        disabled,
        children: /* @__PURE__ */ jsx(
          "div",
          {
            ref: buttonRef,
            "aria-activedescendant": activeDescendant,
            "aria-controls": contentId,
            "aria-describedby": ariaDescribedby,
            "aria-disabled": disabled,
            "aria-expanded": open,
            "aria-haspopup": "listbox",
            "aria-invalid": ariaInvalid,
            "aria-labelledby": ariaLabelledBy,
            "aria-required": ariaRequired,
            className: MultiSelectClasses.trigger,
            "data-disabled": disabled,
            id,
            role: "combobox",
            tabIndex: disabled ? -1 : 0,
            children: /* @__PURE__ */ jsx("div", { className: MultiSelectClasses.triggerContent, children })
          }
        )
      }
    );
  }
);
MultiSelectTrigger.displayName = "MultiSelect.Trigger";

export { MultiSelectTrigger };
//# sourceMappingURL=multi-select-trigger.js.map
