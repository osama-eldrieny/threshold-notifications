"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const menu = require('../../menu/menu.cjs');
const multiSelect_styles = require('../multi-select.styles.cjs');
const useMultiSelect = require('../hooks/use-multi-select.cjs');

const MultiSelectTrigger = React.forwardRef(
  ({ children, ...props }, ref) => {
    const {
      setTriggerWidth,
      disabled,
      triggerRef,
      contentId,
      open,
      activeDescendant,
      "aria-invalid": ariaInvalid,
      "aria-describedby": ariaDescribedby,
      "aria-labelledby": ariaLabelledBy,
      "aria-required": ariaRequired,
      id
    } = useMultiSelect.useMultiSelect();
    const buttonRef = React.useRef(null);
    React.useEffect(() => {
      const resizeObserver = new ResizeObserver((entries) => {
        for (const entry of entries) {
          setTriggerWidth(entry.target.getBoundingClientRect().width);
        }
      });
      if (buttonRef.current) {
        resizeObserver.observe(buttonRef.current);
      }
      return () => {
        resizeObserver.disconnect();
      };
    }, [setTriggerWidth, buttonRef]);
    return /* @__PURE__ */ jsxRuntime.jsx(
      menu.default.Trigger,
      {
        ...props,
        ref: (node) => {
          if (typeof ref === "function") {
            ref(node);
          } else if (ref) {
            ref.current = node;
          }
          triggerRef.current = node;
        },
        disabled,
        children: /* @__PURE__ */ jsxRuntime.jsx(
          "div",
          {
            ref: buttonRef,
            "aria-activedescendant": activeDescendant,
            "aria-controls": contentId,
            "aria-describedby": ariaDescribedby,
            "aria-disabled": disabled,
            "aria-expanded": open,
            "aria-haspopup": "listbox",
            "aria-invalid": ariaInvalid,
            "aria-labelledby": ariaLabelledBy,
            "aria-required": ariaRequired,
            className: multiSelect_styles.MultiSelectClasses.trigger,
            "data-disabled": disabled,
            id,
            role: "combobox",
            tabIndex: disabled ? -1 : 0,
            children: /* @__PURE__ */ jsxRuntime.jsx("div", { className: multiSelect_styles.MultiSelectClasses.triggerContent, children })
          }
        )
      }
    );
  }
);
MultiSelectTrigger.displayName = "MultiSelect.Trigger";

exports.MultiSelectTrigger = MultiSelectTrigger;
//# sourceMappingURL=multi-select-trigger.cjs.map
