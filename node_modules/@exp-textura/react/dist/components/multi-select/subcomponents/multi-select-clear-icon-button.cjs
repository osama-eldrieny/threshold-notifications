"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const clsx$1 = require('clsx');
const icons = require('@exp-textura/icons');
const iconButton = require('../../icon-button/icon-button.cjs');
const multiSelect_styles = require('../multi-select.styles.cjs');
const useMultiSelect = require('../hooks/use-multi-select.cjs');

const MultiSelectClearIconButton = ({
  children,
  className,
  "aria-label": customAriaLabel,
  hideWhenEmpty = true
}) => {
  const { clearValue, value, triggerRef, disabled } = useMultiSelect.useMultiSelect();
  const buttonClasses = clsx$1.clsx(multiSelect_styles.MultiSelectClasses.clearIconButton, className);
  const commonProps = {
    "aria-label": customAriaLabel || "clear all selected values"
  };
  if (value.length === 0 && hideWhenEmpty) {
    return null;
  }
  return /* @__PURE__ */ jsxRuntime.jsx(
    iconButton.default,
    {
      buttonType: "secondary",
      className: buttonClasses,
      disabled,
      size: "sm",
      onClick: (e) => {
        e.stopPropagation();
        e.preventDefault();
        clearValue();
        triggerRef.current?.focus();
      },
      onKeyDown: (e) => {
        e.stopPropagation();
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          clearValue();
          triggerRef.current?.focus();
        }
      },
      onPointerDown: (e) => e.preventDefault(),
      ...commonProps,
      children: children || /* @__PURE__ */ jsxRuntime.jsx(icons.Close, {})
    }
  );
};
MultiSelectClearIconButton.displayName = "MultiSelect.ClearIconButton";

exports.MultiSelectClearIconButton = MultiSelectClearIconButton;
//# sourceMappingURL=multi-select-clear-icon-button.cjs.map
