"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const multiSelect_styles = require('../multi-select.styles.cjs');
const useMultiSelect = require('../hooks/use-multi-select.cjs');
const multiSelectChip = require('./multi-select-chip.cjs');
const multiSelectChipCloseButton = require('./multi-select-chip-close-button.cjs');

const MultiSelectValues = ({ children }) => {
  const { value, allOptions, placeholder, disabled } = useMultiSelect.useMultiSelect();
  let triggerItems;
  if (value.length === 0) {
    triggerItems = /* @__PURE__ */ jsxRuntime.jsx(jsxRuntime.Fragment, { children: placeholder });
  } else if (typeof children === "function") {
    triggerItems = /* @__PURE__ */ jsxRuntime.jsx(jsxRuntime.Fragment, { children: value.map((v) => {
      const option = allOptions.find((opt) => opt.value === v);
      return children({ value: v, option });
    }) });
  } else if (React.Children.count(children) > 0) {
    triggerItems = React.Children.map(children, (child) => {
      if (React.isValidElement(child) && child.type.displayName === "AvatarStack") {
        return React.cloneElement(child, { ...child.props, size: "xsm" });
      }
      return child;
    });
  } else {
    triggerItems = /* @__PURE__ */ jsxRuntime.jsx(jsxRuntime.Fragment, { children: value.map((v) => {
      const option = allOptions.find((opt) => opt.value === v);
      return /* @__PURE__ */ jsxRuntime.jsxs(multiSelectChip.MultiSelectChip, { children: [
        option?.label,
        /* @__PURE__ */ jsxRuntime.jsx(multiSelectChipCloseButton.MultiSelectChipCloseButton, { value: v })
      ] }, v);
    }) });
  }
  return /* @__PURE__ */ jsxRuntime.jsx("div", { className: multiSelect_styles.MultiSelectClasses.triggerItems, "data-disabled": disabled, children: triggerItems });
};
MultiSelectValues.displayName = "MultiSelect.Values";

exports.MultiSelectValues = MultiSelectValues;
//# sourceMappingURL=multi-select-values.cjs.map
