"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const avatar = require('../../avatar/avatar.cjs');
const chip = require('../../chip/chip.cjs');

const MultiSelectAvatarChip = React.forwardRef(
  ({ children, ...props }, ref) => {
    const childrenWithAvatarSize = React.Children.map(children, (child) => {
      if (React.isValidElement(child) && child.type === avatar.default) {
        return React.cloneElement(child, { size: "xsm" });
      }
      return child;
    });
    return /* @__PURE__ */ jsxRuntime.jsx(chip.default, { hasStartAdornment: true, size: "sm", type: "outline", variant: "neutral", ...props, ref, children: childrenWithAvatarSize });
  }
);
MultiSelectAvatarChip.displayName = "MultiSelect.AvatarChip";

exports.MultiSelectAvatarChip = MultiSelectAvatarChip;
//# sourceMappingURL=multi-select-avatar-chip.cjs.map
