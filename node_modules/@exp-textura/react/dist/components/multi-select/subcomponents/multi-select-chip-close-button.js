"use client";
import { jsx } from 'react/jsx-runtime';
import Chip from '../../chip/chip.js';
import { useMultiSelect } from '../hooks/use-multi-select.js';

const MultiSelectChipCloseButton = ({
  value,
  onClick,
  ...props
}) => {
  const { onSingleValueChange, triggerRef, disabled } = useMultiSelect();
  const handleClose = (e) => {
    e.stopPropagation();
    onSingleValueChange(value);
    onClick?.(e);
    triggerRef.current?.focus();
  };
  return /* @__PURE__ */ jsx(
    Chip.CloseButton,
    {
      ...props,
      "aria-disabled": disabled,
      tabIndex: disabled ? -1 : 0,
      onClick: handleClose,
      onPointerDown: (e) => e.preventDefault()
    }
  );
};
MultiSelectChipCloseButton.displayName = "MultiSelect.ChipCloseButton";

export { MultiSelectChipCloseButton };
//# sourceMappingURL=multi-select-chip-close-button.js.map
