"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef, Children, isValidElement, cloneElement } from 'react';
import Avatar from '../../avatar/avatar.js';
import Chip from '../../chip/chip.js';

const MultiSelectAvatarChip = forwardRef(
  ({ children, ...props }, ref) => {
    const childrenWithAvatarSize = Children.map(children, (child) => {
      if (isValidElement(child) && child.type === Avatar) {
        return cloneElement(child, { size: "xsm" });
      }
      return child;
    });
    return /* @__PURE__ */ jsx(Chip, { hasStartAdornment: true, size: "sm", type: "outline", variant: "neutral", ...props, ref, children: childrenWithAvatarSize });
  }
);
MultiSelectAvatarChip.displayName = "MultiSelect.AvatarChip";

export { MultiSelectAvatarChip };
//# sourceMappingURL=multi-select-avatar-chip.js.map
