"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
const menu = require('../../menu/menu.cjs');
const useMultiSelect = require('../hooks/use-multi-select.cjs');
const multiSelect_styles = require('../multi-select.styles.cjs');
const multiSelectItem = require('./multi-select-item.cjs');

const MultiSelectItems = ({
  className,
  style,
  children,
  selectAll = "Select all",
  noResults = "No results found"
}) => {
  const {
    filteredOptions,
    pendingValue,
    focusedValue,
    setHasSelectAll,
    hasSelectAll,
    searchValue,
    activeDescendant,
    contentId
  } = useMultiSelect.useMultiSelect();
  React.useEffect(() => {
    const showSelectAll2 = selectAll && !searchValue;
    if (showSelectAll2 && !hasSelectAll) {
      setHasSelectAll(true);
    }
    if (!showSelectAll2 && hasSelectAll) {
      setHasSelectAll(false);
    }
  }, [selectAll, hasSelectAll, setHasSelectAll, searchValue]);
  const showSelectAll = selectAll && filteredOptions.length > 1 && !searchValue;
  const renderItems = () => {
    if (filteredOptions.length === 0) {
      return /* @__PURE__ */ jsxRuntime.jsx(menu.default.Item, { disabled: true, className: multiSelect_styles.MultiSelectClasses.noResults, children: noResults });
    }
    if (typeof children === "function") {
      return filteredOptions.map(
        (option) => children({
          option,
          isSelected: pendingValue.includes(option.value),
          isFocused: focusedValue === option.value
        })
      );
    }
    return filteredOptions.map((option) => /* @__PURE__ */ jsxRuntime.jsx(multiSelectItem.MultiSelectItem, { value: option.value, children: option.label }, option.value));
  };
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "div",
    {
      "aria-activedescendant": activeDescendant,
      "aria-multiselectable": "true",
      className: clsx$1.clsx(multiSelect_styles.MultiSelectClasses.items, className),
      id: contentId,
      role: "listbox",
      style,
      children: [
        showSelectAll && /* @__PURE__ */ jsxRuntime.jsx(multiSelectItem.MultiSelectItem, { value: "select-all", children: selectAll }),
        renderItems()
      ]
    }
  );
};
MultiSelectItems.displayName = "MultiSelect.Items";

exports.MultiSelectItems = MultiSelectItems;
//# sourceMappingURL=multi-select-items.cjs.map
