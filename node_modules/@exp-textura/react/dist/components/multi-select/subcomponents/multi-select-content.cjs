"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const menu = require('../../menu/menu.cjs');
const multiSelect_styles = require('../multi-select.styles.cjs');
const useMultiSelect = require('../hooks/use-multi-select.cjs');

const MultiSelectContent = React.forwardRef(
  ({ children, ...props }, ref) => {
    const {
      triggerWidth,
      contentRef,
      "aria-labelledby": ariaLabelledBy,
      theme,
      variant,
      styleSlots,
      isOpening,
      isOutsideClick
    } = useMultiSelect.useMultiSelect();
    const hasLabel = ariaLabelledBy || props["aria-label"] || props["aria-labelledby"];
    return /* @__PURE__ */ jsxRuntime.jsx(
      menu.default.Content,
      {
        ref: (node) => {
          if (typeof ref === "function") {
            ref(node);
          } else if (ref) {
            ref.current = node;
          }
          contentRef.current = node;
        },
        "aria-label": hasLabel ? void 0 : "Options",
        "aria-labelledby": ariaLabelledBy,
        "aria-orientation": void 0,
        collisionPadding: 10,
        isOpening,
        role: "dialog",
        style: {
          width: triggerWidth ? `${triggerWidth}px` : "auto",
          ...props.style
        },
        onCloseAutoFocus: (e) => {
          if (isOutsideClick.current) {
            e.preventDefault();
            isOutsideClick.current = false;
          }
        },
        onPointerDownOutside: () => {
          isOutsideClick.current = true;
        },
        ...props,
        children: /* @__PURE__ */ jsxRuntime.jsx(menu.default.Group, { children: /* @__PURE__ */ jsxRuntime.jsx(
          multiSelect_styles.StyledMultiSelect,
          {
            className: multiSelect_styles.MultiSelectClasses.content,
            currentTheme: theme,
            styleSlots,
            variant,
            children
          }
        ) })
      }
    );
  }
);
MultiSelectContent.displayName = "MultiSelect.Content";

exports.MultiSelectContent = MultiSelectContent;
//# sourceMappingURL=multi-select-content.cjs.map
