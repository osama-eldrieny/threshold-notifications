"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { DividerClasses, StyledDivider } from './divider.styles.js';

const Divider = forwardRef(
  ({
    className,
    variant = "default",
    orientation = "horizontal",
    size = "sm",
    decorative = false,
    asChild = false,
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(DividerClasses.root, DividerClasses[size], className);
    return /* @__PURE__ */ jsx(
      StyledDivider,
      {
        ref,
        asChild,
        className: rootClasses,
        currentTheme: theme,
        "data-orientation": orientation,
        decorative,
        variant,
        ...props
      }
    );
  }
);
Divider.displayName = "Divider";

export { Divider as default };
//# sourceMappingURL=divider.js.map
