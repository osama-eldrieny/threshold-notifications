import { ComponentProps } from '../../types';
import { CSSObject } from '@emotion/react';
import { UseFormReturn, UseFormProps, SubmitHandler, FieldValues } from 'react-hook-form';
export interface FormContextType {
    formSize?: "default" | "sm";
}
export type BaseFormProps<T extends FieldValues> = Omit<ComponentProps, "onSubmit" | "children"> & {
    children: React.ReactNode | ((methods: UseFormReturn<T>) => React.ReactNode);
    formSize?: "default" | "sm";
    onSubmit?: SubmitHandler<T>;
    styleSlots?: {
        root?: CSSObject;
        sm?: CSSObject;
    };
};
export type ControlledFormProps<T extends FieldValues> = BaseFormProps<T> & {
    methods: UseFormReturn<T>;
    options?: never;
};
export type UncontrolledFormProps<T extends FieldValues> = BaseFormProps<T> & {
    methods?: never;
    options?: UseFormProps<T>;
};
export type FormProps<T extends FieldValues> = ControlledFormProps<T> | UncontrolledFormProps<T>;
//# sourceMappingURL=form.types.d.ts.map