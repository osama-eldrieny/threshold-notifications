"use client";
import { jsx } from 'react/jsx-runtime';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { useForm, FormProvider } from 'react-hook-form';
import { FormClasses, StyledForm } from './form.styles.js';
import { FormContext } from './hooks/use-form-size.js';

const FormLayout = ({
  className,
  children,
  onSubmit,
  formSize = "default",
  methods,
  ...props
}) => {
  const theme = useTexturaTheme();
  const rootClasses = clsx(FormClasses.root, className, {
    [FormClasses.sm]: formSize === "sm"
  });
  return /* @__PURE__ */ jsx(FormProvider, { ...methods, children: /* @__PURE__ */ jsx(FormContext.Provider, { value: { formSize }, children: /* @__PURE__ */ jsx(
    StyledForm,
    {
      noValidate: true,
      className: rootClasses,
      currentTheme: theme,
      ...props,
      onSubmit: onSubmit ? methods.handleSubmit(onSubmit) : void 0,
      children: typeof children === "function" ? children(methods) : children
    }
  ) }) });
};
const Form = (props) => {
  if (props.methods) {
    return /* @__PURE__ */ jsx(FormLayout, { ...props, methods: props.methods });
  }
  const methods = useForm(props.options);
  return /* @__PURE__ */ jsx(FormLayout, { ...props, methods });
};
Form.displayName = "Form";

export { Form as default };
//# sourceMappingURL=form.js.map
