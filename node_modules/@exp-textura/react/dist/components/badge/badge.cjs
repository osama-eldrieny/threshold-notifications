"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const badge_styles = require('./badge.styles.cjs');

const Badge = React.forwardRef(
  ({ className, variant = "default", type, children, ...props }, ref) => {
    const theme = useTheme.default();
    const typeClassName = type && badge_styles.BadgeClasses[type.replace(/-(\w)/g, (_, c) => c.toUpperCase())];
    const rootClasses = clsx$1.clsx(badge_styles.BadgeClasses.root, typeClassName, className);
    return /* @__PURE__ */ jsxRuntime.jsx(
      badge_styles.StyledBadge,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children: type?.includes("dot") ? null : children
      }
    );
  }
);
Badge.displayName = "Badge";

exports.default = Badge;
//# sourceMappingURL=badge.cjs.map
