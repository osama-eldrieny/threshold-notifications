"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import { BadgeClasses, StyledBadge } from './badge.styles.js';

const Badge = forwardRef(
  ({ className, variant = "default", type, children, ...props }, ref) => {
    const theme = useTexturaTheme();
    const typeClassName = type && BadgeClasses[type.replace(/-(\w)/g, (_, c) => c.toUpperCase())];
    const rootClasses = clsx(BadgeClasses.root, typeClassName, className);
    return /* @__PURE__ */ jsx(
      StyledBadge,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children: type?.includes("dot") ? null : children
      }
    );
  }
);
Badge.displayName = "Badge";

export { Badge as default };
//# sourceMappingURL=badge.js.map
