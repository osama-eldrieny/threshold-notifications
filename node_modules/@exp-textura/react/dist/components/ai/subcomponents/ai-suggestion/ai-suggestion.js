"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../../../theme/hooks/use-theme.js';
import Skeleton from '../../../skeleton/skeleton.js';
import { AISuggestionClasses, StyledAISuggestion, AISuggestionListClasses, StyledAISuggestionList } from './ai-suggestion.styles.js';

const AISuggestionListLabel = forwardRef(
  ({ children, className, ...props }, ref) => {
    const rootClasses = clsx(AISuggestionListClasses.label, className);
    return /* @__PURE__ */ jsx("div", { ref, className: rootClasses, ...props, children });
  }
);
AISuggestionListLabel.displayName = "AI.SuggestionListLabel";
const AISuggestionList = forwardRef(
  ({ children, className, variant = "default", ...props }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(AISuggestionListClasses.root, className);
    return /* @__PURE__ */ jsx(
      StyledAISuggestionList,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children
      }
    );
  }
);
AISuggestionList.displayName = "AI.SuggestionList";
const AISuggestion = forwardRef(
  ({ children, className, type = "button", variant = "default", loading = false, ...props }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(AISuggestionClasses.root, className);
    if (loading) {
      return /* @__PURE__ */ jsx(
        StyledAISuggestion,
        {
          "aria-busy": "true",
          as: "div",
          className: rootClasses,
          currentTheme: theme,
          "data-loading": "true",
          variant,
          ...props,
          children: /* @__PURE__ */ jsx(Skeleton, { style: { height: theme.toRem(12) } })
        }
      );
    }
    return /* @__PURE__ */ jsx(
      StyledAISuggestion,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        type,
        variant,
        ...props,
        children
      }
    );
  }
);
AISuggestion.displayName = "AI.Suggestion";

export { AISuggestion, AISuggestionList, AISuggestionListLabel };
//# sourceMappingURL=ai-suggestion.js.map
