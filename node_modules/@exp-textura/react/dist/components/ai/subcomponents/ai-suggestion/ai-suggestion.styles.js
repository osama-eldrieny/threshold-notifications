"use client";
import styled from '@emotion/styled';
import { getExcludedDOMProps, getStyleSlots } from '../../../../system/utils/component-utils.js';
import { useMemo } from 'react';

const suggestionListComponentName = "AISuggestionList";
const AISuggestionListClasses = {
  root: "exp-textura_ai_suggestion-list",
  label: "exp-textura_ai_suggestion-list_label"
};
const generateSuggestionListStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = useMemo(
    () => getStyleSlots(
      suggestionListComponentName,
      theme,
      variant,
      styleSlotsProp
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "flex",
    flexDirection: "column",
    gap: theme.toRem(theme.spacing[8]),
    width: "100%",
    paddingTop: theme.toRem(theme.spacing[16]),
    [`.${AISuggestionListClasses.label}`]: {
      ...theme.typography.body.mdEmphasis,
      // Bold 14px
      color: theme.color.content.default,
      marginBottom: 0,
      ...styleSlots?.label
    },
    ...styleSlots?.root
  };
};
const StyledAISuggestionList = styled("div", {
  shouldForwardProp: (prop) => !getExcludedDOMProps().includes(prop)
})((props) => generateSuggestionListStyles(props));
const suggestionComponentName = "AISuggestion";
const AISuggestionClasses = {
  root: "exp-textura_ai_suggestion"
};
const generateSuggestionStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = useMemo(
    () => getStyleSlots(
      suggestionComponentName,
      theme,
      variant,
      styleSlotsProp
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "flex",
    alignItems: "center",
    width: "100%",
    gap: theme.toRem(theme.spacing[8]),
    padding: `${theme.toRem(theme.spacing[8])} ${theme.toRem(theme.spacing[8])}`,
    marginInlineStart: `calc(${theme.toRem(theme.spacing[8])} * -1)`,
    borderRadius: theme.toRem(theme.border.radius.sm),
    backgroundColor: "transparent",
    border: "none",
    cursor: "pointer",
    textAlign: "left",
    textDecoration: "none",
    color: theme.color.content.default,
    ...theme.typography.body.md,
    "&:hover:not(:disabled)": {
      backgroundColor: theme.color.background.defaultHover,
      color: theme.color.content.strong,
      "& > svg": {
        color: theme.color.content.iconStrong,
        ...styleSlots?.iconHover
      },
      ...styleSlots?.hover
    },
    "&:active:not(:disabled)": {
      backgroundColor: theme.color.background.defaultPressed
    },
    "&:disabled": {
      opacity: 0.5,
      cursor: "not-allowed",
      ...styleSlots?.disabled
    },
    "&[data-loading='true']": {
      opacity: 1,
      cursor: "default",
      pointerEvents: "none",
      userSelect: "none",
      backgroundColor: "transparent",
      ...styleSlots?.loading
    },
    "& > svg": {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      width: theme.toRem(16),
      height: theme.toRem(16),
      color: theme.color.content.icon,
      ...styleSlots?.icon
    },
    ...styleSlots?.root
  };
};
const excludedSuggestionDOMProps = [...getExcludedDOMProps(), "type"];
const StyledAISuggestion = styled("button", {
  shouldForwardProp: (prop) => !excludedSuggestionDOMProps.includes(prop)
})((props) => generateSuggestionStyles(props));

export { AISuggestionClasses, AISuggestionListClasses, StyledAISuggestion, StyledAISuggestionList };
//# sourceMappingURL=ai-suggestion.styles.js.map
