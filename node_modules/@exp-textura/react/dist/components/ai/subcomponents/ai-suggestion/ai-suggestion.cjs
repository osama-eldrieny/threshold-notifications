"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../../../theme/hooks/use-theme.cjs');
const skeleton = require('../../../skeleton/skeleton.cjs');
const aiSuggestion_styles = require('./ai-suggestion.styles.cjs');

const AISuggestionListLabel = React.forwardRef(
  ({ children, className, ...props }, ref) => {
    const rootClasses = clsx$1.clsx(aiSuggestion_styles.AISuggestionListClasses.label, className);
    return /* @__PURE__ */ jsxRuntime.jsx("div", { ref, className: rootClasses, ...props, children });
  }
);
AISuggestionListLabel.displayName = "AI.SuggestionListLabel";
const AISuggestionList = React.forwardRef(
  ({ children, className, variant = "default", ...props }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(aiSuggestion_styles.AISuggestionListClasses.root, className);
    return /* @__PURE__ */ jsxRuntime.jsx(
      aiSuggestion_styles.StyledAISuggestionList,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children
      }
    );
  }
);
AISuggestionList.displayName = "AI.SuggestionList";
const AISuggestion = React.forwardRef(
  ({ children, className, type = "button", variant = "default", loading = false, ...props }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(aiSuggestion_styles.AISuggestionClasses.root, className);
    if (loading) {
      return /* @__PURE__ */ jsxRuntime.jsx(
        aiSuggestion_styles.StyledAISuggestion,
        {
          "aria-busy": "true",
          as: "div",
          className: rootClasses,
          currentTheme: theme,
          "data-loading": "true",
          variant,
          ...props,
          children: /* @__PURE__ */ jsxRuntime.jsx(skeleton.default, { style: { height: theme.toRem(12) } })
        }
      );
    }
    return /* @__PURE__ */ jsxRuntime.jsx(
      aiSuggestion_styles.StyledAISuggestion,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        type,
        variant,
        ...props,
        children
      }
    );
  }
);
AISuggestion.displayName = "AI.Suggestion";

exports.AISuggestion = AISuggestion;
exports.AISuggestionList = AISuggestionList;
exports.AISuggestionListLabel = AISuggestionListLabel;
//# sourceMappingURL=ai-suggestion.cjs.map
