"use client";
import { jsx, jsxs } from 'react/jsx-runtime';
import { forwardRef, Children, isValidElement, useState, useId, useCallback, useContext, createContext } from 'react';
import TextareaAutosize from 'react-textarea-autosize';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../../../theme/hooks/use-theme.js';
import Button from '../../../button/button.js';
import IconButton from '../../../icon-button/icon-button.js';
import Menu from '../../../menu/menu.js';
import ChipBar from '../../../chip-bar/chip-bar.js';
import { Add, ArrowUp, ChevronRight } from '@exp-textura/icons';
import { enforceAIItemProps } from '../ai-utils.js';
import { AIPromptClasses, StyledAIPrompt, StyledAIPromptTaskBar } from './ai-prompt.styles.js';

const AIPrompt = forwardRef(
  ({ className, thinking, children, variant = "default", ...props }, ref) => {
    const theme = useTexturaTheme(), rootClasses = clsx(AIPromptClasses.root, className);
    return /* @__PURE__ */ jsx(
      StyledAIPrompt,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        "data-thinking": thinking,
        thinking,
        variant,
        ...props,
        children
      }
    );
  }
);
AIPrompt.displayName = "AI.Prompt";
const AIPromptContext = forwardRef(
  ({ className, children, ...props }, ref) => {
    const theme = useTexturaTheme();
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(AIPromptClasses.context, className), children: /* @__PURE__ */ jsx(
      ChipBar,
      {
        selectionMode: "none",
        styleSlots: { root: { gap: theme.toRem(theme.spacing[4]) } },
        ...props,
        children
      }
    ) });
  }
);
AIPromptContext.displayName = "AI.PromptContext";
const AIPromptInput = forwardRef(
  ({ className, value, onChange, onSubmit, placeholder = "Ask anything", disabled, ...props }, ref) => {
    const handleKeyDown = (e) => {
      if (e.key === "Enter" && !e.shiftKey) {
        e.preventDefault();
        if (value?.trim()) {
          onSubmit?.(value);
        }
      }
    };
    const handleChange = (e) => {
      onChange?.(e.target.value);
    };
    return /* @__PURE__ */ jsx(
      TextareaAutosize,
      {
        ref,
        className: clsx(AIPromptClasses.input, className),
        disabled,
        maxRows: 10,
        minRows: 1,
        placeholder,
        value,
        onChange: handleChange,
        onKeyDown: handleKeyDown,
        ...props
      }
    );
  }
);
AIPromptInput.displayName = "AI.PromptInput";
const AIPromptStatus = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(AIPromptClasses.status, className), ...props, children });
  }
);
AIPromptStatus.displayName = "AI.PromptStatus";
const AIPromptStatusLabel = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(AIPromptClasses.statusLabel, className), ...props, children });
  }
);
AIPromptStatusLabel.displayName = "AI.PromptStatusLabel";
const AIPromptStatusActions = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(AIPromptClasses.statusActions, className), ...props, children });
  }
);
AIPromptStatusActions.displayName = "AI.PromptStatusActions";
const AIPromptButton = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx(
      Button,
      {
        ref,
        buttonType: "primary",
        className: clsx(AIPromptClasses.button, className),
        ...props,
        children: children || /* @__PURE__ */ jsx(ArrowUp, {})
      }
    );
  }
);
AIPromptButton.displayName = "AI.PromptButton";
const AIPromptFooter = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(AIPromptClasses.footer, className), ...props, children });
  }
);
AIPromptFooter.displayName = "AI.PromptFooter";
const AIPromptFooterLeading = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(AIPromptClasses.footerLeading, className), ...props, children: enforceAIItemProps(children) });
  }
);
AIPromptFooterLeading.displayName = "AI.PromptFooterLeading";
const AIPromptFooterTrailing = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(AIPromptClasses.footerTrailing, className), ...props, children: enforceAIItemProps(children) });
  }
);
AIPromptFooterTrailing.displayName = "AI.PromptFooterTrailing";
const AIPromptAttachmentMenuTrigger = forwardRef(({ children, ...props }, ref) => {
  return /* @__PURE__ */ jsx(IconButton, { ref, buttonType: "secondary", ...props, children: children || /* @__PURE__ */ jsx(Add, {}) });
});
AIPromptAttachmentMenuTrigger.displayName = "AI.PromptAttachmentMenuTrigger";
const AIPromptAttachmentMenu = forwardRef(
  ({ children, "aria-label": ariaLabel = "Add attachment", ...props }, ref) => {
    let trigger = null;
    const content = [];
    Children.forEach(children, (child) => {
      if (!isValidElement(child)) {
        content.push(child);
        return;
      }
      const type = child.type;
      const displayName = type?.displayName || "";
      const isTrigger = displayName === "AI.PromptAttachmentMenuTrigger" || displayName.includes(".Trigger");
      if (isTrigger) {
        trigger = child;
      } else {
        content.push(child);
      }
    });
    return /* @__PURE__ */ jsxs(Menu, { ref, ...props, children: [
      trigger ? /* @__PURE__ */ jsx(Menu.Trigger, { children: trigger }) : /* @__PURE__ */ jsx(Menu.Trigger, { children: /* @__PURE__ */ jsx(AIPromptAttachmentMenuTrigger, { "aria-label": ariaLabel }) }),
      /* @__PURE__ */ jsx(Menu.Content, { children: content })
    ] });
  }
);
AIPromptAttachmentMenu.displayName = "AI.PromptAttachmentMenu";
const AIPromptTaskBarContext = createContext(null);
const useAIPromptTaskBarContext = () => {
  const context = useContext(AIPromptTaskBarContext);
  if (!context) {
    throw new Error("AIPromptTaskBar subcomponents must be used within AIPromptTaskBar");
  }
  return context;
};
const AIPromptTaskBar = forwardRef(
  ({
    className,
    children,
    expanded: expandedProp,
    defaultExpanded = false,
    onExpandChange,
    variant = "default",
    ...props
  }, ref) => {
    const theme = useTexturaTheme();
    const [expandedState, setExpandedState] = useState(defaultExpanded);
    const isControlled = expandedProp !== void 0;
    const expanded = isControlled ? expandedProp : expandedState;
    const contentId = useId();
    const toggle = useCallback(() => {
      const newExpanded = !expanded;
      if (!isControlled) {
        setExpandedState(newExpanded);
      }
      onExpandChange?.(newExpanded);
    }, [expanded, isControlled, onExpandChange]);
    return /* @__PURE__ */ jsx(AIPromptTaskBarContext.Provider, { value: { expanded, toggle, contentId }, children: /* @__PURE__ */ jsx(
      StyledAIPromptTaskBar,
      {
        ref,
        className: clsx(AIPromptClasses.taskBar, className),
        currentTheme: theme,
        "data-expanded": expanded,
        variant,
        ...props,
        children: /* @__PURE__ */ jsx("div", { className: AIPromptClasses.taskBarPopup, "data-expanded": expanded, children })
      }
    ) });
  }
);
AIPromptTaskBar.displayName = "AI.PromptTaskBar";
const AIPromptTaskBarHeader = forwardRef(
  ({ className, children, ...props }, ref) => {
    const { expanded, toggle, contentId } = useAIPromptTaskBarContext();
    const handleKeyDown = useCallback(
      (e) => {
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          toggle();
        }
      },
      [toggle]
    );
    return /* @__PURE__ */ jsxs(
      "div",
      {
        ref,
        "aria-controls": contentId,
        "aria-expanded": expanded,
        className: clsx(AIPromptClasses.taskBarHeader, className),
        role: "button",
        tabIndex: 0,
        onClick: toggle,
        onKeyDown: handleKeyDown,
        ...props,
        children: [
          /* @__PURE__ */ jsx(ChevronRight, {}),
          /* @__PURE__ */ jsx("div", { children })
        ]
      }
    );
  }
);
AIPromptTaskBarHeader.displayName = "AI.PromptTaskBarHeader";
const AIPromptTaskBarContent = forwardRef(
  ({ className, children, ...props }, ref) => {
    const { expanded, contentId } = useAIPromptTaskBarContext();
    return /* @__PURE__ */ jsx(
      "div",
      {
        ref,
        "aria-hidden": !expanded,
        className: clsx(AIPromptClasses.taskBarContent, className),
        "data-expanded": expanded,
        id: contentId,
        ...props,
        children: /* @__PURE__ */ jsx("div", { className: AIPromptClasses.taskBarContentInner, children })
      }
    );
  }
);
AIPromptTaskBarContent.displayName = "AI.PromptTaskBarContent";

export { AIPrompt, AIPromptAttachmentMenu, AIPromptAttachmentMenuTrigger, AIPromptButton, AIPromptContext, AIPromptFooter, AIPromptFooterLeading, AIPromptFooterTrailing, AIPromptInput, AIPromptStatus, AIPromptStatusActions, AIPromptStatusLabel, AIPromptTaskBar, AIPromptTaskBarContent, AIPromptTaskBarHeader, useAIPromptTaskBarContext };
//# sourceMappingURL=ai-prompt.js.map
