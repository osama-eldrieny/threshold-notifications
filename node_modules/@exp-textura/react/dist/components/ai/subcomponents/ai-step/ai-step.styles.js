"use client";
import styled from '@emotion/styled';
import { getExcludedDOMProps, getStyleSlots } from '../../../../system/utils/component-utils.js';
import { useMemo } from 'react';

const componentName = "AIStep";
const AIStepClasses = {
  step: "exp-textura_ai_step",
  title: "exp-textura_ai_step_title",
  tool: "exp-textura_ai_step_tool",
  summary: "exp-textura_ai_step_summary"
};
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = useMemo(
    () => getStyleSlots(
      componentName,
      theme,
      variant,
      styleSlotsProp
      // getVariantConfig(theme),
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "flex",
    flexDirection: "column",
    gap: theme.toRem(theme.spacing[16]),
    width: "100%",
    [`.${AIStepClasses.title}`]: {
      ...theme.typography.label.md,
      fontWeight: 700,
      color: theme.color.content.strong,
      ...styleSlots?.title
    },
    [`.${AIStepClasses.tool}`]: {
      width: "100%",
      ...styleSlots?.tool
    },
    [`.${AIStepClasses.summary}`]: {
      color: theme.color.content.default,
      "p, ul, ol": {
        margin: 0
      },
      ...styleSlots?.summary
    },
    ...styleSlots?.step,
    ...styleSlots?.root
  };
};
const excludedDOMProps = getExcludedDOMProps();
const StyledAIStep = styled("div", {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));

export { AIStepClasses, StyledAIStep };
//# sourceMappingURL=ai-step.styles.js.map
