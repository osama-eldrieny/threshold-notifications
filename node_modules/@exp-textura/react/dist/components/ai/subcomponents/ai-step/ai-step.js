"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../../../theme/hooks/use-theme.js';
import { AIStepClasses, StyledAIStep } from './ai-step.styles.js';
import { Markdown } from '../../../../vendor/.pnpm/react-markdown@10.1.0_@types_react@18.3.28_react@19.2.5/node_modules/react-markdown/lib/index.js';

const AIStep = forwardRef(
  ({ className, children, variant = "default", ...props }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(AIStepClasses.step, className);
    return /* @__PURE__ */ jsx(
      StyledAIStep,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children
      }
    );
  }
);
AIStep.displayName = "AI.Step";
const AIStepTitle = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(AIStepClasses.title, className), ...props, children });
  }
);
AIStepTitle.displayName = "AI.StepTitle";
const AIStepSummary = forwardRef(
  ({ className, children, markdown = true, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(AIStepClasses.summary, className), ...props, children: markdown && typeof children === "string" ? /* @__PURE__ */ jsx(Markdown, { children }) : children });
  }
);
AIStepSummary.displayName = "AI.StepSummary";
const AIStepTool = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(AIStepClasses.tool, className), ...props, children });
  }
);
AIStepTool.displayName = "AI.StepTool";

export { AIStep, AIStepSummary, AIStepTitle, AIStepTool };
//# sourceMappingURL=ai-step.js.map
