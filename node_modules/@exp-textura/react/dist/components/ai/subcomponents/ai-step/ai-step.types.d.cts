import { ComponentProps, Theme } from '../../../../types';
import { CSSObject } from '@emotion/react';
import { ReactNode } from 'react';
export interface AIStepProps extends ComponentProps {
    children?: ReactNode;
    currentTheme?: Theme;
    variant?: string;
    styleSlots?: {
        root?: CSSObject;
        step?: CSSObject;
        title?: CSSObject;
        tool?: CSSObject;
        summary?: CSSObject;
    };
}
export interface AIStepTitleProps extends ComponentProps {
    children?: ReactNode;
}
export interface AIStepSummaryProps extends ComponentProps {
    children?: ReactNode;
    markdown?: boolean;
}
export interface AIStepToolProps extends ComponentProps {
    children?: ReactNode;
}
//# sourceMappingURL=ai-step.types.d.ts.map