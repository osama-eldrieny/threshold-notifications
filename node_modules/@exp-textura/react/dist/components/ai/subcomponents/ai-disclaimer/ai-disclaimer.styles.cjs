"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const styled = require('@emotion/styled');
const componentUtils = require('../../../../system/utils/component-utils.cjs');
const React = require('react');

const componentName = "AIDisclaimer";
const AIDisclaimerClasses = {
  root: "exp-textura_ai_disclaimer"
};
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = React.useMemo(
    () => componentUtils.getStyleSlots(
      componentName,
      theme,
      variant,
      styleSlotsProp
      // getVariantConfig(theme),
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "block",
    width: "100%",
    paddingBottom: theme.toRem(theme.spacing[4]),
    textAlign: "center",
    ...theme.typography.body.sm,
    color: theme.color.content.weak,
    ...styleSlots?.root
  };
};
const excludedDOMProps = componentUtils.getExcludedDOMProps();
const StyledAIDisclaimer = styled("div", {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));

exports.AIDisclaimerClasses = AIDisclaimerClasses;
exports.StyledAIDisclaimer = StyledAIDisclaimer;
//# sourceMappingURL=ai-disclaimer.styles.cjs.map
