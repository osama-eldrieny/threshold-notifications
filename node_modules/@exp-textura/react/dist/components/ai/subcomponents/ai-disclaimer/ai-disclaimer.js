"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../../../theme/hooks/use-theme.js';
import { StyledAIDisclaimer, AIDisclaimerClasses } from './ai-disclaimer.styles.js';

const AIDisclaimer = forwardRef(
  ({ className, children, variant = "default", ...props }, ref) => {
    const theme = useTexturaTheme();
    return /* @__PURE__ */ jsx(
      StyledAIDisclaimer,
      {
        ref,
        className: clsx(AIDisclaimerClasses.root, className),
        currentTheme: theme,
        variant,
        ...props,
        children
      }
    );
  }
);
AIDisclaimer.displayName = "AI.Disclaimer";

export { AIDisclaimer };
//# sourceMappingURL=ai-disclaimer.js.map
