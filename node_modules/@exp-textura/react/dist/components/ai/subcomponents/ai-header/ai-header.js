"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef, Children } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../../../theme/hooks/use-theme.js';
import { enforceAIItemProps } from '../ai-utils.js';
import { AIHeaderClasses, StyledAIHeader } from './ai-header.styles.js';

const AIHeader = forwardRef(
  ({ className, children, variant = "default", ...props }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(AIHeaderClasses.root, className);
    return /* @__PURE__ */ jsx(
      StyledAIHeader,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children
      }
    );
  }
);
AIHeader.displayName = "AI.Header";
const AIHeaderTitle = forwardRef(
  ({ className, children, headingLevel = 3, ...props }, ref) => {
    const Tag = `h${headingLevel}`;
    const processedChildren = Children.map(children, (child) => {
      if (typeof child === "string") {
        return /* @__PURE__ */ jsx("span", { className: AIHeaderClasses.titleText, children: child });
      }
      return child;
    });
    return /* @__PURE__ */ jsx(Tag, { ref, className: clsx(AIHeaderClasses.title, className), ...props, children: processedChildren });
  }
);
AIHeaderTitle.displayName = "AI.HeaderTitle";
const AIHeaderActions = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(AIHeaderClasses.actions, className), ...props, children: enforceAIItemProps(children) });
  }
);
AIHeaderActions.displayName = "AI.HeaderActions";

export { AIHeader, AIHeaderActions, AIHeaderTitle };
//# sourceMappingURL=ai-header.js.map
