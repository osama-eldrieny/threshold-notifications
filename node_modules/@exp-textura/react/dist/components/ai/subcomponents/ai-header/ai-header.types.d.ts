import { ComponentProps, Theme } from '../../../../types';
import { CSSObject } from '@emotion/react';
import { ReactNode } from 'react';
export interface AIHeaderProps extends ComponentProps {
    children?: ReactNode;
    currentTheme?: Theme;
    variant?: string;
    styleSlots?: {
        root?: CSSObject;
        title?: CSSObject;
        titleIcon?: CSSObject;
        titleText?: CSSObject;
        actions?: CSSObject;
    };
}
export interface AIHeaderTitleProps extends ComponentProps {
    children?: ReactNode;
    headingLevel?: 1 | 2 | 3 | 4 | 5 | 6;
}
export interface AIHeaderActionsProps extends ComponentProps {
    children?: ReactNode;
}
//# sourceMappingURL=ai-header.types.d.ts.map