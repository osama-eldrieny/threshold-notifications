"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef, Children, isValidElement, cloneElement } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../../../theme/hooks/use-theme.js';
import { StyledAIRating, AIRatingClasses } from './ai-rating.styles.js';

const AIRating = forwardRef(
  ({ className, children, variant = "default", ...props }, ref) => {
    const theme = useTexturaTheme();
    const enforcedChildren = Children.map(children, (child) => {
      if (isValidElement(child)) {
        const type = child.type;
        const props2 = child.props;
        const newProps = {};
        if (type?.displayName === "IconButton") {
          if (!props2.buttonType) {
            newProps.buttonType = "secondary";
          }
        }
        if (Object.keys(newProps).length > 0) {
          return cloneElement(child, newProps);
        }
      }
      return child;
    });
    return /* @__PURE__ */ jsx(
      StyledAIRating,
      {
        ref,
        className: clsx(AIRatingClasses.root, className),
        currentTheme: theme,
        role: "group",
        variant,
        ...props,
        children: enforcedChildren
      }
    );
  }
);
AIRating.displayName = "AI.Rating";

export { AIRating };
//# sourceMappingURL=ai-rating.js.map
