"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../../../theme/hooks/use-theme.cjs');
const aiRating_styles = require('./ai-rating.styles.cjs');

const AIRating = React.forwardRef(
  ({ className, children, variant = "default", ...props }, ref) => {
    const theme = useTheme.default();
    const enforcedChildren = React.Children.map(children, (child) => {
      if (React.isValidElement(child)) {
        const type = child.type;
        const props2 = child.props;
        const newProps = {};
        if (type?.displayName === "IconButton") {
          if (!props2.buttonType) {
            newProps.buttonType = "secondary";
          }
        }
        if (Object.keys(newProps).length > 0) {
          return React.cloneElement(child, newProps);
        }
      }
      return child;
    });
    return /* @__PURE__ */ jsxRuntime.jsx(
      aiRating_styles.StyledAIRating,
      {
        ref,
        className: clsx$1.clsx(aiRating_styles.AIRatingClasses.root, className),
        currentTheme: theme,
        role: "group",
        variant,
        ...props,
        children: enforcedChildren
      }
    );
  }
);
AIRating.displayName = "AI.Rating";

exports.AIRating = AIRating;
//# sourceMappingURL=ai-rating.cjs.map
