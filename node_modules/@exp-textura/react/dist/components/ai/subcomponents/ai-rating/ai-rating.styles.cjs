"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const styled = require('@emotion/styled');
const componentUtils = require('../../../../system/utils/component-utils.cjs');
const React = require('react');

const componentName = "AIRating";
const AIRatingClasses = {
  root: "exp-textura_ai_rating"
};
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = React.useMemo(
    () => componentUtils.getStyleSlots(componentName, theme, variant, styleSlotsProp),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "flex",
    alignItems: "center",
    gap: theme.toRem(theme.spacing[4]),
    ...styleSlots?.root
  };
};
const excludedDOMProps = componentUtils.getExcludedDOMProps();
const StyledAIRating = styled("div", {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));

exports.AIRatingClasses = AIRatingClasses;
exports.StyledAIRating = StyledAIRating;
//# sourceMappingURL=ai-rating.styles.cjs.map
