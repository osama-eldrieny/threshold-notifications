import { ComponentProps, Theme } from '../../../../types';
import { CSSObject } from '@emotion/react';
import { ReactNode } from 'react';
export interface AIMessageProps extends ComponentProps {
    role?: "user" | "assistant";
    children?: ReactNode;
    currentTheme?: Theme;
    variant?: string;
    plainText?: boolean;
    styleSlots?: {
        root?: CSSObject;
        messageUser?: CSSObject;
        messageAssistant?: CSSObject;
        markdown?: CSSObject;
        plainText?: CSSObject;
    };
}
export interface AIMessageListProps extends ComponentProps {
    children?: ReactNode;
    scrollBehavior?: "auto" | "smooth" | "instant";
    autoScroll?: boolean;
    styleSlots?: {
        root?: CSSObject;
    };
}
export interface AIMarkdownProps extends ComponentProps {
    children?: ReactNode;
}
//# sourceMappingURL=ai-message.types.d.ts.map