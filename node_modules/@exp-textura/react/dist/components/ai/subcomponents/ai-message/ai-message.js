"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef, Children, isValidElement, useImperativeHandle } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../../../theme/hooks/use-theme.js';
import { useChatScroll } from '../../hooks/use-chat-scroll.js';
import { AIMessageClasses, StyledAIMessage, AIMessageListClasses, StyledAIMessageList } from './ai-message.styles.js';
import { Markdown } from '../../../../vendor/.pnpm/react-markdown@10.1.0_@types_react@18.3.28_react@19.2.5/node_modules/react-markdown/lib/index.js';

const AIMessage = forwardRef(
  ({ className, role = "assistant", children, variant = "default", plainText = false, ...props }, ref) => {
    const theme = useTexturaTheme();
    const isUser = role === "user";
    const roleClass = isUser ? AIMessageClasses.messageUser : AIMessageClasses.messageAssistant;
    const rootClasses = clsx(AIMessageClasses.root, roleClass, className);
    const processedChildren = [];
    let textBuffer = [];
    const flushBuffer = () => {
      if (textBuffer.length > 0) {
        const content = textBuffer.join("");
        if (plainText) {
          processedChildren.push(
            /* @__PURE__ */ jsx(
              "div",
              {
                className: AIMessageClasses.plainText,
                children: content
              },
              `auto-content-${processedChildren.length}`
            )
          );
        } else {
          processedChildren.push(
            /* @__PURE__ */ jsx(AIMarkdown, { children: content }, `auto-content-${processedChildren.length}`)
          );
        }
        textBuffer = [];
      }
    };
    Children.forEach(children, (child) => {
      if (typeof child === "string" || typeof child === "number") {
        textBuffer.push(child);
      } else if (isValidElement(child)) {
        flushBuffer();
        processedChildren.push(child);
      }
    });
    flushBuffer();
    return /* @__PURE__ */ jsx(
      StyledAIMessage,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        plainText,
        role,
        variant,
        ...props,
        children: processedChildren
      }
    );
  }
);
AIMessage.displayName = "AI.Message";
const AIMarkdown = forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsx("div", { ref, className: clsx(AIMessageClasses.markdown, className), ...props, children: typeof children === "string" ? /* @__PURE__ */ jsx(Markdown, { children }) : children });
  }
);
AIMarkdown.displayName = "AI.Markdown";
const AIMessageList = forwardRef(
  ({
    className,
    children,
    scrollBehavior = "smooth",
    autoScroll = true,
    variant = "default",
    role = "log",
    "aria-live": ariaLive = "polite",
    "aria-atomic": ariaAtomic = false,
    ...props
  }, forwardedRef) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(AIMessageListClasses.root, className);
    const {
      ref: scrollRef,
      onScroll,
      scrollToBottom
    } = useChatScroll(children, { enabled: autoScroll });
    useImperativeHandle(
      forwardedRef,
      () => Object.assign(scrollRef.current, {
        scrollToBottom
      })
    );
    return /* @__PURE__ */ jsx(
      StyledAIMessageList,
      {
        ref: scrollRef,
        "aria-atomic": ariaAtomic,
        "aria-live": ariaLive,
        className: rootClasses,
        currentTheme: theme,
        "data-scroll-behavior": scrollBehavior,
        role,
        tabIndex: 0,
        variant,
        onScroll,
        ...props,
        children
      }
    );
  }
);
AIMessageList.displayName = "AI.MessageList";

export { AIMarkdown, AIMessage, AIMessageList };
//# sourceMappingURL=ai-message.js.map
