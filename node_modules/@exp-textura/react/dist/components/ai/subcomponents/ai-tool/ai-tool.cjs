"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../../../theme/hooks/use-theme.cjs');
const icons = require('@exp-textura/icons');
const aiTool_styles = require('./ai-tool.styles.cjs');

const AIToolHeader = React.forwardRef(
  ({ className, children, onClick, expanded, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx(
      "button",
      {
        ref,
        "aria-expanded": expanded,
        className: clsx$1.clsx(aiTool_styles.AIToolClasses.header, className),
        type: "button",
        onClick,
        ...props,
        children
      }
    );
  }
);
AIToolHeader.displayName = "AI.ToolHeader";
const AIToolLabel = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx("span", { ref, className: clsx$1.clsx(aiTool_styles.AIToolClasses.headerContent, className), ...props, children });
  }
);
AIToolLabel.displayName = "AI.ToolLabel";
const AIToolIndicator = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx("span", { ref, className: clsx$1.clsx(aiTool_styles.AIToolClasses.icon, className), ...props, children: children || /* @__PURE__ */ jsxRuntime.jsx(icons.ChevronDown, {}) });
  }
);
AIToolIndicator.displayName = "AI.ToolIndicator";
const AIToolContent = React.forwardRef(
  ({ className, children, ...props }, ref) => {
    return /* @__PURE__ */ jsxRuntime.jsx("div", { ref, className: clsx$1.clsx(aiTool_styles.AIToolClasses.content, className), ...props, children });
  }
);
AIToolContent.displayName = "AI.ToolContent";
const AITool = React.forwardRef(
  ({
    className,
    expanded: expandedProp,
    onExpandedChange,
    children,
    variant = "default",
    ...props
  }, ref) => {
    const theme = useTheme.default(), rootClasses = clsx$1.clsx(aiTool_styles.AIToolClasses.root, className);
    const [internalExpanded, setInternalExpanded] = React.useState(props.defaultExpanded ?? false);
    const isControlled = expandedProp !== void 0;
    const isExpanded = isControlled ? expandedProp : internalExpanded;
    const contentRef = React.useRef(null);
    const innerRef = React.useRef(null);
    const handleToggle = () => {
      const newState = !isExpanded;
      if (!isControlled) {
        setInternalExpanded(newState);
      }
      onExpandedChange?.(newState);
    };
    React.useEffect(() => {
      const container = contentRef.current;
      const inner = innerRef.current;
      if (!container || !inner) return;
      const syncHeight = () => {
        if (isExpanded) {
          container.style.maxHeight = `${inner.scrollHeight}px`;
        } else {
          container.style.maxHeight = "0px";
        }
      };
      syncHeight();
      const observer = new ResizeObserver(() => {
        if (isExpanded) {
          container.style.maxHeight = `${inner.scrollHeight}px`;
        }
      });
      observer.observe(inner);
      return () => {
        observer.disconnect();
      };
    }, [isExpanded]);
    const renderChildren = () => {
      let header = null;
      const content = [];
      React.Children.forEach(children, (child) => {
        if (!React.isValidElement(child)) {
          content.push(child);
          return;
        }
        if (child.type.displayName === "AI.ToolHeader" || child.type === AIToolHeader) {
          header = child;
        } else {
          content.push(child);
        }
      });
      const headerElement = header ? React.cloneElement(header, {
        onClick: (e) => {
          handleToggle();
          header?.props.onClick?.(e);
        },
        expanded: isExpanded
      }) : null;
      if (!headerElement) {
        return /* @__PURE__ */ jsxRuntime.jsx(jsxRuntime.Fragment, { children: content });
      }
      return /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
        headerElement,
        /* @__PURE__ */ jsxRuntime.jsx(
          "div",
          {
            ref: contentRef,
            "aria-hidden": !isExpanded,
            className: aiTool_styles.AIToolClasses.body,
            "data-expanded": isExpanded,
            children: /* @__PURE__ */ jsxRuntime.jsx("div", { ref: innerRef, children: content })
          }
        )
      ] });
    };
    return /* @__PURE__ */ jsxRuntime.jsx(
      aiTool_styles.StyledAITool,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        "data-expanded": isExpanded,
        variant,
        ...props,
        children: renderChildren()
      }
    );
  }
);
AITool.displayName = "AI.Tool";

exports.AITool = AITool;
exports.AIToolContent = AIToolContent;
exports.AIToolHeader = AIToolHeader;
exports.AIToolIndicator = AIToolIndicator;
exports.AIToolLabel = AIToolLabel;
//# sourceMappingURL=ai-tool.cjs.map
