"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../../../theme/hooks/use-theme.js';
import { AIFooterClasses, StyledAIFooter } from './ai-footer.styles.js';

const AIFooter = forwardRef(
  ({ className, children, variant = "default", ...props }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(AIFooterClasses.root, className);
    return /* @__PURE__ */ jsx(
      StyledAIFooter,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        variant,
        ...props,
        children
      }
    );
  }
);
AIFooter.displayName = "AI.Footer";

export { AIFooter };
//# sourceMappingURL=ai-footer.js.map
