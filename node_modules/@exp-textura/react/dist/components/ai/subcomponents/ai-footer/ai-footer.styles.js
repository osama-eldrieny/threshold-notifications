"use client";
import styled from '@emotion/styled';
import { getExcludedDOMProps, getStyleSlots } from '../../../../system/utils/component-utils.js';
import { useMemo } from 'react';

const componentName = "AIFooter";
const AIFooterClasses = {
  root: "exp-textura_ai_footer"
};
const generateStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = useMemo(
    () => getStyleSlots(
      componentName,
      theme,
      variant,
      styleSlotsProp
      // getVariantConfig(theme),
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    marginTop: "auto",
    padding: `${theme.toRem(0)} ${theme.toRem(theme.spacing[8])} ${theme.toRem(theme.spacing[8])}`,
    width: "100%",
    ...styleSlots?.root
  };
};
const excludedDOMProps = getExcludedDOMProps();
const StyledAIFooter = styled("div", {
  shouldForwardProp: (prop) => !excludedDOMProps.includes(prop)
})((props) => generateStyles(props));

export { AIFooterClasses, StyledAIFooter };
//# sourceMappingURL=ai-footer.styles.js.map
