"use client";
import { jsxs, jsx } from 'react/jsx-runtime';
import { forwardRef, Children, isValidElement, cloneElement } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../../../theme/hooks/use-theme.js';
import CircleProgress from '../../../circle-progress/circle-progress.js';
import { SelectCircleArea, AttentionCircleSolid, CheckCircleSolid } from '@exp-textura/icons';
import { AITaskClasses, StyledAITask, AITaskListClasses, StyledAITaskList } from './ai-task.styles.js';

const AITask = forwardRef(
  ({ className, children, state = "pending", variant = "default", styleSlots, ...props }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(AITaskClasses.task, className);
    const renderIcon = () => {
      switch (state) {
        case "loading":
          return /* @__PURE__ */ jsx(
            CircleProgress,
            {
              styleSlots: {
                indeterminate: {
                  width: theme.toRem(16),
                  height: theme.toRem(16),
                  ...styleSlots?.loader
                },
                indeterminateCircle: {
                  strokeWidth: theme.toRem(4),
                  ...styleSlots?.loaderCircle
                }
              }
            }
          );
        case "completed":
          return /* @__PURE__ */ jsx(CheckCircleSolid, {});
        case "error":
          return /* @__PURE__ */ jsx(AttentionCircleSolid, {});
        case "pending":
        default:
          return /* @__PURE__ */ jsx(SelectCircleArea, {});
      }
    };
    return /* @__PURE__ */ jsxs(
      StyledAITask,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        "data-state": state,
        state,
        styleSlots,
        variant,
        ...props,
        children: [
          /* @__PURE__ */ jsx("div", { "aria-hidden": "true", className: AITaskClasses.icon, "data-state": state, children: renderIcon() }),
          /* @__PURE__ */ jsx("div", { children })
        ]
      }
    );
  }
);
AITask.displayName = "AI.Task";
const AITaskList = forwardRef(
  ({ className, children, variant = "default", ...props }, ref) => {
    const theme = useTexturaTheme();
    const rootClasses = clsx(AITaskListClasses.list, className);
    const childrenWithProps = Children.map(children, (child) => {
      if (isValidElement(child)) {
        return cloneElement(child, { role: "listitem" });
      }
      return child;
    });
    return /* @__PURE__ */ jsx(
      StyledAITaskList,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        role: "list",
        variant,
        ...props,
        children: childrenWithProps
      }
    );
  }
);
AITaskList.displayName = "AI.TaskList";

export { AITask, AITaskList };
//# sourceMappingURL=ai-task.js.map
