"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const styled = require('@emotion/styled');
const componentUtils = require('../../../../system/utils/component-utils.cjs');
const React = require('react');

const taskComponentName = "AITask";
const AITaskClasses = {
  task: "exp-textura_ai_task",
  icon: "exp-textura_ai_task_icon"
};
const generateTaskStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = React.useMemo(
    () => componentUtils.getStyleSlots(taskComponentName, theme, variant, styleSlotsProp),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "flex",
    alignItems: "center",
    gap: theme.toRem(theme.spacing[8]),
    ...theme.typography.body.md,
    color: theme.color.content.strong,
    [`.${AITaskClasses.icon}`]: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      width: theme.toRem(16),
      height: theme.toRem(16),
      color: theme.color.content.weak,
      flexShrink: 0,
      fontSize: theme.toRem(16),
      borderRadius: "50%",
      '&[data-state="pending"]': {
        ...styleSlots?.iconPending
      },
      '&[data-state="completed"]': {
        color: theme.color.background.utility.success,
        ...styleSlots?.iconCompleted
      },
      '&[data-state="loading"]': {
        ...styleSlots?.iconLoading
      },
      '&[data-state="error"]': {
        color: theme.color.background.utility.critical,
        ...styleSlots?.iconError
      },
      ...styleSlots?.icon
    },
    ...styleSlots?.root
  };
};
const StyledAITask = styled("div", {
  shouldForwardProp: (prop) => !componentUtils.getExcludedDOMProps().includes(prop)
})((props) => generateTaskStyles(props));
const taskListComponentName = "AITaskList";
const AITaskListClasses = {
  list: "exp-textura_ai_task_list"
};
const generateTaskListStyles = (props) => {
  const { currentTheme: theme, variant, styleSlots: styleSlotsProp } = props;
  if (!theme) return {};
  const styleSlots = React.useMemo(
    () => componentUtils.getStyleSlots(
      taskListComponentName,
      theme,
      variant,
      styleSlotsProp
    ),
    [theme, variant, styleSlotsProp]
  );
  return {
    display: "flex",
    flexDirection: "column",
    gap: theme.toRem(theme.spacing[8]),
    ...styleSlots?.root
  };
};
const StyledAITaskList = styled("div", {
  shouldForwardProp: (prop) => !componentUtils.getExcludedDOMProps().includes(prop)
})((props) => generateTaskListStyles(props));

exports.AITaskClasses = AITaskClasses;
exports.AITaskListClasses = AITaskListClasses;
exports.StyledAITask = StyledAITask;
exports.StyledAITaskList = StyledAITaskList;
//# sourceMappingURL=ai-task.styles.cjs.map
