import { ComponentProps } from '../../../../types';
import { CSSObject } from '@emotion/react';
import { ReactNode } from 'react';
export type AITaskState = "pending" | "loading" | "completed" | "error";
export interface AITaskProps extends ComponentProps {
    /**
     * The state of the task.
     * @default "pending"
     */
    state?: AITaskState;
    children?: ReactNode;
    styleSlots?: {
        root?: CSSObject;
        icon?: CSSObject;
        iconPending?: CSSObject;
        iconLoading?: CSSObject;
        iconCompleted?: CSSObject;
        iconError?: CSSObject;
        loader?: CSSObject;
        loaderCircle?: CSSObject;
    };
}
export interface AITaskListProps extends ComponentProps {
    children?: ReactNode;
    styleSlots?: {
        root?: CSSObject;
    };
}
//# sourceMappingURL=ai-task.types.d.ts.map