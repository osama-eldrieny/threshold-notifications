"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../../../theme/hooks/use-theme.cjs');
const circleProgress = require('../../../circle-progress/circle-progress.cjs');
const icons = require('@exp-textura/icons');
const aiTask_styles = require('./ai-task.styles.cjs');

const AITask = React.forwardRef(
  ({ className, children, state = "pending", variant = "default", styleSlots, ...props }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(aiTask_styles.AITaskClasses.task, className);
    const renderIcon = () => {
      switch (state) {
        case "loading":
          return /* @__PURE__ */ jsxRuntime.jsx(
            circleProgress.default,
            {
              styleSlots: {
                indeterminate: {
                  width: theme.toRem(16),
                  height: theme.toRem(16),
                  ...styleSlots?.loader
                },
                indeterminateCircle: {
                  strokeWidth: theme.toRem(4),
                  ...styleSlots?.loaderCircle
                }
              }
            }
          );
        case "completed":
          return /* @__PURE__ */ jsxRuntime.jsx(icons.CheckCircleSolid, {});
        case "error":
          return /* @__PURE__ */ jsxRuntime.jsx(icons.AttentionCircleSolid, {});
        case "pending":
        default:
          return /* @__PURE__ */ jsxRuntime.jsx(icons.SelectCircleArea, {});
      }
    };
    return /* @__PURE__ */ jsxRuntime.jsxs(
      aiTask_styles.StyledAITask,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        "data-state": state,
        state,
        styleSlots,
        variant,
        ...props,
        children: [
          /* @__PURE__ */ jsxRuntime.jsx("div", { "aria-hidden": "true", className: aiTask_styles.AITaskClasses.icon, "data-state": state, children: renderIcon() }),
          /* @__PURE__ */ jsxRuntime.jsx("div", { children })
        ]
      }
    );
  }
);
AITask.displayName = "AI.Task";
const AITaskList = React.forwardRef(
  ({ className, children, variant = "default", ...props }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(aiTask_styles.AITaskListClasses.list, className);
    const childrenWithProps = React.Children.map(children, (child) => {
      if (React.isValidElement(child)) {
        return React.cloneElement(child, { role: "listitem" });
      }
      return child;
    });
    return /* @__PURE__ */ jsxRuntime.jsx(
      aiTask_styles.StyledAITaskList,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        role: "list",
        variant,
        ...props,
        children: childrenWithProps
      }
    );
  }
);
AITaskList.displayName = "AI.TaskList";

exports.AITask = AITask;
exports.AITaskList = AITaskList;
//# sourceMappingURL=ai-task.cjs.map
