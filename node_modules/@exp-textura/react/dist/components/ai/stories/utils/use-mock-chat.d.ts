export interface Message {
    id: string;
    role: "system" | "user" | "assistant" | "data";
    content: string;
    parts?: {
        type: "text";
        text: string;
    }[];
}
type UseMockChatOptions = {
    initialMessages?: Message[];
    onFinish?: (message: Message) => void;
};
export declare function useMockChat(options?: UseMockChatOptions): {
    messages: Message[];
    status: "ready" | "submitted" | "streaming";
    stop: () => void;
    reset: () => void;
    sendMessage: (userMessage: {
        role: "user";
        parts: {
            type: "text";
            text: string;
        }[];
    }) => Promise<void>;
    data: unknown[];
};
export {};
//# sourceMappingURL=use-mock-chat.d.ts.map