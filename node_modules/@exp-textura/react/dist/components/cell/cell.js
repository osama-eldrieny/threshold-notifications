"use client";
import { jsx, jsxs } from 'react/jsx-runtime';
import React, { forwardRef, cloneElement } from 'react';
import { clsx } from 'clsx';
import '@emotion/react';
import useTexturaTheme from '../../theme/hooks/use-theme.js';
import Avatar from '../avatar/avatar.js';
import AssetIcon from '../asset-icon/asset-icon.js';
import Badge from '../badge/badge.js';
import InlineBadge from '../inline-badge/inline-badge.js';
import Button from '../button/button.js';
import Chip from '../chip/chip.js';
import IconButton from '../icon-button/icon-button.js';
import InfoBlock from '../info-block/info-block.js';
import SvgIcon from '../svg-icon/svg-icon.js';
import { ArrowLineDownCurveRight, ChevronRight, ChevronDown } from '@exp-textura/icons';
import { StyledCell, CellClasses } from './cell.styles.js';

const CellRoot = forwardRef(
  ({ className, variant = "default", ...props }, ref) => {
    const theme = useTexturaTheme();
    return /* @__PURE__ */ jsx(
      StyledCell,
      {
        ref,
        className: clsx(CellClasses.root, className),
        currentTheme: theme,
        variant,
        ...props
      }
    );
  }
);
CellRoot.displayName = "Cell";
const AssetIconCell = ({ children, className, style, ...props }) => {
  const theme = useTexturaTheme();
  return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsx(AssetIcon, { className: CellClasses.assetIcon, size: "md", ...props, children }) });
};
AssetIconCell.displayName = "Cell.AssetIcon";
const AvatarCell = ({ children, className, style, ...props }) => {
  const theme = useTexturaTheme();
  return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsx(Avatar, { className: CellClasses.avatar, size: "sm", ...props, children }) });
};
AvatarCell.displayName = "Cell.Avatar";
const BadgeCell = ({ children, className, style, ...props }) => {
  const theme = useTexturaTheme();
  return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsx(Badge, { className: CellClasses.badge, type: "roomy", ...props, children }) });
};
BadgeCell.displayName = "Cell.Badge";
const InlineBadgeCell = ({ children, className, style, ...props }) => {
  const theme = useTexturaTheme();
  return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsx(InlineBadge, { className: CellClasses.inlineBadge, ...props, children }) });
};
InlineBadgeCell.displayName = "Cell.InlineBadge";
const ButtonCell = ({ children, className, style, ...props }) => {
  const theme = useTexturaTheme();
  return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsx(Button, { buttonType: "secondary", className: CellClasses.button, size: "sm", ...props, children }) });
};
ButtonCell.displayName = "Cell.Button";
const ChipCell = ({ children, className, style, ...props }) => {
  const theme = useTexturaTheme();
  return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsx(Chip, { className: CellClasses.chip, ...props, children }) });
};
ChipCell.displayName = "Cell.Chip";
const IconButtonCell = ({ children, className, style, ...props }) => {
  const theme = useTexturaTheme();
  return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsx(IconButton, { buttonType: "secondary", className: CellClasses.iconButton, size: "sm", ...props, children }) });
};
IconButtonCell.displayName = "Cell.IconButton";
const InfoBlockCell = ({ children, className, style, ...props }) => {
  const theme = useTexturaTheme();
  const newChildren = React.Children.map(children, (child) => {
    if (React.isValidElement(child) && child.type.displayName === "InfoBlock.Visual") {
      const visualChildren = React.Children.map(
        child.props.children,
        (visualChild) => {
          if (React.isValidElement(visualChild) && visualChild.type.displayName === "AssetIcon") {
            return cloneElement(visualChild, {
              size: visualChild.props.size || "md"
            });
          } else if (React.isValidElement(visualChild) && visualChild.type.displayName === "Avatar") {
            return cloneElement(visualChild, {
              size: visualChild.props.size || "sm"
            });
          }
          return visualChild;
        }
      );
      return cloneElement(child, { children: visualChildren });
    }
    return child;
  });
  return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsx(InfoBlock, { className: CellClasses.infoBlock, ...props, children: newChildren }) });
};
InfoBlockCell.displayName = "Cell.InfoBlock";
const Link = forwardRef(
  ({ children, className, style, as, ...props }, ref) => {
    const theme = useTexturaTheme();
    const Component = as || "a";
    return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsx(Component, { ref, className: CellClasses.link, ...props, children }) });
  }
);
Link.displayName = "Cell.Link";
const Number = ({ children, className, style, ...props }) => {
  const theme = useTexturaTheme();
  return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsx("div", { className: CellClasses.number, ...props, children }) });
};
Number.displayName = "Cell.Number";
const SvgIconCell = ({ children, className, style, ...props }) => {
  const theme = useTexturaTheme();
  return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsx(SvgIcon, { className: CellClasses.svgIcon, fontSize: theme.toRem(24), ...props, children }) });
};
SvgIconCell.displayName = "Cell.SvgIcon";
const SubRowIndicatorCell = ({
  children,
  className,
  style,
  icon = /* @__PURE__ */ jsx(ArrowLineDownCurveRight, {})
}) => {
  const theme = useTexturaTheme();
  return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsxs("span", { className: CellClasses.subRowIndicator, children: [
    /* @__PURE__ */ jsx("span", { "aria-hidden": "true", children: icon }),
    children
  ] }) });
};
SubRowIndicatorCell.displayName = "Cell.SubRowIndicator";
const ExpandToggleCell = ({
  isExpanded,
  canExpand,
  onToggle,
  children,
  className,
  style,
  collapsedIcon = /* @__PURE__ */ jsx(ChevronRight, {}),
  expandedIcon = /* @__PURE__ */ jsx(ChevronDown, {}),
  expandAriaLabel = "Expand row",
  collapseAriaLabel = "Collapse row"
}) => {
  const theme = useTexturaTheme();
  if (!canExpand) {
    return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsx("span", { className: CellClasses.expandToggle, children }) });
  }
  return /* @__PURE__ */ jsx(StyledCell, { className: clsx(CellClasses.root, className), currentTheme: theme, style, children: /* @__PURE__ */ jsxs("span", { className: CellClasses.expandToggle, children: [
    /* @__PURE__ */ jsx(
      "button",
      {
        "aria-expanded": isExpanded,
        "aria-label": isExpanded ? collapseAriaLabel : expandAriaLabel,
        tabIndex: -1,
        type: "button",
        onClick: (e) => {
          e.stopPropagation();
          onToggle();
        },
        children: isExpanded ? expandedIcon : collapsedIcon
      }
    ),
    children
  ] }) });
};
ExpandToggleCell.displayName = "Cell.ExpandToggle";
const Cell = Object.assign(CellRoot, {
  AssetIcon: AssetIconCell,
  Avatar: AvatarCell,
  Badge: BadgeCell,
  InlineBadge: InlineBadgeCell,
  Button: ButtonCell,
  Chip: ChipCell,
  IconButton: IconButtonCell,
  InfoBlock: InfoBlockCell,
  Link,
  Number,
  SvgIcon: SvgIconCell,
  ExpandToggle: ExpandToggleCell,
  SubRowIndicator: SubRowIndicatorCell
});

export { Cell as default };
//# sourceMappingURL=cell.js.map
