"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const RadixRadioGroup = require('@radix-ui/react-radio-group');
const componentUtils = require('../../system/utils/component-utils.cjs');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const radioGroup_styles = require('./radio-group.styles.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const RadixRadioGroup__namespace = /*#__PURE__*/_interopNamespaceDefault(RadixRadioGroup);

const RadioGroupRoot = React.forwardRef(
  ({
    className,
    defaultValue,
    value,
    onValueChange,
    variant = "default",
    required = false,
    disabled = false,
    orientation = "vertical",
    size = "default",
    children,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const rootClasses = clsx$1.clsx(radioGroup_styles.RadioGroupClasses.root, className, {
      [radioGroup_styles.RadioGroupClasses.sm]: size === "sm"
    });
    const handleValueChange = React.useCallback(
      (newValue) => {
        onValueChange?.(newValue);
      },
      [onValueChange]
    );
    return /* @__PURE__ */ jsxRuntime.jsx(
      radioGroup_styles.StyledRadioGroup,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        defaultValue,
        disabled,
        orientation,
        required,
        variant,
        ...props,
        "aria-required": required ? "true" : "false",
        value,
        onValueChange: handleValueChange,
        children: React.Children.map(children, (child) => {
          if (!React.isValidElement(child)) {
            return null;
          }
          return React.cloneElement(child, {
            disabled: disabled || child.props.disabled || void 0
          });
        })
      }
    );
  }
);
RadioGroupRoot.displayName = "RadioGroup";
const Item = React.forwardRef(({ className, disabled, label, ...props }, ref) => {
  const uniqueId = componentUtils.useUniqueId("radio-group-item");
  const id = props.id ?? uniqueId;
  return /* @__PURE__ */ jsxRuntime.jsxs("div", { className: clsx$1.clsx(radioGroup_styles.RadioGroupClasses.itemWrapper), children: [
    /* @__PURE__ */ jsxRuntime.jsx(
      RadixRadioGroup__namespace.Item,
      {
        ref,
        className: clsx$1.clsx(radioGroup_styles.RadioGroupClasses.item, className),
        role: "radio",
        ...props,
        disabled,
        id,
        children: /* @__PURE__ */ jsxRuntime.jsx(RadixRadioGroup__namespace.Indicator, { className: radioGroup_styles.RadioGroupClasses.indicator })
      }
    ),
    /* @__PURE__ */ jsxRuntime.jsx("label", { className: radioGroup_styles.RadioGroupClasses.itemLabel, "data-disabled": disabled, htmlFor: id, children: label })
  ] });
});
Item.displayName = "RadioGroup.Item";
const RadioGroup = Object.assign(RadioGroupRoot, {
  Item
});

exports.default = RadioGroup;
//# sourceMappingURL=radio-group.cjs.map
