"use client";
'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
require('@emotion/react');
const useTheme = require('../../theme/hooks/use-theme.cjs');
const icons = require('@exp-textura/icons');
const chip_styles = require('./chip.styles.cjs');

const ChipContext = React.createContext(null);
const useChipContext = () => {
  const context = React.useContext(ChipContext);
  if (!context) {
    throw new Error("Chip compound components cannot be rendered outside the Chip component");
  }
  return context;
};
const ChipRoot = React.forwardRef(
  ({
    className,
    variant = "default",
    type = "subtle",
    size = "default",
    onClose,
    show: showProp = true,
    isOverflow = false,
    hasStartAdornment = false,
    children,
    ...props
  }, ref) => {
    const theme = useTheme.default();
    const [show, setShow] = React.useState(showProp);
    const rootClasses = clsx$1.clsx(
      chip_styles.ChipClasses.root,
      {
        [chip_styles.ChipClasses.sm]: size === "sm",
        [chip_styles.ChipClasses.outline]: type === "outline",
        [chip_styles.ChipClasses.overflow]: isOverflow,
        [chip_styles.ChipClasses.hasStartAdornment]: hasStartAdornment
      },
      className
    );
    const handleClose = React.useCallback(() => {
      setShow(false);
      onClose?.();
    }, [onClose]);
    React.useEffect(() => {
      setShow(showProp);
      if (!showProp) {
        onClose?.();
      }
    }, [onClose, showProp]);
    if (!show) return null;
    const contextValue = {
      handleClose,
      type
    };
    return /* @__PURE__ */ jsxRuntime.jsx(ChipContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsxRuntime.jsx(
      chip_styles.StyledChip,
      {
        ref,
        className: rootClasses,
        currentTheme: theme,
        role: isOverflow ? "button" : void 0,
        tabIndex: isOverflow ? 0 : void 0,
        variant,
        ...props,
        children
      }
    ) });
  }
);
ChipRoot.displayName = "Chip";
const CloseButton = ({
  children,
  className,
  onClick,
  "aria-label": customAriaLabel,
  ...props
}) => {
  const { handleClose, type } = useChipContext();
  const buttonClasses = clsx$1.clsx(
    chip_styles.ChipClasses.closeButton,
    {
      [chip_styles.ChipClasses.closeButtonOutline]: type === "outline"
    },
    className
  );
  const commonProps = {
    "aria-label": customAriaLabel || "close current chip",
    role: "button",
    onClick: (e) => {
      onClick?.(e);
      handleClose();
    },
    tabIndex: 0,
    onKeyDown: (e) => {
      if (e.key === "Enter" || e.key === " ") {
        e.preventDefault();
        handleClose();
      }
    }
  };
  if (children && React.isValidElement(children)) {
    return React.cloneElement(children, {
      className: buttonClasses,
      ...commonProps,
      ...props,
      "aria-hidden": false
    });
  }
  return /* @__PURE__ */ jsxRuntime.jsx("span", { className: buttonClasses, ...commonProps, ...props, children: /* @__PURE__ */ jsxRuntime.jsx(icons.Clear, {}) });
};
CloseButton.displayName = "Chip.CloseButton";
const Chip = Object.assign(ChipRoot, {
  CloseButton
});

exports.default = Chip;
//# sourceMappingURL=chip.cjs.map
