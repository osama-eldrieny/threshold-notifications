"use client";
import { jsx } from 'react/jsx-runtime';
import InlineBadge from '../../inline-badge/inline-badge.js';
import { clsx } from 'clsx';
import { MainNavClasses } from '../main-nav.styles.js';

const MainNavInlineBadge = ({ children, className, ...props }) => {
  return /* @__PURE__ */ jsx(
    InlineBadge,
    {
      className: clsx(MainNavClasses.itemInlineBadge, className),
      size: "sm",
      styleSlots: {
        root: {
          marginInlineStart: "auto"
        }
      },
      variant: "newInverse",
      ...props,
      children
    }
  );
};
MainNavInlineBadge.displayName = "MainNav.InlineBadge";

export { MainNavInlineBadge };
//# sourceMappingURL=main-nav-inline-badge.js.map
