"use client";
import { jsx, Fragment } from 'react/jsx-runtime';
import React, { useState, useRef, useEffect } from 'react';
import Menu from '../../menu/menu.js';
import Accordion from '../../accordion/accordion.js';
import { clsx } from 'clsx';
import { useMainNav, MainNavItemParentContext } from '../hooks/use-main-nav.js';
import { MainNavClasses } from '../main-nav.styles.js';
import { MainNavItem } from './main-nav-items.js';
import { MainNavUser } from './main-nav-user.js';

const MainNavMenuRoot = ({
  disableAccordion = false,
  children,
  ...props
}) => {
  const { collapsed, theme } = useMainNav();
  const [menuOpen, setMenuOpen] = useState(false);
  const hasCurrentItem = (children2) => {
    return React.Children.toArray(children2).some((child) => {
      if (!React.isValidElement(child)) {
        return false;
      }
      if (child.type.displayName === MainNavItem.displayName && child.props.isCurrent) {
        return true;
      }
      if (child.props.children) {
        return hasCurrentItem(child.props.children);
      }
      return false;
    });
  };
  const content = React.Children.toArray(children).find(
    (child) => React.isValidElement(child) && child.type.displayName === MainNavMenuContent.displayName
  );
  const isContentCurrent = content ? hasCurrentItem(content) : false;
  const mapToMenuChildren = (children2) => {
    return React.Children.map(children2, (child) => {
      if (!React.isValidElement(child)) {
        return child;
      }
      const displayName = child.type.displayName;
      switch (displayName) {
        case MainNavMenuTrigger.displayName: {
          const trigger = child.props.children;
          const triggerDisplayName = trigger.type?.displayName;
          const isNavItemTrigger = React.isValidElement(trigger) && (triggerDisplayName === MainNavItem.displayName || triggerDisplayName === MainNavUser.displayName);
          if (isNavItemTrigger) {
            const clonedTrigger = React.cloneElement(
              trigger,
              {
                disableTooltip: menuOpen || trigger.props.disableTooltip
              }
            );
            return /* @__PURE__ */ jsx(
              Menu.Trigger,
              {
                ...child.props,
                "data-main-nav-item-current": isContentCurrent || child.props.isCurrent,
                children: clonedTrigger
              }
            );
          }
          return /* @__PURE__ */ jsx(Menu.Trigger, { ...child.props, children: trigger });
        }
        case MainNavMenuContent.displayName:
          return /* @__PURE__ */ jsx(
            Menu.Content,
            {
              sideOffset: collapsed ? theme.toPx("0.86rem") : theme.toPx("1.14rem"),
              ...child.props,
              children: mapToMenuChildren(child.props.children)
            }
          );
        case MainNavMenuGroup.displayName:
          return /* @__PURE__ */ jsx(Menu.Group, { ...child.props, children: mapToMenuChildren(child.props.children) });
        case MainNavMenuLabel.displayName:
          return /* @__PURE__ */ jsx(Menu.Label, { ...child.props, children: child.props.children });
        case MainNavItem.displayName:
          return /* @__PURE__ */ jsx(Menu.Item, { asChild: true, "data-main-nav-item-current": child.props.isCurrent, children: React.cloneElement(child, { ...child.props, disableTooltip: true }) });
        case MainNavMenuSub.displayName:
          return /* @__PURE__ */ jsx(Menu.Sub, { ...child.props, children: mapToMenuChildren(child.props.children) });
        case MainNavMenuSubTrigger.displayName: {
          const visualTriggerElement = child.props.children;
          if (React.isValidElement(visualTriggerElement) && visualTriggerElement.type?.displayName === MainNavItem.displayName) {
            const itemElement = visualTriggerElement;
            const { children: itemChildren, ...itemProps } = itemElement.props;
            return /* @__PURE__ */ jsx(
              Menu.SubTrigger,
              {
                "data-main-nav-item-current": isContentCurrent || child.props.isCurrent,
                ...child.props,
                ...itemProps,
                children: itemChildren
              }
            );
          }
          return /* @__PURE__ */ jsx(Menu.SubTrigger, { ...child.props, children: mapToMenuChildren(visualTriggerElement) });
        }
        case MainNavMenuSubContent.displayName:
          return /* @__PURE__ */ jsx(Menu.SubContent, { sideOffset: theme.toPx("0.86rem"), ...child.props, children: mapToMenuChildren(child.props.children) });
        default:
          return child;
      }
    });
  };
  const getTransformedChildrenForMenu = (children2) => {
    const transformedChildren = React.Children.map(children2, (innerChildNode) => {
      if (React.isValidElement(innerChildNode) && innerChildNode.props.as === "button") {
        return React.cloneElement(innerChildNode, {
          as: "div",
          className: clsx(MainNavClasses.item, innerChildNode.props.className),
          "data-main-nav-item-current": isContentCurrent || innerChildNode.props.isCurrent,
          disableTooltip: true,
          role: void 0,
          tabIndex: -1
        });
      }
      return innerChildNode;
    });
    return transformedChildren;
  };
  const mapToAccordionChildren = (children2) => {
    return React.Children.map(children2, (child) => {
      if (!React.isValidElement(child)) {
        return child;
      }
      const displayName = child.type.displayName;
      switch (displayName) {
        case MainNavMenuTrigger.displayName:
          return /* @__PURE__ */ jsx(Accordion.Header, { ...child.props, children: getTransformedChildrenForMenu(child.props.children) });
        case MainNavMenuContent.displayName:
          return /* @__PURE__ */ jsx(Accordion.Content, { ...child.props, children: mapToAccordionChildren(child.props.children) });
        case MainNavMenuGroup.displayName:
          return React.Children.map(child.props.children, (groupChild) => {
            if (React.isValidElement(groupChild) && groupChild.type.displayName === MainNavMenuLabel.displayName) {
              const typedGroupChild = groupChild;
              return /* @__PURE__ */ jsx("span", { children: typedGroupChild.props.children });
            }
            return groupChild;
          });
        case MainNavItem.displayName:
          return child;
        case MainNavMenuSub.displayName:
          return /* @__PURE__ */ jsx(MainNavItemParentContext.Provider, { value: "menu", children: /* @__PURE__ */ jsx(Menu, { ...props, alignOffset: theme.toPx("-0.286rem"), side: "end", children: mapToAccordionChildren(child.props.children) }) });
        case MainNavMenuSubTrigger.displayName:
          return /* @__PURE__ */ jsx(Menu.Trigger, { ...child.props, children: child.props.children });
        case MainNavMenuSubContent.displayName:
          return /* @__PURE__ */ jsx(Menu.Content, { sideOffset: theme.toPx("1.72rem"), ...child.props, children: mapToMenuChildren(child.props.children) });
        default:
          return child;
      }
    });
  };
  const menuChildren = mapToMenuChildren(children);
  const accordionChildren = mapToAccordionChildren(children);
  if (collapsed || disableAccordion) {
    const menuProps = props;
    return /* @__PURE__ */ jsx(MainNavItemParentContext.Provider, { value: "menu", children: /* @__PURE__ */ jsx(
      Menu,
      {
        ...menuProps,
        alignOffset: theme.toPx("-0.286rem"),
        side: "end",
        onOpenChange: (open) => {
          setMenuOpen(open);
          menuProps.onOpenChange?.(open);
        },
        children: menuChildren
      }
    ) });
  } else {
    return /* @__PURE__ */ jsx(MainNavItemParentContext.Provider, { value: "accordion", children: /* @__PURE__ */ jsx(Accordion, { ...props, children: /* @__PURE__ */ jsx(AccordionSectionWithScroll, { children: accordionChildren }) }) });
  }
};
MainNavMenuRoot.displayName = "MainNav.Menu";
const MainNavMenuTrigger = ({ children }) => /* @__PURE__ */ jsx(Fragment, { children });
MainNavMenuTrigger.displayName = "MainNav.MenuTrigger";
const MainNavMenuContent = ({ children }) => /* @__PURE__ */ jsx(Fragment, { children });
MainNavMenuContent.displayName = "MainNav.MenuContent";
const MainNavMenuGroup = ({ children }) => /* @__PURE__ */ jsx(Fragment, { children });
MainNavMenuGroup.displayName = "MainNav.MenuGroup";
const MainNavMenuLabel = ({ children }) => /* @__PURE__ */ jsx(Fragment, { children });
MainNavMenuLabel.displayName = "MainNav.MenuLabel";
const MainNavMenuSub = ({ children }) => /* @__PURE__ */ jsx(Fragment, { children });
MainNavMenuSub.displayName = "MainNav.MenuSub";
const MainNavMenuSubTrigger = ({ children }) => /* @__PURE__ */ jsx(Fragment, { children });
MainNavMenuSubTrigger.displayName = "MainNav.MenuSubTrigger";
const MainNavMenuSubContent = ({ children }) => /* @__PURE__ */ jsx(Fragment, { children });
MainNavMenuSubContent.displayName = "MainNav.MenuSubContent";
const AccordionSectionWithScroll = ({ children, ...props }) => {
  const sectionRef = useRef(null);
  const { contentRef } = useMainNav();
  useEffect(() => {
    const sectionElement = sectionRef.current;
    if (sectionElement) {
      const handleScroll = () => {
        sectionElement.scrollIntoView({ behavior: "smooth", block: "nearest" });
      };
      const observer = new MutationObserver(() => {
        const isExpanded = sectionElement.getAttribute("data-expanded") === "true";
        if (isExpanded) {
          if (sectionElement) {
            sectionElement.addEventListener("transitionend", handleScroll, { once: true });
          }
        }
      });
      observer.observe(sectionElement, {
        attributes: true,
        attributeFilter: ["data-expanded"]
      });
      return () => {
        observer.disconnect();
        if (sectionElement) {
          sectionElement.removeEventListener("transitionend", handleScroll);
        }
      };
    }
  }, [contentRef]);
  return /* @__PURE__ */ jsx(Accordion.Section, { ref: sectionRef, ...props, children });
};
const MainNavMenu = Object.assign(MainNavMenuRoot, {
  Trigger: MainNavMenuTrigger,
  Content: MainNavMenuContent,
  Group: MainNavMenuGroup,
  Label: MainNavMenuLabel,
  Sub: MainNavMenuSub,
  SubTrigger: MainNavMenuSubTrigger,
  SubContent: MainNavMenuSubContent
});

export { MainNavMenu, MainNavMenuContent, MainNavMenuGroup, MainNavMenuLabel, MainNavMenuRoot, MainNavMenuSub, MainNavMenuSubContent, MainNavMenuSubTrigger, MainNavMenuTrigger };
//# sourceMappingURL=main-nav-menu.js.map
