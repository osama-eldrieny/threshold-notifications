"use client";
import { jsx } from 'react/jsx-runtime';
import { clsx } from 'clsx';
import { MainNavClasses } from '../main-nav.styles.js';
import { useMainNav } from '../hooks/use-main-nav.js';

const MainNavHeader = ({ className, style, children, ...props }) => {
  const { dataMainNavState } = useMainNav();
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: clsx(MainNavClasses.header, className),
      "data-main-nav-state": dataMainNavState,
      style,
      ...props,
      children
    }
  );
};
MainNavHeader.displayName = "MainNav.Header";
const MainNavContent = ({ className, style, children, ...props }) => {
  const { dataMainNavState, contentRef } = useMainNav();
  return /* @__PURE__ */ jsx(
    "div",
    {
      ref: contentRef,
      className: clsx(MainNavClasses.content, className),
      "data-main-nav-state": dataMainNavState,
      style,
      ...props,
      children
    }
  );
};
MainNavContent.displayName = "MainNav.Content";
const MainNavFooter = ({ className, style, children, ...props }) => {
  const { dataMainNavState } = useMainNav();
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: clsx(MainNavClasses.footer, className),
      "data-main-nav-state": dataMainNavState,
      style,
      ...props,
      children
    }
  );
};
MainNavFooter.displayName = "MainNav.Footer";

export { MainNavContent, MainNavFooter, MainNavHeader };
//# sourceMappingURL=main-nav-layout.js.map
