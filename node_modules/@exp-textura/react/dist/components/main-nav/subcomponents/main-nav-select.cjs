"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const select = require('../../select/select.cjs');
const React = require('react');
const useMainNav = require('../hooks/use-main-nav.cjs');

const MainNavSelect = ({ children }) => {
  const { collapsed, theme } = useMainNav.useMainNav();
  const selectChild = React.Children.only(children);
  if (!React.isValidElement(selectChild) || selectChild.type !== select.default) {
    return null;
  }
  const clonedSelect = React.cloneElement(selectChild, {
    ...selectChild.props,
    collisionPadding: 0,
    "data-main-nav-state": collapsed ? "collapsed" : "expanded",
    side: "right",
    sideOffset: collapsed ? theme.toPx("0.86rem") : theme.toPx("1.14rem")
  });
  return clonedSelect;
};
MainNavSelect.displayName = "MainNav.Select";

exports.MainNavSelect = MainNavSelect;
//# sourceMappingURL=main-nav-select.cjs.map
