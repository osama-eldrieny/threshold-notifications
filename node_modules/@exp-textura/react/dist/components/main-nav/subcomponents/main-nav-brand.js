"use client";
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import { Sidebar } from '@exp-textura/icons';
import { MainNavClasses } from '../main-nav.styles.js';
import { useMainNav } from '../hooks/use-main-nav.js';

const MainNavBrand = forwardRef(
  ({ className, brandImg, squareBrandImg, ...props }, ref) => {
    const { dataMainNavState, collapsed } = useMainNav();
    return /* @__PURE__ */ jsx(
      "a",
      {
        ref,
        className: clsx(MainNavClasses.brand, className),
        "data-main-nav-state": !brandImg ? "collapsed" : dataMainNavState,
        ...props,
        children: !collapsed && brandImg ? brandImg : squareBrandImg
      }
    );
  }
);
MainNavBrand.displayName = "MainNav.Brand";
const MainNavCollapseTrigger = forwardRef(({ className, children, onClick, ...props }, ref) => {
  const { dataMainNavState, collapsed, setIsCollapsed } = useMainNav();
  const handleClick = (e) => {
    onClick?.(e);
    setIsCollapsed(!collapsed);
  };
  return /* @__PURE__ */ jsx(
    "button",
    {
      ref,
      "aria-label": "toggle main-nav",
      className: clsx(MainNavClasses.collapseTrigger, className),
      "data-main-nav-state": dataMainNavState,
      onClick: handleClick,
      ...props,
      children: children || /* @__PURE__ */ jsx(Sidebar, {})
    }
  );
});
MainNavCollapseTrigger.displayName = "MainNav.CollapseTrigger";

export { MainNavBrand, MainNavCollapseTrigger };
//# sourceMappingURL=main-nav-brand.js.map
