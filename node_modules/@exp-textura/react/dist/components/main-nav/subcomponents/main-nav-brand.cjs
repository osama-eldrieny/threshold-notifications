"use client";
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const clsx$1 = require('clsx');
const icons = require('@exp-textura/icons');
const mainNav_styles = require('../main-nav.styles.cjs');
const useMainNav = require('../hooks/use-main-nav.cjs');

const MainNavBrand = React.forwardRef(
  ({ className, brandImg, squareBrandImg, ...props }, ref) => {
    const { dataMainNavState, collapsed } = useMainNav.useMainNav();
    return /* @__PURE__ */ jsxRuntime.jsx(
      "a",
      {
        ref,
        className: clsx$1.clsx(mainNav_styles.MainNavClasses.brand, className),
        "data-main-nav-state": !brandImg ? "collapsed" : dataMainNavState,
        ...props,
        children: !collapsed && brandImg ? brandImg : squareBrandImg
      }
    );
  }
);
MainNavBrand.displayName = "MainNav.Brand";
const MainNavCollapseTrigger = React.forwardRef(({ className, children, onClick, ...props }, ref) => {
  const { dataMainNavState, collapsed, setIsCollapsed } = useMainNav.useMainNav();
  const handleClick = (e) => {
    onClick?.(e);
    setIsCollapsed(!collapsed);
  };
  return /* @__PURE__ */ jsxRuntime.jsx(
    "button",
    {
      ref,
      "aria-label": "toggle main-nav",
      className: clsx$1.clsx(mainNav_styles.MainNavClasses.collapseTrigger, className),
      "data-main-nav-state": dataMainNavState,
      onClick: handleClick,
      ...props,
      children: children || /* @__PURE__ */ jsxRuntime.jsx(icons.Sidebar, {})
    }
  );
});
MainNavCollapseTrigger.displayName = "MainNav.CollapseTrigger";

exports.MainNavBrand = MainNavBrand;
exports.MainNavCollapseTrigger = MainNavCollapseTrigger;
//# sourceMappingURL=main-nav-brand.cjs.map
