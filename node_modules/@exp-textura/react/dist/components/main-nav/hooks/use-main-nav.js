"use client";
import { createContext, useContext } from 'react';

const MainNavContext = createContext(void 0);
const useMainNav = () => {
  const context = useContext(MainNavContext);
  if (context === void 0) {
    throw new Error("useMainNav must be used within a <MainNav> component");
  }
  return context;
};
const MainNavItemParentContext = createContext(void 0);
const useMainNavItemParent = () => {
  const context = useContext(MainNavItemParentContext);
  return context;
};

export { MainNavContext, MainNavItemParentContext, useMainNav, useMainNavItemParent };
//# sourceMappingURL=use-main-nav.js.map
